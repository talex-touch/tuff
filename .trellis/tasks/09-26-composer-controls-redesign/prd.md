# 输入框工具栏重做与发送按钮动效（灵动岛式）

父任务：`09-25-home-session-polish`。状态：planning（先调研出方案，老板看过再落地）。

## 老板原话（2026-09-26，截图标注在输入框工具栏与快捷胶囊上）

> 这几个按钮都可以重新设计下，和目前的样式结构不统一，动效展示也有点问题。
> 右侧的发送按钮也没有动效，可以参考之前的 pilot 和苹果灵动岛，比如整体会收缩、响应、渐变之类的，你看着办。

## 范围

- 输入框工具栏：左侧 +（附件）、「权限 · 完全允许」胶囊、右侧「Tuff 智能 高 ▾」模型胶囊、麦克风、发送按钮。
- 快捷胶囊（输入框下方 4 个）：将由 `09-25-home-assistant-push` 第 6 步换成 `TxChoiceCard`，本任务不重做胶囊，只保证工具栏与新卡片风格一致。

## 待调研（research/）

1. 旧 pilot（`apps/pilot`，已于 `dab519726` 移除，用 `git show dab519726^:<path>` 读）的输入框与发送按钮：结构、状态（空 / 可发送 / 发送中 / 可停止）、动效实现。
2. 苹果灵动岛式的形变：同一个胶囊在状态间连续形变（尺寸、圆角、内容交叉淡入、按压收缩、弹性回弹、渐变）。
3. 现状：`HomePage.vue` 里工具栏的模板与样式、各按钮的状态来源（`canSend`、`isStreaming`、语音、附件、权限、模型选择），现有 tuffex 组件（按钮、胶囊、`TxFusionSurface`、液体弹簧 `springSteps` / `linear()` 曲线）能复用什么。
4. 与发送分裂动画（`09-25-send-split-fusion`）的时序关系：发送按钮在按下发送、分裂、回复流式期间的状态序列。

## 已知约束

- 与发送分裂动画、助理推送第 6 步同改 `HomePage.vue`：本任务在这两者之后落地；新组件尽量独立成文件，`HomePage.vue` 只做接线。
- 遵守 tuffex 的一个光源阴影规则、`prefers-reduced-motion` 降级。

## 已拍板（2026-09-26，老板看过 research/proposal.md 后）

- **D1 停止形态：长成「■ 停止」胶囊**（不是 32px 圆）。回复进行中，发送键向左长成约 72px 的「■ 停止」胶囊，盖住麦克风位置（流式期间麦克风让位），更接近灵动岛；发送那一刻的形变要顺（不能每帧重排撑动整行——用绝对定位 / transform 做形变，别让相邻控件被挤动）。
- **D10 麦克风：这次就接上听写**。按下开始听写，识别文字进输入框草稿，带音量电平反馈；接线走现有语音 SDK（`VoiceSdk.asrStream` 一类），不另造一套。
- **D11「高」：真实接入推理强度**。模型胶囊里的档位必须是真实设置：可选档位、持久化、随请求传给模型；各提供方按自身能力映射（支持的传参，不支持的不显示或禁用），不许再是写死的文案。
- 其余决策点（D2–D9、D13、D14）按 proposal.md 的推荐执行；D12 已由 `09-26-composer-paste-anywhere` 放开（回复中可附图）。
- 注意：proposal.md 的时序表按已废弃的融合分裂写成（onPull / onBreak / onRelease）；现行发送动画是 `09-25-send-split-fusion` 的「托起」（lift：按下即动，`onClear` 离开输入框，首次触地 `impact`），落地前按托起重对时序。

## 已拍板（2026-09-26，第二轮，看过 research/reasoning-effort.md 与 dictation.md 后）

- **D10-a**：输入框麦克风听写**不受**「语音输入」总开关管（总开关管全局 Fn 手势与浮窗；点输入框按钮本身就是明确同意）。规范里补一句范围。
- **D10-f**：听写进行中，麦克风像停止键一样**向左长成「听写胶囊」**（波形 + 计时），与停止胶囊同一套灵动岛语言；听写期间暂时盖住模型胶囊（绝对定位形变，相邻控件不被挤动）。
- **D10-d**：听写中点发送 = **结束并发送**（`stop()`，最终文字落定后自动发出）。
- **D10-d（回车，2026-09-26 老板确认）**：听写中在输入框按回车同样 = **结束并发送**，与点发送键、发送快捷键一致（`handleKeydown` → `pressSend()`）；调研 §4.1 里「回车 = 只结束听写」的写法以本条为准。
- 其余按调研推荐：D10-b 只做点按切换；D10-c 不做 AI 整理（`deliveryTiming: 'live'`，保留服务端去口水词）；D10-e 静音自动停用主进程默认 1.5s。
- 推理强度（D11）拆为独立子任务 `09-26-reasoning-effort`（跨层 + Nexus 服务端）；本任务只负责模型胶囊的显示与菜单接线。
