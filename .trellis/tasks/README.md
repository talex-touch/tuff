# Trellis Tasks

This directory is live workflow state: task ownership, status, requirements, plans, and evidence change as work progresses. Use `.trellis/scripts/get_context.py` and `.trellis/scripts/task.py` as the authoritative commands for the current task and task lifecycle; this README is navigation only.
