# Implementation: External native session adoption

1. Add the typed discover request/result event and SDK method with exact runtime normalization.
2. Implement the bounded main-only four-provider metadata scanner with injected filesystem roots for tests.
3. Add discovery-specific pointer upsert semantics and host-only Local AI handler.
4. Add Project-store action, Project dropdown item, loading guard, localized result feedback, and refresh.
5. Delegate permanent scanner/store/transport/renderer regression tests to the Tester agent.
6. Run focused tests, utils build, Node/Web typechecks, privacy inventory, changed lint, and diff checks.
7. Prove explicit per-project adoption and native continuation in an isolated Electron profile using throwaway archives.
