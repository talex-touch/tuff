# Baseline — 2026-09-26 old build (before this task), real dev app over CDP

Probe: `/tmp/tuff-cdp/app-search-probe.mjs` types a query into the running CoreBox (140ms per key) and stamps every added `.BoxItem` with its arrival time relative to the first keystroke.

## `code` (4 keystrokes, last at 433ms)

| arrival | rows | source |
|---|---|---|
| 178ms | 6 | 对话 (conversation provider, fast layer snapshot) |
| 1573ms | 11 | 应用 — Xcode, Claude Code URL Handler, Visual Studio Code, … appended at #1–#11 |
| 2116ms | 50 | 文件 |

Apps arrived 1.14s after the last keystroke, as a late batch under the conversation rows — the fast-layer snapshot went out without them.

## `obs` (3 keystrokes, last at 301ms)

| arrival | rows | source |
|---|---|---|
| 189ms | 6 | 对话 |
| 1069ms | 50 | 文件 — `obscuretestdata`, `astral_observatory_*.png`, … placed from #0 |
| — | 0 | 应用 — **Obsidian never appeared within 3.5s** |

The app provider either timed out at the gather's 3s budget or was still queued behind file reads on the shared read worker; either way the user saw files and no app.

## Query-time SQL measured with sqlite3 on the live 9.18GB index (same shapes as `app-provider.onSearch`)

| query | ms |
|---|---|
| keyword IN (precise) | 1 |
| prefix LIKE | 2 |
| FTS `MATCH 'code'` with `provider='app-provider'` post-filter | 501 |
| FTS `MATCH 'f'` | 64 |
| FTS `MATCH 'obsidian'` | 2 |
| file-provider FTS `MATCH 'code*'` | 697 |
| file-provider prefix LIKE, rare prefix | 1151 |

Read worker: one FIFO slot, queue ≤ 64, 15s timeout, active query not interruptible; 4× `SEARCH_INDEX_READ_TIMEOUT` today.
