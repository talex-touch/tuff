[
  {
    "component": "typing-indicator",
    "dimension": "D1",
    "category": "stale-source-ref",
    "severity": "medium",
    "summary": "`<TuffDocSourceLink />` is used with no `path` prop, so it auto-derives `packages/tuffex/packages/components/src/typing-indicator/index.ts` from the route slug — a directory that does not exist, since TxTypingIndicator lives in `src/chat/`.",
    "evidenceFile": "apps/nexus/content/docs/dev/components/typing-indicator.zh.mdc",
    "evidenceLine": 127,
    "evidenceQuote": "<TuffDocSourceLink />",
    "suggestion": "TuffDocSourceLink.vue:16-22 builds `packages/tuffex/packages/components/src/${slug}/index.ts` from the `/docs/dev/components/:slug` route, and `ls packages/tuffex/packages/components/src/*typing*` returns no match. Both the 'View source' GitHub link and the printed path are wrong (404). Pass the explicit path on both locales: `<TuffDocSourceLink path=\"packages/tuffex/packages/components/src/chat/index.ts\" />` in typing-indicator.zh.mdc:127 and typing-indicator.en.mdc:127. Check other sub-component doc pages (chat-composer, avatar-variants) for the same auto-derivation trap.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "fusion",
    "dimension": "D4",
    "category": "dead-css-var",
    "severity": "low",
    "summary": "`--tx-fusion-blur` is written into the stage inline style but no CSS rule in the component consumes it, so consumers who override the variable to tune blur get no effect (blur only reaches the DOM via the SVG `stdDeviation` attribute).",
    "evidenceFile": "packages/tuffex/packages/components/src/fusion/src/TxFusion.vue",
    "evidenceLine": 66,
    "evidenceQuote": "    '--tx-fusion-blur': `${props.blur}px`,",
    "suggestion": "Grepping `tx-fusion-blur` across the repo returns only this line, the compiled dist copies, and the assertion at fusion.test.ts:31 — the scoped `<style>` block (lines 107-166) uses `--tx-fusion-duration`, `--tx-fusion-easing`, and `--tx-fusion-gap` but never `--tx-fusion-blur`. Either delete the dead variable (and its test assertion), or reword fusion.zh.mdc:1487 / fusion.en.mdc:1487 which currently implies blur is applied through the CSS variable ('`gap` 和 `blur` 会写入像素单位 CSS 变量') so readers do not try to theme it from outside.",
    "verdict": "PLAUSIBLE"
  },
  {
    "component": "timeline",
    "dimension": "D1",
    "category": "stale-source-ref",
    "severity": "medium",
    "summary": "Both timeline docs cite a screenshot at `.codex-screenshots/nexus-timeline-permission-orchestration-demo-playwright-2026-05-28.png`, but the `.codex-screenshots/` directory does not exist anywhere in the repo and is not gitignored.",
    "evidenceFile": "apps/nexus/content/docs/dev/components/timeline.zh.mdc",
    "evidenceLine": 125,
    "evidenceQuote": "- 截图:`.codex-screenshots/nexus-timeline-permission-orchestration-demo-playwright-2026-05-28.png`。",
    "suggestion": "Either commit the screenshot (and gitignore-exempt the directory) or drop the screenshot bullet from timeline.zh.mdc:125 and timeline.en.mdc:125. Note this pattern is repo-wide (tag-input, cascader, transfer, segmented-slider docs cite the same missing directory), so a sweep is warranted.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "slider",
    "dimension": "D2",
    "category": "invalid-demo-usage",
    "severity": "high",
    "summary": "SliderSliderElasticTooltipDemo.vue is a gutted skeleton: all 24 state refs are initialized to `''`/`false` and the entire preset system (`presets`, `applyPreset`, `watch(preset, ...)`) documented in the 200-line inline code block is absent, so `:tooltip-tilt=\"tiltMode === 'on'\"` is permanently false and the 'Elastic Tooltip' playground demonstrates nothing.",
    "evidenceFile": "apps/nexus/app/components/content/demos/SliderSliderElasticTooltipDemo.vue",
    "evidenceLine": 12,
    "evidenceQuote": "const tiltMode = ref('')\nconst tooltipDistortSkewDeg = ref(false)",
    "suggestion": "Port the real state from slider.zh.mdc lines 111-315 into the demo file: type each ref (`tiltMode = ref<'on'|'off'>('on')`, `tooltipDistortSkewDeg = ref(8)`, etc.), add the six `Preset` objects plus `applyPreset()` and `watch(preset, applyPreset, { immediate: true })`. Numeric props like `tooltipJellyFrequency` must be numbers, not `''`, or `Math.max(0, '')` silently zeroes the effect.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "slider",
    "dimension": "D2",
    "category": "invalid-demo-usage",
    "severity": "high",
    "summary": "SliderSliderFormatValueDemo.vue binds `:format-value` to `ref('')` (a string) while `SliderProps.formatValue` is typed `(value: number) => string`, so `displayValue` falls through to `String(clampedValue)` and the '格式化显示 / Formatted Display' section renders an unformatted number instead of the documented `30%`.",
    "evidenceFile": "apps/nexus/app/components/content/demos/SliderSliderFormatValueDemo.vue",
    "evidenceLine": 4,
    "evidenceQuote": "const formatValue = ref('')",
    "suggestion": "Replace with the function from the doc's inline block: `const formatValue = (next: number) => `${next}%`` and `const value = ref(30)`. Note this only avoids a crash because `''` is falsy — any truthy string would make `props.formatValue(clampedValue.value)` throw at TxSlider.vue:108.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "image-uploader",
    "dimension": "D4",
    "category": "i18n-hardcoded",
    "severity": "medium",
    "summary": "The add tile label `Upload` and the `Remove {name}` aria-label are hardcoded English string literals in the template, and the component exposes neither slots nor label props, so a zh-locale host cannot localize the only visible text the component renders.",
    "evidenceFile": "packages/tuffex/packages/components/src/image-uploader/src/TxImageUploader.vue",
    "evidenceLine": 115,
    "evidenceQuote": "<span class=\"tx-image-uploader__add-text\">Upload</span>",
    "suggestion": "Add optional `uploadText?: string` and `removeLabel?: (name?: string) => string` props (or an `add` slot) defaulting to the current English strings, and document them in the Props table of image-uploader.zh.mdc / .en.mdc — the Slots table currently states no customization point exists at all.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "form",
    "dimension": "D4",
    "category": "dead-prop",
    "severity": "medium",
    "summary": "`TxForm`'s `disabled` and `size` props are pushed into the form context but nothing in the library injects them — TxFormItem is the only consumer of `TX_FORM_CONTEXT_KEY` and it reads only `labelPosition`/`labelWidth`, so `<TxForm disabled>` has zero effect.",
    "evidenceFile": "packages/tuffex/packages/components/src/form/src/TxFormItem.vue",
    "evidenceLine": 124,
    "evidenceQuote": "      `tx-form-item--label-${form?.labelPosition.value ?? 'left'}`,",
    "suggestion": "Either make TxFormItem apply the context `disabled`/`size` (e.g. render `fieldset[disabled]` or an `is-disabled` class and a size modifier), or state plainly in both Props tables that `disabled`/`size` are context-only values that no TuffEx input currently consumes, so `disabled` must be passed to each control.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "spinner",
    "dimension": "D1",
    "category": "behavior-mismatch",
    "severity": "medium",
    "summary": "The Interaction Contract claims the spinner inherits ambient text color via `currentColor`, but `.tx-spinner` hard-sets `color: var(--tx-text-color-secondary, #909399)` on its own root, so a parent's `color` never tints it.",
    "evidenceFile": "packages/tuffex/packages/components/src/spinner/src/TxSpinner.vue",
    "evidenceLine": 151,
    "evidenceQuote": "  color: var(--tx-text-color-secondary, #909399);",
    "suggestion": "Either change the rule to `color: inherit` (or `var(--tx-spinner-color, currentColor)`) so ambient color really cascades, or fix the bullet in spinner.zh.mdc:121 / spinner.en.mdc:121 to say the spinner defaults to the secondary text color and must be tinted by setting `color` on the `TxSpinner` element itself (e.g. `<TxSpinner style=\"color: var(--tx-color-primary)\" />`).",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "spinner",
    "dimension": "D4",
    "category": "type-drift",
    "severity": "low",
    "summary": "TxSpinner.vue declares props with the runtime object form and never imports the `SpinnerProps` interface that types.ts exports and the docs advertise, so the published type is a hand-maintained duplicate that can silently drift from the runtime contract.",
    "evidenceFile": "packages/tuffex/packages/components/src/spinner/src/TxSpinner.vue",
    "evidenceLine": 8,
    "evidenceQuote": "const props = defineProps({\n  size: {\n    type: Number,\n    default: 16,\n  },",
    "suggestion": "Replace the runtime object with `withDefaults(defineProps<SpinnerProps>(), { size: 16, strokeWidth: 2, fallback: false, visible: true })` and import the type from `./types`, so the exported `SpinnerProps` becomes the single source of truth the Props table is checked against.",
    "verdict": "PLAUSIBLE"
  },
  {
    "component": "spinner",
    "dimension": "D2",
    "category": "dead-demo-branch",
    "severity": "low",
    "summary": "SpinnerSpinnerDemo.vue and SpinnerSpinnerSizesDemo.vue wrap byte-identical markup in `v-if=\"locale === 'zh'\"` / `v-else` branches, and two extra unreferenced copies (SpinnerBasicDemo.vue, SpinnerSizesDemo.vue) exist that are in neither demo-registry.ts nor any doc.",
    "evidenceFile": "apps/nexus/app/components/content/demos/SpinnerSpinnerDemo.vue",
    "evidenceLine": 6,
    "evidenceQuote": "  <div v-if=\"locale === 'zh'\">\n      <TxSpinner />\n  </div>\n  <div v-else>\n      <TxSpinner />\n  </div>",
    "suggestion": "Drop the `useI18n()` import and the duplicated locale branches from both demos (leaving a single `<TxSpinner />` / sizes row), and delete the orphaned SpinnerBasicDemo.vue and SpinnerSizesDemo.vue which no doc or registry entry references.",
    "verdict": "PLAUSIBLE"
  },
  {
    "component": "select",
    "dimension": "D4",
    "category": "i18n-default",
    "severity": "low",
    "summary": "The `placeholder` prop defaults to the hardcoded Chinese string `'请选择'` while every other text default in the same withDefaults block is English (`'Search'`, `'Add item'`, `'Loading...'`, `'No results'`), which is why the English doc has to document a Chinese default.",
    "evidenceFile": "packages/tuffex/packages/components/src/select/src/TxSelect.vue",
    "evidenceLine": 19,
    "evidenceQuote": "placeholder: '请选择',",
    "suggestion": "Change the default to `'Please select'` for consistency with the other English defaults (and update select.zh.mdc:350 / select.en.mdc:350), or route all five text defaults through the library's locale mechanism.",
    "verdict": "PLAUSIBLE"
  },
  {
    "component": "flat-dropdown",
    "dimension": "D2",
    "category": "invalid-demo-usage",
    "severity": "high",
    "summary": "Both the inline doc example and the real FlatDropdownBasicDemo pass `variant=\"default\"` to `TxButton`, but `'default'` is not part of `ButtonProps['variant']` ('primary' | 'secondary' | 'ghost' | 'danger' | 'success' | 'warning' | 'info' | 'flat' | 'bare'), so the button renders an unstyled `variant-default` class.",
    "evidenceFile": "apps/nexus/app/components/content/demos/FlatDropdownBasicDemo.vue",
    "evidenceLine": 15,
    "evidenceQuote": "        <TxButton size=\"sm\" :variant=\"open ? 'primary' : 'default'\">",
    "suggestion": "Change to `:variant=\"open ? 'primary' : 'secondary'\"` (the value `normalizedVariant` falls back to when nothing is set) in the demo and in the `code:` blocks at flat-dropdown.zh.mdc:32 and flat-dropdown.en.mdc:32.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "dialog",
    "dimension": "D1",
    "category": "stale-source-ref",
    "severity": "medium",
    "summary": "`BottomDialogProps.stay` and `.icon` carry JSDoc promising behavior the template never implements (auto-close timer, icon rendering); the published docs correctly call them \"reserved\"/\"not rendered\", so IDE intellisense contradicts the docs.",
    "evidenceFile": "packages/tuffex/packages/components/src/dialog/src/types.ts",
    "evidenceLine": 96,
    "evidenceQuote": "   * Duration in milliseconds before the dialog auto-closes.\n   * Set to 0 to disable auto-close.",
    "suggestion": "Update both JSDoc blocks to match dialog.en.mdc:193/196 (\"Reserved auto-close duration prop; current runtime does not start a timer from `stay` alone\" and \"Legacy icon class prop; not rendered by the current template\"), mark them `@deprecated`, or implement them in TxBottomDialog.vue.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "tree-select",
    "dimension": "D2",
    "category": "invalid-demo-usage",
    "severity": "high",
    "summary": "The primary TreeSelect demo `TreeSelectTreeSelectDemo.vue` builds its nodes with `{ label, value }` instead of the required `{ key, label }`, so every node's `key` is `undefined` and the live demo is functionally broken (all rows share the same undefined v-for key, selecting any node emits `undefined`, and expansion never works).",
    "evidenceFile": "apps/nexus/app/components/content/demos/TreeSelectTreeSelectDemo.vue",
    "evidenceLine": 17,
    "evidenceQuote": "      { label: 'Alpha', value: 'alpha' },",
    "suggestion": "Rename the demo's local `TreeNode.value` field to `key` (or import `TreeSelectNode` from `@talex-touch/tuffex/tree-select` so the compiler catches it) and align the node data with the doc's inline snippet (General / Account / Danger Zone incl. the `disabled` node) so the rendered demo actually matches the documented example. Note the doc itself already warns against this exact mistake at tree-select.zh.mdc:207 (\"`nodes` 使用 `key`/`label`，不要沿用旧选择器的 `value` 字段\").",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "tree-select",
    "dimension": "D1",
    "category": "stale-source-ref",
    "severity": "medium",
    "summary": "The Review Notes cite `.codex-screenshots/nexus-tree-select-permission-orchestration-demo-playwright-2026-05-28.png` as the verification artifact, but neither that file nor the `.codex-screenshots/` directory exists anywhere in the repo (and it is not gitignored), so the claimed screenshot evidence is unverifiable.",
    "evidenceFile": "apps/nexus/content/docs/dev/components/tree-select.zh.mdc",
    "evidenceLine": 208,
    "evidenceQuote": "- 截图参考：`.codex-screenshots/nexus-tree-select-permission-orchestration-demo-playwright-2026-05-28.png`。",
    "suggestion": "Either commit the screenshot to a real tracked path (e.g. `apps/nexus/public/docs/...`) and update the reference in both tree-select.zh.mdc:208 and tree-select.en.mdc:208, or remove the screenshot bullet. The same dangling `.codex-screenshots/` path appears in 18 component docs, so this is worth fixing corpus-wide in one pass.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "chat-composer",
    "dimension": "D4",
    "category": "dead-code",
    "severity": "low",
    "summary": "`textareaRef` is created and bound but never used — the file contains a `void textareaRef.value` no-op statement — and the component exposes nothing, so hosts cannot refocus the composer after send.",
    "evidenceFile": "packages/tuffex/packages/components/src/chat/src/TxChatComposer.vue",
    "evidenceLine": 60,
    "evidenceQuote": "void textareaRef.value",
    "suggestion": "Delete the `void` statement and add `defineExpose({ focus: () => textareaRef.value?.focus(), blur: () => textareaRef.value?.blur() })`, then document an `Expose` table in both language files (the Best Practices already assume hosts drive the composer after send).",
    "verdict": "PLAUSIBLE"
  },
  {
    "component": "floating",
    "dimension": "D1",
    "category": "doc-behavior-mismatch",
    "severity": "medium",
    "summary": "Both language docs state pointer coordinates are measured from the container's top-left corner, but `syncPointerOffset()` subtracts `rect.width/2` and `rect.height/2`, i.e. the origin is the container **centre** — so readers computing expected offsets get double the real value and the wrong sign on the left/top half.",
    "evidenceFile": "apps/nexus/content/docs/dev/components/floating.en.mdc",
    "evidenceLine": 66,
    "evidenceQuote": "- Pointer coordinates are measured relative to the floating container's top-left corner.",
    "suggestion": "Change floating.en.mdc:66 and floating.zh.mdc:66 to say the offset is measured from the container centre (`TxFloating.vue:92-93`: `pointerOffset.x = pointerClient.x - rect.left - rect.width / 2`). The test suite already names this contract explicitly ('keeps elements at rest while the pointer sits on the container centre').",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "floating",
    "dimension": "D4",
    "category": "dead-code",
    "severity": "low",
    "summary": "`TxFloatingElement.register()` falls back to `0.01` when `props.depth` is nullish, but `withDefaults` already sets `depth: 1`, so the fallback is unreachable and the two competing defaults contradict the documented `depth` default of `1`.",
    "evidenceFile": "packages/tuffex/packages/components/src/floating/src/TxFloatingElement.vue",
    "evidenceLine": 22,
    "evidenceQuote": "  const resolvedDepth = props.depth ?? 0.01",
    "suggestion": "Drop the `?? 0.01` and pass `props.depth` directly, since line 12 already declares `depth: 1` in `withDefaults`.",
    "verdict": "PLAUSIBLE"
  },
  {
    "component": "stack",
    "dimension": "D2",
    "category": "invalid-demo-usage",
    "severity": "medium",
    "summary": "The \"行内堆叠 / Inline Stack\" example nests `<TxStack>` inside a `<p>`, but TxStack's root is a `<div>` (TxStack.vue:30) — invalid HTML that browsers fix by closing the `<p>` early, so the example does not render as the inline flow it demonstrates.",
    "evidenceFile": "apps/nexus/content/docs/dev/components/stack.zh.mdc",
    "evidenceLine": 51,
    "evidenceQuote": "  <p>\n    状态：\n    <TxStack inline direction=\"horizontal\" align=\"center\" gap=\"0.5rem\">",
    "suggestion": "Replace `<p>` with `<span>` (or a `<div>` with `display: inline`) in both stack.zh.mdc:51 and stack.en.mdc:51, or give TxStack a `tag`/`as` prop so `inline` can render a `<span>` root and legitimately live inside a paragraph.",
    "verdict": "PLAUSIBLE",
    "refutedReason": "The evidence is right (stack.zh.mdc:49-59 nests `<TxStack inline>` in a `<p>`, and TxStack.vue:30 roots on a `<div>`), but the stated failure mechanism is wrong for a Vue SFC. I compiled that exact template with the repo's @vue/compiler-dom 3.5.39: the AST keeps `<div>` as a child of `<p>` and the render fn emits `_createElementBlock(\"p\", null, [ _createTextVNode(\" status: \"), _createElementVNode(\"div\", {class:...}) ])`. Vue builds the DOM via createElement, so the browser's \"close `<p>` early\" parser rule never runs and the inline-flex child does render inline with the text — i.e. the example does render as the inline flow it demonstrates. The claimed consequence is disproven; only a compile-time warning (\"<div> cannot be child of <p>\") and a potential SSR-hydration mismatch remain, neither of which the finding reports."
  },
  {
    "component": "skeleton",
    "dimension": "D4",
    "category": "redundant-logic",
    "severity": "low",
    "summary": "`itemStyle` branches on `props.variant === 'text'` but both branches call the identical expression `toCssUnit(props.height)`, making the ternary dead code that hides an unimplemented text-variant height rule.",
    "evidenceFile": "packages/tuffex/packages/components/src/skeleton/src/TxSkeleton.vue",
    "evidenceLine": 25,
    "evidenceQuote": "  const height = props.variant === 'text' ? toCssUnit(props.height) : toCssUnit(props.height)",
    "suggestion": "Reduce to `const height = toCssUnit(props.height)`, or implement the intended text-variant behavior (e.g. line-height-derived height) and add a test for it in skeleton.test.ts.",
    "verdict": "PLAUSIBLE"
  },
  {
    "component": "flex",
    "dimension": "D2",
    "category": "unlinked-demo",
    "severity": "low",
    "summary": "The two `## 组合示例` examples (wrapping toolbar, reverse direction) are plain ```vue fences with no TuffDemoWrapper, while an unreferenced `FlexBasicDemo.vue` duplicating FlexFlexDemo sits in the demos directory.",
    "evidenceFile": "apps/nexus/content/docs/dev/components/flex.zh.mdc",
    "evidenceLine": 35,
    "evidenceQuote": "```vue\n<template>\n  <TxFlex align=\"center\" justify=\"space-between\" wrap=\"wrap\" gap=\"0.75rem\">",
    "suggestion": "Convert the two composition snippets into real demo files rendered through `:::TuffDemoWrapper` so they are type-checked and previewable, and delete the orphan `apps/nexus/app/components/content/demos/FlexBasicDemo.vue` (identical markup to FlexFlexDemo but without the i18n branch).",
    "verdict": "PLAUSIBLE"
  },
  {
    "component": "rating",
    "dimension": "D2",
    "category": "invalid-demo-usage",
    "severity": "medium",
    "summary": "`RatingRatingDemo.vue` binds `ref('')` (a string) to `v-model` of a `number` `modelValue` while `show-text` is on, which crashes rendering with `rating.toFixed is not a function`.",
    "evidenceFile": "apps/nexus/app/components/content/demos/RatingRatingDemo.vue",
    "evidenceLine": 4,
    "evidenceQuote": "const score = ref('')",
    "suggestion": "Either delete the file (no doc references it — it is an orphan alongside GlassSurfaceControlsDemo.vue) or change it to `const score = ref(3.5)`. Verified by probe: mounting TxRating with `modelValue: ''` and `showText: true` throws `$setup.rating.toFixed is not a function`.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "segmented-slider",
    "dimension": "D2",
    "category": "invalid-demo-usage",
    "severity": "high",
    "summary": "SegmentedSliderSegmentedSliderDemo.vue passes `const segments = ref('')` (an empty string) into `:segments`, so the demo referenced by both zh and en docs renders an empty track with zero segments instead of the documented Small/Medium/Large/XL slider.",
    "evidenceFile": "apps/nexus/app/components/content/demos/SegmentedSliderSegmentedSliderDemo.vue",
    "evidenceLine": 4,
    "evidenceQuote": "const segments = ref('')",
    "suggestion": "Replace lines 4-5 with the working implementation that already exists in the orphaned apps/nexus/app/components/content/demos/SegmentedSliderBasicDemo.vue: `const value = ref(1)` and the four-entry `segments` array ({value:0,label:'Small'} ... {value:3,label:'XL'}), matching the doc snippet.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "segmented-slider",
    "dimension": "D2",
    "category": "invalid-demo-usage",
    "severity": "high",
    "summary": "SegmentedSliderSegmentedSliderCustomDemo.vue initialises `priceSegments`/`ratingSegments`/`priceValue`/`ratingValue` as empty strings, so the 'Custom Options' demo renders two blank tracks instead of the Free/Pro/Team/Enterprise and 1-5 star sliders shown in the doc code block.",
    "evidenceFile": "apps/nexus/app/components/content/demos/SegmentedSliderSegmentedSliderCustomDemo.vue",
    "evidenceLine": 4,
    "evidenceQuote": "const priceSegments = ref('')",
    "suggestion": "Restore the real data from SegmentedSliderCustomDemo.vue: `priceValue = ref('pro')` with the four plan segments and `ratingValue = ref(3)` with the five star segments, exactly as the doc snippet in segmented-slider.zh.mdc lines 55-70 declares.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "checkbox",
    "dimension": "D1",
    "category": "stale-source-ref",
    "severity": "medium",
    "summary": "The `## Source` / Review Notes sections claim `checkbox.test.ts` verifies keyboard toggles, but the test file contains no keydown/keyup trigger at all (only `trigger('click')`).",
    "evidenceFile": "apps/nexus/content/docs/dev/components/checkbox.en.mdc",
    "evidenceLine": 188,
    "evidenceQuote": "- Coverage: `packages/tuffex/packages/components/src/checkbox/__tests__/checkbox.test.ts` verifies click and keyboard toggles, ARIA labelling, slot precedence, variant classes, and disabled blocking.",
    "suggestion": "Either add a keyboard test to `checkbox.test.ts` (Enter/Space on the native button, plus a disabled case) or drop 'keyboard toggles' from the coverage claim in both checkbox.en.mdc:188 and checkbox.zh.mdc:188 and instead note that keyboard activation is inherited from the native `<button>` element.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "outline-border",
    "dimension": "D2",
    "category": "misleading-demo",
    "severity": "medium",
    "summary": "The Mask Clipping demo pairs `variant=\"ring\"` with `clip-shape=\"hexagon\"`, but the ring is a `box-shadow` on the root (which keeps `border-radius: 9999px` from the default `shape=\"circle\"`) while only the inner content layer is hexagon-masked, so the outline does not follow the hexagon.",
    "evidenceFile": "apps/nexus/app/components/content/demos/OutlineBorderMaskClipDemo.vue",
    "evidenceLine": 6,
    "evidenceQuote": "    clip-mode=\"mask\"",
    "suggestion": "Either drop the ring from this demo (`variant=\"border\"` has the same problem — use no outline, or a hexagon-shaped drop-shadow filter) or state in the doc's Mask note that ring/border outlines always follow `border-radius` and cannot trace `clipShape=\"hexagon\"`. Update the identical snippet in outline-border.zh.mdc:40-48 and .en.mdc:40-48.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "outline-border",
    "dimension": "D1",
    "category": "undocumented-behavior",
    "severity": "medium",
    "summary": "`clipShape=\"squircle\"` combined with `clipMode=\"clipPath\"` silently falls back to `inset(0 round var(--tx-outline-radius))` (a plain rounded rect), but the docs only call out the rounded/rect fallback and advertise `squircle` as a first-class clip shape.",
    "evidenceFile": "packages/tuffex/packages/components/src/outline-border/src/TxOutlineBorder.vue",
    "evidenceLine": 148,
    "evidenceQuote": "  return 'inset(0 round var(--tx-outline-radius))'",
    "suggestion": "Add a bullet to the Interaction Contract in both languages: `clipShape=\"squircle\"` is only honoured by `clipMode=\"mask\"`; under `clipPath` it degrades to a rounded inset. Alternatively emit an actual squircle `path()` in `resolveClipPath`.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "permission-state",
    "dimension": "D4",
    "category": "missing-emits-declaration",
    "severity": "medium",
    "summary": "The docs list `primary` and `secondary` as component events, but `TxPermissionState.vue` declares no `defineEmits`; the handlers only reach `TxEmptyState` through single-root attribute fallthrough, so they are absent from the component's typed emit surface.",
    "evidenceFile": "packages/tuffex/packages/components/src/permission-state/src/TxPermissionState.vue",
    "evidenceLine": 9,
    "evidenceQuote": "const props = defineProps<PermissionStateProps>()",
    "suggestion": "Add `const emit = defineEmits<EmptyStateEmits>()` and forward explicitly (`@primary=\"emit('primary')\" @secondary=\"emit('secondary')\"` on `<TxEmptyState>`), so IDE completion, devtools and the docs agree — and so forwarding survives a future `inheritAttrs: false` or an added wrapper element. Also add an emit-forwarding assertion to permission-state.test.ts.",
    "verdict": "PLAUSIBLE",
    "refutedReason": "This is exactly the attrs-passthrough/base-component-inheritance case, and the finding itself concedes the mechanism works. TxPermissionState.vue is a single-root `<TxEmptyState v-bind=\"props\" variant=\"permission\">` (line 13); TxEmptyState.vue:22 declares `defineEmits<EmptyStateEmits>()` and emits `primary`/`secondary` (lines 330/341), so `onPrimary`/`onSecondary` fall through and fire correctly at runtime — no failure scenario exists. The docs are not misleading either: permission-state.en.mdc:39 explicitly states 'Generated `primary` / `secondary` events come from the wrapped `TxEmptyState` action buttons' before listing them at 62-67. It is also a deliberate family-wide pattern, not an oversight: error-state and blank-slate wrap TxEmptyState the same way with `defineProps` only (`PermissionStateProps = Omit<EmptyStateProps, 'variant'>`, types.ts:3). The residual 'typed emit surface' concern is not even a type error under vue-tsc defaults (`strictTemplates` is off), so this reduces to an autocomplete nit rather than a medium-severity defect."
  },
  {
    "component": "container",
    "dimension": "D4",
    "category": "css-precedence",
    "severity": "medium",
    "summary": "`fluid` and `responsive` used together silently cancel `fluid`: `.tx-container.is-responsive` has equal specificity to `.is-fluid` but is declared later, so max-width is re-capped at 640/768/1024/1280px instead of `none`.",
    "evidenceFile": "packages/tuffex/packages/components/src/container/src/TxContainer.vue",
    "evidenceLine": 73,
    "evidenceQuote": ".tx-container.is-responsive {",
    "suggestion": "Either scope the responsive rules with `.tx-container.is-responsive:not(.is-fluid)` so `fluid` always wins, or document in the props table that `fluid` is ignored when `responsive` is set.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "switch",
    "dimension": "D1",
    "category": "hallucinated-api",
    "severity": "medium",
    "summary": "The Interaction Contract claims the root renders `tabindex=\"0\"` when enabled and `tabindex=\"-1\"` when disabled, but TxSwitch renders a native `<button>` with no tabindex at all — switch.test.ts asserts `tabindex` is undefined in both states.",
    "evidenceFile": "apps/nexus/content/docs/dev/components/switch.zh.mdc",
    "evidenceLine": 177,
    "evidenceQuote": "- 根节点渲染 `role=\"switch\"`、`aria-checked`、`aria-disabled`，启用时 `tabindex=\"0\"`。",
    "suggestion": "Rewrite lines 177-178 (and switch.en.mdc:177-178) to describe what TxSwitch.vue:41-53 actually renders: a native `<button type=\"button\" role=\"switch\">` with `aria-checked` / `aria-disabled` / the native `disabled` attribute, which is focusable when enabled and removed from the tab order by `disabled` — no explicit tabindex is set.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "switch",
    "dimension": "D1",
    "category": "stale-source-ref",
    "severity": "medium",
    "summary": "Review Notes claim `switch.test.ts` covers \"keyboard emissions\" and \"disabled tabindex state\", but the test file has no Enter/Space keydown case and instead asserts tabindex is never rendered.",
    "evidenceFile": "apps/nexus/content/docs/dev/components/switch.zh.mdc",
    "evidenceLine": 246,
    "evidenceQuote": "- **实测覆盖:** `switch.test.ts` 覆盖 active aria 状态、尺寸 class、点击与键盘派发、禁用阻断，以及禁用时的 tabindex/aria 状态。",
    "suggestion": "Either add real keyboard cases to switch.test.ts (native button click semantics for Enter/Space) or correct the coverage line in both zh/en to say the suite covers aria state, size classes, click emission, and disabled blocking only.",
    "verdict": "CONFIRMED",
    "refutedReason": null
  },
  {
    "component": "cascader",
    "dimension": "D4",
    "category": "i18n-hardcoded",
    "severity": "low",
    "summary": "TxCascader mixes hardcoded locales: the `placeholder` default is Chinese (`'请选择'`) while the panel strings `Search`, `No results`, `Loading`, and `aria-label=\"Clear\"` are hardcoded English.",
    "evidenceFile": "packages/tuffex/packages/components/src/cascader/src/TxCascader.vue",
    "evidenceLine": 16,
    "evidenceQuote": "  placeholder: '请选择',",
    "suggestion": "Pick one default locale (English, matching the rest of the panel) and expose the panel strings as props (e.g. `searchPlaceholder`, `emptyText`, `loadingText`) so apps can localize; the en doc currently has to document `'请选择'` as the default (cascader.en.mdc:148).",
    "verdict": "PLAUSIBLE"
  }
]