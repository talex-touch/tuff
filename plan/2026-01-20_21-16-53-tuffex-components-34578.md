---
mode: plan
cwd: /Users/talexdreamsoul/Workspace/Projects/talex-touch
task: Tuffex components 3/4/5/7/8 plan and Vitest projects include
complexity: medium
planning_method: builtin
created_at: 2026-01-20T21:16:57+0800
---

# Plan: Tuffex components 3/4/5/7/8

🎯 任务概述
针对组件清单第 3/4/5/7/8 项形成实施路线，包含 API、实现、测试与文档，并确保 tree 能力可复用。

📋 执行计划
1. 范围确认：明确 3/4/5/7/8 对应组件名称与功能边界，核对与现有组件重叠点。
2. 现有模式调研：阅读 3+ 类似组件的 props/emits/types 与样式结构，确定统一约束。
3. API 设计：为每个组件定义 `types.ts`，明确必填/可选 props、事件与 slot 约定。
4. 组件实现：完成 SFC 与逻辑，tree 相关能力抽离为独立基础组件并复用。
5. 对外导出：更新 `index.ts` 与 `components.ts`，补齐注册入口。
6. 测试覆盖：为每个组件补齐核心交互、空态、边界条件测试，必要时 mock 外部依赖。
7. 文档补充：新增 docs/示例与 sidebar 条目，说明使用方式与注意事项。
8. 验证回归：执行 vitest，关注 utils/node 与 components/jsdom 的拆分执行效果。

## 组件范围清单（TUFEX-010）

说明：若缺少外部“组件清单”，本次以 `packages/tuffex/docs/components/index.md` 与
`packages/tuffex/packages/components/src/components.ts` 为组件来源，形成范围清单并补充候选组件。

| 编号 | 组件 | 功能边界 | 新增/复用 | 重叠/依赖 |
| --- | --- | --- | --- | --- |
| 3 | TxTree | 树形展示与选择；展开/折叠、选中与过滤 | 复用（已有组件） | 依赖 TxCardItem/TxCheckbox；与 TreeSelect/Cascader 重叠 |
| 4 | TxTreeSelect | 下拉树选择；单/多选、搜索、清空 | 复用 + 规范化 | 依赖 TxTree/TxPopover/TxInput；与 Select/SearchSelect 重叠 |
| 5 | TxCascader | 级联选择；路径选择、异步加载、搜索 | 复用 + 规范化 | 依赖 TxPopover/TxSearchInput/TxCardItem；与 TreeSelect 重叠 |
| 7 | TxSearchSelect | 搜索下拉选择；单/多选与过滤 | 复用 + 规范化 | 依赖 TxSearchInput/TxPopover；与 Select 重叠 |
| 8 | TxCommandPalette | 命令面板；快捷搜索与动作触发 | 复用 + 规范化 | 依赖 Input/List/Overlay；与 SearchInput/Modal 重叠 |

### 可补充组件建议
- TxTransfer：批量多选与左右穿梭，覆盖管理后台常见场景。
- TxAutocomplete：输入提示/联想，补强搜索输入生态。
- TxTreeTable：树形 + 表格合体，用于层级数据管理。


⚠️ 风险与注意事项
- API 与现有组件不一致导致使用成本上升。
- tree 抽离过程引入回归或破坏已有容器内部逻辑。
- 组件测试依赖 DOM/浏览器 API，需确保环境隔离。

📎 参考
- `packages/tuffex/packages/components/src/components.ts`
- `packages/tuffex/packages/components/src/tree-select/src/TxTreeSelect.vue`
- `packages/tuffex/vitest.config.ts`
- projects include: components `packages/components/src/**/__tests__/**/*.test.ts`
- projects include: utils `packages/utils/__tests__/**/*.test.ts`
