# Issue 清扫交接（2026-08-06 ~ 08-07）

> 本轮关闭 17 条、合入 20 个 PR、新建 4 条。本文档只记**接手所需**的信息：剩下 55 条卡在哪、下一步做什么、以及哪些「先做会出错」。

## 一、贯穿全程的一条结论

本轮遇到 **5 次**「记录自称完成、实际未验证」，其中**一次是我自己的**：

| 记录说 | 实际 |
|---|---|
| 审计 R1：截图 addon 已修、#321 已关 | 只有契约测试接线，**发布路径没有**；#321 仍 open |
| 八份审计报告：分库 flag「默认关闭」 | 代码是 `true`，自 2026-08-05 起 |
| 审计 R4：`[x]` 已修 | 对应任务 `planning`、**0/8 验收未勾** |
| #480：三层内存物化 | 三层**都已改成流式** |
| **我在 #1070 报 35/35** | **实际 30/35**——我测了工作树、提交了另一个状态 |

**一致性不是证据。** 八份报告口径统一，只是同一句话被抄了八次。每次真相都来自读代码。

配套的方法论：**任何「证明某处不存在」的扫描，必须先跑一条已知必然命中的正对照**。本轮我亲手产出过 4 个「全绿但无效」的扫描（POSIX 字符类让 rg 报错被 `2>/dev/null` 吞掉；zsh 不对未加引号的参数做词分割；锚点漏掉带引号的 key；子串误匹配 `core-js` vs `core-js-compat`）。

## 二、剩余 55 条按「卡在什么」分组

### A. 需要你拍板（5 条）——我不会替你决定

| # | 决策点 |
|---|---|
| #311 | macOS 保持 arm64-only 并显式拒绝，还是补 x64/Universal 全矩阵 |
| #346 | searchCache 保留/移除的阈值。**前提已被推翻**：「命中率天然低」未获证实，真正缺陷是没有测试断言「相同查询→相同键」 |
| #483 | 51 条告警的负责人与目标日期。**分诊表已给**：19 个包、每个都有补丁、全部 critical 已由 #329 清零 |
| #1031 / #1032 | 设计语言（诊断色阶、Beta 徽章） |

### B. 需要真机 / CI 证据（约 12 条）

**#321 / #322 是同一个缺口的两个实例**：`build-and-release.yml` **不构建任何 Cargo addon**（对 `screenshot`/`audio`/`cargo`/`rust` 零命中；正对照 `packages/tuff-native` 命中 2 次）。构建 screenshot 的 `native-protocol.yml` 是**契约测试**工作流，不产出发布物。

> ⚠️ **顺序约束**：不能先加 preflight 要求再接 CI——干净安装不产出这两个 `.node`，加进 `requiredModuleNames` 会让**每次发布构建立刻失败**。必须先让发布流程构建它们。

audio addon 的接线基线已实测：构建 3m38s / 2.56MB / 11 个导出 / `{"supported":true,"platform":"macos"}`。

其余：#309 及 8 个子项、#482（actionlint 半条已由 PR #1102 解决，只剩截图）。

### C. 等你推送（7 条）

#1019 #1023 #1024 #1026 #1027 #1029 #1032 **描述的是你本地未推送的代码**（当时本地超前 origin 57 个提交 + 49 个脏文件）。

> ⚠️ #1027 若照它说的改，等于**给一个 origin 上尚不存在的路由放宽 widget 安全白名单**。

### D. 架构类（4 条）——已从「无从下手」推进到「有实测地基」

| # | 已交付 | ⚠️ 先做会出错的一步 |
|---|---|---|
| #349 | 296 处 `process.platform` 归类：**只有 209 比较 + 2 switch 是真分支**，53 是数据传递 | `withOSAdapter` 返回 `T | undefined`，**没有类型化三态**，验收条 4 要不到。**先补契约再迁移** |
| #339 | `plugin.ts` 4013 行（+31%）是真巨类；`quick-ops` **只有 69 个成员**、850 行是声明式描述符 | 按 2 空格缩进数成员会把对象字面量键算进去，得 390（真值 5.6 倍） |
| #343 | 10 个「小 service」里**只有 1 个**是无策略转发；2 处影子再导出层（引用数 2:2、1:2、3:5） | 只按大小裁会删掉两个承载真实策略的文件 |
| #334 | 依赖闸门卡在**记账**：三项在代码里都成立，两项的 Trellis 记录 0 验收未勾（已补 7 条证据） | — |
| #351 | search-index 域的 owner 守卫已落地（`check:search-index-writers`） | 其余 20+ 文件的域未映射 |

### E. 其余独立项（约 27 条）

#305 #307 #308 #323 #324 #325 #326 #327 #330 #337 #341 #344 #348 #350 #352 #353 #354 #967–#971 #1098 #1107 #213

其中我已给出可直接接手的数据：
- **#323**：CoreApp 全量 **23 文件 / 54 用例失败**，**全部确定性**（隔离重跑数目一致）——所以 issue 的「分类三：不确定性测试」在这批里是空的。6 个带 `[ path ]` 后缀是**收集期错误**，修法与另外 17 个完全不同，应先切开。
- **#1107**：审计报告**没有生成器**；机制是「选择性刷新」——08-07 的 11 条里 5 条与 08-06 逐字节相同。建议给每条 bullet 标注最后验证日期。
- **#1098**：Nuxt 4.5 / devtools 3.x 卡在 `unhead` 2→3 大版本迁移；**Route B 更便宜**——`vue-sonner` 是 Nuxt 进入 CoreApp 生产闭包的原因。

## 三、本轮新增的守卫（都自带 `--self-test` 正对照）

| 命令 | 防什么 |
|---|---|
| `pnpm check:build-allowlist` | allowlist 陈旧项 / 未审阅的安装脚本 |
| `pnpm check:doc-metadata` | README 与 LICENSE/manifest 漂移、根缺 homepage |
| `pnpm -C apps/core-app check:search-index-writers` | 绕过单写者契约的新写入 |
| `pnpm -C apps/nexus typecheck` | Volar 插件静默丢失（vue-tsc 打印错误仍 exit 0） |
| `apps/core-app/scripts/file-scan-memory-benchmark.mjs` | 扫描内存上界（可加参数扩展）|

每个都有一条断言「真实树是干净的」——**什么都匹配不到的扫描器，看起来和干净仓库一模一样**。
