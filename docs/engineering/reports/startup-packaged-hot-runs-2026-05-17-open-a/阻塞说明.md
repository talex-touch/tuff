# Packaged Startup Blocker Note

## Context

- Date: 2026-05-17
- App artifact: `apps/core-app/dist/mac-arm64/tuff.app`
- Artifact version: `2.4.10-beta.23`
- Current repo baseline: `2.4.10-beta.25`

## Result

This artifact cannot provide valid packaged startup timing evidence.

- `open -W -n -j -a apps/core-app/dist/mac-arm64/tuff.app` exits before app startup with `NSOSStatusErrorDomain Code=-10827` / `kLSNoExecutableErr`.
- Direct executable launch via `apps/core-app/dist/mac-arm64/tuff.app/Contents/MacOS/tuff` aborts before JS startup; the macOS crash report shows `SIGABRT` in AppKit/HIServices during application registration.
- No isolated benchmark `userData/tuff/logs` directory is created, so `Startup health check passed`, `Renderer ready`, and `StartupAnalytics.totalStartupTime` cannot be measured from this artifact.

## Next Action

Rebuild or provide a current `2.4.10-beta.25+` packaged artifact, then rerun:

```sh
TUFF_STARTUP_REPORT_DATE="2026-05-17" pnpm -C "apps/core-app" run startup:bench:packaged:hot
TUFF_STARTUP_REPORT_DATE="2026-05-17" pnpm -C "apps/core-app" run startup:bench:packaged:cold
```
