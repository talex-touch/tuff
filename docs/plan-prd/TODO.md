# Tuff TODO

> 更新时间：2026-06-19
> 定位：当前 2 周执行清单。历史完整清单已归档到 `docs/plan-prd/docs/archive/TODO-pre-compression-2026-05-14.md`；长期债务见 `docs/plan-prd/docs/TODO-BACKLOG-LONG-TERM.md`。

## 当前执行窗口

- 当前基线：`2.4.10`；当前代码版本已到 root / CoreApp `2.4.12-beta.8`，当前本地 `HEAD=6b156fa31 fix(nexus): hide team credit pool for personal accounts`，且工作区存在大量未提交改动，必须按 related-only 拆分验证；当前短期执行 SoT 为 `docs/plan-prd/04-implementation/Roadmap-vNext-2026-06-18.md` 与 `docs/plan-prd/04-implementation/Current-Execution-Plan-2026-06-17.md`。GitHub Actions release matrix、GitHub prerelease 创建与 Nexus BETA latest sync 已成功的最近完整链路仍以 `2.4.11-beta.6` 发布后 Gate D strict 复核为证据；`2.4.11` release checklist 已记录 `quality:pr` / `publish:check` / `publish:check:pack` / `typecheck:all` / `quality:release` / `git diff --check` 通过，但 Nexus 资产 sha256/signatureUrl、signature endpoint 仍是 R1 Release Integrity debt；公共包发布不再作为独立 Roadmap / blocker / evidence 项，版本变更后以 GitHub 自动发版 workflow 结果为准。
- 当前主线：按 Roadmap vNext 推进 R0-R9；先完成口径清理，再补 R1 Release Integrity 与 R2 AI packaged Electron 文本/OCR evidence。owner 已完成的平台人工验证不再作为 Roadmap 待办或 blocker。
- 下一版本门槛：`2.5.0` AI 桌面入口收口，Stable 只承诺 CoreBox 文本 + 显式 OCR、provider routing 与固定失败路径；OmniPanel Writing Tools 为 MVP/Beta，Workflow/Review Queue/Skills/Automation 保持 Beta。当前 AI 不能仅凭 SDK/schema/focused tests 宣称体验闭环，必须补 packaged Electron 文本/OCR成功、固定失败路径与 Local/Ollama routing 证据；2026-06-19 必修项见下方 “AI Stable 收口 TODO”。
- 执行约束：PR lint 已收敛为 changed-file lint；`quality:release` 仍保留全仓 lint/build，需单独按 release 口径收口；近期自动化审计未发现新的 P0 fixed fake-success，AI compat live tree 已通过 retired intelligence endpoint 410 合同测试钉住 fail-closed 行为；旧 compat registry / legacy allowlist / size allowlist 已退场，不再作为 live SoT。公共包发布不再作为独立 Roadmap blocker，版本变更后以 GitHub 自动发版 workflow 结果为准；owner 已完成的平台人工验证不再作为待办追踪。
- 2026-05-23 Nexus Data Governance 8 项进度与截图分支清理计划已落到 `docs/plan-prd/01-project/NEXUS-DATA-GOVERNANCE-PROGRESS-2026-05-23.md` 和 `docs/plan-prd/04-implementation/ActiveGoalBranchCleanup-2026-05-23.md`；当前口径仍是 in progress，不把本地代码/单元测试证据等同于生产完成。
- 2026-06-07 当前工作区收口口径：当时 `master` 与 `origin/master` 已同步（`origin/master...HEAD` 为 `0 0`），`HEAD` 为 `686ec013e fix(cli): refresh publish auth before upload`。此前 TuffEx/Nexus/renderer/Intelligence UI dirty slice 已收口，不再把“清理大脏 worktree”作为 blocker；TuffEx/CoreApp dialog trusted HTML boundary、Widget sandbox evidence、AI provider 设置页 FlipDialog、Local Skills provider gate、PreviewSDK benchmark/parser boundary、DatePicker adaptive field calendar 与 CLI publish 旧 token 刷新均已按 related-only 小切片提交推送。
- 2026-06-07 整体完成情况与下一步总览：提交节奏已恢复为“小功能完成即提交推送”。AI 侧已收口 provider 设置页交互与 Local Skills provider 门控展示，但 2.5.0 Stable 仍缺 packaged Electron 文本/OCR 成功与失败路径 evidence；搜索/预览侧已完成 PreviewSDK failure summary、parser boundary 与 14-case benchmark，Search Provider / Indexed Source 抽象仍需继续推进 File write/store、Browser Bookmarks official lifecycle、Everything productionization 与 Quicklinks 持久 feed；TuffEx 侧已完成 on-demand style/export/audit、trusted dialog HTML boundary、DatePicker adaptive field calendar 与 visual smoke evidence，下一步转向主路径语义控件和示例插件全量样式入口清理；Nexus 侧已有 Data Governance / Provider Registry / release notes sanitizer / docs render 构建证据，下一步聚焦数据收集治理闭环、文档渲染 SEO metadata/prerender evidence 与 provider registry 生产/preview evidence。
- 2026-06-01 当前验证口径：最近 Indexing Runtime 小切片已通过 `IndexedSourceSnapshotCacheService` / Browser Bookmarks / TuffEx Tabs focused tests、CoreApp `typecheck:node` 与 `git diff --check`；后续继续改代码前先确认未跟踪文件状态，并在每个提交批次记录最贴近改动的 focused test 或明确说明仅文档变更。
- 2026-06-02 当前审计口径：新增 UI/兼容/占位增量审计报告，继续未发现生产路径 P0 fixed fake-success；当前最需要先证实的是 TuffEx `base.css`、组件子路径 `style.css`、on-demand style plugin 与 dev/prod alias 的一致性。当前 shell 的 `node` 为 Codex `v24.14.0`，且 `pnpm` / `corepack` 不在 `PATH`，不符合仓库 Volta Node `22.16.0` 与 `pnpm@10.32.1` 约束；本轮只确认 `git diff --check` 通过，TuffEx build/audit/typecheck 必须在正常开发环境补跑。
- 2026-06-02 UI/安全小切片已落地：`SharedPluginDetailReadme` 与 update release notes 已走统一 safe Markdown renderer；preload loading overlay 已改为 `textContent` / `createElement`；CoreApp 已新增 `UiPreferenceStorage` 并迁移 plugin widget preview size 与 group block expand state；`FeatureCard`、`AppList`、`SectionItem`、`PluginNew` 已完成首批语义控件/focus/keyboard 切片；cloud preset singleton 已改为 `reason: "not-shipped"` 的 fail-closed unavailable contract；Nexus `deviceId` 明确为 local web marker，privacy settings 明确为本地浏览器偏好。Focused tests 已新增，但当前 Codex Node `v24.14.0` + Rollup optional native module 签名问题阻塞 Vitest，需在 Volta Node `22.16.0` + `pnpm@10.32.1` 环境补跑。
- 2026-06-03 当前审计口径：新增 UI/兼容/占位增量审计报告，继续未发现生产路径 P0 fixed fake-success；Markdown sanitizer、preload DOM 构建、cloud preset fail-closed、TuffEx README 按需推荐已经进入实现切片；剩余高信号点是 TuffEx dev/prod 样式一致性、HTML trusted boundary、Widget runtime sandbox evidence 与 UI 语义控件。
- 2026-06-04/05 当前审计口径：Nexus TuffEx public docs 推荐导入已改为 `base.css` + 组件子路径 + 局部 `style.css`，Nexus release notes `notesHtml` 已接共享 sanitizer；TuffEx build/audits、Nexus typecheck/build 与 PreviewSDK focused test 已在 Node `22.16.0` + pnpm `10.32.1` 通过。
- 2026-06-06 当前审计口径：新增 UI/兼容/占位增量审计报告，继续未发现生产路径 P0 fixed fake-success；TuffEx/CoreApp dialog trusted HTML boundary 与 Widget runtime sandbox evidence 已完成 focused 切片；剩余高信号点收敛为主路径 `div/span @click` 语义控件与 File write/store boundary。
- 2026-06-07 当前审计口径：新增 UI/兼容/占位增量审计报告，继续未发现生产路径 P0 fixed fake-success；当时确认 `HEAD=686ec013e`、root/CoreApp `2.4.11-beta.7`、`@talex-touch/tuff-cli@0.0.7`。`tuffcli publish` 的过期 app JWT 自动刷新、API key scope preflight、`TUFF_NON_INTERACTIVE=1` fail-closed 与 upload POST device headers 已有 focused tests 和 Nexus auth probe 合同证据；公共包发布后续统一以 GitHub 自动发版 workflow 结果为准。
- 2026-06-13 当前审计口径：`touch-music` / `touch-image` 已完成 starter asset、模板 README、时间戳配置残留与 `touch-music` 全量 TuffEx 样式入口清理；继续未发现新的生产路径 P0 fixed fake-success。本轮 vNext 新增 `docs/plan-prd/04-implementation/Roadmap-vNext-2026-06-18.md` 作为 R0-R9 收口清单，优先完成 R0 口径清理、R1 Release Integrity、R2 AI Stable 与 R3 Indexing Runtime；2026-06-16 已补 Windows UWP `explorer.exe shell:AppsFolder` handoff 早退不误报、Everything 授权根 slash/case 回归与 Windows 本地图标 `tfile` round-trip focused coverage。
- 2026-06-13 Nexus docs SEO/prerender 切片：docs 页面已补 OG siteName/locale/image、Twitter large image、canonical/alternate/robots 与 TechArticle JSON-LD 口径，并抽出可测 SEO head helper；`no_prefix` 继续作为全站 i18n 策略，docs 过渡方案为显式 `/en/docs/**`、`/zh/docs/**` route + 页内 locale 同步。prerender evidence helper 复用实际 Nexus route 枚举并验证真实 `content/docs` 根索引、developer quickstart、components index、guide start 与 docs API；`build/check-worker-bundle.mjs` 静态路由期望同步到 `/en/docs`/`/zh/docs`。已通过 focused prerender/SEO Vitest 与 scoped ESLint，生产/preview 抓取或 visual smoke 仍按后续条件恢复。
- 2026-06-13 Nexus Agent 首页叙事同步：公开站首页已从“电影感/泛效率工具”收敛为“本地优先的桌面 Agent 指令中心”，但本轮只改 Nexus 文案与轻量展示结构，不改变 CoreApp 运行时能力。TODO 与 Roadmap 统一口径：模型路由进入 `2.5.0` Stable provider routing evidence；桌面上下文、Agent 工具执行进入 MVP/Beta 证据路线，不替代 CoreBox 文本/OCR Stable evidence；全键盘、文件搜索按既有 CoreBox/Search 主线继续补平台 evidence；CDP 浏览器控制、可视化交互、Token 节省、Skills、Computer Use、MiniApp、ACP、自动化与沙箱只能作为 Beta/Experimental 或后续预告，不写入 Stable 已交付承诺。
- 2026-06-13 Plugin widget surface follow-up：CoreApp widget manager 已复用缓存注册 payload 避免重复预编译读取/运行时编译，renderer widget registry 对同一 `widgetId + hash` 幂等跳过重复注册；`interaction.forceMax` 明确为 CoreBox 最大高度的显式合同，普通 widget 不再按类型默认强制最大高度，Clipboard History 与 Translation 全高官方 widget 已在 manifest 显式声明。Focused tests 覆盖 widget manager、widget registry、plugin feature activation 与 CoreBox resize。
- 2026-06-16 CoreBox send-mode follow-up：`interaction.sendMode` 进入 Plugin SDK / Tuff DSL 合同，widget 默认发送模式，非 widget feature 可显式启用；激活期间 CoreBox 右侧主操作由 pin 替换为 send，避免 feature composer 被固定窗口语义抢占。Nexus 长期授权二次认证跳转期间不再上报 browser closed，防止 App device-auth poll 误判取消。
- 2026-06-14 Plugin widget package integrity evidence：packaged precompiled widget 注册前已校验 `metaPath` JSON 与 manifest `build.widgets` entry 的 `featureId` / `widgetId` / `sourcePath` / `compiledPath` / `hash` / `runtime` / `runtimeStage` 对齐；不匹配或 malformed meta 返回 `WIDGET_PRECOMPILED_INTEGRITY_MISMATCH`，不读取源码、不运行时编译，并清理缓存后广播 widget failure。该切片只补 package integrity focused coverage，renderer visual smoke 与真实 packaged plugin matrix 仍后置。
- 2026-06-14 Plugin trust-boundary evidence：新增仓库官方插件 manifest focused test，钉住 supported `sdkapi`、permission reasons、显式 `manifest.searchProviders`、`search.root-results` consent gate、高风险 shell provider scope 映射、显式 `interaction.forceMax` 与 metadata-only indexed source 声明；同日补齐 package-level SDK marker test 口径，确认 `260615` 为 current marker、`260428` 仍受支持，且当前只有 `touch-intelligence` 按真实 runtime 需要迁到 newest marker，避免把官方插件 manifest 迁移误写成盲目批量升级；同时补 widget activation 默认不 force max 的运行时回归，以及 Everything indexed source privacy-safe backend/path-filtering/root-count evidence；Everything indexed source 现在在 Windows enabled/available 但无授权 File roots 时显式降级为 `indexing-root-policy-file-roots-empty`，evidence 仍只暴露 root count 与 path-filtering 统计，不暴露 root path。同日继续补 `touch-dev-utils`、`touch-text-tools` 与 `touch-translation` 的 `clipboard.write` 执行期 request/block focused evidence，并新增 plugin renderer `ClipboardEvents.write` transport guard 回归，确认 SDK/Surface 写剪贴板路径在权限拒绝时不会进入 clipboard mutation handler；`touch-intelligence:intelligence-ask` 新 widget 已纳入显式 full-height allowlist。后续扫描已确认官方插件生成的辅助复制动作不再使用宿主 `copy` action，残留 `type: "copy"` 仅作为旧 item 兼容处理或测试夹具。同日 `touch-translation` provider config 已补 secret 写入失败 fail-closed：`usePluginSecret()` 持久化失败时普通 storage 不更新，运行时 provider config 也不保留本次提交的明文 secret；配置弹窗保存 secret provider 前会刷新 secure-store health，不可用时阻止保存并显示原因。`touch-snipaste`、`touch-window-presets`、`touch-workspace-scripts`、`touch-quick-actions`、`touch-system-actions`、`touch-browser-open`、`touch-window-manager`、`touch-browser-data`、`touch-browser-bookmarks`、`touch-batch-rename`、`touch-snippets` 与 `touch-dev-toolbox` 执行期 shell/network/fs/clipboard permission block 现在保留 `permission-denied`、`permission-sdk-unavailable`、`permission-request-failed` 等具体原因，并补 package-level official plugin focused coverage；`touch-system-actions` 仍保持 `open-main-window` 为不依赖 shell permission 的原生窗口动作。该批切片只作为插件可信边界最近路径证据，不宣称 release integrity、真实平台 Everything、系统 secure-store 后端 evidence、真实浏览器读取/打开/文件重命名/剪贴板写入/窗口操作/Snipaste/工作区命令/系统快捷动作平台执行或 durable job history 已闭环。
- `P1-I18N-LEXICON-CATALOG`：Tuff 2.6.0 i18n / Domain Lexicon / Cloud Catalog 收敛方向已定，并新增 `docs/plan-prd/03-features/i18n-lexicon-catalog-2.6.0-prd.md`。先落 locale core、LocalizedText/LocalizedList、单位 Domain Lexicon、插件 SDK facade 与 CatalogService baseline；新增 UI 文案禁止中文 fallback，新增领域数据禁止散写 locale 分支，cloud catalog 必须签名/hash/schema 校验后导入 SQLite。该项目前只作为 2.6.0 规划与质量约束，不宣称运行时已实现。
- 2026-06-06 PreviewSDK benchmark 边界切片：benchmark 结果已新增 ability mismatch / budget exceeded failure summary；AdvancedExpression 已收紧白名单字符/函数边界，避免抢占 TimeDelta 与 Percentage；常见高级函数/常量/幂运算先走静态 fast path 再 fallback `mathjs`；TimeDelta 已拒绝 `now + abc` 这类无效相对时间假成功并支持 `1小时30分钟后` 中文组合时长，Percentage 已支持 `100 增加 20%` 中文增减表达，UnitConversion `canHandle()` 已收紧为已知同类别单位；本地 `benchmark:preview` 当前 14/14 matched、0 budget exceeded，P50 ~0.37ms、P95/max ~0.69ms。
- 2026-06-06 Local Skills Provider 门控展示切片：设置页已把 ready / approval-required / unavailable / installed high-risk provider 统计收敛到可测 helper，未安装 gated provider 不再进入“已安装高风险需审批”提示，同时把已关联 scene hints 直接展示在 skill chip 中；focused Vitest、scoped ESLint、CoreApp `typecheck:web` 与 `git diff --check` 已通过。
- 2026-06-06 AI Provider 设置页交互切片：`IntelligenceModelConfig` 的模型管理、默认模型选择与系统指令配置已从 legacy drawer 切到共享 `FlipDialog`，保持 provider 配置数据流、模型拉取/添加/选择处理与 `intelligenceSettings.updateProvider()` 持久化行为不变；已通过 scoped ESLint、CoreApp `typecheck:web` 与 `git diff --check`。
- 2026-06-18 AI 2.5.0 Step 0/1 已收口：Step 0 锁定 Stable 只覆盖 CoreBox 文本 + 显式 OCR、provider routing 与固定失败路径；Step 1 最近路径自动化已通过 CoreApp AI focused tests、`plugins/touch-intelligence` build、`packages/tuffex` typecheck 与 scoped `git diff --check`。AI 不是空壳，CoreApp 已有 Intelligence module、provider runtime、workflow service、agent/tool channels，OmniPanel Writing Tools 与 Assistant typed transport 也已有 dev 切片；但不能宣称 Stable 体验闭环。下一步是补 `2.5.0` CoreBox 文本/OCR packaged Electron evidence、Nexus invoke 未登录/provider 不可用/quota 不足/model 不支持、permission denied、Local/Ollama routing、provider metadata chips 与 packaged Electron UI evidence。OmniPanel Writing Tools、Workflow Use Model、Review Queue 与模板中心保持 MVP/Beta 证据队列，不能替代 Stable 文本/OCR evidence。`2.5.3` / `2.5.4` / `2.5.5` / `2.5.8` 维持 PRD 锁方向，不抢 `2.5.0` 稳定化窗口。
- 2026-06-19 QuickOps Flow sessions / stop-all-sessions / system-info / tuff-diagnostics / disk-space / directory-usage / network-status / battery-status / system-proxy / public-ip / dns-query / file-hash / file-base64 / recent-download / common-directory / path-format / temp-text-file / temp-directory 切片：`QuickOpsModule` 已新增 Flow target `quickops.sessions`、`quickops.stop-all-sessions`、`quickops.system-info`、`quickops.tuff-diagnostics`、`quickops.disk-space`、`quickops.directory-usage`、`quickops.network-status`、`quickops.battery-status`、`quickops.system-proxy`、`quickops.public-ip`、`quickops.dns-query`、`quickops.file-hash`、`quickops.file-base64`、`quickops.recent-download`、`quickops.common-directory`、`quickops.path-format`、`quickops.temp-text-file` 与 `quickops.temp-directory`，分别复用 CoreBox `QuickOpsSessionManager.list()` 的只读快照、provider `stopAllSessions()`、`createSystemInfo()` / `formatSystemInfo()`、provider `getDiagnosticsInfo()` 与 `formatDiagnosticsInfo()`、`createDiskSpaceInfo()` / `formatDiskSpaceInfo()`、`createDirectoryUsageInfo()` / `formatDirectoryUsageInfo()`、`createNetworkStatusInfo()` / `formatNetworkStatusInfo()`、`createBatteryStatusInfo()` / `formatBatteryStatusInfo()`、`createSystemProxyInfo()` / `formatSystemProxyInfo()`、`lookupPublicIp()`、`parseDnsQuery()` / `createDnsQueryInfo()` / `formatDnsQueryInfo()`、`computeFileHashes()`、`encodeFileBase64()`、`findRecentDownloadFile()`、`resolveCommonDirectory()`、`createFilePathInfo()` / `resolveFilePathTarget()`、`createTempTextFile()` 与 `createTempDirectory()` 返回当前 QuickOps 运行会话快照、确认型停止全部 QuickOps 运行会话结果、本地 OS/platform/CPU/memory/uptime/load average 摘要、脱敏 Tuff 诊断摘要、Home/Tuff Data 文件系统容量摘要、关键目录 bounded shallow/deep 占用摘要、本地地址 / DNS server / 环境变量代理摘要、本地电池状态摘要、本地脱敏代理摘要、显式 opt-in 公网 IP 查询结果、基础 / deep DNS 记录、单文件 MD5 / SHA1 / SHA256、1MB 上限内单文件 Base64 编码、Downloads 首层最近普通文件元数据、受限常用目录元数据、原始路径 / Shell 路径 / file URL / Windows-WSL 互转格式，以及仅写入 Tuff 临时工作区的文本文件 / 目录创建结果；`quickops.sessions` / `quickops.stop-all-sessions` 不泄露 BrowserWindow / timeout runtime 对象，`quickops.disk-space`、`quickops.directory-usage`、`quickops.battery-status`、`quickops.system-proxy`、`quickops.public-ip`、`quickops.dns-query`、`quickops.file-hash`、`quickops.file-base64`、`quickops.recent-download`、`quickops.path-format`、`quickops.temp-text-file` 与 `quickops.temp-directory` 读取、探测、解析或写入失败会返回 degraded ack。Flow `fileBase64` 仅包含编码，不包含 Base64 decode-to-temp-file 写入能力；Flow `recentDownload` 仅返回元数据，不打开、不移动最近下载文件；Flow `commonDirectory` 仅返回受限目录元数据，不打开目录、不写剪贴板；Flow `tempTextFile` / `tempDirectory` 标记 `requireConfirm=true`，只写入 `app.getPath('temp')/tuff-quickops` 且不打开目录、不写剪贴板；Flow `publicIp` 标记 `requireConfirm=true` 且必须 payload 显式 `allowLookup: true` 才会外联，否则返回 `public-ip-disabled`；Flow `stopAllSessions` 标记 `requireConfirm=true`，只释放已有 QuickOps runtime 会话，不执行端口 kill、文件操作或系统长期设置变更。现有结构化 Flow target 列表已同步到 QuickOps PRD，但真实 AI UI 自然语言编排 evidence、真实确认 UI / evidence、高风险执行治理、企业策略与真实平台 evidence 仍后置。
- 2026-06-19 QuickOps / Flow `requireConfirm` dispatcher 门禁切片：`FlowDispatchOptions` 与 Flow consent transport response 已新增 `confirmationToken` / `requiresConfirmation` 合同；`FlowBus.dispatch()` 对所有 `requireConfirm=true` target 在 delivery 前强制消费 app UI 发放的一次性 confirmation token，持久 sender consent 不能绕过本次确认，token 复用会 fail-closed 为 `flow-confirmation-required`；`FlowSelector` 在 `requiresConfirmation` 时不会直接选择目标，而是通过 app UI `grantConsent` 返回 `confirmationToken` 后再 dispatch；同一切片保留同步 ack 返回，避免本地 delivery handler 已 ACK 后被覆盖成 DELIVERED。该切片只是执行门禁，不替代真实确认 UI 文案/取消路径/截图录屏 evidence，不开放端口 kill、批量文件操作或长期系统设置变更。
- 2026-06-19 QuickOps / Flow confirmation UI 文案切片：`FlowSelector` 已根据 `requiresConfirmation` 与 sender consent 状态区分普通授权、本次执行确认、授权并确认三类标题/说明/按钮；只需本次确认时仅保留拒绝与确认发送，授权+确认时提供“仅本次允许并发送”和“授权并发送”，focused renderer test 覆盖 requireConfirm target 不直发、取消不发 token / 不 dispatch、确认后携带 `confirmationToken` 选择目标，以及缺少 consent 时展示组合授权确认动作。该切片仍只覆盖 UI 文案与基础取消 affordance，不替代真实确认 UI 截图/录屏 evidence、企业策略或高风险治理。
- 2026-06-19 QuickOps sessions SDK facade 切片：`QuickOpsEvents.sessions.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 Flow 的脱敏 session snapshot response；`createQuickOpsSdk().sessions()`、插件 `quickOps.sessions()` 与 `plugin.quickOps.sessions()` 返回同一结构化运行态快照，不开放插件执行 API，也不替代 packaged quit、真实确认 UI 或真实运行平台 evidence。
- 2026-06-19 QuickOps systemInfo SDK facade 切片：`QuickOpsEvents.systemInfo.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的本地 `createSystemInfo()` / `formatSystemInfo()`；`createQuickOpsSdk().systemInfo()`、插件 `quickOps.systemInfo()` 与 `plugin.quickOps.systemInfo()` 返回同一结构化本地系统摘要，不读敏感路径、不外联、不开放执行 API，也不替代真实平台 system-info evidence。
- 2026-06-19 QuickOps tuffDiagnostics SDK facade 切片：`QuickOpsEvents.tuffDiagnostics.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `getDiagnosticsInfo()` 与 `formatDiagnosticsInfo()`；`createQuickOpsSdk().tuffDiagnostics()`、插件 `quickOps.tuffDiagnostics()` 与 `plugin.quickOps.tuffDiagnostics()` 返回同一脱敏诊断摘要，不读取日志内容、不复制完整配置、不开放执行 API，也不替代真实排障 evidence。
- 2026-06-19 QuickOps diskSpace SDK facade 切片：`QuickOpsEvents.diskSpace.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `createDiskSpaceInfo()` 与 `formatDiskSpaceInfo()`；`createQuickOpsSdk().diskSpace()`、插件 `quickOps.diskSpace()` 与 `plugin.quickOps.diskSpace()` 返回同一本地 Home/Tuff Data 文件系统容量摘要或 degraded 详情，不开放执行 API，也不替代真实平台目录权限 evidence。
- 2026-06-19 QuickOps directoryUsage SDK facade 切片：`QuickOpsEvents.directoryUsage.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `createDirectoryUsageInfo()` 与 `formatDirectoryUsageInfo()`；`createQuickOpsSdk().directoryUsage({ deep? })`、插件 `quickOps.directoryUsage({ deep? })` 与 `plugin.quickOps.directoryUsage({ deep? })` 返回同一 bounded 关键目录 shallow/deep 占用摘要或 degraded 详情，不开放任意目录扫描或执行 API，也不替代真实平台目录权限 evidence。
- 2026-06-19 QuickOps queryLocalIp SDK facade 切片：`QuickOpsEvents.queryLocalIp.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `getLocalIpAddresses()` 与 `formatLocalIpInfo()`；`createQuickOpsSdk().queryLocalIp()`、插件 `quickOps.queryLocalIp()` 与 `plugin.quickOps.queryLocalIp()` 返回同一本机非 internal 地址列表或 `local-ip-unavailable` degraded reason，不外联、不开放执行 API，也不替代真实平台网络 evidence。
- 2026-06-19 QuickOps portStatus SDK facade 切片：`QuickOpsEvents.portStatus.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `parsePortQuery()`、`probeLocalTcpPort()` 与 `createPortReleaseCommand()`；`createQuickOpsSdk().portStatus({ port/text })`、插件 `quickOps.portStatus({ port/text })` 与 `plugin.quickOps.portStatus({ port/text })` 返回本地 TCP 端口 available / occupied / degraded 状态和 copy-only 释放命令文本，不执行 kill / Stop-Process、不开放执行 API，也不替代真实端口 kill / 二次确认 UI evidence。
- 2026-06-19 QuickOps dnsQuery SDK facade 切片：`QuickOpsEvents.dnsQuery.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `parseDnsQuery()`、`createDnsQueryInfo()` 与 `formatDnsQueryInfo()`；`createQuickOpsSdk().dnsQuery({ hostname/text, deep? })`、插件 `quickOps.dnsQuery({ hostname/text, deep? })` 与 `plugin.quickOps.dnsQuery({ hostname/text, deep? })` 返回本地 resolver 基础 / deep DNS 记录或 degraded 详情，不外联 HTTP、不修改系统 DNS、不开放执行 API，也不替代真实平台 DNS evidence。
- 2026-06-19 QuickOps fileHash SDK facade 切片：`QuickOpsEvents.fileHash.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `resolveFileHashPath()` 与 `computeFileHashes()`；`createQuickOpsSdk().fileHash({ path/filePath/text })`、插件 `quickOps.fileHash({ path/filePath/text })` 与 `plugin.quickOps.fileHash({ path/filePath/text })` 返回单个本地文件 MD5 / SHA1 / SHA256 或 degraded 详情，不写文件、不做 Base64 decode、不开放执行 API，也不替代真实平台文件权限 / 大文件 evidence。
- 2026-06-19 QuickOps fileBase64 SDK facade 切片：`QuickOpsEvents.fileBase64.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `resolveFileBase64Path()` 与 `encodeFileBase64()`；`createQuickOpsSdk().fileBase64({ path/filePath/text })`、插件 `quickOps.fileBase64({ path/filePath/text })` 与 `plugin.quickOps.fileBase64({ path/filePath/text })` 返回 1MB 上限内单个本地文件 Base64 或 degraded 详情，不包含 Base64 decode-to-temp-file 写入能力、不开放执行 API，也不替代真实平台文件权限 / 大文件 / 解码写入 evidence。
- 2026-06-19 QuickOps recentDownload SDK facade 切片：`QuickOpsEvents.recentDownload.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `findRecentDownloadFile()`；`createQuickOpsSdk().recentDownload()`、插件 `quickOps.recentDownload()` 与 `plugin.quickOps.recentDownload()` 仅返回 Downloads 首层最近普通文件元数据或 degraded 详情，不打开、不移动、不复制文件，也不替代最近下载文件真实平台打开 / 移动 evidence。
- 2026-06-19 QuickOps commonDirectory SDK facade 切片：`QuickOpsEvents.commonDirectory.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `resolveCommonDirectory()`；`createQuickOpsSdk().commonDirectory({ query/folder/directory/id/text })`、插件 `quickOps.commonDirectory({ query/folder/directory/id/text })` 与 `plugin.quickOps.commonDirectory({ query/folder/directory/id/text })` 仅返回 Desktop/Downloads/Documents/App Data/Logs 等受限常用目录元数据，不打开目录、不写剪贴板、不开放任意路径枚举，也不替代真实平台系统打开 evidence。
- 2026-06-19 QuickOps pathFormat SDK facade 切片：`QuickOpsEvents.pathFormat.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `resolveFilePathTarget()` 与 `createFilePathInfo()`；`createQuickOpsSdk().pathFormat({ path/filePath/text })`、插件 `quickOps.pathFormat({ path/filePath/text })` 与 `plugin.quickOps.pathFormat({ path/filePath/text })` 仅返回原始路径 / Shell 路径 / file URL / Windows-WSL 互转文本，不检查文件存在、不打开、不写剪贴板，也不替代真实文件搜索结果动作 evidence。
- 2026-06-19 QuickOps formatText SDK facade 切片：`QuickOpsEvents.formatText.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 Flow 的 bounded text transform helper；`createQuickOpsSdk().formatText({ text/value/content, mode/format })`、插件 `quickOps.formatText({ text/value/content, mode/format })` 与 `plugin.quickOps.formatText({ text/value/content, mode/format })` 仅返回 upper/lower/camel/snake/kebab 本地纯转换结果或 `missing-text` skipped 详情，不读写剪贴板、不外联、不开放执行 API，也不替代真实 AI UI 自然语言编排 evidence。
- 2026-06-19 QuickOps networkStatus SDK facade 切片：`QuickOpsEvents.networkStatus.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `createNetworkStatusInfo()` 与 `formatNetworkStatusInfo()`；`createQuickOpsSdk().networkStatus()`、插件 `quickOps.networkStatus()` 与 `plugin.quickOps.networkStatus()` 返回同一本地非 internal 地址、DNS server 与环境变量代理摘要，不外联、不开放执行 API，也不替代真实平台网络 evidence。
- 2026-06-19 QuickOps batteryStatus SDK facade 切片：`QuickOpsEvents.batteryStatus.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `createBatteryStatusInfo()` 与 `formatBatteryStatusInfo()`；`createQuickOpsSdk().batteryStatus()`、插件 `quickOps.batteryStatus()` 与 `plugin.quickOps.batteryStatus()` 返回同一本地电量 / 充电状态摘要或 degraded 详情，不开放执行 API，也不替代真实平台电池 evidence。
- 2026-06-19 QuickOps systemProxy SDK facade 切片：`QuickOpsEvents.systemProxy.get` 已作为 typed transport 只读事件接入 `QuickOpsModule`，复用 CoreBox/Flow 的 `createSystemProxyInfo()` 与 `formatSystemProxyInfo()`；`createQuickOpsSdk().systemProxy()`、插件 `quickOps.systemProxy()` 与 `plugin.quickOps.systemProxy()` 返回同一本地脱敏系统代理摘要或 degraded 详情，不外联、不修改系统代理、不开放执行 API，也不替代真实平台系统代理 evidence。
- 2026-06-19 QuickOps evidence gate 切片：CoreApp 新增 `quickops-evidence/v1` manifest/template/strict verifier 与 `quickops:evidence:template` / `quickops:evidence:verify` 脚本，覆盖 packaged session cleanup、screen-clean 视觉、macOS/Windows/Linux 只读 network/system、files/temp、真实确认/高风险/企业策略、SDK/transport surface、Flow/AI adapter 六类七个 evidence case；`quickops:evidence:template --writeChecklist --writePlan` 会默认写入 manifest、checklist 与 collection plan，`--output` 可覆盖 manifest 路径；strict gate 要求 passed 状态、artifact path、视觉截图/录屏、逐项 checklist、真实存在且非空 artifact 才能通过；packaged cleanup case 额外要求 `quickops-packaged-session-cleanup/v1` JSON artifact，解析 packaged build、manifest version、isolated userData、正常 quit、session 覆盖、清理后 power blocker / overlay / timer / stopwatch 归零和 runtime object / raw userData redaction，拒绝 dev-only、external kill、覆盖不足或清理未完成 artifact；platform readonly case 额外要求 `quickops-platform-readonly/v1` JSON artifact 集合覆盖 macOS/Windows/Linux 三个平台，解析 artifact 是否来自当前平台、local-only 网络约束、外部 HTTP 计数为 0、端口不执行 kill、DNS 使用本地 resolver 且不修改系统 DNS、系统代理凭据脱敏、battery/disk/directory degraded reason 和 Home/userData/proxy redaction，拒绝平台混用、外联、fake success 或缺失 reason；files/temp case 额外要求 `quickops-files-and-temp/v1` JSON artifact，解析 file hash/base64 正常与 degraded 边界、recent download found/empty/permission/duplicate/explicit absolute move target、common directory allowlist、path format raw/shell/file URL/Windows-WSL 且无打开/剪贴板副作用、temp workspace 写入边界和 Home/userData/file contents redaction，拒绝 Base64 原始 payload、decode-to-temp-file、任意目录、覆盖移动目标、temp workspace 越界或缺失 degraded reason；confirmation/policy case 额外要求 `quickops-confirmation-policy/v1` JSON artifact，解析真实用户确认面、visual artifact refs、完整 `requireConfirm` target 覆盖、state/file confirm 与 cancel 路径、confirmation token、本地策略 disabled reason、diagnostics visibility、高风险默认阻断和 payload/clipboard/file/Home/userData redaction，拒绝 metadata-only 确认、取消后 dispatch、高风险直接执行、策略 reason 漂移或缺少真实视觉引用；`quickops:surface:audit --strict` 可生成 SDK/transport surface JSON artifact，并纳入 TouchPlugin runtime 注入 facade 方法集与 typed transport invoke 映射；`quickops:flow-ai:audit` 可生成 `quickops-flow-ai-adapter-audit/v1` JSON artifact，校验 Flow target 目录、`requireConfirm` 集合、结构化 dispatch 覆盖、策略阻断 ACK、audit/clipboard 脱敏、degraded ACK、Flow confirmation token 路径和 plugin-side Flow dispatch adapter / trace / runtime dispatch bridge 合同；manifest required evidence 也要求 runtime facade 暴露同一只读方法集并只调用 `QuickOpsEvents.*`，且 Flow/AI adapter artifact 必须存在插件侧 Flow dispatch adapter、request-target-confirmation-result trace、高风险阻断和 runtime dispatch bridge evidence；strict verifier 会解析 `quickops-packaged-session-cleanup/v1`、`quickops-platform-readonly/v1`、`quickops-files-and-temp/v1`、`quickops-confirmation-policy/v1`、`quickops-surface-audit/v1` 与 `quickops-flow-ai-adapter-audit/v1` artifact，拒绝 `gate.passed=false`、runtime facade 漂移或 runtime bridge 缺失。该切片只提供采集与验收门禁，不生成伪 evidence；当前 Flow 源级 coverage、插件侧 Flow dispatch adapter/trace 与 runtime dispatch bridge 可审计，`quickops:flow-ai:audit --strict` 可通过 source-level gate，但仍不替代真实 packaged quit、真实视觉、真实平台、真实确认 UI、企业策略、真实 packaged plugin SDK runtime probe 或完整 AI UI 自然语言编排 artifacts。
- 2026-06-19 QuickOps Flow/AI adapter 插件化切片：CoreApp 侧 `quick-ops-natural-language-adapter.ts` 与对应 focused test 已删除，官方 `plugins/touch-quickops` 承接 source-level Flow dispatch adapter / trace 合同；插件命令路由会为低风险状态控制生成有界 Flow dispatch item 和脱敏 `flowAdapterTrace`，确认型动作只渲染 App UI `confirmationToken` 需求，高风险 `kill port` 类请求 fail-closed 且不生成 dispatch payload。`quickops:flow-ai:audit` 默认改为读取 `plugins/touch-quickops/index.js` 与 `index.test.cjs`，adapter signals 更新为 `plugins/touch-quickops/index.js:flow-dispatch-adapter` / `plugins/touch-quickops/index.test.cjs:contract`；CoreApp 继续保留 QuickOps runtime host、Flow targets、confirmation、policy 与 evidence gate，不再承载 QuickOps 业务自然语言 resolver。
- 2026-06-19 QuickOps 状态型停止入口回归切片：官方 `plugins/touch-quickops` root-results 与 CoreApp runtime-host focused tests 已覆盖 keep-awake、system-awake、timer、pomodoro、screen-clean 与 stopwatch 六类运行会话，并断言插件状态入口与 Flow target 为每类会话暴露 stop / reset 主操作；QuickOps PRD 安全验收中“状态型工具都有停止入口”转为完成。该切片只约束官方插件 CoreBox 状态入口和 Flow facade/stopAllSessions 的停止路径，不替代 packaged quit cleanup、真实运行平台、真实确认 UI 或完整 AI 编排 evidence。
- 2026-06-19 QuickOps 敏感剪贴板日志回归切片：CoreApp QuickOpsModule focused test 已覆盖 Flow `copy-to-clipboard` 写入敏感文本时，ACK 只返回长度与截断状态，本地 QuickOps audit 只记录 target/decision/confirmation/payload key，不包含剪贴板正文；PRD 安全验收同步为部分完成。该切片只证明 QuickOps Flow ack/audit 当前不泄露剪贴板正文，不替代全仓日志/持久化审计路径复扫或真实 evidence。
- 2026-06-19 QuickOps Flow requireConfirm / plugin adapter 回归切片：CoreApp QuickOpsModule focused test 已断言所有会改变状态、外联、写入剪贴板/文件或打开本地目录的 QuickOps Flow target 都必须注册为 `requireConfirm=true`，并保留只读、停止、暂停、恢复、分段和格式化等低风险 target 不强制确认；`quickops:flow-ai:audit` 已把同一确认集合、FlowBus 一次性 confirmation token、FlowSelector 确认 token 发放、策略阻断 ACK、ACK/degraded/audit 脱敏、插件侧 Flow dispatch adapter/trace 和 runtime dispatch bridge 合同纳入结构化 artifact 检查；插件 adapter 输出 Flow dispatch item、脱敏 trace 和高风险 blocked 结果，runtime bridge 只对低风险插件 action 调用 `flow.dispatch()`，对确认型和高风险请求保持不 dispatch；PRD 的“Flow / AI 调用复用同一确认模型”同步为 Flow 侧与 source-level plugin bridge 部分完成。该切片不替代真实 AI UI 接入、真实确认 UI 截图/录屏、高风险执行治理或企业策略 evidence。
- 2026-06-19 QuickOps typed transport / no raw channel 回归切片：CoreApp QuickOpsModule focused test 已锁定当前对外注册面只包含 `QuickOpsEvents.*` typed transport event、`flowTargetRegistry.registerTarget('quickops', ...)` target 列表和单一 `flowBus.registerDeliveryHandler('quickops', ...)` delivery handler；PRD 的“无 legacy raw channel / SDK bypass”同步为 QuickOps 模块当前注册面部分完成。该切片不替代全仓 legacy alias/raw channel 复扫、插件侧真实 SDK 调用 evidence 或发布包 runtime evidence。
- 2026-06-19 QuickOps SDK/transport surface evidence gate 切片：CoreApp `quickops-evidence/v1` 新增 required case `quickops-sdk-transport-surface`，并新增 `quickops:surface:audit` 命令输出 `quickops-surface-audit/v1` JSON，采集 QuickOpsModule typed transport 注册面、`flowTargetRegistry('quickops')`、单一 Flow delivery handler、transport/plugin SDK 只读 facade、TouchPlugin runtime 注入 facade、禁用 stateful/destructive SDK 方法和 forbidden raw channel pattern；该 case 的 required evidence 已显式要求 TouchPlugin runtime facade 同步只读方法集与 typed transport invoke 映射，verifier 也会解析 artifact 内容而不是只信 checkedEvidence。该切片只把无 SDK bypass 要求纳入 strict evidence gate 并提供本地静态 + runtime-injection source artifact，不替代真实 packaged plugin SDK runtime probe、全仓 legacy channel 审计或发布包 evidence。
- 2026-06-19 QuickOps 插件运行时 facade 回归切片：CoreApp TouchPlugin focused test 已覆盖 `featureUtil.quickOps` / `featureUtil.plugin.quickOps` 完整只读方法集，补齐 `auditRecent()` 运行时入口，并断言所有方法都通过 `QuickOpsEvents.*` typed transport invoke。该切片只覆盖插件注入 util 的当前 runtime contract，不替代真实 packaged plugin SDK runtime probe、全仓 legacy channel 审计或发布包 evidence。
- 2026-06-19 QuickOps 官方插件抽离方向已入 TODO：新增 `plugins/touch-quickops` 作为官方迁移插件，并已把首批只读 Ops 入口承接到插件侧 root-results provider，包括 capability、sessions、auditRecent、system info、tuff diagnostics、disk space、directory usage、network status、local ip、battery status 和 system proxy 摘要渲染；插件只通过 `globalThis.quickOps` 过渡 facade 取数，不执行状态修改、不开放高风险动作、不绕过 Flow confirmation；后续按抽离边界推进，把仍可迁移的业务 runtime、session manager、settings 入口、插件侧状态/诊断面板与可扩展编排分阶段迁入官方插件或插件专属 runtime boundary，CoreApp 只保留必要 OS/Electron host capability、Flow confirmation、policy/evidence gate 和迁移期 typed bridge。
- 2026-06-19 QuickOps 未闭环项已拆到 P0 表：packaged quit artifact、清洁屏幕 / 纯色屏幕真实视觉、三平台真实 network/system/files artifacts、真实确认 UI、真实高风险治理、企业集中策略 / 审计 / 组织级锁定、完整 AI UI 自然语言编排分别独立追踪；后续不能再用 focused tests、source-level audit、manifest 或 mock artifact 替代真实 evidence。
- 2026-06-19 Intelligence architecture convergence 切片：CoreApp `intelligence-config` / `intelligence-sdk` 已复用 `@talex-touch/tuff-intelligence` resolver 处理 capability alias、prompt binding 与 provider route 归一；Nexus direct invoke 的 capability message builder 已拆到 helper，`keywords.extract` / `intent.detect` 支持 direct invoke，`image.generate` / `image.edit` / `audio.tts` / `embedding.generate` 保持 fail-closed；Nexus provider 调用已抽到可替换 provider adapter boundary，默认 LangChain 适配器只在具体 adapter 文件内构造；Nexus Agent graph runner 已新增 shared-runtime-compatible `runtimePort` bridge，可把 AEP `assistant.delta/final` 转成 Lab stream events，legacy LangGraph 仍为默认 fallback。新增 `mise run intelligence:*` 本地 gate 与 `.github/workflows/intelligence.yml` parity workflow；本轮只声明 focused/local gate 收敛，不替代 packaged Electron AI Stable evidence 或 live GitHub Actions 运行证据。
- 2026-06-01 当前 goal/progress：本轮 P1-APP-DATA 的主目标是把跨平台 indexing apps、实时监听、搜索补漏与 provider 设置收敛到统一 SDK/Runtime 抽象。已落地 Search Provider SDK 边界、插件 provider 注册 policy、`search.root-results` 权限 gate、Settings provider enable/order、source-to-provider link、File indexing progress ETA diagnostics、Browser Bookmarks consent-gated skeleton、Quicklinks linked provider enablement；剩余按 File write/store boundary、Browser Bookmarks 官方插件 owned lifecycle、Everything productionization、Quicklinks persistent feed/UI evidence、Browser History/System Settings/Obsidian/VSCode 的顺序推进。

## 2026-06-18 Roadmap vNext

- 已新增 `docs/plan-prd/04-implementation/Roadmap-vNext-2026-06-18.md`，作为 R0-R9 阶段化执行 SoT。
- 当前顺序：R0 口径清理 → R1 Release Integrity → R2 AI 2.5.0 Stable → R3 Search / Indexing Runtime；R4-R7 并行小切片推进，R8/R9 不抢当前稳定化窗口。
- 当前发布口径：公共包发布不再作为独立 Roadmap / blocker / evidence 项；版本变更后以 GitHub 自动发版 workflow 结果为准。
- 当前验证口径：owner 已完成的平台人工验证，后续不再作为 Roadmap 待办、平台后续或阻塞项。
- 当前 pricing 口径：公开站只承诺 Pioneer 阶段 `0 元 / $0`；正式 Pro/Plus/Team/Enterprise 价格、credits 单价与团队席位价未定，不能写成已发布；单一口径见 `docs/plan-prd/04-implementation/Pricing-SoT-2026-06-18.md`。
- 当前 evidence 口径：AI Stable、Nexus Governance 与 Platform Capability 分别以 `docs/plan-prd/04-implementation/Evidence-Matrix-AI-Stable-2026-06-18.md`、`docs/plan-prd/04-implementation/Evidence-Matrix-Nexus-Governance-2026-06-18.md`、`docs/plan-prd/04-implementation/Evidence-Matrix-Platform-2026-06-18.md` 为验收/回归矩阵。

## 2026-06-17 当前执行 Plan

- `docs/plan-prd/04-implementation/Current-Execution-Plan-2026-06-17.md` 已同步为 Roadmap vNext 口径：R0 口径清理、R1 Release Integrity、R2 AI Stable、R3 Indexing Runtime、R4-R9 后续能力。
- 当前 guardrail：不把本地 mock、dry-run、preflight 或 focused test 写成生产完成；不在无确认情况下执行 `git push`、tag/release、生产 API/DB 操作。

## 2026-06-19 AI Stable 收口 TODO

> 目标：把 `2.5.0` CoreBox AI Stable 从 partial failure evidence 推进到可验收 packaged Electron evidence；禁止用 mock、dry-run、focused test 或 CDP 链路截图冒充体验闭环。

### Must Fix on 2026-06-19

- [ ] 采集 packaged CoreBox `provider unavailable` failure evidence：使用 `/tmp/tuff-ai-stable-visible-user-data-*` 临时 profile，保留截图/JSON trace，并更新 `coreapp-visible-ai-stable-2026-06-18` manifest、checklist、README、Evidence Matrix 与 CHANGES。
- [ ] 采集 packaged CoreBox `logged-out` failure evidence：确认是真实未登录/无 session 状态，不把 provider unsupported 或 permission denied 误标为 logged-out。
- [ ] 采集 packaged CoreBox `copy failure` evidence：优先验证回答复制动作在失败/不可复制状态下的可见恢复提示；若实现路径不足，记录 blocker，不伪造 UI。
- [ ] 做 Local/Ollama routing 预检：确认本机 Ollama/local provider 是否可用；若可用，采真实 routing trace，证明 `local-default`/Local provider 不访问 disabled Nexus；若不可用，记录 blocker 与环境需求。
- [ ] 修复或解释 `NEXUS_STREAM_UNSUPPORTED` routing/capability binding：确认 disabled Nexus 不再被 `text.chat` stream 候选误选，或把原因记录为成功态 blocker。
- [ ] 仅在有效 provider/model 或可控 Local/Ollama 环境就绪后，采集 CoreBox `text.chat` 成功 evidence；不能用 model unsupported / permission denied partial evidence 冒充成功。
- [ ] 仅在显式 OCR 输入和真实 provider/model 可用后，采集 `vision.ocr -> text.chat` 成功 evidence；否则保留 open。

### Acceptance Checklist

- [ ] 每新增一条 evidence 后，更新 `docs/engineering/reports/coreapp-visible-ai-stable-2026-06-18/coreapp-visible-experience-manifest.json` 与 `COREAPP_VISIBLE_EXPERIENCE_CHECKLIST.md`。
- [ ] 同步 `docs/plan-prd/04-implementation/Evidence-Matrix-AI-Stable-2026-06-18.md`、`docs/engineering/reports/coreapp-visible-ai-stable-2026-06-18/README.md` 与 `docs/plan-prd/01-project/CHANGES.md`。
- [ ] 复跑 strict verify：`pnpm -C "apps/core-app" run visible:experience:verify -- --input "../../docs/engineering/reports/coreapp-visible-ai-stable-2026-06-18/coreapp-visible-experience-manifest.json" --requireAllPassed --requireArtifactPaths --requireVisualArtifacts --requireCheckedEvidence --requireExistingArtifacts --requireNonEmptyArtifacts`；若仍 exit `1`，必须记录剩余 gate failures，且 `missingArtifactCount` 不能回退。
- [ ] 复跑 focused test：`pnpm -C "apps/core-app" exec vitest run "src/main/modules/platform/coreapp-visible-experience-evidence.test.ts"`。
- [ ] 验证 JSON 可解析、关键 PNG 为有效图片、`git diff --check` 无输出、无残留 packaged CDP/Tuff 进程。
- [ ] raw/probe/pid/重复截图继续放入 ignored `raw/` 或 `_local/`；顶层只保留 curated evidence。

### Not Stable Blockers Yet

- [ ] OmniPanel Writing Tools provider-backed copy/retry/replace：MVP/Beta evidence 队列，不关闭 AI Stable。
- [ ] Workflow `Use Model`、Review Queue、Template Center、Skills、Automation：Beta evidence 队列，不抢 `2.5.0` Stable 收口。
- [ ] 全局 `docs/engineering/reports/` 历史瘦身：已有 hygiene 规则，后续如继续压缩，先 review `coreapp-visible-assistant-2026-05-21/` keep set，不批量删除历史报告。

## 2026-06-17 touch-intelligence 未闭环补充

- CoreBox AI Ask widget 已完成真实 streaming 主链路提交，但本地实测暴露 provider routing 问题：禁用区的 Tuff Nexus 仍可能因 runtime token 注入和 `text.chat` capability binding 被带入流式调用，导致 `NEXUS_STREAM_UNSUPPORTED`；当前修复方向是保留 Nexus provider 的 disabled 状态，并让 capability options 只返回 provider 与 binding 均启用的候选。
- Ollama / Local Model 是当前用户预期 provider；当前已补 `LocalProvider.chatStream()` 优先走 Ollama `/api/chat` + NDJSON stream、404 才回退 OpenAI-compatible，但仍需在 CoreApp 重启后用真实本地 Ollama 验证 `local-default` 作为 `text.chat` 首选 provider 时，`intelligence.stream()` 不再访问 Nexus，且 token delta 能进入 widget props 增量渲染。
- `touch-intelligence` 仍需 packaged/dev runtime evidence：重启 CoreApp、重载插件、确认 Enter/send 显式发送、无输入暂停自动提交、图片上下文不触发 image-only OCR、权限拒绝/SDK 不可用/error fallback UI、历史持久化与深色主题均按预期工作。
- Stream 能力范围仍需显式收口：Stable 当前只承诺 `text.chat`；`vision.ocr` 维持非流式，translate/summarize/code 等能力是否扩展 stream 要单独评估 provider 支持、fallback 语义与 UI evidence。
- 后续质量门禁：继续保留 related-only 提交；至少跑 CoreApp AI focused tests、plugin focused tests、`packages/tuffex` typecheck、`plugins/touch-intelligence` build、runtime plugin sync 与 `git diff --check`，若全量 typecheck 仍被既有 `useSearch.core.test.ts` fixture 类型问题阻塞，需记录为非本轮回归。

## P0 - 2.4.11 必须收口

| ID | 事项 | 状态 | 验收/证据 |
| --- | --- | --- | --- |
| P0-COMPAT | legacy/compat/size 债务关闭或降权 | 进行中 | 以 2026-05-15 兼容总结、2026-05-16 深度审计、Quality Baseline、长期债务池与最近路径代码证据为准关闭或记录降权理由；禁止新增 legacy/raw channel/旧 storage protocol/旧 SDK bypass。 |
| P0-RELEASE-CHECKLIST | 2.4.11 release checklist 与质量门禁收口 | 进行中 | 以 `docs/plan-prd/04-implementation/Roadmap-vNext-2026-06-18.md` 与 `docs/plan-prd/04-implementation/Release-2.4.11-Closure-2026-06-13.md` 为本轮 SoT；跑最近路径 Nexus docs navigation Vitest/scoped ESLint/typecheck、`pnpm quality:pr`、`pnpm publish:check`、`pnpm publish:check:pack`、`pnpm quality:release` 与 `git diff --check`。若 `quality:release` 失败，记录失败项与替代验证，不宣称全仓 release gate 已绿；包 manifest 预检只作为 CI hygiene，不作为公共包发布 blocker。 |
| P0-AI-COMPAT | AI 兼容占位成功响应退场 | 进行中 | 历史 `retired-ai-app` 中的 `livechat/random`、prompt detail、catch-all 已不在当前 live tree；当前 Nexus production 兼容面已钉住 `intelligence-lab/*` 与旧 `intelligence-agent/orchestrator/*` 返回 HTTP `410` 与迁移目标，不返回可消费 fake payload。证据：`apps/nexus/test/api/admin/intelligence-compat-retired.api.test.ts`。 |
| P0-SECRET | CLI 与插件 secret storage 收口 | 进行中 | CLI token 已通过 `CliCredentialStore` 做 POSIX `0700/0600` 权限缓解与 Windows ACL warning；`touch-translation` provider secret 已迁入 `usePluginSecret()` 并清理普通配置，配置弹窗已展示 secure-store available/degraded/unavailable health；插件侧已新增只读 `plugin.secret.health()` / `usePluginSecret().health()` 以暴露 secure-store degraded/unavailable 状态。2026-06-16 CoreApp secure-store 已 hard-cut Electron `safeStorage` / Keychain / Credential Locker / libsecret 路径，仅使用本机随机 root 密钥加密；旧 `safe-storage` envelope 不再解密，用户需重新登录或重填插件 secret；2026-06-17 Auth 登录凭证默认语义已恢复为 `auth.useSecureStorage=true`，缺省或旧默认关闭且未用户显式覆盖的配置会迁回本机 root 密钥 secure-store；关闭入口仅保留在高级设置，用户显式关闭后仍进入 session-only；2026-06-14 `touch-translation` network/AI permission helper 已改为只缓存 granted，不缓存 denied，避免用户拒绝一次后必须插件重载才能重新授权；同日补齐 legacy provider config 明文 secret 迁移失败路径，检测到旧 `providers_config` secret 时仍回写 sanitized metadata，避免 `apiKey` / `secretKey` / `token` 类字段回灌到普通 plugin storage。仍需遗留 secret 清理提示、重新登录/重配体验与 packaged runtime build/sync evidence。 |
| P0-SHELL-CAP | 插件 shell capability 统一诊断 | 进行中 | `plugins/touch-workspace-scripts` 首切已完成：safe-shell 缺失时 fail-closed，不再回退裸 shell；CoreBox item 暴露 `available` / `permission-missing` / `unsupported`、permission、unsupported reason、command source 与 audit 字段；执行前校验空命令、换行/null payload 与 cwd 目录可用性，并返回 `started` / `blocked` / `cancelled` 状态。2026-05-18 已完成 `plugins/touch-browser-open` 第二切：浏览器打开 item 增加 `system.shell` capability/audit metadata 与 non-mutating permission check，网页搜索 item 增加 shell open platform/audit metadata；Linux 指定浏览器打开明确 `linux-specific-browser-open-unsupported`，复制 URL 不再依赖 shell 权限；执行结果统一返回 `started` / `blocked`。同日第三切已收口 `plugins/touch-system-actions` 与 `plugins/touch-window-manager`。2026-05-20 已完成 `plugins/touch-snipaste` 首切：执行前显式 `system.shell` permission gate、CoreBox item capability metadata、非法 args blocked、`started/blocked/failed` 统一返回；`plugins/touch-window-presets` 展示阶段已改 non-mutating permission check，执行阶段才 request，并保留 Windows-only reason。2026-05-21 已完成 `plugins/touch-quick-actions`：safe-shell 缺失时 fail-closed，不再回退裸 `exec`；展示阶段只做 non-mutating permission check，CoreBox item 暴露 shell capability/audit/status/reason；执行结果统一返回 `started` / `blocked` / `failed` / `cancelled`，且执行时按 action id 回查平台 allowlist，忽略 item payload 注入的 shell command。2026-05-22 `touch-browser-bookmarks` 外链打开已补 `network.internet` 可选权限、展示期 capability/audit metadata 与执行期 request/block 状态；`touch-browser-data` 默认浏览器打开本地书签 URL 已补同一权限口径，展示期只做 non-mutating permission check，执行期 request 并在拒绝时返回 blocked，避免绕过权限口径直接调用 `openUrl`。2026-06-14 `touch-dev-toolbox` 配置链接打开已补 `network.internet` request/block 具体 reason 保真，拒绝授权、permission SDK 缺失、request failed 或运行时缺少 `openUrl` 时返回 blocked，不再静默调用外部打开能力；`touch-batch-rename` apply/undo 已补 `fs.write` request/block 具体 reason 保真，focused test 覆盖 denied、permission SDK unavailable 与 request failed，确保 apply 不写 `last-rename.json`、undo 不读取 rollback state，且 permission SDK 缺失/异常时 `fs.read` 预览入口不暴露 apply；`touch-snippets` 复制片段与导出 snippet pack 已补 `clipboard.write` request/block 具体 reason 保真，拒绝、permission SDK 缺失或 request failed 时不写剪贴板、不更新 usage metadata；`touch-emoji-symbols` 复制 Emoji/符号、`touch-dev-utils` / `touch-text-tools` 复制工具输出、`touch-translation` 复制翻译结果、`touch-browser-bookmarks` 复制 URL、`touch-browser-data` 复制本地书签 URL 与 `touch-intelligence` 复制回答均已补同一 `clipboard.write` request/block，拒绝或 permission SDK 缺失/异常时返回 blocked 且不写剪贴板；`touch-translation` network/AI helper 也已改为 permission SDK 缺失/异常 fail-closed，只缓存 granted，不缓存 denied；`touch-intelligence` CoreBox AI Ask 执行期已补 permission SDK 缺失/异常 fail-closed，缺边界时不调用 Intelligence SDK 并展示 `PERMISSION_DENIED` 可重试错误；`touch-browser-open` 在 permission SDK 缺失或 check/request 异常时已 fail-closed，展示期标记 `permission-sdk-unavailable`，执行期保留 `permission-sdk-unavailable`、`permission-denied`、`permission-request-failed` 具体 reason，不进入 shell/network/clipboard mutation；`touch-quick-actions` 已补同类 permission SDK 缺失/异常 fail-closed，执行期在确认与 shell runner 前 blocked；`touch-system-actions` 已补同类 fail-closed，并保留 `permission-sdk-unavailable`、`permission-denied`、`permission-request-failed` 具体 reason，且 `open-main-window` 原生动作不被 shell 权限前置拦截；`touch-workspace-scripts` 已补同类 fail-closed，run-command 在缺 permission SDK 时不弹确认、不调用 shell runner；`touch-window-manager` 已补同类 fail-closed，并保留 `permission-sdk-unavailable`、`permission-denied`、`permission-request-failed` 具体 reason，缺 permission SDK 时不进入 AppleScript/PowerShell/open 窗口操作；`touch-window-presets` 与 `touch-snipaste` 已补同类 fail-closed，缺 permission SDK 时分别不进入 PowerShell preset / Snipaste spawn；`touch-browser-data` 与 `touch-browser-bookmarks` 已补同类 fail-closed，并保留 `permission-sdk-unavailable`、`permission-denied`、`permission-request-failed` 具体 reason，缺边界时不读取本地 Bookmarks JSON、不调用默认浏览器打开外链、不写剪贴板；`touch-dev-utils` / `touch-text-tools` 已补 clipboard permission SDK 缺失/异常 fail-closed，缺边界时不写剪贴板；`touch-emoji-symbols`、`touch-text-tools`、`touch-dev-utils` 与 `touch-translation` 生成的辅助复制动作已从宿主 `copy` action 改为 plugin action，避免绕过插件执行期 `clipboard.write` gate；复扫确认官方插件生成路径已无宿主 `copy` action 残留，兼容旧 item 的 `type: "copy"` 处理仍保留并继续走 gate。仍需继续复核剩余 shell/OS/network/fs/clipboard capability surface。 |
| P0-PREVIEW-SDK | PreviewSDK 内核与动态执行治理 | 进行中 | `packages/utils/core-box/preview` 已落地最小 SDK、纯 payload 协议、安全声明、ability inventory；BasicExpression/AdvancedExpression/Percentage/TextStats/Color/ScientificConstants/UnitConversion/TimeDelta 已迁入 SDK，BasicExpression 不再使用 `new Function`，单位换算与 `calculation/unit-converter.ts` 已共用静态转换核心。PreviewSDK 已新增 `resolveWithDiagnostics()`、registry-level max input guard、预算超限标记、deterministic benchmark helper 与 ability mismatch / budget regression failure summary；focused test 覆盖超长输入不进入 ability、payload diagnostics、P50/P95/max duration summary 与 failure summary。2026-06-06 AdvancedExpression 已收紧白名单字符/函数边界，避免抢占 TimeDelta / Percentage，并为常见函数/常量/幂运算新增静态 fast path；TimeDelta 已拒绝 `now + abc` 假成功并支持 `1小时30分钟后`，Percentage 已支持 `100 增加 20%`，UnitConversion `canHandle()` 已收紧为已知同类别单位。2026-06-18 QuickOps Developer P0/P1 已进入 PreviewSDK，覆盖 JSON 格式化/压缩/校验、URL/Base64 编解码、JWT header/payload 解码、Regex 受限测试、Markdown/CSV 表格转换、时间戳与日期 / 固定 UTC offset 时区转换、UUID v4 与短 ID 生成、大小写/命名风格转换，并支持 CoreBox 文本剪贴板输入；PreviewSDK 也新增 `preview.currency` 静态离线 fallback，CoreApp 仍保留 Nexus / cache-backed 实时汇率 adapter。CoreApp PreviewProvider 已声明 Text/HTML input passthrough，并为包含 `primaryValue` 的 preview item 暴露 `preview-copy-primary` host copy action。2026-06-19 已补 QuickOps no-result 边界：`uuid history` 不生成 UUID、未知货币代码不进入静态汇率估算、缺少 `/pattern/` 字面量的 Regex 查询不触发预览，CoreApp 剪贴板兜底检测也避免非命令 UUID 查询触发剪贴板读取。当前 `benchmark:preview` 升级为 21-case 门槛，实测 21/21 matched、0 budget exceeded，P50 ~0.38ms、P95 ~2.96ms、max ~10.32ms。QuickOps Power/Screen/Timer 状态型 runtime 保持独立于 PreviewSDK，并已由 `QuickOpsRuntimeHost` / `QuickOpsSessionManager` / `QuickOpsModule` 承载，CoreBox 入口由 `plugins/touch-quickops` 承接；Widget runtime sandbox 只进入 Runtime Safety 清单，不纳入 PreviewSDK 首批。 |
| P0-QUICKOPS | QuickOps 本地系统快捷工具集 | 进行中 | PRD 已新增到 `docs/plan-prd/03-features/tuff-quickops-prd.md`。2026-06-18 Developer P0/P1 已进入 PreviewSDK；同日先由 CoreApp provider 接入；2026-06-19 起官方 `plugins/touch-quickops` 已成为 QuickOps CoreBox root-results 唯一入口，可触发 `keep awake 30m` / `禁止息屏 1小时` / `extend keep awake 15m` / `延长保持唤醒 15分钟` / `prevent system sleep 30m` / `禁止系统睡眠 30分钟` / `timer 10m` / `计时 25分钟` / `pause timer` / `暂停计时` / `resume timer` / `继续计时` / `extend timer 5m` / `延长计时 5分钟` / `pomodoro` / `番茄钟` / `pomodoro cycle` / `循环番茄钟` / `pomodoro cycle 4 rounds 25m 5m` / `循环番茄钟 4轮 25分钟 5分钟` / `pomodoro 25/5` / `长番茄钟` / `pomodoro custom 40/8` / `自定义番茄钟 45/10` / 设置中的自定义番茄钟模板名称或别名 / `pause pomodoro` / `暂停番茄钟` / `resume pomodoro` / `继续番茄钟` / `clean screen` / `清洁屏幕` / `白底清洁屏幕` / `red screen test` / `screen color test` / `蓝色屏幕测试` / `stop clean screen` / `退出清洁屏幕` / `stopwatch` / `秒表` / `秒表分段`，Developer commands 覆盖 JSON / URL / Base64 / JWT 解码 / Regex 受限测试 / Markdown/CSV 表格转换 / 时间戳 / 日期与固定 UTC offset 时区转换 / 静态离线货币估算 / UUID v4 / 短 ID / QR Code SVG，SVG/PNG 保存，状态型能力支持运行中状态项、暂停、恢复、延长、停止动作、快速计时器到点通知、番茄钟 25/5 与 50/10 内置模板、内置模板设置页开关、命令式自定义模板、设置驱动自定义模板列表 schema / 别名解析、专注/休息循环、1-12 轮有限循环、每 N 轮长休息、阶段到点通知和番茄钟会话级显示器保持唤醒联动、清洁屏幕多 display 黑/白底全屏遮罩、红/绿/蓝纯色屏幕测试遮罩和长按 Esc 退出、秒表分段和到期自动清理；QuickOps Network 本地只读查询已接入 `local ip` / `本机 IP`、`port 3000` / `端口 3000` 、`dns example.com` / `DNS 查询 example.com` 与 `network status` / `网络状态`，分别使用 Node `networkInterfaces()` 展示非 internal IPv4 / IPv6、网卡名和复制动作，使用 Node TCP bind 探测本地端口可用 / 已占用 / 无效端口状态，占用时 best-effort 通过 macOS/Linux `lsof` 或 Windows PowerShell `Get-NetTCPConnection` 追加 PID / 进程归因，并在有 PID 时提供 copy-only 终止命令，使用 Node DNS resolver 查询 A / AAAA / CNAME / MX 记录，以及使用 Node `dns.getServers()` 汇总系统 DNS server 列表，并只读检测 `HTTP(S)_PROXY` / `ALL_PROXY` / `NO_PROXY` 环境变量代理状态且脱敏代理凭据，并支持 `proxy status` / `system proxy` / `系统代理` 只读读取 macOS `scutil --proxy`、Windows 当前用户 Internet Settings 与 Linux GNOME proxy mode 的脱敏系统代理摘要；`public ip` / `公网 IP` 已接入默认关闭的 opt-in 查询，只有高级设置启用后才向 `https://api.ipify.org?format=json` 发起一次只读 GET 并标注来源；QuickOps Files 已接入 `open desktop` / `open downloads` / `open documents` / `open app data` / `open logs` 常用目录 open/copy action、`recent download` / `latest download` / `最近下载文件` 最近普通下载文件打开 / 打开所在文件夹 / 复制路径、`move recent download to "/target"` / `移动最近下载到 "/target"` 危险确认后移动到显式绝对目标目录、`scratch note` / `临时文本` Tuff 临时目录内文本创建、`temp dir` / `临时目录` Tuff 临时目录内目录创建、`hash "/path"` 单文件路径命令、官方插件 Files input 首个有效文件路由到 MD5 / SHA1 / SHA256 只读面板、`file base64 "/path"` / `文件 base64` 1MB 单文件上限内编码和官方插件 Files input 首个有效文件路由、`base64 decode file <payload>` 临时文件解码落盘、文件搜索结果已降为路径 copy-only 动作，以及 `copy path "/path"` 路径命令、Files input 和文件搜索结果项保留原始路径 / Shell 转义路径 / file URL / Windows-WSL 互转复制动作；QuickOps System 已接入 `quickops capability` / `quickops 能力` host 能力摘要、`tuff diagnostics` 扩展脱敏诊断包复制、`system info` / `系统信息` 安全系统信息复制、`battery status` / `电池状态` 主动查询低电量提醒、`disk space` / `磁盘空间` 文件系统容量复制、`directory usage` / `目录占用` 关键目录 bounded shallow scan 和 `deep directory usage` / `深度目录占用` bounded recursive scan，覆盖 schema、版本、平台、OS、runtime、CPU、内存、uptime、脱敏 Home/userData/logs 路径、网络计数、QuickOps 默认参数、load average、Home / Tuff userData 所在文件系统 free/used/total 和使用率，以及 Desktop / Downloads / Documents / Tuff Data / Logs 首批直接子项统计与显式深度递归统计，不复制完整配置或原始 Home 路径，不读取敏感路径或外联；公网 IP 真实外部服务 evidence、真实端口 kill / 二次确认 UI、真实系统代理 evidence、真实系统打开 evidence、真实文件权限 / 大文件 evidence、最近下载文件真实平台移动 evidence、真实排障 evidence 和电池真实平台 evidence 仍后置。QuickOps capability 摘要已覆盖 enabled/disabled、supported/degraded/disabled、riskLevel 与 reason，并保留真实端口 kill 为 `copy-only-command` disabled 状态；typed transport、`createQuickOpsSdk()`、插件只读 facade 与 Flow 内建 target `quickops.capabilities`、`quickops.query-local-ip`、`quickops.port-status`、`quickops.dns-query`、`quickops.file-hash`、`quickops.file-base64`、`quickops.recent-download`、`quickops.path-format`、`quickops.keep-awake`、`quickops.stop-keep-awake`、`quickops.system-awake`、`quickops.stop-system-awake`、`quickops.start-timer`、`quickops.pause-timer`、`quickops.resume-timer`、`quickops.stop-timer`、`quickops.start-pomodoro`、`quickops.pause-pomodoro`、`quickops.resume-pomodoro`、`quickops.stop-pomodoro`、`quickops.clean-screen`、`quickops.stop-clean-screen`、`quickops.start-stopwatch`、`quickops.pause-stopwatch`、`quickops.resume-stopwatch`、`quickops.lap-stopwatch`、`quickops.reset-stopwatch`、`quickops.show-notification`、`quickops.copy-to-clipboard`、`quickops.format-text` 和 `quickops.open-folder` 已复用同一 capability、本机 IP、本地端口状态、本地 DNS 查询、本地单文件 Hash、本地单文件 Base64 编码、最近下载文件元数据、本地路径格式化、显示器保持唤醒启动/停止、系统睡眠阻止启动/停止、计时器启动/暂停/恢复/停止、结构化番茄钟启动/暂停/恢复/停止、清洁屏幕 overlay 启动/停止、秒表启动/暂停/恢复/分段/重置、有界本地系统通知、有界本地剪贴板写入、有界文本转换与受限常用目录打开 payload；`quickops.keep-awake`、`quickops.system-awake`、`quickops.start-timer`、`quickops.start-pomodoro`、`quickops.clean-screen`、`quickops.start-stopwatch`、`quickops.show-notification`、`quickops.copy-to-clipboard` 和 `quickops.open-folder` 标记 `requireConfirm=true`，仍不提供高风险执行 API。状态型 runtime 已抽出 `QuickOpsSessionManager`，CoreApp 仅保留 `QuickOpsRuntimeHost` host boundary、typed transport、Flow target、policy/evidence gate 与必要 OS/Electron helper；`AppSetting.quickOps` 已补默认偏好字段，runtime/module 会只读解析全局启用、CoreBox 运行中状态展示、默认时长、内置番茄钟模板启用状态、自定义番茄钟模板列表、清洁屏幕默认黑/白底和公网 IP 查询 opt-in 开关；Settings / Tools 已提供对应默认偏好、内置番茄钟模板开关、高级设置中的公网 IP 查询开关和自定义模板摘要入口，并由 renderer focused test 覆盖控件可见性、非法旧配置归一化、模板开关保留、自定义模板归一化和高级设置可见性；`QuickOpsModule` 已接入 CoreApp 模块加载链路，显式订阅 `BEFORE_APP_QUIT` / `WILL_QUIT` 并在 app quit event 与 module stop/destroy 时清理共享 runtime；CoreApp 托盘 QuickOps 状态面已移除，运行中状态摘要与停止全部动作由官方插件 root-results surface 承接；显示器保持唤醒和番茄钟联动保持唤醒使用 Electron `powerSaveBlocker.start('prevent-display-sleep')`，系统睡眠阻止使用 `powerSaveBlocker.start('prevent-app-suspension')`，清洁屏幕 / 纯色屏幕测试使用本地 `BrowserWindow` data URL overlay，不修改系统电源计划、不读取屏幕内容；overlay data URL 视觉合同已覆盖黑/白底、红/绿/蓝背景、倒计时提示、隐藏光标、长按 Esc 退出和无外链资源；runtime-host 与 QuickOpsModule focused tests 已覆盖 cleanup。QuickOps capability focused test 覆盖 macOS/Windows/Linux/unsupported 平台 reason 归一化。仍未闭环：清洁屏幕 / 纯色屏幕测试真实视觉截图/录屏 evidence、最近下载文件真实平台移动 evidence、真实 AI UI 编排 evidence、企业策略与真实 packaged app quit artifact。 |
| P0-QUICKOPS-PLUGIN-EXTRACTION | QuickOps 抽离为官方独立插件 | 进行中 | 2026-06-19 已新增并扩展 `plugins/touch-quickops` 官方迁移插件，manifest 显式声明 `search.root-results` 与 root provider，并已收紧为显式 QuickOps / system / network / file / disk / diagnostics / settings / session control 触发词，避免污染普通搜索结果。插件侧当前承接 CoreBox QuickOps root-results 入口、首批只读 Ops 面板、只读 settings 摘要和低风险状态控制入口：capability、sessions、auditRecent、system info、tuff diagnostics、disk space、directory usage / deep directory usage、network status、local ip、port status、DNS query、file hash、file Base64、recent download、common directory、path format、format text、battery status、system proxy、`quickops settings`，以及 `stop-all-sessions`、`stop/pause/resume timer`、`stop/pause/resume pomodoro`、`stop clean screen`、`stop keep-awake`、`stop system-awake`、`pause/resume/lap/reset stopwatch`；只读结果通过 `globalThis.quickOps` 过渡 facade 取数和渲染，settings 摘要只读取 capability / diagnostics 不写 host policy，低风险状态控制通过注入的 Flow SDK dispatch 到已有 QuickOps Flow target。`keep-awake`、`system-awake`、`start timer`、`start pomodoro`、`clean-screen`、`start stopwatch`、`copy-to-clipboard`、`show-notification`、`open-folder`、`temp-text-file`、`temp-directory`、`public-ip` 等确认型动作只展示需要 App UI `confirmationToken` 的提示，不执行、不自行生成 token、不绕过 Flow confirmation。插件 manifest 已接受 `text` 与 `files` 输入，文件 Hash/Base64/path format 会把首个有效 Files input 路由到插件只读面板；CoreApp 文件搜索 provider 不再承载 QuickOps Hash/Base64 execute action，只保留索引、打开文件、打开所在目录和路径 copy-only 动作。SearchEngine 已移除默认 `quickOpsProvider` 注册；CoreApp 生产路径已改为 `QuickOpsRuntimeHost` / `quickOpsRuntime` host boundary，并已从旧 `box-tool/addon/quick-ops` 路径迁入 `apps/core-app/src/main/modules/quick-ops/quick-ops-runtime-host.ts` 与 `quick-ops-session-manager.ts`；CoreApp 仅保留 QuickOps runtime、host capabilities（power blocker、overlay、通知、受限文件/网络/系统 helper）、Flow target/confirmation、settings read path、policy/evidence gate 与 typed bridge；旧 `QuickOpsProvider` legacy CoreBox item 兼容壳已移除，CoreApp 不再导出或注册 QuickOpsProvider，CoreApp Tools 设置页不再承载 QuickOps 配置控件。下一批继续把仍可迁移的可写 settings host capability、运行中状态面和可扩展编排迁入官方插件或插件专属 runtime boundary。迁移期间不得降低已有策略 fail-closed、confirmation token、redaction 和 evidence gate 目标；Nexus 文档、evidence scripts 与 packaged plugin runtime probe 需同步证明插件 runtime 可用。 |
| P0-QUICKOPS-PACKAGED-QUIT-EVIDENCE | QuickOps packaged quit 真实证据 | 未开始 | 必须采集真实 packaged app artifact，覆盖 isolated userData、正常退出、运行中 keep-awake / system-awake / timer / pomodoro / screen-clean / stopwatch 会话退出后 power blocker、overlay window、timer、stopwatch runtime 全部归零；不得用 dev run、focused test、manifest 或 source-level audit 替代。对应 artifact schema：`quickops-packaged-session-cleanup/v1`。 |
| P0-QUICKOPS-VISUAL-EVIDENCE | QuickOps 清洁屏幕 / 纯色屏幕真实视觉证据 | 未开始 | 必须采集黑/白清洁屏幕和红/绿/蓝纯色测试的真实截图或录屏，覆盖多 display、倒计时、长按 Esc 或停止入口、无外链资源、不读取屏幕内容；不得用 overlay data URL contract、mock screenshot 或 source-level test 替代。 |
| P0-QUICKOPS-PLATFORM-EVIDENCE | QuickOps 三平台真实 network/system/files 证据 | 未开始 | 必须在 macOS / Windows / Linux 分别采集真实只读 network/system/files artifacts，覆盖 local-only 网络、端口不 kill、DNS 不改系统设置、系统代理脱敏、battery/disk/directory degraded reason、文件 Hash/Base64/最近下载/常用目录/路径格式/temp workspace 边界；不得跨平台复用 artifact。对应 artifact schema：`quickops-platform-readonly/v1` 与 `quickops-files-and-temp/v1`。 |
| P0-QUICKOPS-CONFIRMATION-EVIDENCE | QuickOps 真实确认 UI 证据 | 未开始 | 必须采集真实用户确认界面截图/录屏，覆盖所有 `requireConfirm=true` Flow target、state/file confirm 与 cancel 路径、before execution、一次性 `confirmationToken`、取消后不 dispatch；不得用 metadata-only target、Flow fixture 或 source audit 替代。对应 artifact schema：`quickops-confirmation-policy/v1`。 |
| P0-QUICKOPS-HIGH-RISK-GOVERNANCE | QuickOps 高风险执行治理 | 未开始 | 真实端口 kill、批量文件操作、长期系统设置修改仍不得开放。若后续推进，必须先补策略开关、权限、确认 UI、copy-only / policy-gated fallback、审计、失败恢复、真实平台 artifact 和 Nexus 文档；当前 `allowHighRiskTools=false` 只能证明默认阻断，不代表高风险治理完成。 |
| P0-QUICKOPS-ENTERPRISE-POLICY | QuickOps 企业集中策略 / 审计 / 组织级锁定 | 未开始 | 需要设计并实现集中策略下发、组织级锁定、集中审计、策略 disabled reason 可见性、管理员诊断与本地策略冲突处理；当前本地 `allowStatefulTools` / `allowNetworkTools` / `allowFileTools` / `allowSystemTools` / `allowDeveloperTools` / `allowHighRiskTools` 和内存 audit ring buffer 不等同于企业治理。 |
| P0-QUICKOPS-AI-UI-ORCHESTRATION | QuickOps 完整 AI UI 自然语言编排 | 未开始 | 需要真实 AI UI 接入、自然语言计划展示、确认 UI 串联、read-only 与 requireConfirm target 编排、blocked/degraded 反馈、payload 脱敏 trace 与 packaged UI evidence；当前插件侧 Flow dispatch adapter/trace、runtime dispatch bridge 和 `quickops:flow-ai:audit --strict` 只证明 source-level 合同。 |
| P0-QUICKOPS-NEXUS-DOCS | QuickOps Nexus 用户与开发者文档 | 部分完成 | 2026-06-19 已新增并扩展 `apps/nexus/content/docs/guide/features/quickops.zh.mdc` / `quickops.en.mdc`，覆盖 CoreBox 使用入口、常用命令、Ops 场景速查、官方插件 `quickops settings` 设置摘要、本地策略、安全边界、插件扩展边界、何时写插件和未完成项；已新增并扩展 `apps/nexus/content/docs/dev/api/quickops.zh.mdc` / `quickops.en.mdc`，覆盖插件 `quickOps` 只读 facade、transport domain SDK、扩展路径选择、`quickops.*` Flow target 目录、第三方扩展模式、插件自有 Ops checklist、内置 QuickOps 贡献 checklist、可写 settings host capability 待设计边界和扩展建议；Guide / Dev / API 索引、`plugin-context` 全局对象说明、`docs/INDEX.md` 与 `docs/plan-prd/README.md` 已接入对应入口。该项只完成当前用户/开发者文档、扩展边界与入口一致性切片；真实平台 evidence、packaged quit evidence、真实 AI UI 自然语言编排、高风险治理、企业集中策略 / 审计 / 组织级锁定仍未闭环，后续能力变化需继续同步 Nexus 与 PRD。 |
| P0-QUICKOPS-EVIDENCE-PLAN | QuickOps evidence 采集计划 | 进行中 | 2026-06-19 `quickops:evidence:template` 已新增 `--writePlan`，且在 `--writeChecklist` 或 `--writePlan` 启用时默认写入 `quickops-evidence-manifest.json`，避免只生成 checklist/plan 但缺主 manifest；采集计划把 `quickops:surface:audit`、`quickops:flow-ai:audit` 的 source-level artifact 与 packaged quit、真实视觉、三平台、文件/temp、确认 UI、高风险边界和企业策略 evidence 分开采集，并保留 strict verifier 命令；packaged quit case 已要求 `quickops-packaged-session-cleanup/v1` artifact 并解析 packaged/quit/session/cleanup/redaction/gate 字段；platform readonly case 已要求 `quickops-platform-readonly/v1` artifact 集合覆盖 macOS/Windows/Linux，并解析 local-only 网络、端口不 kill、DNS 不改系统设置、代理脱敏和 degraded reason；files/temp case 已要求 `quickops-files-and-temp/v1` artifact 并解析 Hash/Base64、recent download、common directory、path format、temp workspace 与 redaction 字段；confirmation/policy case 已要求 `quickops-confirmation-policy/v1` artifact 并解析真实确认面、visual artifact refs、完整 `requireConfirm` target、confirm/cancel、confirmation token、高风险默认阻断、policy disabled reason、diagnostics visibility 与 redaction 字段。该项只补证据采集流程和防混淆 gate，不代表真实 packaged app quit artifact、清洁屏幕截图/录屏、三平台真实 network/system/files evidence、真实确认 UI 或企业集中治理已完成。 |
| P0-QUICKOPS-NETWORK-POLICY | QuickOps 网络工具本地策略基线 | 进行中 | 2026-06-19 本地 `allowNetworkTools` 已默认开启并接入 Settings 高级开关；关闭后 CoreBox 网络类工具返回 `network-tools-disabled-by-policy`，Flow `network-status` / `system-proxy` / `public-ip` / `query-local-ip` / `port-status` / `dns-query` fail-closed 为 `quickops.policyBlocked`，capability 摘要把对应网络能力标记为 disabled。该项只完成本地策略基线；真实网络平台 evidence、真实 AI UI 编排 evidence、完整企业策略下发/审计/组织级锁定、真实确认 UI 和 packaged quit evidence 仍未闭环。 |
| P0-QUICKOPS-FILE-POLICY | QuickOps 文件工具本地策略基线 | 进行中 | 2026-06-19 本地 `allowFileTools` 已默认开启并接入 Settings 高级开关；关闭后 CoreBox 文件类工具返回 `file-tools-disabled-by-policy`，Flow `file-hash` / `file-base64` / `recent-download` / `common-directory` / `path-format` / `temp-text-file` / `temp-directory` / `open-folder` fail-closed 为 `quickops.policyBlocked`，capability 摘要把对应文件能力标记为 disabled。该项只完成本地策略基线；真实文件平台 evidence、真实 AI UI 编排 evidence、完整企业策略下发/审计/组织级锁定、真实确认 UI 和 packaged quit evidence 仍未闭环。 |
| P0-QUICKOPS-SYSTEM-POLICY | QuickOps 系统工具本地策略基线 | 进行中 | 2026-06-19 本地 `allowSystemTools` 已默认开启并接入 Settings 高级开关；关闭后 CoreBox 系统类工具返回 `system-tools-disabled-by-policy`，Flow `system-info` / `tuff-diagnostics` / `disk-space` / `directory-usage` / `battery-status` fail-closed 为 `quickops.policyBlocked`，capability 摘要把对应系统能力标记为 disabled。该项只完成本地策略基线；真实系统平台 evidence、真实 AI UI 编排 evidence、完整企业策略下发/审计/组织级锁定、真实确认 UI 和 packaged quit evidence 仍未闭环。 |
| P0-QUICKOPS-DEVELOPER-POLICY | QuickOps 开发者工具本地策略基线 | 进行中 | 2026-06-19 本地 `allowDeveloperTools` 已默认开启并接入 Settings 高级开关；关闭后 CoreBox PreviewProvider 在调用 PreviewSDK 和读取剪贴板 fallback 前阻断 QuickOps Developer 命令，返回 `developer-tools-disabled-by-policy`；capability 摘要把 `quickops.developer.preview` 标记为 disabled，Flow `format-text` fail-closed 为 `quickops.policyBlocked`。该项只完成 CoreBox PreviewProvider / PreviewSDK 与现有 `format-text` Flow target 的本地策略基线；真实 AI UI 编排 evidence、完整企业策略下发/审计/组织级锁定、真实确认 UI 和 packaged quit evidence 仍未闭环。 |
| P0-TRANSPORT | Transport Wave A retained aliases 继续收口 | 进行中 | 已完成 sync/terminal/opener/auth/CoreBox UI、beginner shortcut、input focus、ui resume、forward key event、input command、input visibility/value request、input monitoring、clipboard allow、provider management、recommendation、preview history/copy、action panel、MetaOverlay bridge、layout control、uiMode enter/exit 等 alias 迁移切片；2026-05-20 MetaOverlay built-in/item actions 已统一经 renderer action pipeline 处理，插件 action 仍走 action-executed 通道；继续按 canonical event + legacy alias registry + dual listen 策略推进，禁止改变 wire name 语义。 |
| P0-STARTUP | CoreApp 启动异步化长尾跟进 | 进行中 | Dev 与 packaged hot/cold benchmark、CoreBox 核心状态、App Index workbench 已完成最近路径验证；2026-05-20 已补 CoreBox app launch handoff：App Provider background launch 先 await immediate hide，再异步执行 app，避免慢启动期间可见 CoreBox 卡死；2026-05-28 已新增 CoreApp 性能基线与优化执行计划，定义启动/CoreBox/runtime/build 四条主线指标、报告结构、低风险排序和高风险门槛；2026-06-18 已落地第一组最小安全切片：renderer `appSettings` hydration 首屏前 600ms soft timeout，main startup foreground/deferred 两段加载，先仅将 `ExtensionLoader` 与 `FileSystemWatcher` 串行后台化；同日第二组切片已将 `UpdateService` release cache hydrate、macOS autoUpdater setup 与 startup auto-check 移到 `ALL_MODULES_LOADED` 后，并让 storage update stream 在销毁期直接 end；第三组切片已将 `DownloadCenter` network monitor / scheduler start 延后到 onInit 返回后，初始网络检查使用 light mode；第四组切片已将 `SystemUpdate` 状态 ensure、FX rate hydrate、poll/startup refresh 延后到 `ALL_MODULES_LOADED` 后；同日新增 `docs/plan-prd/04-implementation/performance/WindowsStartupOptimization-2026-06-18.md`，明确 Windows 优化先分层采集首窗显示、CoreBox 可交互、provider/full ready，再推进插件摘要缓存、Everything 后台检测、FileSystemWatcher idle、renderer 首屏拆包和 Windows 包签名/asar/unpacked 审计；`typecheck`、targeted lint/test 已恢复通过；dev startup benchmark 修复后 Run 12-20 连续通过且 0 WARN / 0 ERROR。后续继续用 Windows packaged hot/cold benchmark 复核真实设备体验。 |

## P1 - 近期推进但不阻塞 2.4.11

| ID | 事项 | 状态 | 验收/证据 |
| --- | --- | --- | --- |
| P1-TOUCH-WIDGET | TouchWidget Arrow/WebComponent beta | 进行中 | `arrow` 与 `webcomponent` runtime 以 beta 标记接入 Widget pipeline；保持 `.vue` 默认兼容。2026-06-06 已新增 `WidgetSandboxEvidence` renderer 诊断合同，focused tests 覆盖依赖 allowlist/未声明 require、storage/cookie isolation 与 window/document escape handles；后续补真实示例插件、packaged/dev source 手动验收、navigator/worker/postMessage 等扩展拦截与 `@arrow-js/sandbox` 单独安全评估。 |
| P1-AI-250 | Tuff 2.5.0 AI 桌面入口 | 进行中 | Step 0 需求口径已收口：2.5.0 Stable 只承诺 CoreBox `text.chat`、显式 `vision.ocr -> text.chat`、Nexus invoke/provider routing、Local/Ollama 首选不访问 disabled Nexus、未登录/provider unavailable/quota exhausted/model unsupported/permission denied 固定失败路径与 provider/model/trace metadata；OmniPanel Writing Tools MVP、Workflow `Use Model`、Review Queue 与 3 个 P0 模板已进入 dev/Beta 切片，但不作为 Stable blocker。CoreBox AI Ask、handoff session、Nexus invoke credits 扣减、CoreApp credits summary、Tuff-native Tool Kit foundation、OmniPanel Writing Tools MVP、Workflow service、agent/tool channels 与 Assistant typed transport 都不是空壳；2026-05-17 已有 source/UI-helper focused coverage，但仍缺 packaged Electron text/OCR answer/failure evidence，不能标记为体验闭环通过。2026-06-14 `touch-intelligence` CoreBox AI Ask 执行期已补 focused test，确认拒绝 `intelligence.basic` 时不调用 Intelligence SDK，并推送可重试 `PERMISSION_DENIED` 错误项。后续先补 CoreBox 文本/OCR packaged evidence、Nexus invoke 固定失败路径、Local/Ollama routing log/trace 与 provider metadata chips；再补 OmniPanel provider-backed copy/retry/replace、Workflow Review Queue packaged run、TTS 队列/播放服务与持久缓存评估。Nexus 首页新增的模型路由、桌面上下文与 Agent 工具执行叙事必须回落到 provider/model/trace chips、剪贴板/选区/OCR 上下文 evidence、内置工具/MCP/插件执行失败路径 evidence，不得仅凭官网文案视为完成；固定验收矩阵见 `docs/plan-prd/04-implementation/Evidence-Matrix-AI-Stable-2026-06-18.md`。 |
| P1-AI-253 | Tuff 2.5.3 本地知识检索与上下文构建 | 已定方向 | 已新增 `docs/plan-prd/03-features/ai-2.5.3-local-knowledge-retrieval-prd.md`；方向为 SQLite / FTS5 / metadata / Context Builder 优先，embeddings 与 rerank 作为增强项。2.5.3 聚焦 documents/chunks schema、FTS5 召回、metadata 过滤、citation 与上下文拼接，不把向量数据库作为 MVP 第一优先级。 |
| P1-AI-254 | Tuff 2.5.4 ContextHygiene 与自动记忆治理 | 已定方向 | 已新增 `docs/plan-prd/03-features/ai-2.5.4-context-hygiene-memory-prd.md`；方向为 Session Boundary / Checkpoint / Rolling Summary / MemoryPolicy / ContextPackage。P0 收窄为 CoreBox-only、feature flag、deterministic scope、SQLite session/turn/checkpoint/package log/tombstone、token budget prune 与 explain metadata；P1 再做结构化压缩和 Memory suggested flow。关键约束：新 session 不默认继承旧原文，敏感/secret turn 不进 FTS/embedding/context log/sync payload，插件不直接读 MemoryStore，LangChain 外部 tracing/cache/vectorstore 默认禁用，删除 memory 必须 tombstone 并在 prepareTurn 二次过滤。 |
| P1-AI-255 | Tuff 2.5.5 本地开源模型运行时 | 已定方向 | 已新增 `docs/plan-prd/03-features/ai-2.5.5-local-model-runtime-prd.md`；方向为不强依赖 Ollama，优先内置 GGUF / llama.cpp runtime，模型权重按需下载到用户数据目录；Ollama 仅作为可选兼容后端。2.5.5 聚焦本地模型目录、下载/删除、健康状态、文本能力本地优先可回退与 runtime 可观测，不抢占 2.4.11 稳定化与债务退场主线。 |
| P1-AI-258 | Tuff 2.5.8 ASR Provider Runtime | 已定方向 | 已新增 `docs/plan-prd/03-features/ai-2.5.8-asr-provider-runtime-prd.md`；方向为本地 `whisper.cpp` + 云端 ASR provider 抽象，支持 `local-only/cloud-only/auto` 策略。2.5.8 聚焦音频文件转写、provider health、隐私策略、音频 artifact 生命周期与统一 transcript result；TTS、语音唤醒和 streaming 转写不进入 Stable。 |
| P1-TRANSLATION-IMAGE | Translation 剪贴板图片翻译与 DivisionBox 展示 | 进行中 | 2026-05-19 已新增 `image.translate.e2e` Intelligence capability，并把 Tuff Nexus provider 接到 `corebox.screenshot.translate` scene，Translation 的 `screenshot-translate` 已可识别剪贴板图片 data URL、调用 Intelligence/Nexus 图片翻译并用 detached DivisionBox widget 展示结果；DivisionBox 支持 `header.show=false` 与 `ui.showInput=false`，Translation manifest 已声明 `window.create`，SDK open 已兼容直接 `SessionInfo` 返回以避免误判失败。2026-05-21 Assistant 剪贴板图片翻译动作已复用同一 scene/helper，直接读取当前剪贴板图片转 base64 后打开图片翻译 pin window，并把 data URL/raw base64 归一化继续收敛到 `normalizeImageBase64Payload`；已补 data URL helper、Nexus scene、DivisionBox header height、SDK open、Assistant clipboard image/helper、visible evidence gate focused tests 与不读取剪贴板内容的 `smoke:assistant` 悬浮球/VoicePanel 可见入口 smoke；仍需 packaged Electron 下的真实剪贴板图片翻译、腾讯 adapt、空剪贴板图片提示与 CoreBox/provider fallback 手工证据。 |
| P1-PROVIDER | Nexus Provider Registry / Scene 编排 | 进行中 | 已有 D1 secure store、Scene run、Dashboard dry-run/execute、AI mirror、health/usage ledger、capability adapter readiness 与最小策略路由；旧 `intelligence_providers` 表退场已补实施计划 `docs/plan-prd/04-implementation/NexusIntelligenceProviderRetirement-2026-05-16.md`。2026-05-17 已在 `docs/engineering/reports/coreapp-visible-evidence-2026-05-17/provider-migration-dry-run.md` 补本地隔离 API/bridge dry-run 证据，使用 migration API handler + 真实 migration bridge + Mock D1 确认 dry-run 输出不会声称 registry-primary ready、不含 secret，且不会写 registry / secure store；Provider Registry Admin observability list 已补 initial/filtered/no-attention/no-unknown/no-failed empty states 与 “show all” 过滤恢复动作，Usage Ledger / Health tabs 已补 failed/planned/estimated/completed/unhealthy/degraded/healthy next-action guidance，以及 all/attention/status 本地过滤和计数；Provider 列表 capability chip 已显示 adapter ready/missing，且通过轻量 adapter registry 避免列表接口加载执行期 Scene adapter 依赖；这些仍不是真实 Dashboard/Admin UI evidence。后续仍需按 Phase 0/1 采集真实 dry-run/execute evidence 和真实 Provider Admin health/usage/scene/filter 视觉证据，再推进 registry-primary reads、user-scope OCR 自动绑定、success rate/配额/dynamic pricingRef。 |
| P1-DATA-GOV | Nexus 数据治理、插件分析、存储/通知/Provider quota | 进行中 | 统一治理 cockpit 已覆盖匿名访问/搜索偏好、插件 analytics/conversion、上传状态、通知健康、存储策略/告警、local/memory storage smoke evidence、Provider usage/quota、operations summary、read-only operations report snapshot、secure credential reference 与 object storage executor；direct invoke quota 与 Provider Registry quota admin 已并入同一治理口径；2026-05-25 已补 Data Governance / Provider Registry admin hydration、locale reconciliation 与 i18n 字典稳定化，并完成本地 Wrangler Pages + Miniflare seeded admin 的认证 cockpit 浏览器证据。8 项进度矩阵见 `docs/plan-prd/01-project/NEXUS-DATA-GOVERNANCE-PROGRESS-2026-05-23.md`，storage sizing/smoke/alert runbook 见 `docs/plan-prd/04-implementation/NexusStorageGovernanceRunbook-2026-05-24.md`，notification channel/live-send/privacy runbook 见 `docs/plan-prd/04-implementation/NexusNotificationGovernanceRunbook-2026-05-24.md`，细节以 `docs/plan-prd/01-project/CHANGES.md` 为准。后续只保留生产/preview 认证 operator evidence、真实凭据/live send、SMTP/relay、Web Push VAPID/relay、R2/S3/OSS live storage、生产 D1 migration/backfill、真实 provider quota fail-closed 与更完整运营大屏；固定验收矩阵见 `docs/plan-prd/04-implementation/Evidence-Matrix-Nexus-Governance-2026-06-18.md`。 |
| P1-DATA-GOV-UPLOAD-DIAG | Nexus 上传可靠性 problem attempts | 进行中 | Upload health 已提供 failed/stuck problem attempts 脱敏 hash 摘要、retryable/scheduled/exhausted 聚合与 bounded retry metadata，不暴露原始 attemptId/resourceId。后续用真实上传失败样本和 live object storage evidence 校准原因分类。 |
| P1-NATIVE | Native transport V1 能力 smoke | 后置 | 补 macOS 屏幕录制授权、Windows 多屏、Linux X11/Wayland best-effort 的能力 smoke；打包预览确认 `sharp` 与 ffmpeg/ffprobe 可执行；平台能力只按 `docs/plan-prd/04-implementation/Evidence-Matrix-Platform-2026-06-18.md` 做非阻塞回归跟踪。 |
| P1-NETWORK-SETTINGS | CoreApp 统一网络/代理设置 | 已落地 | 高级设置已接入 shared Network SDK 配置面，支持系统代理/直连/自定义代理、HTTP/HTTPS/SOCKS/PAC、bypass rules、timeout/retry/cooldown；代理认证只展示 secure-store reference 状态，不写明文凭据。后续可补真实代理连通 evidence 与 secure credential 编辑入口。 |
| P1-QUICK-LAUNCH | Quick Launch 能力验收 | 后置 | 验证默认浏览器打开、`network.internet` 授权/拒绝、suggestion 超时降级、URL 打开与网页搜索互不抢占。 |
| P1-PUBLISHER | 插件发布管理端到端证据 | 进行中 | CoreApp `/store/publisher` 与 Nexus assets/API key 流程已接入；2026-05-18 已修复发布认证 scope 口径：`plugin:publish` 隐含 `plugin:read`，新建 API key 默认带 `plugin:read` + `plugin:publish`；CLI 真实 publish 前改用 `/api/dashboard/auth/publisher` 进行 publisher 预检并兼容 API Key，旧 Nexus 缺少该 endpoint 时回退 Dashboard plugin lookup，避免 `/api/auth/me` 误杀 API Key；交互 CLI 已区分本地 token 文件与远端可用登录态，账号/云端插件列表会显示认证或网络失败原因而非伪装为空列表；`--dry-run` 保持本地预览；`touch-snippets` 已收口 text/code/prompt/template 片段的 CoreBox 搜索、复制、保存与管理入口，旧 `touch-text-snippets` / `touch-code-snippets` 已从 CoreBox feature surface 退为 legacy placeholder；仍需补 package policy/security scan、真实 `.tpex` 上传端到端证据，以及旧 snippets 目录删除/迁移策略确认。 |
| P1-CLOUDSHARE | CloudShare 插件内容包发布/安装 MVP | 进行中 | 已新增 `docs/plan-prd/03-features/cloudshare-plugin-content-prd.md`、`CloudShareSDK`、Nexus `/api/store/plugin-content/*`、Store 详情页 Content tab 与 `touch-snippets` snippet pack 导出/导入/list/install 基础能力；2026-05-19 CoreApp Store 插件详情页已接入公开 Content 区块，目标插件已安装时可通过 `plugin-content:install` 将 `tuff.snippet-pack+json` 写入本地 `touch-snippets/snippets.json`，未安装时只提示先安装插件，不做自动串联。仍需补插件 Surface/CoreApp Store 的登录发布 UI、内容审核队列、团队可见与订阅/fork。 |
| P1-APP-DATA | App Data Plugins 与 Everything 收口 | 进行中 | 已新增 `docs/plan-prd/03-features/search/APP-DATA-PLUGINS-AND-EVERYTHING-ROADMAP.md`、`docs/plan-prd/03-features/search/RAYCAST-UTOOLS-CAPABILITY-GAP-MATRIX.md` 与 `docs/plan-prd/03-features/search/INDEXING-RUNTIME-V1-PLAN.md`。2026-05-22 已补 `docs/plan-prd/03-features/search/competitive-analysis-2026-05-22/`，覆盖 Raycast / Alfred / uTools 竞品能力、交互、Workflow、插件跨平台、搜索排序、参数变量、翻译/OCR/AI 命令、Store/SDK、本地数据与执行路线，并附 100 轮交叉审计台账和 70 份 micro-round 记录；2026-05-30 已在 `@talex-touch/utils/search` 暴露 `IndexedSource*` 最小类型合同，并新增 CoreApp `IndexingRuntime` skeleton，覆盖 source registry、diagnostics 聚合、health failure 隔离与 root-based watch event routing；App/File/Everything 已作为 thin core indexed-source adapters 注册，先把现有 provider diagnostics 映射到统一 source health/roots，不迁移 scan/search 行为；同日已通过 `CoreBoxEvents.search.indexingDiagnostics` typed transport 暴露统一 `IndexedSourceDiagnosticsSnapshot`，并在 Advanced Settings 的 File Index 页面新增 Search Source Diagnostics 总览，展示 App/File/Everything source status、itemCount、watch/reconcile state 与 roots/error/reason 摘要；CoreBox no-result 空态也已消费同一 typed diagnostics snapshot，展示 degraded / permission-required / error / warming source 摘要与恢复入口。Runtime task model 已拆出 ScanScheduler、WatchEventRouter、ReconcileEngine、IndexStoreAdapter、IndexingRootPolicy 与 SourceDiagnosticsService；ScanScheduler 已补 batch scan result 统计与 source-level failure isolation；ReconcileEngine 已补 batch reconcile result 统计与 source-level failure isolation；WatchEventRouter 已补 route result 统计与 source/store failure isolation；diagnostics snapshot 已开始合并最近 scan/watch/reconcile runtime task state；SearchIndexStoreAdapter 已接入现有 SQLite/SearchIndexService 的 indexItems/removeItems/removeByProvider，作为 runtime scan/watch delta 的最小真实写入边界；IndexedSourceAdmission SDK 准入口径已新增，App/File/Everything descriptors 已声明 owner/permission/default/clear/rebuild 元数据，后续 Browser Data、Quicklinks、Obsidian、VSCode source 必须先通过 admission 校验再接 runtime；AppIndexedSource 已接入 startup backfill/manual rebuild scan、full sync + macOS mdls reconcile 与 app path watch lifecycle，并输出 Windows Start Menu/UWP/Registry/App Paths/Steam、macOS mdfind/mdls、Linux desktop entries 与 watch roots evidence；Windows app scanner 已新增 `getAppsBySource()`，把 Start Menu、UWP/Get-StartApps、Uninstall Registry、App Paths Registry 与 Steam manifest 拆成一等 scan result，旧 `getApps()` 继续 flatten + dedupe 以保持行为兼容；AppProvider Windows source evidence 已优先消费 scanner grouped result，包括每个子来源的 empty/error reason，DB metadata 推断仅作为 fallback；App scan 已开始把 `ScannedAppInfo` 映射为 `IndexedSourceRecordBatch`，由 `AppIndexedSource.scan()` yield 给 runtime `ScanScheduler` 和 `SearchIndexStoreAdapter` 写入边界；App watch delta 已开始返回 add/change `record` 与 delete `stableKey/path`，且 App watcher 入口已迁到 SearchEngineCore runtime bridge；App reconcile 已使用 full sync / macOS mdls repair 的真实 added/changed/deleted/skipped/errors 统计填充 `IndexedSourceReconcileResult`，不再固定返回 0/0/0；FileIndexedSource 已接入 manual rebuild/worker scan、reconcile、incremental watch updates 与 clear/rebuild lifecycle；File scan 已开始返回 IndexedSourceRecordBatch 给 runtime store boundary，2026-06-14 已补 File 与 Quicklinks 空 `done` batch 保留，确保 runtime store 可观察 completion/flush 信号且普通空中间批次继续过滤；Quicklinks enabled source 缺持久 clear handler 时 `clearIndex` 已改为 `quicklinks-clear-handler-not-configured` fail-closed，disabled source 仍保持 no-op；File watch delta 已开始返回 add/change record 与 delete stableKey/path；全局 file watcher 事件已从 FileProvider 私有订阅迁到 SearchEngineCore runtime bridge；File reconcile 已透传 full scan、reconciliation diff 与 stale cleanup 的真实 added/changed/deleted/skipped/errors 统计；Everything 已拆出 EverythingIndexedSource，并把 path filtering 改为读取 runtime root policy，不再直接读取 FileProvider 私有 watch roots，无 File source roots 时 fail-closed；Browser Bookmarks 已新增 runtime skeleton，以 high privacy + disabled/pending-migration diagnostics 表达 touch-browser-data 仍待迁入 SQLite-backed scan/watch/reconcile。近期目标是把 File provider 内部写入逐步迁到 runtime store 边界，再按 Raycast/uTools 常用能力差距继续补 `touch-browser-data` 真实浏览器书签/历史、Everything 运行证据、App Launcher evidence、Quicklinks 统一模型与 Context Actions；Calculator 显式入口已通过 CoreBox `calc` / `calculator` / `calculate` / `计算` / `换算` 前缀接入 PreviewSDK；`touch-snippets` 首批 placeholders 已支持 `{{date}}` / `{{time}}` / `{{uuid}}` / `{{clipboard}}`，browser-tab、cursor、hot string/autopaste 后置；`touch-emoji-symbols` 首版已支持常用 emoji/symbol 搜索与复制，recent usage、扩展数据集和 Store evidence 后置；Nexus SDK 文档已新增插件开发任务流并把 quickstart/manifest/publish 口径收敛到 `main: "index.js"`、Prelude、`tuff validate/build/publish --dry-run` 与 CloudSync/CloudShare 边界；TuffEx 场景化 demo、基础组件与 per-component docs 首批覆盖已完成；Windows Everything 已完成 CLI 手动选择、诊断 evidence 字段与 runtime root policy 路径过滤代码闭环，仍需收口 SDK/CLI 最终策略、Windows 运行证据与性能基线；2026-05-20 已补 `touch-browser-data` source-level diagnostics、Linux Arc unsupported 口径、profileCount/read-failed reason 与平台感知空态；2026-05-22 已补书签 URL 打开 `network.internet` gate；历史扫描、写索引、disable/clear/rebuild UI 与跨平台 evidence 后置；明确不包含更新系统 Nexus Hard-Cut。 |
| P1-PLUGIN-PRELUDE | 插件 Prelude 工程化 | 已落地 | 新插件推荐使用 `src/prelude/` 组织 Prelude 源码，DevKit dev 态增量打包虚拟 `index.js`，`/_tuff_devkit/update` 暴露变化状态，core-app 监控到 `index.js.changed` 后自动 reload 插件生命周期；`tuff builder` 构建态输出 `dist/build/index.js`。旧根 `index.js`、`manifest.build.index` 与 `index/` 目录保持兼容。 |
| P1-UI-COMPAT-AUDIT | UI/兼容/占位/架构审计后续收口 | 进行中 | 2026-05-25 审计报告见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-05-25.md`，2026-05-29 增量复核见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-05-29.md`，同日 post-slice 复核见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-post-slice-review-2026-05-29.md`；当前未发现新的 P0 fake-success。2026-05-26 已完成 preload debug DOM 文本化与同段 debug console 清理，并通过 preload 单文件 ESLint 与 CoreApp node typecheck。2026-05-29 五个小切片已落地：legacy alias hit telemetry/hard-cut 判定记录不记录 payload；旧 snippets manifest 已 hidden/deprecated 并指向 `touch-snippets`；Nexus evidence source 统一为 `live | d1 | r2 | local-only | memory | open`，`memory/local-only` 不计入生产完成 evidence；TuffEx/CoreApp dialog `message` 改为纯文本，`messageHtml` 显式 trusted-only；TuffEx visual smoke 脚本已覆盖 composition 五个 demo、375/768/1440、light/dark 与 reduced motion，2026-06-01 已补 Nexus/Chrome CDP 30/30 截图/JSON evidence。当前下一步是 release cycle legacy hit=0 观察、UI 语义控件与 File write/store boundary。 |
| P1-UI-COMPAT-AUDIT-20260530 | UI/兼容/占位自动化增量审计跟进 | 进行中 | 2026-05-30 增量报告见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-05-30.md`；本轮继续未发现新的生产路径 P0 fixed fake-success。高信号下一步：补 Windows App indexing / Everything CLI registry PATH；推进 FileProvider 写入边界向 Indexing Runtime store adapter 迁移；清理 `div/span @click` 等 UI 语义债务；把直接 `localStorage` UI 偏好收敛到 facade；将 cloud preset placeholder 从 public singleton coming-soon 文案改为 capability unavailable / hidden contract。TuffEx visual smoke 真实 evidence 已于 2026-06-01 30/30 通过。 |
| P1-UI-COMPAT-AUDIT-20260531 | UI/兼容/占位自动化增量审计跟进 | 进行中 | 2026-05-31 增量报告见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-05-31.md`；继续未发现新的生产路径 P0 fixed fake-success。新增高信号事项：TuffEx Tabs Fragment / async component name 修复已补 focused test，Nexus visual smoke 已于 2026-06-01 产出截图/JSON evidence 且 30/30 通过；TuffEx 已新增组件子路径 exports、dist-local utils declarations、真实局部 `style.css` 生成、`audit:exports` / `audit:types` / `audit:size` 和 Core App 受控注册按需加载，Core App renderer 根入口导入文件数已从 150 降到 0，`tuff-business`、`intelligence-uikit` 与 Nexus app 根入口消费也已降到 0，Nexus app 运行时代码切片由 `audit:size` 预算防回涨；2026-06-01 追加按需样式注入插件，发布态自动补齐 `<component>/style.css`，Core App/Nexus dev 源码 alias 下关闭注入以避免重复 CSS；Core App、Nexus 与 `intelligence-uikit` 应用级入口已迁到 `base.css`，并由 `audit:size` 同时把这些消费侧的 full `style.css` import 预算锁为 0；`audit:size` 已新增 `button` / `input` / `select` / `code-editor` 代表性按需入口运行时依赖图检查，防止子路径误拉根入口或无关组件目录；同日已新增 `@talex-touch/tuffex/base.css` 基础样式入口并纳入 `audit:size` 预算，`TxScroll` 的 BetterScroll pull-down/pull-up 插件也改为功能开启时按需加载并由 `scroll` 按需依赖图审计防回涨，wheel/bounce guard/RAF apply 运行时已拆到 `useScrollWheel`，SFC 从 887 行降到 613 行且产物单体从约 24.4 KiB 降到 14.9 KiB；`TxCodeEditor` 已改为轻量 async wrapper，CodeMirror/YAML runtime 延后到真实渲染时加载，并由 `audit:size` 禁止默认 `code-editor` 入口静态拉入 CodeMirror；`TxButton` 已改为动态加载 `v-wave`，由 `button` 按需图预算禁止静态回涨；空态 wrapper 局部样式已改为轻量引用 `empty-state/style.css` 并由 `audit:size` 防回涨；`TxBaseAnchor` 的 GSAP motion 已抽到动态加载 composable，默认 `base-anchor` / `button` / `select` 静态图由 `audit:size` 禁止拉入 `gsap`；`TxFlipOverlay` 的 GSAP tween/stack-card motion 已抽到动态加载 composable，默认 `flip-overlay` 静态图由 `audit:size` 禁止拉入 `gsap`；`TxRadioGroup` 的延迟模型提交与 indicator motion/drag/keyboard 逻辑已拆到内部 helper，SFC 从 1150 行降到 326 行，并由 `radio` 按需图审计防止误拉根入口；`TxBaseSurface` 的数值解析与 auto-detect/refraction recovery 状态机已拆到内部 helper，SFC 从 1150 行降到 725 行，产物单体从约 41.4 KiB 降到 29.3 KiB，并由 `base-surface` 按需图审计防止误拉根入口；后续可继续按同一模式细拆 refraction 纯模型等较大内部逻辑；`SharedPluginDetailReadme` 默认未 sanitize 应迁到统一 Markdown sanitizer；Update release notes / MainBox completion `v-html` 继续确认 escape 边界；UI preference 直接 `localStorage` 与主路径 `div/span @click` 继续按小切片收口；cloud preset public placeholder 改为 capability unavailable / hidden contract。 |
| P1-UI-COMPAT-AUDIT-20260601 | UI/兼容/占位自动化增量审计跟进 | 进行中 | 2026-06-01 增量报告见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-06-01.md`；继续未发现新的生产路径 P0 fixed fake-success。TuffEx composition visual smoke 30/30 evidence 已闭环。2026-06-02 已收口 Markdown/HTML 默认 sanitize、Update release notes renderer、preload loading overlay DOM 构建、Nexus device/privacy localStorage 口径、CoreApp UI preference facade、首批语义控件与 cloud preset unavailable contract；focused tests 已新增但需在正常 Volta/pnpm 环境补跑。当前仍需继续推进 File write/store boundary。 |
| P1-UI-COMPAT-AUDIT-20260603 | UI/兼容/占位自动化增量审计跟进 | 进行中 | 2026-06-03 增量报告见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-06-03.md`；继续未发现新的生产路径 P0 fixed fake-success。当前 `master` 与 `origin/master` 无 ahead 差异，但 dirty worktree 仍有 304 tracked + 23 untracked 文件，必须先按 TuffEx、Nexus、CoreApp renderer、`intelligence-uikit` 分批收口。TuffEx README 已改为 `base.css` + 子路径推荐，Nexus content docs 仍有旧 full `style.css` 示例；补跑标准环境 build/audit/typecheck 后，再回 File write/store boundary。 |
| P1-UI-COMPAT-AUDIT-20260604 | UI/兼容/占位自动化增量审计跟进 | 进行中 | 2026-06-04 增量报告见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-06-04.md`；继续未发现新的生产路径 P0 fixed fake-success。Nexus TuffEx public docs 推荐示例已改为 `base.css` + 组件子路径 + 局部 `style.css`，旧 full `style.css` 只保留为迁移期兼容说明；Nexus `notesHtml` 已接共享 sanitizer。2026-06-05 当前 dirty worktree 为 314 tracked + 25 untracked 文件；TuffEx build/audits、Nexus typecheck/build 与 PreviewSDK focused test 已在 Node 22 + pnpm 10 通过，下一步补 Nexus visual smoke CDP 证据，并继续收 CoreApp/TuffEx dialog HTML boundary。 |
| P1-UI-COMPAT-AUDIT-20260606 | UI/兼容/占位自动化增量审计跟进 | 进行中 | 2026-06-06 增量报告见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-06-06.md`；继续未发现新的生产路径 P0 fixed fake-success。TuffEx/CoreApp dialog `messageHtml` 已补 `TrustedDialogHtml` branded type 与 `asTrustedDialogHtml()` 显式标记边界；Widget runtime 已补 `WidgetSandboxEvidence` 与 focused tests。`touch-music` / `touch-image` 示例插件 starter 遗留物已在 2026-06-13 清理，下一步优先 UI 语义控件，然后回 File write/store boundary。 |
| P1-UI-COMPAT-AUDIT-20260607 | UI/兼容/占位自动化增量审计跟进 | 进行中 | 2026-06-07 增量报告见 `docs/plan-prd/report/cross-platform-compat-placeholder-ui-architecture-audit-2026-06-07.md`；继续未发现新的生产路径 P0 fixed fake-success。当前工作区 `HEAD=686ec013e` 且与 `origin/master` 同步；CLI publish 旧 token 交互刷新已落地，非交互仍 fail-closed。`touch-music` / `touch-image` 示例插件 starter asset 清理已在 2026-06-13 完成，下一步优先 UI 语义控件，然后回 File write/store boundary。 |

### P1-APP-DATA 当前执行拆解（2026-05-31）

已落地：

- SDK 层：`IndexedSource*` 类型、source admission、lifecycle contract issue、contract summary、task eligibility、source error health helper、diagnostics summary helper、source snapshot cache、profile diagnostics roots/evidence helper、maintenance action policy、recovery recommendation policy、task run gate、runtime task job identity、runtime task state update 与 scan/watch/reconcile/reset task state builder、watch root routing/path policy、watch root set/ownership/path basename policy、watch delta queue、auto-scan preflight、scan eligibility/strategy policy、source progress/evidence、grouped/root evidence、source progress store/unavailable-store summary/clear decision、source reset executor/operation reason builder、source integrity policy/snapshot adapter/adapter field mapping/evidence mapping、indexing progress ETA/stream helpers、worker persist-entry mapper/status snapshot、write plan/date/timestamp normalization、write record chunk/window/manual-context helpers、path update record shaping、full-scan upsert record shaping、reconciliation add upsert record shaping、reconciliation worker disk/db payload shaping、reconciliation fallback diff、index worker file payload shaping、file source record mapping、write flush retry/classified retry/buffer/entry-keyed buffer/executor/batch metric/result mapper/runtime/runtime config/snapshot/result snapshot/failure snapshot/failure retry metadata/evidence、write runtime emitter（record batch / add-change-delete delta / progress snapshot build + emit）、Search Provider descriptor/config/manifest resolver、indexed source provider config enablement summary、`manifest.indexedSources` metadata resolver 已进入 `@talex-touch/utils`；Quicklinks、Browser Bookmarks、Browser History、System Settings 已有 descriptor template。
- Runtime 层：CoreApp 已新增 `IndexingRuntime`、scan/watch/reconcile/reset task 统计、统一 `updateIndexedSourceTaskState()` last* + recentTasks 更新、root policy、diagnostics typed transport、source admission/lifecycle/progress contract diagnostics、source health failure 到 SDK-standard error health 的兜底映射、source health summary 到 SDK-standard diagnostics summary 的聚合、Settings maintenance SDK 与 `SearchIndexStoreAdapter` 最小写入边界；runtime task job id/queuedAt 生成已下沉到 SDK `IndexedSourceRuntimeTaskJobFactory`，scan `lastScan`、watch `lastWatch`、reconcile `lastReconcile`、reset `lastReset` 与 history entry 构建已开始走 SDK task-state builder，CoreApp 仅保留兼容 re-export；single-source scan/reconcile 已复用 task eligibility guard，避免 Settings 维护入口绕过 batch guard；IndexingRuntime 现在持有共享 SDK `IndexedSourceTaskRunGate` 并注入 ScanScheduler/ReconcileScheduler，reset 也进入同 source/kind running guard，运行中会返回 `reset-already-running` diagnostics 而不是重复清理索引；runtime clear 会同时清理内存态 run gate，为后续 retry/debounce/durable job history 留统一入口，但当前仍保持 running 时拒绝的既有行为。
- Provider 层：App/File/Everything 已作为 thin indexed-source adapter 接入统一 diagnostics 与 runtime lifecycle；Everything path filtering 改读 runtime root policy，避免绕过 File source roots；Provider registry 已把 plugin registration issue / provider id collision 作为 `SearchProviderConfigResponse.issues` 暴露给 Settings，补齐插件 provider 权限阻断的用户可见诊断。
- App 层：Windows scanner grouped result 到 source evidence 的 ready/degraded、empty/error reason、sourceLabel 与 scanner metadata 映射已下沉到 `IndexedSourceGroupedEvidenceService`；watch roots 到 root evidence 的 rootCount/roots/empty reason 映射已下沉到 `IndexedSourceRootEvidenceService`；App scan batch 与 watch delta 已复用 `IndexedWriteRuntimeEmitterService` 输出统一 runtime record/delta 协议；AppProvider 现在只保留平台 key/label、manual synthetic row、watch root path 读取与 DB metadata fallback。
- File 层：watch path policy、watch root set/ownership policy、watch delta queue、auto-scan preflight、scan eligibility/strategy policy、source progress/evidence、source progress store/unavailable-store summary/clear decision、source reset executor/operation reason builder、source integrity policy/snapshot adapter/adapter field mapping/evidence mapping、progress ETA/stream、path-record write plan/date normalization/write record chunk/window/manual-context helpers/path update record shaping/full-scan upsert record shaping、reconciliation add upsert record shaping、reconciliation worker disk/db payload shaping、reconciliation fallback diff、insert/update/delete executor、worker scheduler、worker persist-entry mapper/status snapshot、write flush retry/classified retry/buffer/entry-keyed buffer/executor/batch metric/result mapper/runtime/runtime config/snapshot/idle snapshot/result snapshot/failure snapshot/failure retry metadata/evidence、index worker file payload shaping、file row 到 `IndexedSourceRecord` mapping、write runtime emitter、post-write side-effect 已下沉到 `@talex-touch/utils/search` SDK；File indexed source 已把 FileProvider 私有 indexing status 适配为统一 diagnostics `progress`，包含 stage、current/total、百分比、estimatedRemainingMs、estimatedCompletionAt、averageItemsPerSecond、speedSampleCount 与 estimateBasis；FileProvider watch service 现在只保留 CoreApp watcher 注册、pending permission 读取、设置持久化、scan_progress 读取、idle/battery gate 与平台适配；FileProvider scan strategy service 只保留 completed path 读取、yield 与日志；FileProvider incremental write planner 只保留 File row 类型别名、diffing 配置和 manual summary 输出，update record 的 existing-row identity、name/extension/size、ctime/mtime、type/isDir shaping 已复用 SDK helper；FileProvider incremental write 只保留 buildRecord、existing rows 读取、insert/update delegation 与日志，manual trigger path set 和空 manual summary 已复用 SDK helper；FileProvider full scan run 只保留目录扫描、progress、insert delegation 与 completed-path reporting，name/extension/size/ctime/mtime/lastIndexedAt/isDir/type upsert payload shaping 已复用 SDK full-scan helper；FileProvider reconciliation run 只保留目录扫描、DB row 读取、diff delegation 与写入 orchestration，worker disk/db payload 的 path/name/extension/size/ctime/mtime/id shaping 与 main-thread fallback diff 已复用 SDK helper，invalid timestamp 继续 fallback 为 `0`；FileProvider reconciliation insert 只保留 upsert delegation、side-effect dispatch、runtime batch/delta/progress emission 与 source-specific reason wiring，chunk splitting 与 add upsert record 的 path/name/extension/size/ctime/mtime/lastIndexedAt/isDir/type shaping 已复用 SDK helper；FileProvider full scan insert 只保留动态 batch size 读取、upsert delegation、sleep pacing、side-effect dispatch 与 runtime batch/progress emission，record window 切片已复用 SDK helper；FileProvider reconciliation update record shaping 也已复用同一 `toIndexedWriteDate()` helper 处理 `ctime`/`mtime`；FileProvider index worker scheduling 现在复用 `mapIndexedWriteWorkerFilePayload()` 处理 id/path/name/displayName/extension/size/ctime/mtime worker payload shaping，CoreApp 只保留当前时间 fallback 注入与大文件 background dispatch 策略；FileProvider full scan insert、reconciliation add/update/delete 与 stale cleanup delete 已复用 `IndexedWriteRuntimeEmitterService` 输出 record batch、add/change/delete delta 与 progress，App scan batch 与 App/File watch delta 也复用同一 emitter 的 build helper，FileProvider 只注入 source id、现有 upsert/update/delete 和 runtime callback；FileProvider worker status 现在只保留真实 worker status loader 注入；FileProvider index flush buffer 只保留 file worker result 的 `fileId` key selector 适配，flush executor 只保留 File status、withContent metric predicate 和 persist/worker 适配，flush trace idle/结果 snapshot 只注入 File 状态，flush runtime config 默认值已复用 SDK normalization，flush failure retry decision 只保留 SQLite busy 分类与 File-specific reason 命名，retry/backoff/fallback 决策复用 SDK classified retry helper，flush failure metadata 复用 SDK retry metadata helper，index-flush evidence 只保留 source id/label 适配；FileProvider scan_progress store 现在只注入 completed path select/delete、store availability、worker readiness 与 upsert；FileProvider runtime reset 只声明 `file-index` namespace，`remove-by-provider` / `scan-progress-reset` reason 由 SDK helper 生成，scan_progress row-count 到 reset cleared/no-op 结果的决策也复用 SDK helper；FileProvider integrity check 现在只注入 FTS/files row count、runtime reset、orphan keyword cleanup 与 File evidence id/label/reason/metadata 命名，`ftsRows` / `filesRows` / `orphanedKeywordsRemoved` 字段映射由 SDK helper 生成；DB persist、scan_progress 表结构、worker readiness 与 SearchIndex/FTS 语义继续留在 FileProvider adapter / SearchIndex worker 边界等待下一批迁移。
- Settings / 插件层：Settings 已支持 source diagnostics、source-level progress chip、source recovery chip、provider enable/order、source-to-provider link 与 SDK maintenance action policy；progress chip 消费 `IndexedSourceDiagnostics.progress`，统一显示阶段、百分比、剩余时间、ETA、吞吐、样本数、estimateBasis 与 stalled/failed/complete tone；recovery chip 消费 `resolveIndexedSourceRecoveryRecommendation()`，把 admission/lifecycle、permission/disabled、progress failed/stalled、recent task failed/skipped 与 health degraded/error 归一成只读恢复建议，不自动执行维护动作；maintenance buttons 只按 `resolveIndexedSourceMaintenanceActions()` 返回的 enabled 状态可点，后续 Browser Bookmarks、Quicklinks、Obsidian、VSCode source 只要实现 `getProgress()` 并提供 diagnostics 即可复用；插件可声明 `manifest.searchProviders`，第三方 push provider 需 `root-results` + `defaultState: "ask"` + `requiresUserConsent: true` 才能通过 loader policy；插件也可声明 metadata-only `manifest.indexedSources` lifecycle intent，CoreApp loader 会校验 admission/permission 并暴露 `INDEXED_SOURCE_*` diagnostics，但不会自动注册 runtime source；向 root results push/update 已同时受 `search.root-results` 权限 gate 与 provider `enabled === true` 用户配置约束；CoreBox root-result store 对已有 provider-tagged items 也按严格 enabled 过滤，避免旧 push 结果残留。
- Browser Bookmarks：已从 metadata-only intent 推进到官方 `touch-browser-data` owned runtime lifecycle；默认仍 disabled 且诊断 reason 明确为官方 provider 需要用户同意，只有显式启用 `touch-browser-data.browser-bookmarks` 后才读取本机 Chromium `Bookmarks` 文件；descriptor/evidence 已标记 `runtimeOwner: "official-plugin"`、`storage: "sqlite-index"`、`privacy: "high"` 与 persistent plugin-owned indexing enabled 状态；Settings 高级诊断页已提供 Browser Bookmarks consent、disable、rebuild、clear UI，并继续展示 watch roots、evidence chips、recent tasks 与 source health；scan/reconcile/watch/reset/clear 均走统一 Indexed Source runtime handler，scanner-backed batch/delta 复用 `IndexedWriteRuntimeEmitterService`，watch route 只处理 `Bookmarks` 文件变化；profile/browser diagnostics 到 roots/evidence 复用 `IndexedSourceProfileDiagnosticsService`，同轮 diagnostics 复用短 TTL snapshot cache，scan/reconcile/watch/reset 会读取 fresh snapshot；`touch-browser-data` manifest notes 已声明同意后由官方 runtime 读取本地 Bookmarks JSON 并维护 lifecycle，focused test 覆盖 provider contract、disabled no-read、roots/evidence、watch deltas、reset/clear 与 manifest 对齐。
- Quicklinks：已新增 low-privacy `QuicklinksIndexedSource` runtime skeleton 并注册到 CoreApp runtime；它复用 Quicklinks descriptor template 与 `IndexedWriteRuntimeEmitterService`，支持可注入 quicklink snapshot 的 scan batch、reconcile/watch change delta、health/evidence、reset/open/clear handler contract；默认空源只报告 `quicklinks-empty` degraded diagnostics，不读取插件 storage，也不伪装已有内容。`touch-browser-bookmarks.quicklinks` 与 `touch-dev-toolbox.dev-toolbox` 已声明 `indexedSourceId: "quicklinks"`，CoreApp `quicklinks-source-config` 已把 linked provider config 映射到 source enablement：Quicklinks 默认 enabled，显式禁用 `quicklinks` provider 后 source 报告 `quicklinks-provider-disabled`，显式启用任一 linked official provider 会恢复 enabled。2026-06-14 Quicklinks runtime 已接入只读 `root-results-visible` feed，从 `BoxItemManager.getVisibleItems()` 读取已通过 provider enable/order 与 `search.root-results` gate 的 linked provider item，映射 HTTP(S) URL 为 runtime snapshot；无可见 linked item 时仍是 degraded evidence，不读取插件 storage。同日补 ScanScheduler focused evidence，确认 File/Quicklinks 这类空 terminal batch 会进入 `store.applyBatch()` 并计为完成 batch，而不是被中途吞掉；默认 runtime-feed evidence 现在暴露 `clearHandlerConfigured: false`，但不扩展 `IndexedSourceHealth` 合同。

未闭环：

- FileProvider 的 SQLite/FTS 真实写入、scan worker、index worker flush trace、`scan_progress` 表实现与 integrity-triggered reset durable job history 仍未完全迁到 runtime task/store 边界。
- Browser Bookmarks 后续只剩真实浏览器 profile/跨平台 watch root evidence、packaged Electron 设置页截图/录屏 evidence，以及 durable job history/retry/debounce 等 runtime 长尾；metadata-only intent、consent、clear/disable/rebuild UI 与 diagnostics evidence 已在本切片收口。
- Quicklinks 仍需接官方插件持久 feed storage、用户级 clear/rebuild UI 与真实 Settings evidence；当前 `root-results-visible` feed 只是把已可见/已授权的 root results 纳入 runtime scan/reconcile 观察，`clearHandlerConfigured: false` 仅表示默认 runtime feed 没有持久 clear handler，不等同于持久 feed 完成。Browser History、System Settings、Obsidian、VSCode 仍停留在 descriptor/admission 或计划阶段，尚未形成完整 source lifecycle。
- Everything 仍需 SDK/CLI 最终策略、registry PATH 探测、Windows 运行性能/evidence 与 fail-closed 诊断验收。
- Durable job history、跨 source retry/debounce、source health 自动恢复动作仍需补齐；当前 run gate 已由 IndexingRuntime 统一持有并覆盖 scan/reconcile/reset，ScanScheduler 已有空 terminal batch 到 store 边界的 focused evidence，但仍只是内存态同源任务准入 primitive 和最近路径写入证据，recovery recommendation 只是 SDK 诊断 policy 和 Settings 只读提示，不是 durable job scheduler。

下一批执行顺序：

0. 当前提交节奏：继续保持 related-only 小切片，代码/测试/文档一起落地；每个切片通过最近路径验证后再提交推送，不把无关债务混入同一提交。
1. AI 2.5.0 evidence：Step 1 最近路径自动化已通过，但 CoreApp visible experience gate 仍需按八项固定 evidence 收口：CoreBox `text.chat` 成功、显式 `vision.ocr -> text.chat` 成功、未登录、provider unavailable、quota exhausted、model/capability unsupported、permission denied、Local/Ollama routing。下一步仍需采集 packaged Electron UI 截图/录屏 artifact，不能只凭脚本清单或服务测试宣称体验闭环。继续把 provider/model/trace/latency metadata chips 与 Nexus invoke failure evidence 对齐；OmniPanel provider-backed copy/retry/replace、桌面上下文更广输入与 Agent 工具执行（内置工具、MCP、插件）的成功/失败 evidence 保持 Beta 后续。
2. Search Provider / Indexing Runtime：优先 File write/store boundary，把 FileProvider SQLite/FTS 写入、progress、integrity reset 继续收敛到 runtime store/task；Browser Bookmarks official plugin lifecycle 已完成当前 owned runtime 切片，后续转 Everything productionization、Quicklinks persistent feed/UI evidence、Browser History/System Settings/Obsidian/VSCode。
3. PreviewSDK：保持 22-case benchmark 为当前最低门槛；QuickOps Developer P0/P1 已作为本地纯转换能力迁入 SDK，覆盖 JSON / URL / Base64 / JWT 解码 / Regex 受限测试 / Markdown/CSV 表格转换 / 时间戳 / 日期与固定 UTC offset 时区转换 / UUID v4 / 短 ID / QR Code SVG / 大小写转换，PreviewSDK 已提供静态离线 Currency fallback，PreviewProvider 已为主结果提供 host copy action，QR SVG/PNG 额外支持保存到 Tuff 临时目录并复制路径，QR 生成当前支持 v1-v9 / 230 bytes 输入；下一步只接受能增加能力边界或减少 fake-success 的能力迁移，候选为更多 no-result 边界和 benchmark case。
4. QuickOps：保持状态型能力独立于 PreviewSDK；当前 QuickOps runtime/官方插件组合已覆盖显示器保持唤醒开始/查询/延长/停止/到期清理、系统睡眠阻止、快速计时器开始/查询/暂停/恢复/延长/停止/通知/到期清理、番茄钟首个专注段开始/查询/暂停/恢复/停止/通知/到期清理、循环番茄钟专注/休息自动轮换、1-12 轮有限循环、每 N 轮长休息、25/5 与 50/10 内置模板、内置模板开关只读解析、命令式自定义模板、设置驱动自定义模板名称/别名解析、番茄钟会话级显示器保持唤醒联动、清洁屏幕多 display 黑/白底 overlay 开始/查询/停止/长按 Esc/到期/销毁清理、红/绿/蓝纯色屏幕测试 overlay、默认偏好只读解析和秒表开始/查询/暂停/恢复/分段/重置；本机 IP 查询、端口可用性探测（含占用 PID / 进程归因）、DNS 查询、网络状态摘要、公网 IP 默认关闭 opt-in 查询、打开常用目录、最近下载文件、临时文本文件、临时目录、文件 Hash、文件 Base64、复制路径格式、脱敏 Tuff 诊断信息复制、系统信息复制、电池状态、磁盘空间复制和关键目录占用已作为本地只读/低风险结果接入，分别有正常网卡/degraded mock、可用/已占用/非法端口、占用端口 PID / 进程归因、copy-only 终止命令与归因失败 fallback、DNS 查询成功/深度 DNS 查询成功/无记录 degraded、地址/DNS server/环境变量代理网络摘要与空地址/空 DNS 摘要、公网 IP 默认关闭不外联/显式开启成功/外部服务失败和非法地址 degraded、Downloads open/copy action 与 App Data/Logs 映射、最近下载文件按 mtime 选择最新普通文件、危险确认后移动到显式绝对目标目录、空目录 / 权限失败 / 目标缺失 / 目标已存在 degraded、临时文本文件创建/内容上限 degraded、临时目录创建/安全命名、显式路径/官方插件 Files input 首个有效文件路由/缺失文件/目录 degraded、文件 Base64 显式路径/官方插件 Files input 首个有效文件路由/Base64 临时文件解码落盘/非法 Base64 degraded/缺失路径 degraded/超限 degraded、复制路径显式路径/Files input/文件搜索结果路径动作/Windows-WSL 路径互转/无目标 degraded、扩展诊断包脱敏、OS/CPU/内存/uptime/load average 系统信息、macOS/Windows/Linux 电池状态、低电量未充电通知策略，以及 Home/userData 文件系统容量、statfs 失败 degraded、关键目录 bounded shallow/deep scan、Home 路径脱敏与目录权限失败 degraded focused test 覆盖，公网 IP 真实外部服务 evidence、真实端口 kill / 二次确认 UI、真实系统代理 evidence、真实系统打开 evidence、真实文件权限 / 大文件 evidence、最近下载文件真实平台移动 evidence、真实排障 evidence 和电池真实平台 evidence 仍后置；状态型 runtime 已抽出 `QuickOpsSessionManager` 并迁入 `modules/quick-ops` runtime host boundary，`QuickOpsModule` 已接模块生命周期清理和 QuickOps typed transport 只读事件，`packages/utils` 已提供 `createQuickOpsSdk()`，插件 SDK 已提供只读 facade；`plugins/touch-quickops` 已承接只读 root-results 入口和结果渲染，包括 capability、sessions、auditRecent、system info、tuff diagnostics、disk space、directory usage、network status、local ip、portStatus、dnsQuery、fileHash、fileBase64、recentDownload、commonDirectory、pathFormat、formatText、battery status、system proxy 和 `quickops settings` 只读设置摘要，且不产生 mutation action；CoreApp 托盘 QuickOps 状态面已移除，CoreApp Tools 设置页不再承载 QuickOps 配置控件，运行中状态摘要、停止全部动作与 settings 摘要由官方插件 root-results surface 承接；CoreApp 仍保留 `AppSetting.quickOps` schema / settings read path 给 runtime host 只读解析，CoreBox capability 摘要会把被本地策略禁用的 stateful/confirm 工具标记为 `stateful-tools-disabled-by-policy`，把默认关闭的 danger 工具标记为 `high-risk-tools-disabled-by-policy`，Flow 对同类目标返回 `quickops.policyBlocked`；overlay data URL 视觉合同已覆盖黑/白底、红/绿/蓝背景、倒计时提示、隐藏光标、长按 Esc 退出和无外链资源。下一步优先把可写 settings host capability、运行中状态面和可扩展编排继续迁进官方插件或插件专属 runtime boundary，同时补清洁屏幕 / 纯色屏幕测试真实视觉截图/录屏 evidence、完整企业策略下发/审计/锁定、Flow / AI action adapter 与 packaged app quit 证据。
   QuickOps 本地 Flow 审计摘要已通过内存 ring buffer 和 `auditRecent()` 暴露最近 delivery 决策，只保存 target / decision / reason / requiresConfirmation / payload key 列表，不保存 payload 内容；这仍不是企业集中审计、长期留存或组织级锁定。
5. 2.6.0 i18n / Domain Lexicon / Cloud Catalog：先做静态清查与合同设计，落 `AppLocale` / `ShortLocale`、`LocalizedText` / `LocalizedList`、单位/币种/时区/能力标签/搜索别名 registry、插件 `tuff.i18n.*` / `tuff.lexicon.*` facade，以及 CatalogService 下载、验签、schema 校验、SQLite import、activate、rollback 的最小 baseline；新增生产 UI 文案不得再用中文 fallback 或双语三元表达式。
6. TuffEx/UI：`TxDatePicker` adaptive field calendar 与 CoreApp UI Contract pilot 已落地；CoreBox 结果区已移除 `TouchScroll` wrapper、mac 改走 `TxScroll` native、去掉 scrollbar gutter，并将 `TxScroll` refresh 合并到 RAF。下一步优先把 CoreBox footer 从 absolute overlay + `CoreBoxRes-ScrollContent.has-footer` 魔数 padding 重构为容器内 flex footer，统一 footer 高度、结果测高与键盘滚入可视区语义；并继续主路径语义控件、剩余 legacy Tabs/Menu/Drawer 小切片、focus/keyboard evidence 与 visual smoke 条件恢复。
7. Nexus：优先数据收集治理闭环与 provider registry production/preview operator evidence；docs render/SEO 最近路径已覆盖 metadata/OG/canonical/prerender evidence，后续只补生产/preview 抓取、live send/live object storage/D1 migration/backfill、release notes sanitizer 回归与 visual smoke 条件恢复。
8. 平台差异能力：实现相关代码仍必须保留可见 degraded/unsupported reason 与 fail-closed 行为。

建议分批提交：

1. `test(ai): capture corebox ai packaged failure evidence`：已补 CoreBox AI Ask 文本/OCR 与 logged-out/provider/quota/model failure 最近路径 evidence gate；后续补真实 packaged Electron artifacts。
2. `ref(search): move file writes toward runtime store boundary`：收 FileProvider 写入/progress/integrity reset runtime boundary。
3. `ref(ui): improve semantic controls and plugin polish`：收 CoreBox/PluginList/Nexus LanguageToggle/TuffEx NavBar/示例插件 player controls 的语义与 focus；starter asset 清理已完成。
4. `feat(nexus): tighten docs seo and governance evidence`：补 docs SEO metadata/prerender evidence 与 data governance provider evidence。
5. `feat(search): promote browser bookmarks source lifecycle`：已完成 metadata-only source intent 到官方插件 owned runtime lifecycle、consent、clear/disable/rebuild UI 的当前切片；后续仅补真实平台 evidence 与 durable runtime 长尾。

## 已完成/历史不再重复开发

- 2026-05-16 Nexus Intelligence admin 页面已改为 ClientOnly lazy shell，Content trim 已扩展到重复 root SQL dumps；Intelligence lazy 后本地 dist 约 24.06 MiB，Worker executable JS 约 7.76 MiB，root SQL dump trim 预计额外移除 749.3 KiB。
- 2026-05-16 Nexus 全量 Vitest 已恢复通过；同步修正 exchange-rate、Turnstile 与 OAuth context 测试 mock/alias。
- 2026-05-16 Nexus Content sqlite wasm 重复产物已通过 post-build trim 去重，Provider Registry admin 页面已改为 ClientOnly lazy shell；本地 dist 约 24.48 MiB，Worker executable JS 约 8.05 MiB。
- 2026-05-16 Nexus API tests 已全部迁出 `server/api` 到 `test/api`，并新增零容忍 route-tree guard，防止测试/开发文件重新进入 Nitro 生产路由树。
- 2026-05-16 Nexus PWA precache 已排除 Nuxt Content SQL dump、sqlite wasm 与 sqlite worker 资产，并在 Worker bundle analyzer 中增加回潮检查；`nativeSqlite` 仍需保留，直接关闭会触发 `better-sqlite3` build 依赖。
- 2026-05-16 Nexus 公开站低风险清理与性能小切片已完成：生产测试页、`.DS_Store`、旧 GIF 退场；Landing 大图切到压缩 JPG；highlight.js 改为 docs 按需加载；公开页预渲染范围扩大。
- 2026-05-16 Nexus OAuth 回调缺失 session 时已增加错误兜底与重试入口，避免官网登录回调一直停留在处理中状态。
- `2.4.10` 正式版发布已产出 GitHub Release 与 Nexus release metadata：根/CoreApp 版本已切到 `2.4.10`，本地 macOS release build 已用 `--publish=never` 产出 `apps/core-app/dist/mac-arm64/tuff.app` 与 `apps/core-app/dist/tuff.app.zip`；GitHub Actions `Build and Release` 已成功发布 Windows setup、macOS app zip、Linux AppImage/deb、更新 YAML、builder debug 文件与 manifest 资产，并完成 Nexus release metadata sync。公共包发布历史问题已从当前 Roadmap blocker 降权；后续以版本变更后的 GitHub 自动发版 workflow 结果为准。 publish `@talex-touch` scope 返回 `E404 PUT` 阻塞，目标缺失版本为 `@talex-touch/utils@1.0.50`、`@talex-touch/tuffex@0.3.5`、`@talex-touch/unplugin-export-plugin@1.2.16`、`@talex-touch/tuff-cli@0.0.3`、`@talex-touch/tuff-core@0.0.1`、`@talex-touch/tuff-intelligence@0.0.2`。
- Widget production precompile gate 已完成。
- CoreApp 启动异步化 P0/P1/P2/P3 主要代码切片已完成，后续只保留真实设备 benchmark 与长尾补证。
- Quick Launch 搜索引擎模式、补全隔离、图标与旧结果清理已完成。
- 插件禁用后 CoreBox push items 门禁已完成。
- Nexus docs prerender routes 与 docs 切换性能优化已完成。
- 2.4.8 OmniPanel Gate、v2.4.7 Gate A/B/C/D/E、2.4.9 插件完善主线为 historical done；2026-05-18 起独立 OmniPanel Gate workflow 已删除，后续 OmniPanel 回归按普通最近路径本地/PR 验证或显式手动命令执行，不再作为自动质量门禁。

## 长期债务入口

长尾事项统一维护在 `docs/plan-prd/docs/TODO-BACKLOG-LONG-TERM.md`，包括：Transport MessagePort 高频通道、AI lint/typecheck 存量债务、SRP 大文件拆分、AttachUIView/Multi Attach View、Widget Sandbox、Flow Transfer、DivisionBox、Build Signature、Nexus 支付与 TuffEx docs/build 门禁等。

## 文档同步规则

行为/接口/架构/目标变化时至少同步以下入口之一；目标或质量门禁变化时同时同步 Roadmap 与 Quality Baseline：

- `docs/plan-prd/README.md`
- `docs/plan-prd/TODO.md`
- `docs/plan-prd/01-project/CHANGES.md`
- `docs/INDEX.md`
- `docs/plan-prd/01-project/PRODUCT-OVERVIEW-ROADMAP-2026Q1.md`
- `docs/plan-prd/docs/PRD-QUALITY-BASELINE.md`
