# Security P1 待处理项（2026-02-19）

> 说明：本次已完成路径穿越、命令注入封装与通道 key 回收修复，其余待评估项记录为 security P1。

## P1-001: IPC 终端执行权限收敛（已处理）

- **处理结果**：`terminal:create` 已增加 `system.shell` 权限校验，插件侧必须声明权限后方可调用。
- **涉及位置**：
  - `apps/core-app/src/main/modules/terminal/terminal.manager.ts`

## P1-002: 插件命令执行能力边界

- **风险描述**：插件仍允许执行 shell 命令（即使已加入安全包装），需明确权限边界与执行审计策略。
- **涉及位置**：
  - `plugins/touch-workspace-scripts/index.js`
  - `plugins/touch-system-actions/index.js`
  - `plugins/touch-quick-actions/index.js`
- **建议方向**：
  - 引入执行权限申请与二次确认。
  - 增加 allowlist/denylist 与执行审计日志。

## P1-003: tfile:// 访问根目录策略评估

- **风险描述**：当前白名单覆盖 home/userData/temp 与系统应用目录，外置磁盘或企业环境路径需明确策略与扩展方式。
- **涉及位置**：
  - `apps/core-app/src/main/modules/file-protocol/index.ts`
- **建议方向**：
  - 通过配置扩展白名单（用户选择 + 持久化）。
  - 为新增根目录增加审计记录与说明文档。
