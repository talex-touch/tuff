# Tuff R3 Search / Indexing Runtime TODO

> 更新时间：2026-07-16
> 范围：Roadmap R3 / Search Provider 与 Indexing Runtime。专题设计以 `03-features/search/INDEXING-RUNTIME-V1-PLAN.md` 为准；全局执行顺序服从 [`TODO.md`](./TODO.md)。

## 当前口径

- R3 目标是把 File write/store boundary、SQLite/FTS 写入、`scan_progress`、integrity reset 与 durable job history 收敛到 runtime task/store。
- 当前完成度约 `74%`，状态仍为 `active / partial`；本进度记录不改变 goal 定义，后续单独讨论是否调整 goal scope。
- 2026-07-10 进度快照：四类 SQLite evidence producer（preflight、FTS copy simulation、`scan_progress` copy simulation / read-only plan）新增低敏 `evidenceSource.dbIdentity=sha256-realpath-v1:<digest>`；strict aggregate verifier 新增 `database-evidence-correlation` check，要求四份 DB evidence 的 identity 完整且一致，阻止不同 profile DB / 不同路径的 artifacts 拼成同一 closure packet。当前源码 readiness 复跑仍为 `ready` / `blockers=[]`；旧 2026-07-03 isolated packet 因不含 `dbIdentity` 被 2026-07-10 strict artifact 正确阻塞。focused validation 为 12 个 R3 测试文件 / 153 tests 通过，`typecheck:node`、artifact sanity 与 R3 targeted diff/whitespace check 通过；全仓 `git diff --check` 仅被现有 Nexus 文档/示例空白改动阻塞，本批未触碰。该推进不打开或修改真实 profile，整体进度仍约 `74%`。
- 2026-07-04 进度快照：runtime/store evidence、resource migration readiness、isolated preflight / copy-simulation / query-only plan 与 strict aggregate verifier 已进一步收紧；R3 strict verifier 现在同时要求 Settings verification artifact 路径匹配 probe envelope 的 `artifactPaths.verification`，且 JSON 内容匹配 probe 内嵌 `verification` envelope，防止跨 run 拼接 Settings verification、DOM 与截图 artifact。最新 focused validation 为 12 个 R3 相关测试文件 / 151 tests 通过，`typecheck:node`、`git diff --check` 与 query-only plan / isolated-blocked verifier artifact sanity 通过。仍未执行真实 profile migration。
- 2026-06-24 durable job history 已完成非 schema focused 小切片：复用现有 SQLite task state store，不新增表结构，不改变 `scan_progress` / FTS ownership。
- 2026-06-25 durable job history 新增 Settings diagnostics evidence verifier：`pnpm -C "apps/core-app" run settings:indexing-diagnostics:verify -- --input <diagnostics.json> --sourceId file-provider` 会复用 Settings recent task chip helper 校验 `duration/trigger/reason/attempt/code` 已进入用户可见 chip values/summary；2026-07-03 新增 `--requireReadOnlyEnvelope` 与 `--requireNaturalRecentTaskEvidence`，用于真实 packaged/profile evidence 时强制 probe envelope 为 `mode=attach-only` / `profileMutationPolicy=read-only`，并拒绝 seeded recent task、maintenance action 与 fixture-root controlled 标记，避免 isolated/controlled evidence 误过真实 evidence gate。该 gate 只验证真实/packaged probe 产物，不替代截图/录屏 evidence。
- 2026-06-25 durable job history 新增 packaged Settings diagnostics probe 入口：`pnpm -C "apps/core-app" run visible:experience:indexing-diagnostics-probe -- --remoteDebuggingUrl <http://127.0.0.1:port/json/list> --outputDir <evidence-dir>` 可 attach 到已运行 packaged profile，读取 typed `indexed-source` diagnostics、打开 File Index source detail、落盘 diagnostics / verifier / DOM / screenshot artifacts；也支持 isolated userData 启动模式。2026-06-30 已补 `visible:experience:indexing-diagnostics-attach` 专用入口，默认带 `--attachOnly` 并强制要求 loopback `--remoteDebuggingUrl`，用于真实 packaged/profile evidence，避免误走当前不稳定的 npm/tsx cold-start launch 或误连非本机 CDP endpoint。Attach mode 对真实 profile 保持只读，不执行 scan/reset/reconcile/FTS rebuild/schema migration，probe envelope 会标记 `mode=attach-only` / `profileMutationPolicy=read-only`，且 probe 写出的 verifier artifact 会同步启用 read-only + natural evidence gate；isolated launch envelope 会标记 `mode=isolated-launch` / `profileMutationPolicy=isolated-controlled`，可显式使用 `--seedRecentTaskEvidence` 或 `--runMaintenanceAction scan|reconcile|reset` 生成受控 UI/chip evidence，且禁止与 attach mode 混用；`--fixtureRoot` 可为 scan/reconcile evidence 约束 FileProvider base roots，probe 会在 roots 未被约束时 fail gate，避免把默认 Home roots 误收成受控证据。
- 2026-06-25 packaged Settings diagnostics probe 已在本机 `dist/mac-arm64/tuff.app` isolated profile 试采：Settings 与 File Index detail artifact 链路可用，且 source detail 选择已修正为打开 `file-provider` 对应 File Index；isolated profile 没有 recent task history，verifier 正确失败（缺 duration/trigger/reason/attempt/code 可见字段），因此不能关闭 durable job history packaged evidence。
- 2026-06-25 packaged Settings diagnostics probe 已补 controlled seeded evidence 模式，并在本机 isolated `dist/mac-arm64/tuff.app` 生成 `/tmp/tuff-r3-indexing-diagnostics-seeded` artifacts：probe `ok=true`、`seededRecentTaskEvidence=true`、verifier passed，File Index detail 展示 duration / trigger / reason / attempt / code。该证据证明 packaged UI 与 verifier 链路可用，但不是自然真实 profile 的 scan/watch/reconcile/reset history，仍不能替代真实 recent task 采集。
- 2026-06-25 packaged Settings diagnostics probe 已补 isolated maintenance action evidence：本机 isolated `dist/mac-arm64/tuff.app` 通过 `--runMaintenanceAction reset` 触发 typed `app:indexed-source:reset`，生成 `docs/engineering/reports/r3-indexing-runtime-2026-06-25/indexing-diagnostics-probe-maintenance-reset-2026-06-25.json` 等 artifacts；probe `ok=true`、verifier passed，File Index detail 展示 runtime 产生的 reset recent task duration / trigger / reason。该证据强于 seeded evidence，但仍是 isolated profile，不替代真实用户 profile 的自然 scan/watch/reconcile/reset history。
- 2026-06-25 scan/reconcile isolated maintenance fixture evidence 已补齐：重包后的本机 `dist/mac-arm64/tuff.app` 已包含 `TUFF_FILE_PROVIDER_BASE_WATCH_PATHS` marker，`maintenance-reconcile-fixture-2026-06-25` 与 `maintenance-scan-fixture-2026-06-25` probe 均为 `ok=true`、verifier passed，diagnostics roots 均被约束到 `/tmp/tuff-r3-indexing-runtime-fixture-*`。旧 `maintenance-reconcile-2026-06-25` 探索产物与 `maintenance-reconcile-fixture-preflight-2026-06-25` stale bundle preflight 产物仍保留为 failed exploration，不计入 passing evidence；isolated fixture evidence 仍不替代真实用户 profile 的自然 scan/watch/reconcile/reset history。
- 2026-06-30 packaged isolated durable scan evidence 已补同一 isolated DB 闭环：plain Node CDP 对本机 `dist/mac-arm64/tuff.app` 触发 typed `app:indexed-source:scan`，生成 `indexing-diagnostics-probe-packaged-isolated-durable-scan-2026-06-30.json`、`indexing-task-state-packaged-isolated-durable-scan-2026-06-30.json` 与 verifier/preflight/simulation artifacts；结果为 `indexed_source_task_state=1`、`scan_progress=1`、`search_index=35`，Settings verifier 对 duration/trigger/reason 通过，preflight 中 `scanProgressSourceScope=ready` 且已采 SQLite runtime PRAGMA profile；刷新后的更严格 preflight 同时暴露 `source-index-row-parity` blocker（`file-provider` `sourceIndexedRows=0` / `filesRows=35`），因此 SQLite/FTS ownership 仍不能视为 execute-ready。该证据证明 isolated packaged runtime durable task state 与 source-scoped `scan_progress` 链路可闭合，但仍不替代真实 profile migration execute 或自然用户 profile evidence。
- 2026-06-30 已定位上述 isolated DB `source-index-row-parity` blocker 的代码侧边界：FileProvider runtime scan 完成只等待 `files` upsert 与 task history，不保证 file-index worker / `persistAndIndex` flush 已把 `file-provider` rows 写入 durable `search_index`。代码侧已补 `scanIndexedSource` completion drain（覆盖 file-index worker pending、flush buffer、SearchIndex worker pending），且 drain timeout 会让 runtime scan 失败，避免 durable job history 记录成功但 FTS row parity 未闭合；focused tests 已覆盖成功 drain 与 timeout failure。仍需重包后重新采 packaged isolated durable scan + preflight evidence，确认 blocker 从 artifact 中消失。旧 2026-06-30 artifact 继续保留为 failed/blocking evidence，不回写修正。
- 2026-06-30 重包后已尝试重新采 `packaged-isolated-durable-scan-drain-2026-06-30` isolated probe；packaged app 在 CDP 可用前仍 `SIGABRT`，未生成 isolated DB。probe 现已补失败 envelope 落盘，`indexing-diagnostics-probe-packaged-isolated-durable-scan-drain-failure-2026-06-30.json` 记录 `phase=wait-for-cdp`、fixtureRoot bundle preflight passed 与 `signalCode=SIGABRT`，但不替代 scan/preflight 通过证据；最新 crash report 仍指向 HIServices `_RegisterApplication` / `NSApplication.sharedApplication`，早于 CoreApp main-process JS 与 DB 初始化。因此 re-evidence 当前仍被 packaged cold-start 环境问题阻塞，需改用已运行 app 的 attach-only collector 或继续修复 cold-start。
- 2026-06-24 SQLite/FTS 与 `scan_progress` 高风险迁移新增只读 preflight / dry-run plan 工具：`pnpm -C "apps/core-app" run search:index-migration:preflight -- --db <sqlite.db> --output <report.json>`，用于输出并落盘迁移前 row-count、orphan、source-scope、FTS shadow table readiness、SQLite runtime PRAGMA profile（含 `queryOnly=true`）、预计影响 rows、回滚建议与验证步骤；2026-07-03 新增 `evidenceSource` 元数据与 `--requireRealProfileEvidence` 门禁，真实 profile preflight evidence 必须显式使用 `--evidenceScope real-profile --requireRealProfileEvidence`，临时 `/tmp` DB 会被拒绝标记为 real-profile；该工具不执行迁移或清理。
- 2026-07-03 新增 R3 迁移 evidence 聚合 verifier：`pnpm -C "apps/core-app" run search:index-migration:evidence-verify -- --strict ...` 只读聚合 preflight、production readiness、FTS copy simulation、`scan_progress` copy simulation、read-only plan、Settings verification 与 Settings/source detail screenshot artifacts。当前已对 isolated artifact 包落盘 `search-index-migration-evidence-verification-isolated-blocked-2026-07-03.json`，结果 `gate.passed=false`，明确阻塞原因是 preflight 仍为 `isolated-controlled`、`task-history-store=durable-history-empty`、`scanProgressSourceScope=needs-migration`、FTS / `scan_progress` copy simulation 与 `scan_progress` plan 仍为 isolated-controlled、缺 attach-only natural Settings recent task verification、缺 Settings/source detail screenshot artifacts；该 verifier 不打开 SQLite、不执行迁移、不启动 app。
- 2026-06-25 新增生产迁移 readiness auditor：`pnpm -C "apps/core-app" run search:production-migration-readiness -- --output <report.json>` 只读检查 `schema.ts`、`resources/db/migrations`、Drizzle journal 与 `SearchIndexService` FTS runtime creation ownership。2026-07-03 已复跑 source-read-only readiness 并落盘 `search-production-migration-readiness-2026-07-03.json` 与 `search-production-migration-readiness-2026-07-03-journal-gate.json`，结果仍为 `ready` / `blockers=[]`：`0024_search_index_runtime_store.sql` 接入 `indexed_source_task_state` / `search_index` FTS5 durable resource migration，`0025_scan_progress_source_scope.sql` 接入 source-scoped `scan_progress` resource shape，`journalEntriesWithoutSql=[]` / `sqlFilesWithoutJournalEntry=[]` 确认 Drizzle journal 与 SQL 文件一致，且 `legacyFileFtsMigrationPolicy.status=retained` 证明 resource migrations 未触碰 legacy `file_fts`；该 evidence 不打开或修改真实 profile SQLite。
- 2026-06-25 新增 SQLite/FTS durable ownership 副本仿真 CLI：`pnpm -C "apps/core-app" run search:fts-ownership-simulate -- --db <sqlite.db> --output <fts-simulation.json>` 会用 `VACUUM INTO` 生成 simulation copy，并只在副本上应用候选 durable FTS DDL，输出 `search_index` required columns、shadow tables、`search_index_meta`、keyword indexes、legacy `file_fts` 与 row-discard/full-reindex impact；artifact 显式记录 `sourceMutationPolicy=source-not-mutated-copy-execute` 与 `sourceSnapshotUnchanged=true`。2026-07-04 新增 `evidenceSource` 与 `--requireRealProfileEvidence` gate，临时 `/tmp` DB 会被拒绝标记为 real-profile；该工具不修改 `--db`，isolated artifact 不替代真实 migration approval。
- 2026-06-25 legacy `file_fts` 口径已收敛：R3 durable ownership migration 保留 `file_fts` 不改、不迁移、不删除，运行时文件搜索继续以 `search_index` provider rows 为 durable SoT；`file_fts` 退役若需要，必须另起独立高风险迁移批并补等价搜索覆盖 evidence。
- 2026-06-24 `scan_progress` runtime boundary 已具备 source-scoped schema 兼容读写：旧 `path primary key` 表保持原路径，新 `(source_id, path)` 表会按 `file-provider` scope 读取、删除和 worker upsert；该切片不创建/迁移 schema。
- 2026-06-24 `scan_progress` source-scoped schema 已补受控 migration helper 与 CLI：`pnpm -C "apps/core-app" run search:scan-progress-migration -- --db <sqlite.db>` 默认只读输出 approval/schema/data/rollback/verification plan，并在 plan artifact 记录 `queryOnly=true`；2026-07-03 已用 isolated `/tmp/tuff-r3-scan-progress-plan-queryonly-2026-07-03/source.sqlite` 落盘 `scan-progress-source-scope-migration-plan-queryonly-2026-07-03.json`，结果为 `mode=plan`、`executed=false`、`evidenceSource.scope=isolated-controlled`、`plan.status=ready`、`requiresApproval=true`、`rowsToMigrate=2`；2026-07-04 CLI 新增 `--evidenceScope real-profile --requireRealProfileEvidence`，临时 `/tmp` DB 会被拒绝标记为 real-profile，真实关闭必须采 real-profile plan evidence；只有显式传 `--execute --confirm-source-scope-migration` 才会调用 `runScanProgressSourceScopeMigration()` 把 path-only 表迁成 `PRIMARY KEY(source_id, path)` 并保留 `scan_progress_path_only_backup`；2026-06-25 生产启动路径已复用该 helper，在正常 DB migration 后执行受控迁移，遇到 unsafe rows / backup / staging blocker 时保留旧表并继续兼容读写；尚未执行真实 profile 迁移。
- 2026-06-25 新增 `scan_progress` source-scope 副本仿真 CLI：`pnpm -C "apps/core-app" run search:scan-progress-simulate -- --db <sqlite.db> --output <simulation.json>` 会用 `VACUUM INTO` 生成 simulation copy，并只在副本上执行 source-scope migration helper，输出源库前后 scan_progress snapshot、copy migration 前后 plan/snapshot、backup rows 与 gate；artifact 显式记录 `sourceMutationPolicy=source-not-mutated-copy-execute` 与 `sourceSnapshotUnchanged=true`。2026-07-04 新增 `evidenceSource` 与 `--requireRealProfileEvidence` gate，临时 `/tmp` DB 会被拒绝标记为 real-profile；该工具不修改 `--db`，用于在真实执行确认前验证迁移草案，但 isolated artifact 不替代真实 profile simulation evidence。
- source-scoped `scan_progress` schema migration 属高风险数据结构变更；代码路径与 resource migration 已接入，执行真实 profile 前仍必须单独确认影响范围并保留 preflight evidence。
- focused tests 可以证明边界合同，不能等同于完整 durable scheduler 或 SQLite/FTS 迁移完成。

## 已完成

- Watch-root policy、scan eligibility、scan strategy、runtime reset、task retry/debounce、recovery wait metadata 已完成多处 hygiene。
- Scan progress 读写边界已过滤空路径、重复路径、非法 timestamp，并返回真实写入 row count summary。
- FileProvider runtime reset cleanup 已覆盖当前 watch roots 下的 scan-progress cleanup。
- Runtime automatic scan/reconcile retry policy 已在同类成功任务后重置失败 backoff。
- App Semantic Alias Catalog（应用语义别名目录）已完成无 schema 小切片：AppProvider 复用现有 SearchIndex alias/token 通道，为已安装主流 IM、Design、Dev、Office、Browser、AI、DB/API、Git、Screenshot/OCR、Media、Archive/Transfer、Security、Remote/VM、国内高频工具等应用补内置语义别名，使 `im`、`design`、`ps`、`vsc`、`code`、`codex`、`ai`、`db`、`api`、`screenshot`、`password` 等查询可召回对应已安装应用；启动时按 catalog version 对既有 app SearchIndex keyword 做一次性回填，避免旧索引缺少新增别名；本切片不新增未安装应用推荐、不新增设置页、不改变 SQLite schema。
- Settings recovery chip display 已输出结构化 wait metadata。
- FileProvider incremental DB persist、FTS write/delete summary 与 index worker flush trace 已进入 indexed source runtime/store evidence；本阶段未新增 schema/migration、真实 watcher 或 Settings packaged evidence。
- Durable job history focused slice 已落地：`scan/watch/reconcile/reset` recent task history 记录 `durationMs`、`reason`、`trigger`、`attempt`、`errorCode`、`errorMessage` 等低敏审计字段；生产 `SearchEngineCore` 继续通过 `SqliteIndexingTaskStateStore` 写入 `indexed_source_task_state`；Settings diagnostics task chip 展示 duration / trigger / reason / attempt / code；focused tests 覆盖 SDK builder、runtime hydrate/history、SQLite store sanitize/clone 与 Settings display。
- Settings diagnostics evidence verifier 已落地：`settings:indexing-diagnostics:verify` 可读取 CoreBox indexing diagnostics JSON 或 packaged probe envelope，并按真实 Settings display helper 生成 recent task chips，验证 jobId/time/summary 以及 durable audit fields 可见；真实 packaged/profile evidence 可加 `--requireReadOnlyEnvelope --requireNaturalRecentTaskEvidence` 强制 attach-only/read-only envelope 且拒绝 seeded / maintenance action / fixture-root controlled 标记，isolated controlled envelope 会 fail gate；该 verifier 不回显 raw diagnostics/root path，也不声明 packaged evidence 已完成。
- Packaged Settings diagnostics probe 入口已落地：`visible:experience:indexing-diagnostics-probe` 复用 packaged Electron CDP 采集模式，可 attach 到已运行 packaged profile 或启动 isolated userData，自动写出 `indexing-diagnostics-*.json`、`indexing-diagnostics-verification-*.json`、Settings/source detail DOM 与截图路径；focused tests 覆盖 target selection、artifact naming、source detail 行选择、seed/attach 互斥、fixtureRoot attach 禁止、loopback CDP attach guard、fixtureRoot bundle marker preflight、maintenance action isolated-only、probe envelope mutation policy 与 evidence failure matrix。本机 isolated packaged 试采可打开 File Index detail；controlled seeded run 可通过 durable audit gate 并展示 recent task chips；isolated maintenance reset/scan/reconcile fixture run 可通过 typed runtime maintenance action 产生 recent task 并通过 verifier。真实关闭仍需 attach 到已有 scan/watch/reconcile/reset history 的 packaged profile 或先在受控真实 profile 产生维护任务历史。
- SQLite/FTS durable ownership 与 `scan_progress` source-scoped migration 已补只读 readiness preflight / migration dry-run plan：检查 required tables、path-only/source-scoped `scan_progress`、blank/invalid progress rows、legacy `file_fts` retain-unchanged policy、`search_index` FTS5 shadow tables、provider row parity、keyword/meta orphan 与 meta coverage，并输出 journal mode、busy timeout、page size/count、freelist、`queryOnly=true` 等 SQLite runtime PRAGMA profile；keyword orphan、meta orphan 或 meta coverage 缺失都会阻塞 SQLite/FTS ownership dry-run，避免在真实 execute 前带着不一致索引继续推进；`task-history-store` 现在会区分 `indexed_source_task_state` 表存在但 row count 为 0 的 warning，防止空表被误当 durable job history evidence；CLI 支持 `--output <report.json>` 生成审批/evidence 附件，并用 `evidenceSource.scope` 区分 isolated / real-profile，`--requireRealProfileEvidence` 会拒绝 `/tmp` 证据冒充真实 profile；focused tests 覆盖 path-only、blocked、source-scoped clean、unsafe rows、legacy `file_fts` 保留策略、meta coverage blocker、runtime PRAGMA profile、`query_only` evidence、empty task-history warning、real-profile evidence gate 与 report 文件落盘。
- R3 迁移 evidence 聚合 verifier 已落地：`search:index-migration:evidence-verify -- --strict` 要求 real-profile preflight、preflight `task-history-store=durable-history-present` 且 `indexed_source_task_state` rows >= 1、real-profile FTS / `scan_progress` copy simulations、real-profile read-only `scan_progress` plan、source-scoped post-migration preflight、source-read-only production readiness、attach-only natural Settings verification 与 Settings/source detail visible artifacts 同时成立；Settings verification 必须锁定 `sourceId=file-provider`、`minRecentTasks>=1`，并要求完整 audit fields `duration,trigger,reason,attempt,errorCode`；`--settingsProbe` 还会要求 probe envelope 为 `ok=true` / `mode=attach-only` / `profileMutationPolicy=read-only` / `sourceId=file-provider`，拒绝 `seededRecentTaskEvidence`、`maintenanceAction`、`fixtureRoot` controlled markers，要求 `--settingsVerification` 路径匹配 probe envelope 的 `artifactPaths.verification` 且 JSON 内容匹配 probe 内嵌 `verification` envelope，并要求 Settings/source detail screenshots 为 PNG、DOM artifact 证明 source diagnostics group、detail dialog 与 recent task list 存在；focused tests 覆盖 isolated/missing Settings 被拒绝、空 durable task history 被拒绝、isolated copy simulation / scan_progress plan 被拒绝、弱 Settings audit field gate 被拒绝、缺截图/非 PNG/弱 DOM 被拒绝、controlled Settings probe markers、错配 Settings verification/probe artifact、错 source Settings probe 被拒绝和完整 real-profile packet 通过，当前 isolated artifact 包的 strict verification 已落盘为 blocked，防止只靠局部绿灯或纯 JSON verifier 误关 R3。
- 生产迁移 readiness auditor 已落地：`search:production-migration-readiness` 不打开 SQLite、不执行 migration、不 rebuild FTS，只读输出 schema/migration/journal/runtime FTS ownership 状态。focused tests 覆盖 path-only/runtime-created blocker、source-scoped/resource-owned ready fixture、legacy `file_fts` migration touch blocker、Drizzle journal missing / journal entry without SQL / SQL file without journal entry blockers 与 CLI report 落盘；真实仓库当前 source-read-only readiness 已为 `ready`，但不等同真实 profile execute evidence。
- SQLite/FTS durable runtime-store resource migration 已接入资源迁移目录：`0024_search_index_runtime_store.sql` 幂等创建 `indexed_source_task_state`、`idx_indexed_source_task_state_updated_at` 与 durable-shape `search_index` FTS5 table，并写入 Drizzle journal；未执行真实 profile migration，`SearchIndexService` runtime `CREATE IF NOT EXISTS` 仍作为兼容 repair/旧库兜底。
- `scan_progress` source-scoped production schema / migration 已接入：`schema.ts` 将 `scan_progress` 定义为 `PRIMARY KEY(source_id, path)`，`0025_scan_progress_source_scope.sql` 创建新库 source-scoped shape，DatabaseModule 启动后复用受控 helper 迁移旧 path-only rows 到 `file-provider` scope，保留 `scan_progress_path_only_backup`；File watch eligibility、dashboard overview 与 worker fallback 均保留 old/new schema 兼容。
- SQLite/FTS durable ownership 副本仿真已落地：`search:fts-ownership-simulate` 用 `VACUUM INTO` 复制目标 SQLite，再只对 simulation DB 应用候选 durable FTS DDL，校验 `search_index` required columns / FTS5 shadow tables / `search_index_meta` / keyword indexes，并显式输出 legacy `file_fts` retain-unchanged policy、row discard、full-reindex impact、source snapshot unchanged evidence 与 `evidenceSource`。该 gate 比纯 readiness 更接近迁移执行风险，但 isolated artifact 只证明 copy-mutates-copy 策略，strict 关闭仍要求 real-profile simulation evidence。
- `scan_progress` source-scoped runtime compatibility 已落地：`FileProviderScanProgressService`、runtime reset cleanup 与 SearchIndex worker upsert 会检测 `scan_progress` 表是否存在 `source_id`，source-scoped 表只读写当前 `file-provider` rows；旧 path-only 表保持现有 Drizzle 路径。真实 SQLite focused tests 覆盖 source-scoped read/delete/upsert isolation，以及 manual rebuild / integrity repair reset cleanup 不误删其它 source rows。
- `scan_progress` source-scoped controlled helper 与 CLI 已落地：migration plan 会阻塞空 path、非法 timestamp、重复 path、残留 staging/backup table，并显式标记 `queryOnly=true`、requiresApproval / requiresSchemaChange / requiresDataRewrite；2026-07-03 isolated plan artifact 已证明默认 plan 模式 `executed=false` 且不会修改 source DB；execute path 需要 `--confirm-source-scope-migration`，使用 `BEGIN IMMEDIATE`、staging table、backup table 与 path index，在真实临时 SQLite 中覆盖旧 DB 迁移、新 DB 初始化、unsafe rows blocked、未确认拒绝执行与 post-migration source-scoped shape。
- `scan_progress` source-scope 副本仿真已落地：`search:scan-progress-simulate` 用 `VACUUM INTO` 复制目标 SQLite，再只对 simulation DB 执行受控 helper，并校验源库前后 snapshot 未变化、post-migration 副本变为 `PRIMARY KEY(source_id, path)`、path-only backup rows 保留，artifact 直接输出 `sourceSnapshotUnchanged=true` 与 `evidenceSource`。该 gate 比只读 plan 更接近真实执行，但 isolated artifact 只证明 copy-mutates-copy 策略，strict 关闭仍要求 real-profile simulation evidence。
- AppProvider 多词 app 搜索精确命中已从 per-term `keyword_mappings` 查询切换为 `SearchIndexService.lookupByKeywords` 批量读取，保持 term intersection / phrase recall / FTS fallback 语义，降低 CoreBox app 搜索短路径 SQLite 往返。

## 未完成

| 主题 | 状态 | 缺口 |
| --- | --- | --- |
| FileProvider SQLite/FTS durable migration | resource migration ready / packaged re-evidence open / execute evidence open | 只读 preflight 可输出并落盘 FTS shadow table、provider row parity、keyword/meta orphan、meta coverage、dry-run 影响 rows、rollback 与 verification；`0024_search_index_runtime_store.sql` 已把 `indexed_source_task_state` 与 durable-shape `search_index` FTS5 creation 接入资源迁移并被 2026-07-03 source-read-only readiness auditor 识别，Drizzle journal / SQL 文件一致性为 clean，且 resource migrations 未触碰 legacy `file_fts`；FTS copy simulation 可在副本验证 candidate DDL、shadow tables、row discard/full-reindex impact 与 legacy `file_fts` retain-unchanged policy；2026-06-30 `source-index-row-parity` blocker 已补 runtime scan drain 代码侧修复，drain timeout 会 fail scan 而不是记录成功 history，但仍需重包后重新采 isolated packaged preflight，再补真实 profile migration/preflight evidence 与真实 Settings evidence。 |
| `scan_progress` source-scoped schema | production schema/migration path ready / isolated packaged DB evidence ready / real profile evidence open | 只读 preflight 可识别 path-only/source-scoped schema、blank/invalid rows、source/path duplicate 风险，并输出 source-scope 迁移步骤的 approval/blocker/rollback/verification；runtime 读/删/worker upsert/watch eligibility/dashboard 已兼容 old/new schema；controlled CLI 默认只读 plan，execute 需显式确认；DatabaseModule 生产启动路径复用同一 helper 执行受控迁移并保留 backup；2026-06-30 isolated packaged DB preflight/simulation 已证明 source-scoped shape 和 `file-provider` progress row 可闭合；仍需真实 profile preflight/execute evidence、数据清理影响范围确认与 Settings evidence。 |
| Durable job history | focused + verifier/probe + controlled isolated evidence ready / natural profile artifact pending | 非 schema append/update/store、SQLite task state 持久化、runtime hydrate、Settings chip focused tests、diagnostics JSON verifier 与 packaged probe 入口已完成；preflight 会对 `indexed_source_task_state` 空表给 warning，避免表存在但无 durable rows 时误判为 durable history evidence；isolated packaged 试采证明 Settings/File Index detail artifact 链路可用，controlled seeded run 证明 packaged UI/chip/verifier gate 可通过；isolated maintenance reset/scan/reconcile fixture run 与 2026-06-30 isolated durable scan DB snapshot 证明 typed packaged runtime maintenance action 可产生并持久化 recent task、受控 roots 可进入 diagnostics，并进入 Settings verifier；仍需对真实 packaged app 或受控真实 profile 采集自然 scan/watch/reconcile/reset recent task 的 Settings 截图/录屏 artifact，且不等同于 durable retry scheduler。 |
| Quicklinks persistent feed | open | 需要官方插件持久 feed storage、用户级 clear/rebuild UI 与 Settings evidence。 |
| Browser Bookmarks platform evidence | partial | 需要真实浏览器 profile、跨平台 watch root 与 packaged evidence。 |
| Everything productionization | partial | 需要 SDK/CLI 最终策略、registry/PATH 探测、Windows 性能与 fail-closed 诊断 evidence。 |

## 高风险迁移前置确认清单

> 适用范围：SQLite/FTS durable ownership migration 与 `scan_progress` source-scoped schema migration。该清单只定义执行前必须确认的影响范围；未完成确认前，不进入 schema / 数据迁移实现。

### SQLite / FTS durable ownership

- 数据对象范围：确认 `files`、`file_extensions`、`file_index_progress`、`search_index` FTS5 虚表及 shadow tables、`search_index_meta`、`keyword_mappings` 是否纳入同一迁移批次；R3 对 legacy `file_fts` 的决策是保留不改，不做迁移/删除/重建，退役另立高风险批。
- 写入所有权：确认 FileProvider、SearchIndexService、SearchIndexWorker、DbWriteScheduler、runtime task/store 的最终写入边界；迁移后不得出现 main thread 与 worker 对同一索引域并发写入。
- 兼容读写窗口：确认旧表/旧 FTS schema 与新 runtime/store 在一个版本内的 dual-read / dual-write 或只读迁移策略；明确 hard-cut 条件和不可回退点。
- FTS 重建影响：确认 `search_index` 重建、shadow table 清理、keyword mappings 清理、`searchIndex.didMigrate` 触发全量重扫的条件；估算重建耗时、UI degraded 状态与可取消/恢复行为。
- 数据一致性：确认 files rows、FTS rows、keyword_mappings orphan、search_index_meta keyword hash 的一致性检查口径；迁移前后必须有 row-count diff、orphan cleanup diff 和 integrity snapshot。
- SQLite 运行风险：确认 WAL / BUSY / retry / write queue / transaction 粒度 / batch size 对启动、搜索、扫描、删除和 Settings diagnostics 的影响；不得用无上限重试或大事务掩盖锁竞争。
- 回滚策略：确认失败时是保留旧表、重建 FTS、清空 provider-scoped index，还是触发 full rescan；列出哪些失败会导致用户只能等待重扫。
- 证据要求：除 focused tests 外，必须补只读 preflight report artifact（建议用 `--output <report.json>`）/ `migrationDryRun` summary、真实/packaged profile row-count evidence、Settings diagnostics 截图或录屏、`git diff --check` 与 CoreApp 贴近测试。

### `scan_progress` source-scoped schema

- schema 范围：确认从当前 `scan_progress(path primary key, last_scanned)` 迁到 source-scoped 结构时的主键形态，例如 `(source_id, path)`；明确 `source_id` 默认值、索引、唯一约束和空/非法 path 处理。
- 数据归属：确认现有 path-only rows 如何归属到 `file-provider`，以及 Browser Bookmarks、Everything、AppProvider 等 indexed source 是否需要独立 progress scope；避免不同 source 的相同 path 互相污染。
- path 规范化：确认 raw path、normalized path、watch root 展开、大小写敏感、符号链接和跨平台分隔符的迁移规则；迁移前后 pending/completed root 计算必须一致。
- 清理范围：确认 manual rebuild、schema migration、integrity repair、watch root 删除、permission revoked、provider disable 时清理的是 source-scoped rows 还是全局 rows；不得误删其它 source 的进度。
- 兼容读写：已具备 runtime old/new schema 读删写兼容层；controlled helper 使用 staging table 与 `scan_progress_path_only_backup` backfill path-only rows 到 `file-provider` scope。执行前仍需确认 `FileProviderScanProgressService`、worker `upsertScanProgress`、runtime reset、integrity repair 对真实旧 rows 的读取、回填和去重策略；旧版本回退时要能接受 path-only 数据或明确不可回退。
- 用户影响：确认迁移后会触发哪些 watch roots 全量重扫、哪些用户会看到 Settings recovery chip、预计扫描量/耗时、电池/权限 gate 与 failed file retry 是否被重置。
- 数据清理与回滚：确认重复 rows、空 path、非法 timestamp、旧 root 不在当前 watch roots、孤儿 progress 的清理规则；回滚必须说明是否保留 migrated rows、还原 path-only rows 或强制 rescan。
- 验证矩阵：至少覆盖 preflight path-only/source-scoped 报告、旧 DB 迁移、新 DB 初始化、重复 path 跨 source、FTS 重建联动清 progress、manual rebuild、integrity repair、watch root 变更、Settings diagnostics evidence。

## Durable job history 最小设计

> 目标：先补 runtime task/job 的可审计历史和 Settings diagnostics evidence，不改变 SQLite schema，不迁移 SQLite/FTS ownership，也不改 `scan_progress` 结构。

### 设计边界

- 记录对象：scan、reconcile、reset 三类 source maintenance job；Browser Bookmarks、Everything、Quicklinks 后续接入时复用同一 history shape。
- 最小字段：`jobId`、`sourceId`、`kind`、`status`、`queuedAt`、`startedAt`、`finishedAt`、`durationMs`、`reason`、`trigger`、`attempt`、`summary`、`errorCode`、`errorMessage`。
- 状态流转：`queued -> running -> succeeded | failed | skipped | cancelled`；失败可记录 retry metadata，但不在本批实现 durable retry scheduler。
- 存储策略：先复用现有 runtime diagnostics / task store 边界；若需要新持久表，必须先升级为独立 schema 设计批，不在 durable job history 小切片中直接落库。
- Settings 展示：diagnostics recovery chip 显示最近 job 状态、失败 reason、下一步 action；不得展示 raw path、secret、完整 stack trace。
- evidence 要求：focused tests 证明 append/update/store contract；Settings diagnostics 需要 packaged 或真实截图/录屏证明用户可见。

### 推荐执行切片

1. 侦察现有 `IndexingRuntime`、source task store、Settings diagnostics 数据流，确认是否已有可复用 history container。
2. 已完成：增加 append/update 的纯函数与 store/runtime/display focused tests，覆盖 scan/watch/reconcile/reset 成功、失败、skipped 与低敏审计 metadata。
3. 已完成：Settings diagnostics 消费最近 job summary，展示 duration / trigger / reason / attempt / code。
4. 已完成：补 Settings diagnostics JSON verifier，作为 packaged/真实 probe artifact 的门禁。
5. 已完成：补 packaged Settings diagnostics probe 入口，生成 diagnostics / verification / DOM / screenshot artifact 路径。
6. 已完成：在 isolated packaged launch 中用 `--seedRecentTaskEvidence` 生成低敏 controlled evidence，证明 packaged UI / Settings chip / verifier 链路可通过；该模式禁止 attach 到真实 profile。
7. 已完成：在 isolated packaged launch 中用 `--runMaintenanceAction reset` 触发 typed runtime maintenance action，证明 packaged runtime recent task 可进入 Settings File Index detail 与 verifier；该模式禁止 attach 到真实 profile。
8. 已完成：在 isolated packaged launch 中用 `--runMaintenanceAction scan|reconcile --fixtureRoot <tmp>` 触发 typed runtime maintenance action，并通过 bundle marker preflight 与 diagnostics roots gate 证明 controlled fixture roots 生效；该模式禁止 attach 到真实 profile。
9. 待完成：对真实 packaged app 运行 probe 并采自然 recent task 的 Settings packaged/真实截图或录屏 evidence；必要时同步 `INDEXING-RUNTIME-V1-PLAN.md` 与后续 evidence 矩阵。

### 非目标

- 不在未确认影响范围时执行真实 profile `scan_progress` source-scoped schema/data migration。
- 不在未确认影响范围时执行真实 profile `search_index` / FTS ownership migration、rebuild 或 legacy `file_fts` 退役。
- 不把 focused tests、controlled seeded evidence 或 isolated maintenance evidence 写成自然真实 packaged Settings evidence。
- 不用 mock Browser Bookmarks、Everything、Quicklinks evidence 关闭真实平台项。

## 下一步

1. 继续非 schema 小切片，优先移除 FileProvider 内部仍手写 root/path/progress 规则的地方，但不要重复实现已有 runtime write evidence。
2. 修复/替换 `visible:experience:indexing-diagnostics-probe` npm/tsx cold-start 路径；当前 plain Node CDP 可采 isolated durable scan evidence，但 npm/tsx probe 在本机环境仍可能触发 packaged Electron `SIGABRT`。本地 macOS DiagnosticReports 显示 abort 发生在 AppKit/HIServices `_RegisterApplication` / `NSApplication.sharedApplication`，早于 CoreApp main-process JS 日志；真实/packaged profile evidence 先走 `visible:experience:indexing-diagnostics-attach -- --remoteDebuggingUrl <...>` 的 attach-only collector，cold-start 修复则继续把 packaged app launch 与 tsx/file-script 父进程彻底隔离。修复后 attach 到已有 recent task history 的真实/packaged profile，采集 Settings diagnostics JSON、verifier JSON、DOM 与截图，并用 `settings:indexing-diagnostics:verify -- --requireReadOnlyEnvelope --requireNaturalRecentTaskEvidence` 复核 durable job history 可见字段、read-only envelope 与非 controlled evidence 标记；isolated packaged 试采、`--seedRecentTaskEvidence` 与 `--runMaintenanceAction reset|scan|reconcile` controlled fixture evidence 只能证明 artifact / UI / chip / typed runtime maintenance / task-state persistence 链路可用，不能关闭自然 recent task evidence。
3. 对真实 packaged/profile 运行 `search:index-migration:preflight`、`search:scan-progress-simulate` 与正常启动迁移链路，采集 source-scoped `scan_progress` / SQLite FTS execute evidence；当前 source-read-only production readiness 已 ready，但真实 schema/data migration 前仍需按上方高风险迁移前置确认清单单独确认。
4. Browser Bookmarks、Everything、Quicklinks 只用真实平台/evidence artifact 关闭，不用 mock evidence 替代。

## 验证命令

```bash
corepack pnpm -C "packages/utils" exec vitest run "__tests__/search/indexing-watch-root-policy.test.ts" "__tests__/search/indexing-scan-strategy.test.ts" "__tests__/search/indexing-scan-eligibility.test.ts" "__tests__/search/indexing-source-progress-store.test.ts"
corepack pnpm -C "packages/utils" exec vitest run "__tests__/search/indexing-source-task-retry-policy.test.ts"
corepack pnpm -C "packages/utils" exec vitest run "__tests__/search/indexing-source-task-run-gate.test.ts"
corepack pnpm -C "apps/core-app" exec vitest run "src/main/modules/box-tool/addon/files/file-provider-startup.test.ts" "src/main/modules/box-tool/addon/files/services/file-provider-watch-service.test.ts" "src/main/modules/box-tool/addon/files/services/file-provider-scan-progress-service.test.ts" "src/main/modules/box-tool/addon/files/services/file-provider-index-scheduler-service.test.ts"
corepack pnpm -C "apps/core-app" exec vitest run "src/main/modules/box-tool/search-engine/indexing-runtime.test.ts"
corepack pnpm -C "apps/core-app" exec vitest run "src/renderer/src/views/base/settings/indexing-source-diagnostics-display.test.ts"
corepack pnpm -C "apps/core-app" exec vitest run "scripts/settings-indexing-diagnostics-verify.test.ts" "scripts/coreapp-packaged-indexing-diagnostics-probe.test.ts"
corepack pnpm -C "apps/core-app" exec vitest run "src/main/modules/box-tool/search-engine/search-index-migration-preflight.test.ts" "scripts/search-index-migration-preflight.test.ts"
corepack pnpm -C "apps/core-app" exec vitest run "src/main/modules/box-tool/search-engine/scan-progress-schema.test.ts" "scripts/scan-progress-source-scope-migration.test.ts" "src/main/modules/box-tool/addon/files/services/file-provider-scan-progress-service.test.ts" "src/main/modules/box-tool/addon/files/services/file-provider-runtime-reset-service.test.ts" "src/main/modules/box-tool/search-engine/workers/search-index-worker-client.test.ts" "src/main/modules/box-tool/search-engine/workers/search-index-worker-scan-progress.test.ts" "src/main/modules/box-tool/search-engine/workers/search-index-worker.retry.test.ts"
corepack pnpm -C "apps/core-app" run settings:indexing-diagnostics:verify -- --input "<diagnostics.json>" --sourceId file-provider --requireReadOnlyEnvelope --requireNaturalRecentTaskEvidence
corepack pnpm -C "apps/core-app" run visible:experience:indexing-diagnostics-probe -- --remoteDebuggingUrl "http://127.0.0.1:<port>/json/list" --outputDir "<evidence-dir>" --sourceId file-provider
corepack pnpm -C "apps/core-app" run search:production-migration-readiness -- --output "<readiness.json>"
corepack pnpm -C "apps/core-app" run search:fts-ownership-simulate -- --db "<sqlite.db>" --output "<fts-simulation.json>" --evidenceScope real-profile --requireRealProfileEvidence
corepack pnpm -C "apps/core-app" run search:index-migration:preflight -- --db "<sqlite.db>" --output "<report.json>" --evidenceScope real-profile --requireRealProfileEvidence
corepack pnpm -C "apps/core-app" run search:scan-progress-simulate -- --db "<sqlite.db>" --output "<simulation.json>" --evidenceScope real-profile --requireRealProfileEvidence
corepack pnpm -C "apps/core-app" run search:scan-progress-migration -- --db "<sqlite.db>" --output "<plan.json>" --evidenceScope real-profile --requireRealProfileEvidence
corepack pnpm -C "apps/core-app" run search:index-migration:evidence-verify -- --strict --preflight "<report.json>" --productionReadiness "<readiness.json>" --ftsSimulation "<fts-simulation.json>" --scanProgressSimulation "<simulation.json>" --scanProgressPlan "<plan.json>" --settingsVerification "<settings-verification.json>" --settingsProbe "<probe-envelope.json>"
corepack pnpm -C "apps/core-app" run typecheck:node
corepack pnpm -C "apps/core-app" run typecheck:web
git diff --check
```
