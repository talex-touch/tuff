# Nexus Performance TODO

> 更新时间：2026-07-16
> 范围：`apps/nexus` 文档站、生态站、Dashboard、Provider Registry、Data Governance 与公开控制台性能收口。全局执行顺序服从 [`TODO.md`](./TODO.md)。

## Goal

```text
1. 扫描整个 nexus 优化性能，自行测试各种报告。目前加载超级缓慢，希望每个页面切换都很快，而且固定各种模板，拆分，该静态的静态。
2. 各种文档加载也特别慢，对应目前 aireview 组件还没有审批完的全部都要好好优化一下，依次修复，分批做提交，做截图+性能加载 playwright 分析报告等。
```

## 当前口径

- Nexus 性能线与 CoreApp / AI / R3 分开推进，不混入其它主题或验证批次。
- `output/playwright/` 只作为 ignored evidence 目录，不纳入提交。
- 每批保持小切片：Playwright screenshot/HAR/Markdown/JSON、focused 验证或等价静态检查、`git diff --check`。
- production 结论必须用 production build / preview HAR，不用 dev-only HAR 替代。
- 当前既定 goal 约 `98.5%`。pending / aireview、首页 runtime warnings 与 dev SSR TTFB / Nuxt macro 已完成本地收口；最终完成判定仍只等待 deployed production preview/HAR、真实 OAuth callback、authenticated dashboard runtime 与真实 bfcache hit。是否把这些外部证据拆成独立 goal 仍需单独讨论；不再从聊天上下文恢复范围。

## 日期 Goal 进度记录

- 2026-07-12：最终 external production evidence 再审计：`wrangler.toml` 已有生产 `AUTH_ORIGIN=https://tuff.tagzxia.com`，但 `NEXUS_DEPLOYED_PREVIEW_URL`、`NEXUS_DEPLOYED_AUTH_STATE`、`NEXUS_DEPLOYED_PROVIDER_CALLBACK_EVIDENCE` 均未配置；`output/` 中没有含 Playwright `cookies/origins` 的 storage state，也没有真实 provider callback summary/HAR 或 `nexus-cloudflare-pages-preview-runtime-smoke-YYYY-MM-DD`。collector dry-run 与 strict template 正常，`check:runtime-evidence:deployed` 继续仅因 deployed evidence 文件缺失失败。下一步访问生产 origin、复用真实登录态或采集 OAuth callback HAR 涉及生产请求与会话信息，等待明确确认后执行；Goal 保持 `98.5% active`，不以模板或本地证据代替最终 proof。
- 2026-07-12：dev SSR TTFB / Nuxt macro 完成本地收口。fresh process 在 `Vite client warmed up` 后发首请求，Nitro startup 与 route cold TTFB 分开记录；递归剔除 `pages/**/components/**` 嵌套路由，并为无 `definePageMeta` 的 dev macro 请求增加 serve-only 空 meta fast path。五路 cold TTFB：tabs `3356 -> 2102ms`（`-37.4%`）、home `5236 -> 2544ms`（`-51.4%`）、store `3844 -> 2642ms`（`-31.3%`）、sign-in `3256 -> 2283ms`（`-29.9%`）、dashboard `2403 -> 1932ms`（`-19.6%`）；warm 保持 `5-21ms`。focused Vitest `27/27`、Worker guard `19/19`、scoped ESLint、typecheck、production build `500` routes / PWA `9`、Worker/API/runtime/scoped diff guards 通过；证据见 `output/playwright/nexus-dev-ssr-route-macro-2026-07-12.{json,md}`。Goal 仍保持 `98.5% active`，外部 deployed/auth/dashboard/bfcache 证据不变。
- 2026-07-12：首页 runtime warnings 完成本地收口。`useGsapReveal` 不再把 Vue `Ref` 传给 `gsap.context`，异步 GSAP setup 在组件卸载后停止；`useTuffHomeSections` 的 listener/tween 清理统一在 setup 期注册，并把 `autoKill` 放回 `scrollTo` plugin 配置。最终 local Wrangler production smoke 覆盖 `/` desktop、`/new` desktop、`/` mobile，均为 `200`，加载、滚动与截图后 console warning/error `0`、page error `0`、横向溢出 `0`；旧 `Invalid scope`、async lifecycle `onBeforeUnmount` 与 WebGL `ReadPixels` 在当前干净 Chrome 样本中均未复现。focused Vitest `5/5`、scoped ESLint、typecheck、两次 production build 与 scoped diff-check 通过；证据见 `output/playwright/nexus-home-runtime-warnings-2026-07-12.{json,md}`。Goal 仍保持 `98.5% active`，外部 deployed/auth/dashboard/bfcache 证据不变。
- 2026-07-12：pending / aireview 审批队列已按当前内容重新核对：`232/232` localized component docs 为 `syncStatus: reviewed` 且 `verified: true`，覆盖 `116` 个双语 slug，旧 pending worktable 保留为历史批次记录，不再表示当前仍有 `76` 个待审批组件。
- 2026-07-12：docs sidebar/navigation/search 改用显式 locale/scope 静态路径，旧 query endpoint 仅保留动态兼容层。英文组件页 sidebar + component navigation 从旧全语言 `133,390 B / 23,565 B gzip` 降到 `49,779 B / 8,831 B gzip`，减少 `62.68% / 62.53% gzip`；英文 search 从 `448` 条全语言 `100,357 B / 22,955 B gzip` 收到 `224` 条 `49,830 B / 10,837 B gzip`。
- 2026-07-12：本地 Wrangler 复采发现并修复 Cloudflare Pages `_routes.json` 100-rule 截断：旧产物为 `1 include + 99 exclude`，后段 docs detail 路径会落入 Worker 并返回 `404` shell；现在用 `/en/docs`、`/en/docs/*`、`/zh/docs`、`/zh/docs/*` 四条稳定规则覆盖全部 docs，重建后 exclude `64`。Worker guard 同时检查 `25` exact static routes、`2` docs patterns 与 `8` worker-owned routes。
- 2026-07-12：最终 local Wrangler runtime：BaseSurface `46 requests / 25 JS / 12 CSS / 510,675 B / failed 0`，Fusion `46 / 25 / 12 / 512,189 B / failed 0`，BaseSurface -> Card SPA `16 / 9 / 2 / 51,645 B / failed 0`；同 href `2 -> 1`，正文已渲染，SQLite/wasm/content runtime `0`、auth `0`、横向溢出 `0`。证据见 `output/playwright/nexus-aireview-locale-static-payload-2026-07-12.{json,md}` 与两张 locale-static runtime 截图。
- 2026-07-12：本轮 production build `500` prerender routes、PWA precache `9`、dist `34.11 MiB`、Worker `8.91 / 2.13 MiB gzip`、shared entry CSS `269.3 KiB / 47.0 KiB gzip`；main goal 仍保持 `98.5%`，只剩 deployed Cloudflare Pages preview HAR、真实 OAuth callback、authenticated dashboard runtime 与真实 bfcache hit 等外部证据，不把 local Wrangler 结果改写为 deployed production 完成。
- 2026-07-04：当前 Nexus performance separate thread 按既定 goal 约 `98%`。本轮已把 sidebase auth runtime、route matrix、docs production chunk/payload/CSS、PWA precache trim、Cloudflare root SQL dump、bfcache lifecycle blocker 与 local Wrangler runtime smoke 收到 guarded 状态；相关 source guard、runtime evidence checker、deployed preview collector dry-run / non-local / HTTPS guard 与 Nexus-only 复验均已同步。
- 2026-07-04：该进度不是完成声明。仍缺 deployed Cloudflare Pages preview HAR、真实 provider callback smoke、authenticated dashboard runtime smoke、真实 bfcache hit，以及 pending / aireview 组件继续拆分、首页 warnings 与 dev SSR TTFB / Nuxt macro 后续批次。
- 2026-07-04：下一次回来先讨论是否修改或拆分 goal；若 goal 继续保持原范围，则 production chunk/CSS 最终结论仍以 `node build/check-runtime-evidence.mjs --require-deployed-preview` 通过为准。
- 2026-07-04：`review-wlcb1-new-api-dryrun` 是独立 wlcb1 / new-api 只读评估，已归档为完成；不计入 Nexus performance / Cloudflare production gate。当前 Nexus / CF 下一步仍是 deployed preview evidence 采集与 `--require-deployed-preview` gate。
- 2026-07-10：focused 回归确认 runtime evidence checker、deployed collector dry-run 与 performance source guards 仍通过；`--require-deployed-preview` 仍因缺 Cloudflare Pages deployed evidence 正确失败。已将误放在 `server/api` 的 `app-auth/device/approve.post.test.ts` 移至 `test/api/app-auth/device`，未放宽 route-tree guard；目标测试 `3/3`、performance source tests `18/18` 与 server API route-tree 验证均通过。
- 2026-07-10：基于当前 Nexus docs/components 工作树重新完成 production build 与 content asset trim；Nitro prerender `494` routes、PWA precache `9` entries，worker executable JS `8.94 MiB` / gzip `2.13 MiB`，dist `38.27 MiB`，docs detail lifecycle guard `468` routes，初始资产预算 `484` routes / `5` families，worker/dist/chunk/payload/CSS guard 全部通过。已补齐英文 `/pricing`、`/forgot-password` locale contract，并加入 en/zh source guard；重建后缺 key warnings 与中文 fallback 均消失。
- 2026-07-10：当前既定 goal 上报为约 `98.5%`，本轮不擅自修改 goal 范围。UnoCSS production web fonts 改为显式 `UNOCSS_WEBFONTS=true` 才启用，移除首页/旧主题组件的 Google Fonts CSS `@import` 与无效 PWA 字体 runtime cache；最终 production HTML/CSS 远程字体引用 `1 -> 0`，并加入 dist artifact guard。剩余最终结论仍是 deployed Cloudflare Pages preview HAR、真实 provider callback、authenticated dashboard 与真实 bfcache hit；pending / aireview、首页 warnings、dev SSR TTFB 是否拆成独立 goal，回头单独讨论。
- 2026-07-10：production client chunk warning 已按真实加载边界收口：Nuxt manifest 确认 `575,038` bytes Mermaid renderer core 与 `548,948` bytes `TxCodeEditorRuntime.vue` 均为动态能力，prerender HTML 直接引用 `0`；Vite warning limit 对齐为 `600 kB`，同时 `build:analyze-worker` 新增 `600,000` bytes 单 chunk 硬上限与 `>500,000` bytes chunk 禁止进入静态 HTML 首屏的 route-aware guard。重建后通用 chunk-size warning 消失，证据见 `output/playwright/nexus-production-client-chunk-guard-2026-07-10.{json,md}`。
- 2026-07-10：`/notes/[slug]` 不再为单个 sanitizer 引入 `@talex-touch/utils/renderer` 根 barrel，改用 `renderer/shared/markdown-sanitizer` 子路径；production `node:fs/promises` / `node:path` browser-external warnings `3 -> 0`，dist `38.43 -> 38.28 MiB`，worker executable `9.01 -> 8.94 MiB`、gzip `2.15 -> 2.13 MiB`，worker files `649 -> 648`。该变更保持 sanitizer 实现与输出契约不变，只缩小客户端依赖图。
- 2026-07-10：UnoCSS Attributify 改为仅接受 `un-` 前缀，避免 `text`、`shadow`、`position` 等 Vue component props 被误生成为全局 attribute utility；仅有的真实旧式 Attributify 用法 `Counter.vue` 已改为普通 class。shared entry CSS `311,637 -> 301,151 bytes`（`-10,486`），production unprefixed Attributify finding `0`，dist 进一步降到 `38.27 MiB`。本地 production visual smoke 覆盖 home/docs/store/sign-in 桌面与移动 `8/8` PASS，JSON/Markdown 与截图见 `output/playwright/nexus-attributify-visual-smoke-2026-07-10.*`、`nexus-attributify-pw-*-2026-07-10.png`；该 smoke 仍不替代 deployed Cloudflare evidence。
- 2026-07-10：最终 Nexus-only 本地复验与 Task Pulse 已同步为 `98.5% / blocked`：targeted Vitest `61/61`、visual smoke `8/8`、dist `38.27 MiB`、worker executable `8.94 MiB` / gzip `2.13 MiB`、shared entry CSS `301,151` bytes、heavy chunks `2` 且静态 HTML 首屏引用 `0`。`node build/check-runtime-evidence.mjs --require-deployed-preview` 仍只因缺 deployed Cloudflare Pages evidence 正确失败；deployed preview URL、auth storage state 与 provider callback evidence 输入均未配置，因此 goal 保持 active，不作完成声明。
- 2026-07-10：Sentry production client 保留 DSN、初始化与 error capture，同时通过官方 compile-time flags 移除未配置的 debug / tracing / replay runtime；共享 entry JS `449,709 -> 391,590` bytes，gzip `158,782 -> 139,080` bytes（分别减少 `58,119` / `19,702` bytes）。route-aware guard 新增 prerender HTML 直接引用 chunk `420,000` bytes 上限，当前 oversized initial chunks `0`；证据见 `output/playwright/nexus-sentry-tree-shake-client-entry-2026-07-10.{json,md}`。
- 2026-07-10：修正 visual smoke 横向溢出判定为比较 `documentElement.scrollWidth / clientWidth`，发现旧 `nexus-attributify` smoke 对首页 mobile 的 `window.innerWidth=720` 存在假阴性。首页四个实际 section 的负 right 装饰已改为仅 `sm` 以上偏移，最终 home mobile `documentWidth=390 / viewportWidth=390`；home/docs/store/sign-in desktop/mobile 严格 visual smoke `8/8` PASS，见 `output/playwright/nexus-sentry-tree-shake-visual-smoke-2026-07-10.{json,md}` 与同名前缀截图。相关 targeted Vitest `84/84`、production build `494` routes、PWA precache `9`、dist `38.22 MiB`、worker `8.94 / 2.13 MiB`、typecheck、Worker/runtime/API route-tree guard 均通过；并行 `docs.performance.test.ts` 仍有既有全文件 `setTimeout` 断言与当前 aside/outline timer 冲突，本性能线未越界修改。
- 2026-07-11：resumed goal 复查确认 deployed preview URL、auth storage state、provider callback evidence 与 Cloudflare Pages runtime smoke 仍未进入环境或 evidence 目录，最终 production gate 继续等待外部输入。本地已修复 `docs.performance.test.ts` 的 guard 漂移：不再全文件禁止 `setTimeout`，而是仅检查 `scheduleTuffexBackground()` 保持无 timer、使用 `requestIdleCallback` 与 `requestAnimationFrame` fallback；intentional aside/outline delayed mount 不再被误伤。相关性能/runtime/API 集合由 `85/86 -> 86/86`，Worker bundle、runtime evidence、API route tree 与 scoped `git diff --check` 通过，证据见 `output/playwright/nexus-docs-performance-guard-alignment-2026-07-11.{json,md}`。既定进度仍为 `98.5%`，不把本地 guard 修复计作 deployed production 完成。
- 2026-07-11：production 初始资产预算补充实际 gzip 传输门禁，并缓存单资产压缩结果，完整 Worker guard 仍约 `1.5s`。当前各 route family 的 JS / CSS gzip 最大值为 docs `189,823 / 71,256 bytes`、store `241,085 / 75,189`、landing `267,601 / 79,037`、public-info `178,993 / 64,539`、auth `218,073 / 68,276`，均在独立阈值内；shared entry JS / CSS 为 `391,590 / 301,198 bytes`（gzip `138,886 / 57,601`）。性能相关 Vitest `86/86`、Worker/runtime/API/typecheck 与 scoped `git diff --check` 通过，证据见 `output/playwright/nexus-initial-asset-gzip-budget-2026-07-11.{json,md}`。该门禁只防止本地 production artifact 压缩体积回退，既定 goal 仍为 `98.5%`，deployed preview / OAuth / authenticated dashboard / bfcache 结论不变。
- 2026-07-11：landing production HTML 的 below-fold 图片发现链路继续收口。旧 dist 中 `/`、`/new`、`/next` 分别含 `5 / 5 / 1` 个 `rel=prefetch as=image`，且 `/`、`/new` 的四张插件卡 JPEG 会被 SSR URL 提前发现；现在 Nitro response hook 仅移除三条 landing route 的 image-prefetch，`AppleBlurImage` 对四张插件背景使用 `200px` IntersectionObserver 显式 defer，SSR 保留图片节点、alt、hero、waitlist 与完整正文，但不再输出四个 `src` / asset URL。重建后 prefetch finding `0/3`、deferred image URL finding `0/3`，四图合计 `232,581 bytes` 不再由初始 HTML 暴露；production build 仍为 `494` routes、PWA `9`、dist `38.22 MiB`、worker `8.94 / 2.13 MiB`，Vitest `93/93`、Worker/runtime/API/typecheck 与 scoped diff check 通过，证据见 `output/playwright/nexus-landing-image-defer-2026-07-11.{json,md}`。本机 Chrome headless 在新一轮截图前卡于 browser 初始化，因此本条只标记 production artifact `PASS`，不替代既有 visual smoke 或 deployed HAR；goal 仍为 `98.5%`。
- 2026-07-11：landing showcase 视频加载链路完成同类收口。旧 dist 的 `/`、`/new`、`/next` 均在 SSR `<video src>` 暴露 `SearchApp.mp4`，且 `TuffShowcaseDisplayer` 的 immediate watch 会额外创建 3 个临时 video 并并发读取全部 metadata；本地静态访问日志曾观察到 `SearchApp.mp4`、`SearchFileImmediately.mp4`、`PluginTranslate.mp4` 首访全部请求。现在 showcase 未进入 viewport 前不设置 video/poster/glow URL、不启动轮播，删除独立 metadata parser 与 `networkClient`，由实际 video 的 `loadedmetadata` 回传 duration。三条 landing HTML 的 showcase MP4 finding `1/1/1 -> 0/0/0`，保留无 `src`、`preload=none` 的 video 节点；三个视频合计 `1,474,692 bytes` 不再由初始 HTML 或 metadata probe 触发。首页 initial JS `36 -> 34` files、`734,291 -> 716,850 bytes`、gzip `266,431 -> 259,918 bytes`；production build `494` routes、PWA `9`、dist `38.22 MiB`、worker `8.94 / 2.13 MiB`，Vitest `94/94` 与全门禁通过，证据见 `output/playwright/nexus-landing-video-defer-2026-07-11.{json,md}`。post-change Chrome runtime 仍因本机 headless 初始化问题未取得，不替代 deployed HAR；goal 保持 `98.5%`。
- 2026-07-11：shared entry CSS 来源审计确认 TuffEx `base.css` 仅约 `29 KiB / 3.6 KiB gzip`，docs `github-markdown.css` 已保持 route chunk；真正的全局大头是 Uno icon data URI。dashboard 的 `i-cib-linux` 与 TuffEx status-badge 的 `i-simple-icons-linux` 原先分别嵌入大 SVG path，现在在 Nexus Uno config 内保持原 class contract、共同映射到较小的 Carbon Linux glyph，最终编译为一个 selector。shared entry CSS `301,198 -> 291,204 bytes`，gzip `57,601 -> 53,817 bytes`；icon CSS `161,419 -> 151,425 bytes`，gzip `36,757 -> 32,317 bytes`。`build:analyze-worker` 新增 shared entry `295,000 / 57,000 gzip bytes` 专项门禁，production build `494` routes、PWA `9`、dist `38.21 MiB`、worker `8.94 / 2.13 MiB`，focused Vitest `69/69`、Worker/runtime/API/typecheck 与 evidence JSON `81/81` parse 通过。证据见 `output/playwright/nexus-shared-entry-css-icon-compact-2026-07-11.{json,md}`；本条仍是 `PASS_ARTIFACT`，Task Pulse / Goal 同步为 `98.5% / blocked`，只等待 deployed production 外部证据。
- 2026-07-12：继续压缩 Uno 全局 icon data URI，将 dashboard Apple/Linux/Safari/Edge、sign-in Fingerprint、`/new` Snowflake/Figma、landing Tools 与 dashboard nav Machine Learning 共 `9` 个大图标 token 替换为紧凑 glyph；旧 selector 在 production shared entry 中全部为 `0`，新 selector 均存在。shared entry CSS `291,204 -> 282,272 bytes`，gzip `53,817 -> 50,163 bytes`；相对两轮前累计减少 `18,926 / 7,438 gzip bytes`。icon CSS 当前 `142,493 / 28,860 gzip bytes`，门禁收紧为 `286,000 / 54,000 gzip bytes`，并直接拒绝旧 selector 回流。production build `494` routes、PWA `9`、dist `38.21 MiB`、worker `8.94 / 2.13 MiB`、`648` files；Worker guard、focused Vitest `17/17`、API route tree、runtime evidence、typecheck 均通过。local Wrangler `/new` 与 `/sign-in` 视觉 smoke 在 `1280x720` 下无横向溢出、无 console error，截图与数据见 `output/playwright/nexus-shared-entry-css-icon-compact-2026-07-12.{json,md}`、`nexus-icon-compact-*-2026-07-12.png`。
- 2026-07-12：既定 goal 进度仍为 `98.5%`，不因本地 CSS/视觉门禁继续收紧而擅自上调。Dashboard 图标仍只有 source/production artifact proof；最终外部阻塞仍是 deployed Cloudflare Pages preview HAR、真实 OAuth provider callback、authenticated dashboard runtime 与真实 bfcache hit。pending / aireview、首页 warnings、dev SSR TTFB 是否移出或改写 goal，留待回头讨论。
- 2026-07-12：shared entry common icon 第三轮完成：Settings / Fire 保持旧 class contract，但在 Uno shortcut 内映射到紧凑 Remix glyph，并以 compiled icon rule `354 / 562 bytes`、上限 `600 / 750 bytes` 守卫；Spinner / Palette 全 Nexus app/content 机械统一为 `i-ri-loader-4-line` / `i-ri-palette-line`，两个旧 token 的 source 与 production selector finding 均为 `0`。shared entry CSS `282,272 -> 279,155 bytes`、gzip `50,163 -> 49,252 bytes`；icon CSS `142,493 -> 139,376 bytes`、gzip `28,860 -> 27,936 bytes`，总门禁收紧到 `282,000 / 52,000 gzip bytes`。production build 保持 `494` routes、PWA `9`、dist `38.20 MiB`、worker `8.94 / 2.13 MiB`、`648` files，route-family JS/CSS gzip 最大值仍全部在预算内。证据见 `output/playwright/nexus-shared-entry-css-common-icon-compact-2026-07-12.{json,md}`。
- 2026-07-12：本轮 local Wrangler `/new` 在 `1280x720` 真实挂载并显示 `i-ri-palette-line`，旧 Palette token `0`、横向溢出 `0`、console error `0`；`/updates` 页面正常渲染，但本地无已发布 channel，Settings action 未自然挂载。Spinner / Settings / Fire docs demos 因该临时 preview 未绑定 Nuxt Content D1 而无法挂载，明确降级为 source + compiled production artifact proof，不冒充 runtime visual。既定 goal 仍为 `98.5%`，最终外部阻塞不变。
- 2026-07-12：shared entry icon 第四轮将剩余高成本规则拆成“精确身份”与“通用语义”两组。DeepSeek、Claude、GitHub Copilot、Linux、OpenAI、Qwen、Sourcegraph、Cline、Moonshot AI 保持准确品牌/系统 glyph，不用通用符号降级识别，也不转成额外图片请求；Settings line/fill、Data Vis、Deployment Pattern、Queued、AI Status 共 `6` 个旧 class contract 通过 Uno shortcut 映射到紧凑 Remix glyph，并加入 `376 / 273 / 243 / 378 / 322 / 507 bytes` 独立 compiled rule 门禁。shared entry CSS `279,155 -> 275,750 bytes`、gzip `49,252 -> 48,150 bytes`；icon CSS `139,376 -> 135,971 bytes`、gzip `27,936 -> 26,808 bytes`，总门禁收紧为 `278,000 / 50,000 gzip bytes`。production build `494` routes、PWA `9`、dist `38.20 MiB`、worker `8.94 / 2.13 MiB`、`648` files；Worker guard、focused Vitest `17/17`、API route tree、runtime evidence、typecheck 均通过。
- 2026-07-12：round 4 compiled CSS visual 直接加载 `entry.DGkcpFph.css`，`1280x720` 下六个旧 selector `6/6` 可见且 mask 有效，横向溢出 `0`、console error `0`。route-family CSS gzip 最大值降为 docs `61,139`、store `65,071`、landing `68,921`、public-info `54,421`、auth `58,159 bytes`；证据见 `output/playwright/nexus-shared-entry-css-compact-alias-round4-2026-07-12.{json,md}` 与 `nexus-compact-alias-round4-2026-07-12.png`。既定 goal 仍为 `98.5%`，外部 deployed/auth/dashboard/bfcache 阻塞不变。
- 2026-07-12：production deployment payload 继续收口。此前 `nitro.sourceMap` 默认开启，即使 Sentry sourcemap upload 未启用也会随 Cloudflare Pages Worker 发布 `648` 个 `.map`、`1,825,308 bytes`；同时显式 `sourceMap: true` 会阻止 Sentry Nuxt module 使用“临时 hidden map + 上传后自动删除”的默认策略。现在普通 production build 默认关闭 Nitro map；Sentry upload 开启时把所有权交回 Sentry；仅 `NUXT_ENABLE_NITRO_SOURCEMAP=true` 的显式诊断构建保留 map，`NUXT_DISABLE_NITRO_SOURCEMAP=true` 继续作为 force-disable override。
- 2026-07-12：重建后 Worker sourcemap `648 / 1,825,308 bytes -> 0 / 0`，相同 `648` 个 executable files 的 `sourceMappingURL` footer 同时减少 `28,940 bytes`，Worker deployment payload 合计减少 `1,854,248 bytes`；dist `38.20 -> 36.43 MiB`，Worker executable `9,374,124 -> 9,345,184 bytes`、gzip `2,207,530 bytes`。prerender `494`、PWA precache `9`、static routes `19/19`、worker-owned routes `8/8`、Content SQL dumps `3/3` 与 shared entry `275,750 / 48,150 gzip bytes` 均保持。证据见 `output/playwright/nexus-worker-sourcemap-payload-2026-07-12.{json,md}`；Goal 仍为 `98.5%`，不把本地 artifact 收口冒充最终 deployed/auth/dashboard/bfcache 证据。
- 2026-07-12：浏览器 Nuxt Content SQLite 链路已迁出：global search、旧 Search 组件与 policy markdown 分别改走 prerendered `/api/docs/search` metadata index 和缓存 `/api/content/policy`；覆盖路径中的 `queryCollection()` / `searchContent()` consumer 为 `0`。搜索 index 为 `448` records、`100,357 bytes / 22,955 bytes gzip`，仅包含 `id/path/locale/title/description/tags`。
- 2026-07-12：production client nested SQL payload `3 files / 1,327,624 bytes -> 0 / 0`，SQLite wasm、SQLite worker 与 SQLite runtime marker files 均为 `0`；Worker 端 `dump.app.sql`、`dump.docs.sql`、`dump.guides.sql` 共 `1,327,624 bytes` 继续作为 Cloudflare D1 bootstrap 输入保留。`build:analyze-worker` 已从“只拒绝 duplicate wasm”加固为同时拒绝 canonical/duplicate wasm、SQLite worker 文件及 hash client chunk 中的 SQLite 初始化 marker。
- 2026-07-12：本批 production artifact 为 prerender `495`、PWA precache `9`、static routes `20/20`、dist `35,731,509 bytes / 34.08 MiB`、Worker executable `9,340,356 bytes / 2,231,113 bytes gzip`、`650` files；focused Vitest `82/82`、scoped ESLint、typecheck、Worker guard、API route tree、runtime evidence 与 scoped diff check 通过。证据见 `output/playwright/nexus-content-db-payload-2026-07-12.{json,md}`。Goal 保持 `98.5% active`，最终外部 blockers 仍是 deployed Cloudflare Pages preview HAR、真实 OAuth provider callback、authenticated dashboard runtime 与真实 bfcache hit。

## 已完成摘要

- docs route 首屏重型链路已完成主要止血：
  - sidebar metadata 延迟加载
  - docs metadata 避免全量 MDC parse
  - i18n locale messages 懒加载
  - route-local locale messages 拆分
  - dev route-local CSS 污染过滤
  - component docs dev metadata fast path
  - sidebar / pager full-body prefetch 可取消化
  - docs Assistant context 按用户意图构建
  - pending 长文档后半段 deferred render
  - code copy header 原生 DOM 增强
  - toast feedback 动态加载
  - public docs auth/profile graph 移出首访
  - legacy sonner CSS 移出首访
- docs production static shell / payload 已完成一轮收口：
  - 所有 docs detail route 生产 SSR 改为 metadata-only (`body=0`)，`/docs` 与 `/docs/index` 保持完整 SSR。
  - docs 正文通过客户端 idle/full-body fetch 拉取，避免每个 docs HTML 重复携带完整 MDC body。
  - docs navigation 改为 client lazy fetch，component docs 仍只请求 `scope=components`，避免静态 HTML 重复注入全量 nav tree。
  - docs engagement tracker 已移除 `beforeunload` flush，保留 `visibilitychange` / `pagehide` flush，避免 docs 公共页面主动引入 bfcache blocker。
  - `node apps/nexus/build/check-worker-bundle.mjs`：`dist total=36.43 MiB`，worker executable JS `8.91 MiB`，gzip `2.11 MiB`，worker JS 文件 `648` 个，Worker sourcemap `0`，静态路由排除 `19` 条通过。
  - `build:analyze-worker` 现在同时守卫 route files、docs detail HTML payload、docs/store/landing/auth/public-info CSS boundary、public route-family 初始 JS/CSS budget 与 HTML 初始资产引用断链；当前静态路由文件 `19/19` 通过。
  - production build 默认不再访问远程字体 provider：UnoCSS web fonts 仅在 `UNOCSS_WEBFONTS=true` 时启用，组件 CSS 不再 `@import` Google Fonts，PWA 不再维护无请求来源的 Google/gstatic runtime cache；`build:analyze-worker` 会扫描 production HTML/CSS 并拒绝远程字体 URL，当前 finding `0`。
  - production client chunks 使用 route-aware 双层预算：单 chunk 不得超过 `600,000` bytes，超过 `500,000` bytes 的 heavy chunk 不得被任何 prerender HTML 直接引用；当前 heavy chunks `2`、direct HTML references `0`，且 Vite 通用 chunk-size warning 已消失。release notes sanitizer 也已从 renderer 根 barrel 改为直接子路径，Node browser-external warnings `3 -> 0`。Worker sourcemap 默认不随 production artifact 发布；Sentry upload 构建由 Sentry 临时生成并在上传后清理，显式诊断构建才允许保留。
  - UnoCSS Attributify 仅允许 `un-` 前缀，阻断 Vue props 到全局 CSS 的误提取；四轮 icon data URI 压缩后 shared entry CSS 当前 `275,750` bytes（gzip `48,150`），并由 `278,000 / 50,000 gzip bytes` 专项门禁、`11` 个大图标 selector 回流检查与 `8` 个兼容 alias compiled rule-byte 上限守卫；production unprefixed Attributify finding `0`，既有本地 production desktop/mobile visual smoke `8/8`，`/new` Palette runtime 与 round 4 六图 compiled CSS visual 均通过。
  - landing 的 below-fold 图片不再依赖浏览器原生 lazy-load 距离启发式：三条 landing HTML 会移除 `rel=prefetch as=image`，四张插件卡背景只在卡片进入 `200px` observer margin 后设置 `src`；artifact guard 同时拒绝 image-prefetch 与插件 JPEG URL 回到初始 HTML。
  - landing showcase 的三个 MP4 只在实际 showcase 进入 viewport 后加载；移除全量 video metadata 预探测，实际 video 负责回传 duration，artifact guard 拒绝任一 showcase MP4 URL 回到 landing initial HTML。
  - docs detail HTML 全量守卫：`468` 个 docs detail page 最大约 `20.3 KiB`，无 full body marker，无 `/api/docs/navigation` payload；`/docs` 与 `/docs/index` 作为完整 SSR 例外保留。
  - docs deferred runtime API 静态证据已补：`output/playwright/nexus-docs-runtime-route-guard-2026-06-28.{json,md}`，Worker 产物中 `/api/docs/page`、navigation、sidebar-components、view、feedback、comments、engagement 路由 chunks `10/10` 存在；该证据不替代 Cloudflare / wrangler runtime HAR。
  - 初始资产静态证据已补：`output/playwright/nexus-initial-asset-budget-2026-06-25.{json,md}`，docs/store/landing/auth route family 全部未超预算；该证据不替代 Cloudflare / wrangler runtime smoke。
  - public route asset budget 静态证据已补：`output/playwright/nexus-public-route-asset-budget-2026-06-28.{json,md}`，docs/store/landing/public-info/auth 共 `5` 个 route family、`468` 个静态 HTML 全部未超预算，CSS boundary finding `0`；该证据不替代 Cloudflare / wrangler production HAR。
  - 本地生产静态产物 HAR / screenshot smoke 已补：`output/playwright/nexus-local-prod-static-runtime-smoke-2026-06-28.{json,md}`，覆盖 `/`、`/next/`、`/store/`、docs detail、`/pricing/`、`/sign-in/`；该证据为 `PARTIAL`，landing 的 media abort 与 docs full-body `/api/docs/page` 404 来自静态 server 不执行 Worker API，仍需 Cloudflare / wrangler runtime HAR。
  - PWA precache trim 静态证据已补：`output/playwright/nexus-local-prod-pwa-precache-trim-2026-06-28.{json,md}`，`sw.js` precache 从 `1428` 条收敛到 `9` 条，docs route `456 -> 0`，route HTML-like `456 -> 0`，`_nuxt` chunk/asset `958 -> 0`（仅允许 `2` 条 Nuxt build metadata）；public routes 本地 HAR docs request `0`，不再出现千级 docs / `_nuxt` precache fan-out；`check-runtime-evidence` 也守卫 public route `_nuxt` / HTML-like / failed request 预算与 docs detail 静态 serving 预期 API 404 allowlist。该证据仍不替代 Cloudflare / wrangler runtime HAR。
  - Nuxt Content Cloudflare runtime 依赖 dist root `dump.app.sql`、`dump.docs.sql`、`dump.guides.sql` 初始化 D1；`trim-content-assets.mjs` 保留并验证 root SQL dumps，同时删除重复 SQLite wasm 与 prerendered `__nuxt_content/<collection>/sql_dump.txt` 客户端副本。浏览器 Content database consumer 已迁出，production client SQLite wasm/worker/runtime marker 为 `0`；PWA 仍排除 `dump.*.sql`、`__nuxt_content/**`、sqlite/wasm runtime assets。
  - 本地 Wrangler Pages runtime smoke 已补：`output/playwright/nexus-wrangler-prod-runtime-smoke-2026-07-03.{json,md}`，使用本地 D1/KV/R2 bindings；API probes `8/8` OK（含 docs full body、docs navigation、auth session/providers、auth callback error boundary、dashboard auth boundary），route probes `8/8` OK（含 `/`、`/next/`、`/store/`、`/pricing/`、`/sign-in/`、docs detail、dashboard unauth shell、app auth callback shell），`sw.js` precache `9` 条、docs/route HTML/`_nuxt` chunk/content runtime entries 均 `0`，public route docs request `0`，单个 public route `_nuxt` asset 最大 `90`。`node build/check-runtime-evidence.mjs` 已可重复校验 local Wrangler 与 PWA JSON/MD/HAR evidence sidecars、Markdown 摘要关键数值、API payload / redirect budget、route-level 请求预算 / translation key leak 与当前 warning budget（2 个既有 hydration mismatch、未观察到真实 bfcache、public `_nuxt` 正常资产计数）；`node build/check-runtime-evidence.mjs --print-deployed-preview-template` / `check:runtime-evidence:template` 可打印最终 Cloudflare Pages preview evidence schema；`node scripts/collect-deployed-preview-evidence.mjs` / `collect:runtime-evidence:deployed` 默认 dry-run，显式 HTTPS `--base-url` 才采集 deployed preview route/API/HAR/bfcache 骨架，并拒绝本地或非 HTTPS base URL；`node build/check-runtime-evidence.mjs --require-deployed-preview` / `check:runtime-evidence:deployed` 已作为最终 production gate 预留，会强制要求 `nexus-cloudflare-pages-preview-runtime-smoke-YYYY-MM-DD`、非本地 base URL、真实 provider callback、authenticated dashboard 与真实 bfcache hit。该证据为 local Wrangler preview，仍不替代 deployed Cloudflare Pages preview HAR、真实 provider callback 与 authenticated dashboard smoke。
  - docs bfcache lifecycle 静态证据已补：`output/playwright/nexus-docs-bfcache-lifecycle-guard-2026-06-28.{json,md}`，source 与重建后 client artifact 均确认 docs tracker `beforeunload` absent，`pagehide` / `visibilitychange` flush present；当前 `build:analyze-worker` 也守卫 `468` 个 docs detail HTML 的初始 JS 不注册 `beforeunload`；仍需 production preview back/forward cache smoke。
  - 本地 docs -> store/pricing -> back smoke 已补在 `nexus-local-prod-static-runtime-smoke-2026-06-28` 与 `nexus-wrangler-prod-runtime-smoke-2026-07-03`：返回路径正确且 navigation type 为 `back_forward`，但 `pageshow.persisted=false`，未证明真实 bfcache 命中。
- sidebase auth runtime 已完成 guarded 收口：
  - Nuxt 仍保留 `@sidebase/nuxt-auth` server/authjs 能力与 `#auth` server import。
  - sidebase client plugin 与 named `auth` middleware 都在 Nuxt `app:resolve` / `app:templates` 阶段移出 app-side runtime，客户端显式 auth flow 走 `useNexusAuth()`。
  - `build:analyze-worker` 已守卫 `_nuxt` client JS 与 `sw.js`：`SessionRequired`、`nuxt-auth-app-side`、`useAuthState`、`navigateToAuthPages` 等 sidebase app-side marker 不得进入客户端产物。
  - `build:analyze-worker` 已守卫 runtime-only route chunks：`/api/auth/**`、`/api/auth/me`、`/api/app-auth/device/start`、`/api/app-auth/device/poll`、`/api/app-auth/device/approve` 必须存在于 Worker 产物。
  - 静态证据已补：`output/playwright/nexus-worker-runtime-route-guard-2026-06-25.{json,md}`，required worker route chunks `6/6` 通过；`output/playwright/nexus-sidebase-runtime-artifact-guard-2026-06-25.{json,md}` 扫描客户端 runtime 文件 `698` 个，sidebase app-side marker `0`，runtime route chunks `6/6`。
  - 本地 Wrangler smoke 已覆盖 `/api/auth/session`、`/api/auth/providers`、`/api/auth/callback/github?error=AccessDenied` 与 `/auth/app-callback/` shell；仍不替代真实 provider callback flow。
  - Auth handler 已改为模块级 singleton，避免每次 request 重建 `NuxtAuthHandler`；`build:analyze-worker` 已加入 source guard，本地 Wrangler auth probes 未再出现 `You setup the auth handler for a second time` 日志。
  - 守卫覆盖 public docs、`/sign-in`、OAuth callback、protected dashboard、`/team/join`、`/auth/admin-bootstrap` 与 header user menu。
- route matrix 二轮已完成静态路由单一来源：
  - `build/nexus-static-routes.mjs` 作为 production prerender 与 worker budget guard 的共享来源。
  - `/new`、`/login`、`/sign-in`、`/store`、docs API routes 等进入同一 public/static route matrix。
  - 本地 static dist route matrix smoke 已补：`output/playwright/nexus-static-route-matrix-2026-06-24.{json,md}`，`19/19` 路由 OK，sidebase marker `0`，CSS boundary finding `0`，docs detail 样本最大 HTML `20,347` bytes。
  - `/next` 已纳入 landing route-local locale chunk，避免 production hydration 后落回 `landing.hero.heading` 等 key；本地 Wrangler route smoke 中 `/next/` H1 为 `A local desktop Agent center, one touch away.`。
  - `build:analyze-worker` 已守卫 worker-owned app routes 不得被静态化：`/dashboard`、`/dashboard/team`、`/dashboard/admin/provider-registry`、`/dashboard/admin/governance`、`/auth/app-callback`、`/auth/stepup-callback`、`/auth/admin-bootstrap`、`/team/join`。
  - route ownership 静态证据已补：`output/playwright/nexus-worker-owned-route-matrix-2026-06-28.{json,md}`，worker-owned routes `8/8` 通过。
  - 桌面 / 移动 viewport 截图样本已补：home、store、docs detail、sign-in 首屏均可渲染；该证据仅代表 `apps/nexus/dist` 静态文件，不替代 Cloudflare / wrangler runtime。
- authenticated dashboard runtime 下限已补静态守卫：
  - `build:analyze-worker` 已守卫 dashboard smoke 代表性 API route chunks：`/api/dashboard/plugins`、`/api/dashboard/team`、`/api/dashboard/provider-registry/providers`、`/api/dashboard/governance/summary`、`/api/dashboard/storage/status`、`/api/dashboard/telemetry/me` 必须存在于 Worker 产物。
  - 静态证据已补：`output/playwright/nexus-dashboard-runtime-route-guard-2026-06-28.{json,md}`，dashboard runtime route chunks `6/6` 通过；该证据不替代 authenticated Cloudflare / wrangler dashboard runtime smoke。
  - 本地 Wrangler unauth boundary 已补：`/api/dashboard/team` 返回 `401`，`/dashboard/` shell 返回 `200` 并显示登录入口；authenticated dashboard runtime smoke 仍待真实 session。
- Store plugin APIs / Store front page 已完成有界查询与首访 import 止血：`/api/store/search` 下推 D1 approval/category/keyword/latest-version/limit/offset 并默认返回 compact 卡片字段；Store 页面首屏改走 `/api/store/search` + 50 条分页、remote debounce、服务端筛选与 offset load-more；`/api/store/plugins` 统一走 bounded list helper，compact 首屏只返回卡片所需 latest-version 字段，非 compact 仅为当前页插件补齐 approved versions；插件详情弹层、tabs、metadata header 与 shared detail renderer 改为按需懒加载，避免首屏同步拉入交互后才需要的详情代码。
- Dashboard devices / overview / storage / team / account / notifications / oauth / api-keys / admin analytics 首访 import 已继续止血：devices 的 `GeoLeafletMap.client.vue` 改为展开设备地图时按需懒加载；overview 的 `DashboardSparklineChart.client.vue` 与 `GeoLeafletMap.client.vue` 改为图表/地图渲染边界内懒加载；admin analytics 的 geo `GeoLeafletMap.client.vue` 改为 geo 数据边界内按需懒加载；storage 的同步详情 `FlipDialog` 与 sparkline chart client 改为弹层/图表边界内懒加载；team 的 action overlays 与 credit trend chart 同步改为按需懒加载；account 的 profile edit、notifications 的 browser setup、oauth 的 create-app 与 api-keys 的 create-key `FlipDialog` 改为对应弹层打开时按需懒加载；均补 focused performance boundary test，避免 Leaflet/ECharts/dialog 客户端组件进入页面首访同步 import graph。
- `/en/docs/dev/components/tabs` 当前关键状态：
  - 页面 200
  - fresh-ready cold TTFB `3356 -> 2102ms`，warm `14.1 / 11.3ms`
  - `typedPages=false` A/B 已证明不是 Nuxt dev macro 请求根因
  - route-local component macro finding `0`；无 `definePageMeta` 页面走 serve-only 空 meta fast path
- pending / aireview 工作表：`../engineering/reports/nexus-performance-2026-06-21/pending-components-worktable.md`

## 未完成

| 主题 | 状态 | 下一步 |
| --- | --- | --- |
| docs 文档内容加载 | guarded | docs detail static shell 已扩到全 docs；生产 SSR metadata-only、nav lazy fetch、deferred full-body Worker route chunks、build budget / HTML 抽样证据均已补。下一步继续拆可见区 demo / API table lazy boundary。 |
| AI review / aireview 未审批组件 | guarded | 当前 `232/232` localized component docs 已 reviewed + verified；locale-static sidebar/navigation/search payload 与 BaseSurface/Fusion local Wrangler runtime 已复验。旧 worktable 仅保留历史批次。 |
| dev SSR TTFB / Nuxt macro | guarded | fresh-process / Vite-ready 五路基线与复采已分离 Nitro startup 和 route cold TTFB；嵌套 page-local route 递归过滤、空 page-meta dev fast path 与 focused guard 已补。 |
| sidebase auth runtime | guarded | sidebase client plugin + named `auth` middleware 已移出客户端产物，explicit auth/protected routes 有静态守卫；local Wrangler 已覆盖 auth session/providers/callback error boundary 与 app callback shell，仍需真实 provider callback smoke。 |
| route matrix 二轮 | guarded | public/static routes 已合并到 `nexus-static-routes.mjs`；Cloudflare `_routes.json` rule-cap 已用 docs exact/wildcard 规则收口，本地 static desktop/mobile smoke、worker-owned route matrix、dashboard runtime chunk guard、docs bfcache blocker artifact guard、local static back/forward smoke 与 local Wrangler route smoke 已补。仍需 deployed Cloudflare preview、authenticated dashboard 与 bfcache 命中实测。 |
| production chunk / payload / CSS | guarded | docs/store/dashboard/landing/public-info/auth source boundary、docs metadata-only payload、client Content SQLite zero-runtime guard、静态 route files、初始 JS/CSS budget、worker/dist budget、Worker sourcemap retention、PWA precache trim、remote font artifact guard、local static HAR/screenshot smoke 与 local Wrangler runtime smoke 已有守卫；仍需 deployed Cloudflare Pages preview HAR 作为最终结论。 |
| 首页 warnings | guarded | `Invalid scope`、async lifecycle `onBeforeUnmount` 与 ScrollTo `autoKill` 配置已修复；desktop/mobile local Wrangler 加载、滚动、截图 smoke warning/error `0`。WebGL `ReadPixels` 在当前干净 Chrome 样本中未复现。 |

## 下批建议顺序

1. 跑 deployed Cloudflare Pages preview HAR：先用 `cd apps/nexus && node scripts/collect-deployed-preview-evidence.mjs` dry-run 核对采集计划，再用 `--base-url https://<cloudflare-pages-preview-host>`、必要的 `--auth-state output/playwright/<storage-state>.json` 与真实 OAuth 回调摘要 `--provider-callback-evidence output/playwright/<provider-callback-evidence>.json` 采集 docs / store / dashboard / landing / `/new` / auth callback smoke，生成 `output/playwright/nexus-cloudflare-pages-preview-runtime-smoke-YYYY-MM-DD.{json,md}` 与 HAR sidecars，然后跑 `cd apps/nexus && node build/check-runtime-evidence.mjs --require-deployed-preview` 作为 production 结论最终证据。
2. Back/forward cache 与 authenticated dashboard 二轮实测。
3. 真实 OAuth provider callback smoke。

## 验证要求

- 每批至少保留一份 Playwright/HAR 或等价静态证据。
- 文档内容加载优化必须对比 before / after 请求数、failed count、关键 runtime chunk、visible shell。
- production chunk/CSS 最终结论必须跑 production preview；当前仅能标记为 guarded，不能标记 done。
- 2026-07-04 Nexus-only 复验通过：`node apps/nexus/build/check-runtime-evidence.mjs`、`node apps/nexus/build/check-worker-bundle.mjs`、`cd apps/nexus && node build/check-server-api-route-tree.mjs`、targeted `vitest` 53 tests、`nuxt typecheck`、`git diff --check -- "apps/nexus" "docs/plan-prd/TODO-nexus.md"`。
- 2026-07-10 当前工作树 production build / trim / worker budget 通过；targeted `vitest` `61/61`、server API route-tree、runtime evidence checker、deployed collector dry-run、production HTML/CSS remote font finding `0`、client heavy chunks `2 / 0 direct HTML references`、Node browser-external warnings `0`、unprefixed Attributify finding `0`、local visual smoke `8/8` 与 `nuxt typecheck` 通过，`--require-deployed-preview` 因缺最终 Cloudflare evidence 正确失败。
- 2026-07-10 本 goal 涉及文件的 scoped `git diff --check` 与全部 Nexus evidence JSON parse 通过；全量 `git diff --check -- "apps/nexus"` 仍被并行 docs/demo 工作树中的既有 EOF 空行和 trailing whitespace 阻塞，本性能线未越界修改这些文件。
- 2026-07-12 Worker sourcemap payload 批次复验通过：production build / trim、Worker guard、targeted Vitest `36/36`、server API route-tree、runtime evidence checker、`nuxt typecheck` 与 scoped `git diff --check` 均通过；`--require-deployed-preview` 仍仅因缺最终 Cloudflare Pages evidence 正确失败。
- 2026-07-12 Content database payload 批次复验通过：focused Vitest `82/82`、scoped ESLint、Worker guard、server API route-tree、runtime evidence checker、`nuxt typecheck` 与 scoped `git diff --check` 均通过；strict deployed gate 仍按预期仅因缺 Cloudflare Pages preview evidence 失败。
- 2026-07-12 aireview locale-static / route-cap 批次复验通过：production build `500` routes、focused Vitest `80/80`、scoped ESLint、Worker guard、server API route-tree、runtime evidence checker、`nuxt typecheck`、evidence JSON parse、scoped `git diff --check` 与 BaseSurface/Fusion/Card local Wrangler runtime 均通过；strict deployed gate 仍仅因缺 `nexus-cloudflare-pages-preview-runtime-smoke-YYYY-MM-DD.json` 正确失败。
- 2026-07-12 首页 warnings 批次复验通过：focused Vitest `5/5`、scoped ESLint、`nuxt typecheck`、production build `500` routes / PWA precache `9`、desktop/mobile local Wrangler runtime warning/error `0`、page error `0`、横向溢出 `0`，以及 scoped `git diff --check` 通过。
- 2026-07-12 dev SSR route macro 批次复验通过：fresh-ready 五路 cold TTFB 改善 `19.6%-51.4%`，focused Vitest `27/27`、Worker guard `19/19`、scoped ESLint、`nuxt typecheck`、production build `500` routes / PWA precache `9`、Worker/API/runtime/scoped diff guards 均通过；strict deployed gate 仍仅因缺 Cloudflare Pages preview evidence 正确失败。
- `git diff --check` 必须通过。
