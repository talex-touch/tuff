export interface GridProps {
  /** Draw vertical lines at x ticks. */
  x?: boolean
  /** Draw horizontal lines at y ticks. */
  y?: boolean
  /** Suggested tick count for the driving scale. */
  ticks?: number
}
