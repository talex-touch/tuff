# Tuff v2.4.14-beta.8 更新说明

## 摘要

- 外观设置内容恢复，主题材质、壁纸、强调色和动效选项正常显示。
- CoreBox 推荐应用恢复各自图标，不再统一显示空白占位图。
- 本地资源访问继续保持最小权限，只开放应用图标专用缓存目录。

## 变更内容

- 修复外观页把 `ThemeStyle` 嵌套成独立页面壳后，内容区域无法正常呈现的问题。
- 修复 `tfile` 资源协议未纳入 `app-icons` 缓存路径、导致应用图标请求被拒绝的问题；相邻缓存目录仍不可访问。
