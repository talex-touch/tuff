# Tuff v2.4.14-beta.29 更新说明

## 摘要

- OTA 下载的任一分块遇到签名地址 HTTP 403 后，都会切换到备用地址并从当前 Range 继续，避免并发分块继续请求已失效地址。
- 保留已完成的分块和任务级 fallback 状态，后续并发请求直接使用备用地址。
- 非 403 权限错误仍不切换地址，继续 fail-closed。

## 变更内容

- 修复并发 OTA 下载中仅首个失败分块切换 fallback、其他分块继续使用过期签名地址的问题；每个分块首次确认 403 后都复用任务级备用地址并保留已下载进度。
