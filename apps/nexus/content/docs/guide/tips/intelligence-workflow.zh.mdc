# 智能工作流（当前形态）

## 当前结论

- 工作流能力已可用，但当前主路径是 **CoreBox 命令 + 插件能力组合**。
- 文档中的“Workflow Studio”不作为当前稳定入口；请优先使用已上线命令与插件。

## 建议触发方式

- `ai`：智能问答、总结、改写（`touch-intelligence`）
- `fy` / `fy-multi`：翻译与多源翻译（`touch-translation`）
- `script`：执行 workspace 脚本（`touch-workspace-scripts`）

## 组合模板（可直接复用）

| 模板 | 链路 |
| --- | --- |
| 邮件处理 | `fy` 翻译原文 → `ai` 生成正式回复 |
| 研发检查 | `script` 运行 lint/test → `ai` 总结输出 |
| 多源翻译比对 | `fy-multi` 对照结果 → 复制最佳版本 |

## 调试建议

- 先确认插件已安装且权限授权完成。
- 结果异常时优先排查输入与权限，而不是直接更换模型。
- 涉及系统命令时，先用小范围脚本验证再扩展。
