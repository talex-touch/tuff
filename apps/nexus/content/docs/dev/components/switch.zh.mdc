---
title: Switch 开关
description: 轻触反馈与状态切换
category: Form
status: beta
since: 1.0.0
tags: [switch, toggle, state]
syncStatus: reviewed
verified: true
---

# Switch 开关

`TxSwitch` 用于即时生效的二元状态切换（开/关、启用/停用）。需要「先勾选、后提交」的表单语义时用 `TxCheckbox`。

## 基础用法
:::TuffCodeBlock{lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const enabled = ref(true)
  </script>

  <template>
    <TuffSwitch v-model="enabled" />
  </template>
---
:::

## Demo
### Toggle State
点击或使用 Enter / Space 可切换状态，位移与颜色同步变化。
:::TuffDemoWrapper{demo="SwitchToggleStateDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const firstSwitch = ref(false)
  const secondSwitch = ref(true)
  </script>

  <template>
    <div style="display: inline-flex; align-items: center; gap: 12px;">
      <TuffSwitch v-model="firstSwitch" />
      <TuffSwitch v-model="secondSwitch" />
    </div>
  </template>
---
:::

### 不同大小
小尺寸适合密集列表，默认尺寸适合表单，大尺寸适合突出设置项。
:::TuffDemoWrapper{demo="SwitchSizesDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const smallEnabled = ref(true)
  const defaultEnabled = ref(true)
  const largeEnabled = ref(true)
  </script>

  <template>
    <div style="display: flex; align-items: center; gap: 14px;">
      <TuffSwitch v-model="smallEnabled" size="small" />
      <TuffSwitch v-model="defaultEnabled" />
      <TuffSwitch v-model="largeEnabled" size="large" />
    </div>
  </template>
---
:::

### 禁用效果
禁用态会降低透明度、移除聚焦，并阻止点击与键盘切换。
:::TuffDemoWrapper{demo="SwitchDisabledDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const enabled = ref(true)
  const disabledOff = ref(false)
  const disabledOn = ref(true)
  </script>

  <template>
    <div style="display: flex; align-items: center; gap: 14px;">
      <TuffSwitch v-model="enabled" />
      <TuffSwitch v-model="disabledOff" disabled />
      <TuffSwitch v-model="disabledOn" disabled />
    </div>
  </template>
---
:::

### 前后 Label
在开关前后放置状态文本，或与说明文案组成设置行。
:::TuffDemoWrapper{demo="SwitchLabelDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const compact = ref(false)
  const autoSync = ref(true)
  </script>

  <template>
    <div style="display: flex; flex-direction: column; align-items: flex-start; gap: 12px;">
      <label style="display: inline-flex; align-items: center; gap: 10px;">
        <span>关</span>
        <TuffSwitch v-model="compact" />
        <span>开</span>
        <strong>紧凑模式</strong>
      </label>
      <label style="display: flex; justify-content: space-between; align-items: center; gap: 10px; width: 100%; max-width: 320px; padding: 12px 14px; border: 1px solid var(--tx-border-color-light); border-radius: 14px;">
        <span style="display: inline-flex; flex-direction: column; gap: 4px;">
          <strong>自动同步</strong>
          <small style="color: var(--tx-text-color-secondary); font-size: 12px;">{{ autoSync ? '开' : '关' }}</small>
        </span>
        <TuffSwitch v-model="autoSync" />
      </label>
    </div>
  </template>
---
:::

### 自定义颜色
Switch 暂不提供颜色 prop，可通过 CSS 变量或局部 class 覆盖激活态颜色。
:::TuffDemoWrapper{demo="SwitchCustomColorDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const success = ref(true)
  const warning = ref(true)
  const violet = ref(false)
  </script>

  <template>
    <div class="switch-custom-color-demo">
      <label class="switch-custom-color-demo__item is-success">
        <span>成功色</span>
        <TuffSwitch v-model="success" />
      </label>
      <label class="switch-custom-color-demo__item is-warning">
        <span>警告色</span>
        <TuffSwitch v-model="warning" />
      </label>
      <label class="switch-custom-color-demo__item is-violet">
        <span>紫色</span>
        <TuffSwitch v-model="violet" />
      </label>
    </div>
  </template>

  <style scoped>
  .switch-custom-color-demo {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 14px;
  }

  .switch-custom-color-demo__item {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: var(--switch-demo-color);
  }

  .switch-custom-color-demo__item :deep(.tuff-switch.is-active) {
    background-color: var(--switch-demo-color);
  }

  .switch-custom-color-demo__item.is-success {
    --switch-demo-color: var(--tx-color-success);
  }

  .switch-custom-color-demo__item.is-warning {
    --switch-demo-color: var(--tx-color-warning);
  }

  .switch-custom-color-demo__item.is-violet {
    --switch-demo-color: #8b5cf6;
  }
  </style>
---
:::

## API

### Props

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `modelValue` | `boolean` | `false` | 由 `v-model` 控制的开关状态。 |
| `size` | `'small' \| 'default' \| 'large'` | `'default'` | 视觉尺寸。默认尺寸不添加额外 modifier class。 |
| `disabled` | `boolean` | `false` | 移除可聚焦能力，并阻止点击、Enter、Space 切换。 |

### Events

| 事件名 | 参数 | 说明 |
|------|------|------|
| `update:modelValue` | `(value: boolean) => void` | 启用状态下点击、Enter 或 Space 后，携带下一个开关值触发。 |
| `change` | `(value: boolean) => void` | 启用状态下用户切换后，携带同一个下一个开关值触发。 |

### Slots

| 插槽名 | Props | 说明 |
|------|-------|------|
| - | - | `TuffSwitch` 没有插槽；标签与设置说明放在组件外部组合。 |

## 交互契约

- 根节点是原生 `<button>`，渲染 `role="switch"`、`aria-checked`、`aria-disabled`；不设置 `tabindex`——可聚焦性由按钮元素自身提供。
- `disabled=true` 时设置原生 `disabled`（因而移出 Tab 序列）、添加 `is-disabled`，并阻止点击、Enter、Space 触发事件。
- 启用切换会同时触发 `update:modelValue` 与 `change`；组件不会在宿主未处理 `v-model` 更新时自行修改外部状态。
- `size="small"` 与 `size="large"` 会添加尺寸 modifier；`size="default"` 只保留基础 class。

## 组合示例

### 设置项
开关搭配标签、说明文案和状态文本，用于设置列表。
::::TuffDemoWrapper{demo="SwitchSettingsRowDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { reactive } from 'vue'

  const settings = reactive({
    notifications: true,
    focusMode: false,
    autoUpdate: true,
  })
  </script>

  <template>
    <div class="settings-panel">
      <div class="settings-row">
        <div>
          <TxTag label="通知" />
          <p>接收系统和插件的重要提醒。</p>
        </div>
        <TuffSwitch v-model="settings.notifications" />
      </div>
      <div class="settings-row">
        <div>
          <TxTag label="专注模式" color="var(--tx-color-warning)" />
          <p>隐藏非必要提示，减少打断。</p>
        </div>
        <TuffSwitch v-model="settings.focusMode" />
      </div>
    </div>
  </template>

  <style scoped>
  .settings-panel {
    display: flex;
    flex-direction: column;
    width: min(100%, 560px);
  }

  .settings-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }
  </style>
---
::::

## 最佳实践

- Switch 用于即时生效的布尔设置；表单提交项或多选组优先使用 Checkbox。
- 在开关旁提供可读文本标签；组件本身只负责控件，不渲染内置文案。
- 标签与开关保持垂直居中，密集设置行也要保留足够点击区域。
- 自定义激活色只使用局部 class 覆盖，保留基础组件的禁用、焦点与尺寸样式。

## 审阅说明 / Review Notes

- **Model contract:** `TxSwitch` 由 `modelValue` 控制。用户交互会同时派发 `update:modelValue` 与 `change`，参数为下一个 boolean；视觉状态在父级回写新 prop 后更新。
- **键盘契约:** 启用状态响应点击、Enter 与 Space（原生按钮语义）。禁用状态由原生 `disabled` 移出 Tab 序列，保持 `aria-disabled="true"`，且不派发事件。
- **实测覆盖:** `switch.test.ts` 覆盖 active aria 状态、尺寸 class、点击派发、禁用阻断，并断言两种状态下均不渲染 `tabindex`。

## Source

- Component source: `packages/tuffex/packages/components/src/switch/src/TxSwitch.vue`。
- Style source: `packages/tuffex/packages/components/src/switch/style/index.scss`。
- Export entry: `packages/tuffex/packages/components/src/switch/index.ts` 导出 `TuffSwitch`，别名 `TxSwitch`，并使用组件自身的 `TuffSwitch` name 注册。
- Coverage: `packages/tuffex/packages/components/src/switch/__tests__/switch.test.ts` 验证语义、尺寸 class、点击更新与禁用行为。

<TuffDocSourceLink />
