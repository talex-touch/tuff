---
title: ProgressBar 进度条
description: 支持确定进度、不确定加载、分段进度与状态反馈的进度条。
category: Feedback
status: beta
since: 1.0.0
tags: [progress, bar, loading]
syncStatus: reviewed
verified: true
---
# ProgressBar 进度条

## Demo

### 状态进度

:::TuffDemoWrapper{demo="ProgressBarStatefulProgressDemo" code-lang="vue"}
---
code: |
  <template>
    <TxProgressBar :percentage="32" show-text />
    <TxProgressBar :percentage="68" status="warning" show-text />
    <TxProgressBar success message="完成" />
    <TxProgressBar loading message="同步中" />
  </template>
---
:::

### 上传进度

:::TuffDemoWrapper{demo="ProgressBarUploadDemo" code-lang="vue" description="文案行位于轨道上方：主文案取填充色，detail 以分隔点弱化。默认轨道无描边、填充由淡到亮、前端有一点柔光。`flow-effect=\"stardust\"` 会在填充上流动两层白色星点，近层带闪烁；纯色和自定义渐变都能用，渐变进度条的前端柔光会改为白色以匹配。"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const percentage = ref(65)
  </script>

  <template>
    <TxProgressBar
      :percentage="percentage"
      :format="p => `上传中 ${p}%`"
      detail="1.4 MB / 2.3 MB"
      aria-label="上传 report.pdf"
      show-text
      text-placement="top"
      height="6px"
      flow-effect="stardust"
    />
    <TxProgressBar
      :percentage="percentage"
      aria-label="上传 report.pdf"
      height="6px"
      color="linear-gradient(90deg, #3b82f6, #a855f7)"
      flow-effect="stardust"
    />
  </template>
---
:::

### 分段进度

:::TuffDemoWrapper{demo="ProgressBarSegmentsDemo" code-lang="vue" description="悬停某一段：该段放大、其余段变暗，并在上方弹出提示，显示该段的 `label` 及其占 `segmentsTotal` 的比例。请在进度条上方留出提示的空间。"}
---
code: |
  <script setup lang="ts">
  const segments = [
    { value: 25, color: 'linear-gradient(90deg, #60a5fa, #34d399)', label: '视频' },
    { value: 18, color: 'linear-gradient(90deg, #a78bfa, #f472b6)', label: '图片' },
    { value: 12, color: 'linear-gradient(90deg, #fb7185, #f59e0b)', label: '文档' },
  ]
  </script>

  <template>
    <TxProgressBar :segments="segments" :segments-total="100" height="8px" />
  </template>
---
:::

### 后台运营进度

:::TuffDemoWrapper{demo="ComponentsOperationsStatusDemo" code-lang="vue" title="运营状态面板" description="进度条与指标卡、状态徽标共同表达后台健康度。"}
---
code: |
  <template>
    <section class="grid gap-3">
      <TxProgressBar :percentage="86" status="success" height="10px" show-text flow-effect="shimmer" mask-variant="dashed" />
      <TxProgressBar :percentage="72" status="warning" height="10px" show-text flow-effect="wave" />
      <TxProgressBar :percentage="48" height="10px" show-text indicator-effect="sparkle" />
    </section>
  </template>
---
:::

### 状态面板

:::TuffDemoWrapper{demo="ProgressBarStatusPanelDemo" code-lang="vue"}
---
code: |
  <template>
    <TxProgressBar :percentage="80" show-text message="上传中" />
    <TxStatusBadge text="进行中" status="warning" />
  </template>
---
:::

## 交互契约

- 轨道渲染 `role="progressbar"`，并包含 `aria-valuemin="0"` 与 `aria-valuemax="100"`。
- 确定进度会把 `percentage` 限制在 `0..100`，并通过 `aria-valuenow` 暴露。
- `loading` 与 `indeterminate` 会省略 `aria-valuenow`；应使用 `message` 描述当前工作。
- `error` 与 `success` 布尔 props 优先于 `status`。
- `success` 或 `error` 配合 `message` 且 `percentage` 为 `0` 时，视觉宽度会解析为 `100%`，适合完成类标签。
- `message` 优先于 `format`，`format` 优先于默认四舍五入百分比文案。
- 只有存在 `message` 或 `showText=true` 时才显示文本；loading/indeterminate 状态只有存在 `message` 时显示文本。`textPlacement="top"` 与 `outside` 遵循同一规则，只是把文案行渲染在轨道上方。
- `detail` 只在 `textPlacement="top"` 下渲染，位于主文案之后、以分隔点弱化；它是可见文本，不进入 progressbar 的可访问名（仍为 `ariaLabel` > `message` > `Progress`）。
- 默认轨道是文字色 10% 的平铺色块，不渲染 `.tx-progress-bar__mask`、不画描边；`maskVariant="solid" | "dashed"` 才生成描边，`maskBackground` 不为 `'none'` 时才渲染 mask 层。
- 填充默认是 `linear-gradient(90deg, 淡 → 饱和)`；`color` 本身是渐变字符串时原样使用。`.tx-progress-bar__glow` 在所有非 segments 的确定进度条上挂载，位于轨道裁剪之外，且仅在 0% 与 100% 之间可见。渐变 `color` 没有单一色相，所以它的柔光是白色，`top` 文案退回文字色（`--tx-progress-glow`、`--tx-progress-accent`）。
- `flowEffect="stardust"` 在填充上绘制两层白色星点（`::before` 远层：小、慢、稳定；`::after` 近层：大、快、闪烁），各自按整块贴图宽度平移，流动永远没有接缝。星点以 px 定尺寸，5px 和 14px 的进度条颗粒感一致。`'particles'` 是已废弃的别名，渲染同样的两层。任何 flow 效果都不会画在 `segments` 上，不确定态下也不画。
- 宽度变化以 480ms `--tx-ease-out-strong` 过渡；不确定态的五种扫光只动合成属性（`transform`，`split` 另加 `opacity`），不动 `left` / `width`，`prefers-reduced-motion` 下停止扫光。行进式扫光（`sweep`、`classic`、`elastic`）匀速运动且首尾都完全在轨道之外，循环点永远不在画面里，进度条不会出现"一顿一顿"。
- `segments` 会忽略非正数值。填充总宽度使用 `segmentsTotal`，填充区域内的各段宽度按正数 segment 总和归一化。每一段把颜色画在内层 `.tx-progress-bar__segment-fill` 上，并带有 `label · 占比%` 的 `data-tip`（占比相对 `segmentsTotal`；没有 label 时只显示占比）。悬停时该段放大、其余段变暗、提示从进度条上方升起；分段轨道会设为 `overflow: visible` 给放大留出空间。
- `complete` 在解析进度到达 `100` 时每个完成周期只触发一次；进度回落到 `100` 以下后再次完成可再次触发。
- `tooltip` 或 `tooltipContent` 会把进度条包在 `TxTooltip` 中；`tooltipProps` 会透传给该 tooltip。

## API

### Props

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|---------|------|
| `loading` | `boolean` | `false` | 不确定加载模式。 |
| `indeterminate` | `boolean` | `false` | 不暗示 loading 的不确定进度模式。 |
| `indeterminateVariant` | `'classic' \| 'sweep' \| 'bounce' \| 'elastic' \| 'split'` | `'sweep'` | loading/indeterminate 状态的动画变体。 |
| `error` | `boolean` | `false` | 错误状态，覆盖 `status`。 |
| `success` | `boolean` | `false` | 成功状态，覆盖 `status`。 |
| `status` | `'success' \| 'error' \| 'warning' \| ''` | `''` | 视觉状态色。 |
| `message` | `string` | `''` | 可见和可访问文本标签。 |
| `detail` | `string` | `''` | 次要文案（如 `1.4 MB / 2.3 MB`），只在 `textPlacement="top"` 下渲染；不进入可访问名。 |
| `percentage` | `number` | `0` | 限制前的确定进度值。 |
| `segments` | `ProgressSegment[]` | - | 多段进度数据。 |
| `segmentsTotal` | `number` | `100` | 存在 segments 时用于计算填充宽度的总量。 |
| `height` | `string` | `'5px'` | 进度条高度。 |
| `showText` | `boolean` | `false` | 确定进度下显示百分比文本。 |
| `textPlacement` | `'inside' \| 'outside' \| 'top'` | `'inside'` | message/百分比文本位置；`top` 在轨道上方渲染文案行（含可选 `detail`）。 |
| `format` | `(percentage: number) => string` | - | 自定义百分比格式化函数。 |
| `flowEffect` | `'none' \| 'shimmer' \| 'wave' \| 'stardust' \| 'particles'` | `'none'` | 确定进度填充层动效。`stardust` 在填充上流动两层白色星点；`particles` 是它的废弃别名。对 `segments` 无效。 |
| `indicatorEffect` | `'none' \| 'sparkle'` | `'none'` | 进度大于零时的端点指示器效果。 |
| `hoverEffect` | `'none' \| 'glow'` | `'none'` | 外层 hover 效果。 |
| `color` | `string` | - | 自定义填充颜色，覆盖状态颜色。 |
| `maskVariant` | `'solid' \| 'dashed' \| 'plain'` | `'plain'` | 轨道描边样式；`plain` 不画描边。 |
| `maskBackground` | `'none' \| 'blur' \| 'glass' \| 'mask'` | `'none'` | 轨道 mask 层；`none` 不渲染 mask 节点，轨道为平铺色块。 |
| `tooltip` | `boolean` | `false` | 使用解析文本启用 tooltip。 |
| `tooltipContent` | `string` | - | tooltip 内容覆盖。 |
| `tooltipProps` | `Partial<TooltipProps>` | - | 透传给 `TxTooltip` 的额外 props。 |

### ProgressSegment

| 字段 | 类型 | 说明 |
|------|------|------|
| `value` | `number` | 段值，只有正的有限数会渲染。 |
| `color` | `string` | 段填充色，默认回退为进度条填充色。 |
| `label` | `string` | 显示在该段悬停提示中、位于占比之前（`视频 · 25%`）；没有 label 时提示只显示占比。 |

### Events

| 事件名 | 参数 | 说明 |
|------|------|------|
| `complete` | - | 解析进度达到 `100` 时每个完成周期触发一次。 |

### Slots

| 插槽名 | 参数 | 说明 |
|------|------|------|
| - | - | `TxProgressBar` 不暴露插槽。标签和 tooltip 内容通过 `message`、`format`、`tooltipContent`、`tooltipProps` 配置。 |

## 最佳实践

- 只有进度已知时才使用 `percentage`；未知时长使用 `loading` 或 `indeterminate`。
- 不要为了成功态伪造 100%；操作完成时传 `success` 和简短 `message`。
- `message` 同时作为 progressbar 可访问标签，文案应保持简短。
- segment 代表部分总量时，用 `segmentsTotal` 保持分段进度可信。
- 动效只用于高价值进度反馈；过多 shimmer/wave/stardust 会让仪表盘变嘈杂。
- 给每一段都写上 `label`：悬停提示是分段进度条唯一能解释每种颜色含义的地方。
- 默认是无描边的平铺轨道；需要模糊或玻璃质感的轨道时显式传 `maskBackground`（配合 `maskVariant="solid"` 加描边），不要依赖旧默认值。
- 上传、下载这类有量级信息的任务用 `textPlacement="top"` + `detail` 把"已传多少"放在文案行，而不是塞进 `message`（它同时是可访问名）。

## 审阅说明

- 已对照 `packages/tuffex/packages/components/src/progress-bar/src/types.ts`、`TxProgressBar.vue` 与 `progress-bar.test.ts` 核对。
- 现有测试覆盖确定进度钳制、progressbar ARIA 状态、不确定状态省略 `aria-valuenow`、`complete` 每个完成周期只触发一次，以及 segment 按正数总和归一化。
- 实测覆盖（2026-09 重设计）：默认不渲染 mask 节点且 wrapper 无 `--bg-*` class；`--tx-progress-fill` 为 `linear-gradient(90deg, …)`、渐变 `color` 原样透传；光晕节点挂在轨道之外并在 0% / 100% / indeterminate / segments 下不可见或不渲染；`top` 文案行的 label / detail 渲染与显隐规则；全部 `@keyframes tx-progress-*` 块不含 `left` / `width`（含提取器阳性对照）；`prefers-reduced-motion` 下停止扫光。
- 实测覆盖（星点流动与分段悬停）：`stardust` 给填充加 class 且 `particles` 解析为同一个 class；segments 上和不确定态下没有 flow class；渐变 `color` 保留柔光并设置 `--tx-progress-glow: #fff`；两套模板里的分段提示都由 `label` 和相对 `segmentsTotal`（而非 segment 总和）的占比拼成；颜色位于内层 fill 节点；行进式扫光为 `infinite linear`，elastic 从 `-100%` 起、到 `454.5%` 止；星点层按整块贴图宽度平移且不读取 `--tx-progress-color`；分段轨道为 `overflow: visible`。
- 已知未修：`hoverEffect="glow"` 的 `box-shadow` 与 `indicatorEffect="sparkle"` 的光点都位于 `overflow: hidden` 的轨道内，会被裁到轨道高度；源码内有注释标记。
- API 说明：`segments` 只使用正数值。外层填充宽度基于 `segmentsTotal`，填充区域内部各段宽度按正数 segment 总和归一化。
- 可访问性说明：`message` 会作为可访问进度标签，应保持简短并表达状态。

## Source

- Component source: `packages/tuffex/packages/components/src/progress-bar/src/TxProgressBar.vue`.
- Types: `packages/tuffex/packages/components/src/progress-bar/src/types.ts`.
- **实测覆盖:** `packages/tuffex/packages/components/src/progress-bar/__tests__/progress-bar.test.ts` 验证确定进度钳制与 ARIA 状态、不确定状态省略 `aria-valuenow`、`complete` 每个完成周期只触发一次、segment 按正数总和归一化，默认无 mask 节点、渐变填充与光晕位置、`top` 文案行、关键帧不含 `left` / `width` 与 reduced-motion 停播，以及星点流动（class、别名、渐变白色柔光、无缝贴图平移）和分段悬停（提示、内层 fill、可见溢出、匀速扫光）。

<TuffDocSourceLink />

