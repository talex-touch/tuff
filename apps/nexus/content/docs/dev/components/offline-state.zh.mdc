---
title: "OfflineState 离线"
description: "用于离线或网络不可用时的提示。"
category: Feedback
status: beta
since: 1.0.0
tags: [offline, network, state]
syncStatus: migrated
---
# OfflineState 离线

`TxOfflineState` 是 `TxEmptyState` 的快捷封装，自动设置 `variant="offline"`，用于离线或网络不可用时的提示。

## 基础用法

### OfflineState
:::TuffDemoWrapper{demo="OfflineStateOfflineStateDemo" code-lang="vue"}
---
code: |
  <template>
    <TxOfflineState
      description="Network connection lost."
      :primary-action="{ label: 'Retry', type: 'primary' }"
    />
  </template>
---
:::

## API

`TxOfflineState` 继承 `TxEmptyState` 的全部 Props（除 `variant`）、Slots 和 Events，详见 [EmptyState](./empty-state.zh.md)。

## 交互契约

- `TxOfflineState` 始终向 `TxEmptyState` 透传 `variant="offline"`。
- 组件不额外改写 `title`、`description`、`surface` 或 action 配置；显式 props 会原样透传。
- `icon`、`title`、`description`、`actions` 插槽会原样转发给 `TxEmptyState`。
