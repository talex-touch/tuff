---
title: "OfflineState 离线"
description: "用于离线和网络不可用场景的快捷空状态。"
category: Feedback
status: beta
since: 1.0.0
tags: [offline, network, state]
syncStatus: reviewed
verified: true
---
# OfflineState 离线

`TxOfflineState` 是固定 `variant="offline"` 的轻量 `TxEmptyState` 封装。适合应用离线、依赖网络的面板无法加载，或可重试传输错误需要保留当前上下文的场景。

已人工核对 `packages/tuffex/packages/components/src/offline-state/src/TxOfflineState.vue`、`types.ts` 与 `offline-state.test.ts`。

## 基础用法

### OfflineState
:::TuffDemoWrapper{demo="OfflineStateOfflineStateDemo" code-lang="vue"}
---
code: |
  <template>
    <TxOfflineState
      surface="card"
      title="网络不可用"
      description="检查连接后重试，离线期间不会覆盖当前草稿。"
      :primary-action="{ label: '重试连接', type: 'primary' }"
    />
  </template>
---
:::

## 交互契约

- 始终向 `TxEmptyState` 透传 `variant="offline"`。
- 不覆盖显式传入的 `title`、`description`、`surface`、`layout`、`align`、`size`、`icon` 或 action props。
- `icon`、`title`、`description`、`actions` 插槽会原样转发给 `TxEmptyState`。
- 生成按钮触发的 `primary` / `secondary` 事件来自内部 `TxEmptyState`。

## API

### Props

`TxOfflineState` 接收除 `variant` 以外的所有 `EmptyStateProps` 字段。

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|---------|------|
| `title` | `string` | 预设标题 | 覆盖离线状态标题。 |
| `description` | `string` | 预设描述 | 说明网络问题，以及用户当前工作是否安全。 |
| `icon` | `TxIconSource \| string \| null` | 预设插画 | 自定义图标源/class，或传 `null` 隐藏图标区域。 |
| `iconSize` | `number` | 尺寸预设 | 自定义图标像素尺寸。 |
| `layout` | `'vertical' \| 'horizontal'` | `'vertical'` | 空状态布局。 |
| `align` | `'start' \| 'center' \| 'end'` | `'center'` | 内容对齐方式。 |
| `size` | `'small' \| 'medium' \| 'large'` | `'medium'` | 密度和插画尺寸。 |
| `surface` | `'plain' \| 'card'` | `'plain'` | 是否添加卡片边界。 |
| `primaryAction` | `EmptyStateAction` | - | 生成的主操作。 |
| `secondaryAction` | `EmptyStateAction` | - | 生成的次操作。 |
| `actionSize` | `TxButtonProps['size']` | `'small'` | 生成按钮默认尺寸。 |
| `loading` | `boolean` | `false` | 未传自定义图标时显示 spinner。 |

### Events

| 事件名 | 参数 | 说明 |
|------|------|------|
| `primary` | - | 从生成的主操作转发。 |
| `secondary` | - | 从生成的次操作转发。 |

### Slots

| 插槽名 | Props | 说明 |
|------|------|------|
| `icon` | - | 替换预设插画或自定义图标。 |
| `title` | - | 替换解析后的标题。 |
| `description` | - | 替换解析后的描述。 |
| `actions` | - | 替换生成的主/次操作按钮。 |

## 最佳实践

- 可恢复的网络或离线状态使用它；非网络相关的服务端失败应使用错误状态或业务专用错误组件。
- 告知用户本地草稿、队列操作或缓存数据是否仍然安全。
- 主操作应聚焦重试，避免在离线空态里放破坏性操作。
- 面板内建议使用 `surface="card"`，让网络状态与加载后的内容使用同一个视觉容器。

## 审阅说明 / Review Notes

- **Wrapper contract:** `TxOfflineState` 在 `TxEmptyState` 上锁定 `variant="offline"`；标题、描述、surface、actions、icon 与 slot 仍由宿主控制。
- **状态选择:** 适用于可恢复的本地网络/离线状态。非连接原因导致的服务端失败应使用 error-state 模式。
- **实测覆盖:** `offline-state.test.ts` 覆盖固定 variant 透传、props/actions、具名 slot 与 install 注册。

## Source

- Component source: `packages/tuffex/packages/components/src/offline-state/src/TxOfflineState.vue`。
- Base source: `packages/tuffex/packages/components/src/empty-state/src/TxEmptyState.vue`。
- Types: `packages/tuffex/packages/components/src/offline-state/src/types.ts` 导出 `OfflineStateProps = Omit<EmptyStateProps, 'variant'>`。
- Coverage: `packages/tuffex/packages/components/src/offline-state/__tests__/offline-state.test.ts` 覆盖 wrapper 透传与 install 行为。

<TuffDocSourceLink />
