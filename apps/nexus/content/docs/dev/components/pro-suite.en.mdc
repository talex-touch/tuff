---
title: Pro Suite Overview
description: Rich interaction, visual effects and low-level primitives
category: ProSuite
status: beta
since: 0.3.9
tags: [pro, suite, overview]
syncStatus: reviewed
verified: false
---

## Positioning

The Pro suite carries heavy interaction and visual expression: **Interaction** means complete interactive pieces like the command palette, search panel, both editors and the virtual list; **Effects** is the expression layer — glass surfaces, border beams, gradual blur, text motion and liquid; **Primitives** (BaseSurface, BaseAnchor, Floating, AutoSizer, ResizeBox) are the foundation the rest of the family is built on, rarely used directly by apps. Everything imports through the `@talex-touch/tuffex/pro` entry. Data visualization and the chart family moved to the **Data** suite — see the [Charts overview](/docs/dev/components/charts).

## Preview

Every cell is a live, interactive component; click the name in the top-left corner to open its documentation.

::DocsComponentsGallery{suite="pro"}
::

## Groups at a Glance

Every component in the suite, grouped by category. The list is read from each doc's `category` frontmatter, so new components appear on their own.

::DocsSuiteCatalog{suite="pro"}
::

