---
title: "SearchSelect"
description: "Searchable select input with local filtering, remote search events, and Popover-backed options panel."
category: Form
status: beta
since: 1.0.0
tags: [search, select, form]
syncStatus: reviewed
verified: true
---
# SearchSelect

`TxSearchSelect` combines a text input, local option filtering, optional remote search, and a Popover-backed options panel. Disabled options stay visible but cannot be selected.

The dropdown layer is hosted by `TxPopover` with full-width reference anchoring and no reference-click toggle.

## Basic Usage

### SearchSelect
:::TuffDemoWrapper{demo="SearchSelectSearchSelectDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref<string | number>('')

  const options = [
    { value: 'foo', label: 'Foo' },
    { value: 'bar', label: 'Bar' },
    { value: 'baz', label: 'Baz' },
    { value: 'disabled', label: 'Disabled', disabled: true },
  ]
  </script>

  <template>
    <div style="display: flex; flex-direction: column; gap: 10px; width: 320px;">
      <TxSearchSelect v-model="value" :options="options" placeholder="Search and pick" />
      <div style="font-size: 12px; color: var(--tx-text-color-secondary, #909399);">
        Value: {{ value }}
      </div>
    </div>
  </template>
---
:::

## Remote Search

When `remote=true`, input changes (debounced) and Enter trigger the `search` event; fetch externally and write results back to `options`.

### SearchSelect (remote)
:::TuffDemoWrapper{demo="SearchSelectSearchSelectRemoteDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref<string | number>('')
  const query = ref('')
  const loading = ref(false)

  const options = ref<Array<{ value: string, label: string }>>([])

  async function onSearch(q: string) {
    query.value = q
    loading.value = true
    await new Promise(resolve => setTimeout(resolve, 220))

    const base = Array.from({ length: 18 }).map((_, i) => `Result ${i + 1}`)
    const list = base
      .filter(s => s.toLowerCase().includes(q.trim().toLowerCase()))
      .slice(0, 12)

    options.value = list.map(s => ({ value: s, label: s }))
    loading.value = false
  }
  </script>

  <template>
    <div style="display: flex; flex-direction: column; gap: 10px; width: 320px;">
      <TxSearchSelect
        v-model="value"
        remote
        :loading="loading"
        :options="options"
        placeholder="Type to remote-search"
        @search="onSearch"
      />

      <div style="font-size: 12px; color: var(--tx-text-color-secondary, #909399);">
        Query: {{ query || '-' }}
      </div>
    </div>
  </template>
---
:::

## Notes

- Local mode filters by `options.label` and shows all when input is empty.
- Remote mode requires updating `options` in the `search` event; results are not cached.
- Closing the dropdown keeps input and panel state; reopening does not auto-reset.

## Dashboard Filter Toolbar

In dashboard list filters, `TxSearchSelect` works best for structured scopes such as range, type, or status. Keep keyword entry in `TxSearchInput`, and use `TxSearchEmpty` for the no-match recovery path.

:::TuffDemoWrapper{demo="ComponentsSearchFiltersDemo" code-lang="vue" title="Dashboard filter toolbar" description="A screenshot-verifiable composition of search input, search select, and search empty state."}
---
code: |
  <script setup lang="ts">
  import { computed, ref } from 'vue'

  const query = ref('')
  const selectedScope = ref<string | number>('all')
  const scopeOptions = [
    { value: 'all', label: 'All' },
    { value: 'docs', label: 'Docs' },
    { value: 'tasks', label: 'Tasks' },
  ]
  const filteredRecords = computed(() => (query.value ? [] : scopeOptions))
  </script>

  <template>
    <section class="grid gap-3">
      <TxSearchInput v-model="query" placeholder="Search plugins / docs / tasks" remote />
      <TxSearchSelect
        v-model="selectedScope"
        :options="scopeOptions"
        placeholder="Filter scope"
        panel-background="glass"
      />
      <TxSearchEmpty v-if="!filteredRecords.length" surface="card" />
    </section>
  </template>
---
:::

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|------|
| `modelValue` | `string \| number` | `''` | Selected option value controlled by v-model. |
| `placeholder` | `string` | `'Search'` | Placeholder forwarded to the search input. |
| `disabled` | `boolean` | `false` | Disables the input, popover, focus open, and remote search emission. |
| `clearable` | `boolean` | `true` | Shows the input clear affordance and allows clearing the selected value. |
| `options` | `TxSearchSelectOption[]` | `[]` | Option records displayed in the dropdown; disabled options cannot be picked. |
| `loading` | `boolean` | `false` | Shows a small spinner in the input suffix while results are loading. |
| `remote` | `boolean` | `false` | Disables local filtering and emits `search` so the caller can provide external results. |
| `searchDebounce` | `number` | `200` | Debounce delay in milliseconds before emitting remote `search` after input changes. |
| `dropdownMaxHeight` | `number` | `280` | Maximum options panel height in pixels. |
| `dropdownOffset` | `number` | `6` | Popover offset from the input reference. |
| `panelVariant` | `'solid' \| 'dashed' \| 'plain'` | `'solid'` | Popover panel surface variant. |
| `panelBackground` | `'pure' \| 'mask' \| 'blur' \| 'glass' \| 'refraction'` | `'refraction'` | Card background style |
| `panelShadow` | `'none' \| 'soft' \| 'medium'` | `'soft'` | Popover panel shadow strength. |
| `panelRadius` | `number` | `18` | Popover panel corner radius. |
| `panelPadding` | `number` | `6` | Popover panel inner padding. |

### Events

| Event | Params | Description |
|------|------|------|
| `update:modelValue` | `(v: string \| number)` | v-model update emitted after selecting an enabled option or clearing. |
| `change` | `(v: string \| number)` | Emitted with the same value after selection or clear. |
| `search` | `(q: string)` | Emitted in remote mode after debounce, and immediately on Enter. |
| `select` | `(opt: TxSearchSelectOption)` | Emitted with the picked enabled option before the dropdown closes. |
| `open` | - | Emitted when the options panel opens. |
| `close` | - | Emitted when the options panel closes; pending remote-search debounce is cleared. |

### Types

:::TuffCodeBlock{lang="ts"}
---
code: |
  export interface TxSearchSelectOption {
    value: string | number
    label: string
    disabled?: boolean
  }
---
:::

### Slots

`TxSearchSelect` does not expose public slots. The input prefix, loading suffix, empty state, and option rows are internal so keyboard and Popover behavior stay consistent.

### Expose

| Name | Type | Description |
|------|------|-------------|
| `open()` | `() => void` | Opens the options panel unless the component is disabled. |
| `close()` | `() => void` | Closes the options panel. |
| `focus()` | `() => void` | Focuses the internal `TxInput`. |
| `blur()` | `() => void` | Blurs the internal `TxInput`. |
| `clear()` | `() => void` | Calls the internal input clear affordance. |

## Best Practices

- Keep `options` small in local mode; large remote sources should use `remote`, `searchDebounce`, and external result hydration.
- Keep `label` stable and human-readable because local filtering only checks `option.label`.
- Do not put free-form keyword search here when the value is not selected from a controlled option list; use `TxSearchInput` instead.
- Treat disabled options as explanatory entries only. They remain visible but cannot be selected and should not be the only available result.

## Review Notes

- Source: `packages/tuffex/packages/components/src/search-select/src/TxSearchSelect.vue`
- Recommendation: pass `options` directly for small local lists; use `remote` + `search` for external data sources and write results back through `options`.
- Interaction: disabled options remain visible but cannot be selected; clearing emits both `update:modelValue` and `change` with an empty value.
- Surface: the panel is hosted by `TxPopover`; use `panelBackground`, `panelShadow`, and `panelRadius` to align with the surrounding surface hierarchy.


## Source

- Component source: `packages/tuffex/packages/components/src/search-select/src/TxSearchSelect.vue`.
- Types: `packages/tuffex/packages/components/src/search-select/src/types.ts`.
- **Verified coverage:** `packages/tuffex/packages/components/src/search-select/__tests__/search-select.test.ts` verifies local filtering with option selection, debounced remote search emission, and empty-state rendering when no results match.

<TuffDocSourceLink />
