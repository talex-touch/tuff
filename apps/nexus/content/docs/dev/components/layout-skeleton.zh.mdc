---
title: LayoutSkeleton 布局骨架
description: 布局级骨架占位
category: Status
status: beta
since: 1.0.0
tags: [skeleton, layout, loading]
syncStatus: reviewed
verified: true
---

## 用法

### 布局占位
容器决定骨架尺寸。骨架画出一个完整的应用布局：带分隔线的顶栏、按比例收缩的侧栏（首项为当前项），以及以标题开头的内容区。
:::TuffDemoWrapper{demo="LayoutSkeletonLayoutPlaceholderDemo" code-lang="vue"}
---
code: |
  <template>
    <div style="height: 240px;">
      <TxLayoutSkeleton />
    </div>
  </template>
---
:::

### 面板占位
布局骨架搭配卡片容器使用。
:::TuffDemoWrapper{demo="LayoutSkeletonPanelPlaceholderDemo" code-lang="vue"}
---
code: |
  <template>
    <TxCard>
      <TxLayoutSkeleton />
    </TxCard>
  </template>
---
:::

### 最佳实践

- 外层容器必须给出明确高度；组件只填满 `width: 100%` 与 `height: 100%`，不会替页面决定尺寸。
- 页面、面板、侧边摘要和详情栏加载时，如果最终布局形状稳定，可以使用它保持结构预期。
- 骨架画的是“顶栏 + 侧栏 + 内容”的应用布局；最终界面没有侧栏或顶栏时，改用 `TxSkeleton`、`TxCardSkeleton` 或 `TxListItemSkeleton` 按真实结构组合，数据到达时布局才不会跳动。
- 不要把它当作表格行占位。表格主体应使用 `TxSkeleton`、行级占位或 `TxDataTable loading`。
- 保持文档、SSR、hydration 与视觉测试的确定性；侧栏文字条与内容行宽度刻意使用固定序列，而不是运行时随机数。

## API 参考

### Props
::TuffPropsTable
---
rows:
  - name: '—'
    type: '-'
    default: '-'
    description: '无额外 props，尺寸由容器决定'
---
::

### Events

`TxLayoutSkeleton` 不会 emit 自定义事件。

### Slots

`TxLayoutSkeleton` 不暴露插槽。组件始终渲染下方记录的固定布局骨架。

## 概述

- `TxLayoutSkeleton` 无 props、events 或 slots。
- 组件固定渲染 36px 顶栏（品牌方块、标题条、两枚小胶囊与头像，底部一条 hairline）、6 个 sidebar item（首项带浅底，表示当前项）与 8 条内容行（首行是标题，第 5 行另起一段）。
- 侧栏宽度是 `clamp(72px, 30%, 200px)`，用 hairline 与内容区分隔：窄容器里侧栏按比例收缩，内容区不会被挤成一条。内容区背景透明，暗色下不会比外框更暗。
- 侧栏文字条与内容行的宽度都取自固定序列，不依赖运行时随机数。
- 根节点是 `aria-hidden="true"`，装饰性骨架不进入无障碍树。页面/面板的加载态应由宿主播报（例如区域上的 `aria-busy`），就绪后再切换到真实内容。

## 技术实现

- 已对照 `packages/tuffex/packages/components/src/layout-skeleton/src/TxLayoutSkeleton.vue` 与 `layout-skeleton.test.ts` 核对。
- API 说明：该组件有意不提供 props。尺寸、比例和位置必须由父容器决定。
- 渲染说明：侧栏文字条与内容行宽度是固定值，不使用随机数，因此 SSR、hydration、文档快照和视觉测试保持稳定。
- 布局修复（2026-09-23）：通用的 `.tx-layout-skeleton__line`（`height: 100%`）原本写在尺寸规则之后、以同等优先级胜出，顶栏条撑满整个顶栏贴到外框，侧栏文字条和头像一样高；现在通用形状规则前置。侧栏原为固定 200px，窄容器里占去约三分之二、把内容区挤成一条；内容区原用 `--tx-bg-color-page`，暗色下比外框更暗，像一个洞。
- 动效降级：每个占位块（含新增的品牌方块、胶囊与头像）都走共享的 `skeleton-surface`，`prefers-reduced-motion: reduce` 时只停闪烁、保留占位块。
- **实测覆盖:** 固定骨架结构、装饰性 `aria-hidden` 根节点、6 个 sidebar item、8 条内容行、确定性内容宽度和 install 注册；`skeleton/__tests__/skeleton-motion.test.ts` 对编译后的样式断言每个闪烁都有 reduced-motion 兜底、兜底不隐藏占位块、组件不自带 keyframes。

- Component source: `packages/tuffex/packages/components/src/layout-skeleton/src/TxLayoutSkeleton.vue`.
- Types: `packages/tuffex/packages/components/src/layout-skeleton/index.ts` exports `TxLayoutSkeletonInstance`.
- Coverage: `packages/tuffex/packages/components/src/layout-skeleton/__tests__/layout-skeleton.test.ts` verifies fixed scaffold counts, deterministic content widths, and install registration.

<TuffDocSourceLink />
