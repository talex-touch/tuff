---
title: CopyButton
description: 带复制完成反馈与错误事件的剪贴板按钮。
category: Basic
status: beta
since: 1.0.0
tags: [copy, clipboard, action]
syncStatus: reviewed
verified: true
---
# CopyButton

`TxCopyButton` 将字符串写入剪贴板，并在成功后短暂切换按钮文案。组件优先使用 `navigator.clipboard.writeText`，不可用时回退到隐藏 textarea 与 `document.execCommand('copy')`。

已人工核对 `packages/tuffex/packages/components/src/copy-button/src/TxCopyButton.vue` 与 `copy-button.test.ts`。

## 基础用法

### CopyButton
::::TuffDemoWrapper{demo="CopyButtonCopyButtonDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  const installCommand = 'pnpm add @talex-touch/tuffex'
  </script>

  <template>
    <TxCopyButton
      :text="installCommand"
      copy-label="复制安装命令"
      copied-label="已复制"
    />
  </template>
---
::::

## 交互契约

- `disabled` 或正在复制时点击会被忽略。
- 只有剪贴板写入成功后才会触发 `copy`。
- 两种剪贴板策略都失败时触发 `error`，参数是捕获到的错误。
- 成功后 `copiedLabel` 会显示 `timeout` 毫秒，然后恢复为 `copyLabel`。
- 默认插槽接收 `{ copied, copying }`，用于自定义按钮内容。

## API

### Props

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|---------|------|
| `text` | `string` | `''` | 写入剪贴板的文本。 |
| `copyLabel` | `string` | `'Copy'` | 空闲状态文案和 aria-label。 |
| `copiedLabel` | `string` | `'Copied'` | 成功状态文案和 aria-label。 |
| `disabled` | `boolean` | `false` | 禁用按钮并阻止复制。 |
| `timeout` | `number` | `1400` | 复制完成状态重置延迟，单位 ms。 |
| `size` | `'sm' \| 'md'` | `'sm'` | 按钮密度。 |

### Events

| 事件名 | 参数 | 说明 |
|------|------|------|
| `copy` | `(text: string)` | 剪贴板写入成功后触发。 |
| `error` | `(error: unknown)` | 剪贴板写入失败时触发。 |

### Slots

| 插槽名 | Props | 说明 |
|------|------|------|
| `default` | `{ copied, copying }` | 自定义按钮文案或内容。 |

## 最佳实践

- `copyLabel` 应明确说明目标，例如“复制 Token”或“复制命令”。
- 复制值很关键时处理 `error`；浏览器可能拒绝非可信手势触发的剪贴板写入。
- 复制事件触发时，不要把密钥写入普通文档示例或 analytics。
- 将待复制文本保存在宿主状态中，便于审计和测试。

## 审阅说明

- 组件源码:`packages/tuffex/packages/components/src/copy-button/src/TxCopyButton.vue` 确认 Clipboard API、隐藏 textarea 回退、disabled/copying 保护、已复制状态定时器、`copy` / `error` 事件、ARIA label 切换和默认插槽 props。
- **实测覆盖:** Coverage: `packages/tuffex/packages/components/src/copy-button/__tests__/copy-button.test.ts` 覆盖剪贴板成功、已复制视觉状态、禁用无操作和错误派发。
- 导出入口:`packages/tuffex/packages/components/src/copy-button/index.ts` 使用 `withInstall` 包装组件并导出 `TxCopyButtonInstance`。

## Source
<TuffDocSourceLink />
