---
title: OS Icon
description: 用于平台标签的内联操作系统图标。
category: Basic
status: beta
since: 1.0.0
tags: [platform, os, icon]
syncStatus: reviewed
verified: true
---
# OS Icon

`TxOsIcon` 根据 `platform` / `os` 字符串渲染 macOS、Windows 或 Linux 图标。适合发布矩阵、设备列表和平台相关设置项。

已人工核对 `packages/tuffex/packages/components/src/os-icon/src/TxOsIcon.vue`。

## 基础用法

### OS Icon
::::TuffDemoWrapper{demo="OsIconOsIconDemo" code-lang="vue"}
---
code: |
  <template>
    <TxOsIcon platform="darwin" os="macOS 15" />
    <TxOsIcon platform="win32" os="Windows 11" />
    <TxOsIcon platform="linux" os="Ubuntu 24.04" />
  </template>
---
::::

## 交互契约

- `platform` 与 `os` 会拼接并转小写后再做识别。
- 包含 `darwin`、`mac` 或 `os x` 时显示 macOS。
- 包含 `windows`、`win32` 或 `win64` 时显示 Windows。
- 包含 `linux`、`ubuntu`、`debian`、`fedora` 或 `arch` 时显示 Linux。
- 未识别值会回退到 macOS 图标；平台必须明确时，请在图标旁放可见文本。
- SVG 设置了 `aria-hidden`，可访问名称由外层文本承载。

## API

### Props

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|---------|------|
| `platform` | `string` | `''` | 运行时或平台标识，例如 `darwin`、`win32`、`linux`。 |
| `os` | `string` | `''` | 可读 OS 字符串，也会参与识别。 |

### Events

`TxOsIcon` 不派发组件事件。

## Slots

`TxOsIcon` 不暴露插槽。请搭配相邻的可见文本表达语义，不要依赖装饰性 SVG 承载含义。

## 最佳实践

- 在表格或列表中始终搭配可见平台文本。
- 优先在数据源层归一化平台标识，不要依赖模糊展示名。
- 用 CSS 字号或文本工具类调整图标大小；组件本身使用 `1em`。
- 将回退行为视为视觉默认值，不要当作校验结果。

## 审阅说明

- 组件源码:`packages/tuffex/packages/components/src/os-icon/src/TxOsIcon.vue` 确认字符串归一化、平台识别顺序、macOS 回退、`aria-hidden` SVG 和 `1em` 尺寸。
- **实测覆盖:** Coverage: `packages/tuffex/packages/components/src/os-icon/__tests__/os-icon.test.ts` 覆盖 Windows 别名、Linux 发行版别名、macOS 别名/默认回退，以及装饰性 SVG 的无障碍与 class 形态。
- 导出入口:`packages/tuffex/packages/components/src/os-icon/index.ts` 使用 `withInstall` 包装组件并导出 `TxOsIconInstance`。

## Source
<TuffDocSourceLink />
