---
title: "DatePicker"
description: "`TxPicker` `YYYY-MM-DD`"
category: Form
syncStatus: migrated
---
# DatePicker

`TxPicker` `YYYY-MM-DD`

<script setup lang="ts">
import DatePickerBasicDemo from '~/components/content/demos/DatePickerBasicDemo.vue'
import DatePickerBasicDemoSource from '~/components/content/demos/DatePickerBasicDemo.vue?raw'
</script>

## Basic Usage

### DatePicker
:::TuffDemoWrapper{demo="DatePickerDatePickerDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const visible = ref(false)
  const value = ref('2026-01-02')
  </script>

  <template>
    <div style="width: 360px; padding: 16px; border: 1px solid var(--tx-border-color); border-radius: 12px;">
      <div style="display: flex; gap: 8px; align-items: center;">
        <TxButton variant="primary" @click="visible = true">
          Open date picker
        </TxButton>
        <div style="color: var(--tx-text-color-secondary);">
          Value: {{ value }}
        </div>
      </div>

      <TxDatePicker
        v-model="value"
        v-model:visible="visible"
        title="Date"
        min="2025-01-01"
        max="2030-12-31"
      />
    </div>
  </template>
---
:::

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|------|
| `modelValue` | `string` | `''` | Description for modelValue. |
| `visible` | `boolean` | `false` | Description for visible. |
| `popup` | `boolean` | `true` | Description for popup. |
| `title` | `string` | `'Select date'` | Description for title. |
| `min` | `string` | - | Description for min. |
| `max` | `string` | - | Description for max. |
| `disabled` | `boolean` | `false` | Description for disabled. |
| `showToolbar` | `boolean` | `true` | Description for showToolbar. |
| `confirmText` | `string` | `'Confirm'` | Description for confirmText. |
| `cancelText` | `string` | `'Cancel'` | Description for cancelText. |
| `closeOnClickMask` | `boolean` | `true` | Description for closeOnClickMask. |

### Events

| Event | Params | Description |
|------|------|------|
| `update:modelValue` | `(value: string)` | Description for update:modelValue. |
| `change` | `(value: string)` | Description for change. |
| `update:visible` | `(visible: boolean)` | Description for update:visible. |
| `confirm` | `(value: string)` | Description for confirm. |
| `cancel` | - | Description for cancel. |
| `open` | - | Description for open. |
| `close` | - | Description for close. |
