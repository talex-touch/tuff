---
title: Pagination 分页
description: 大数据列表的分页导航
category: Data
syncStatus: reviewed
verified: true
---

# Pagination 分页

用于数据列表或表格的分页切换。

<script setup lang="ts">
import { ref } from 'vue'

const page = ref(1)
</script>

## 基础用法
### 分页导航
在多页数据间切换。
:::TuffDemoWrapper{demo="PaginationPaginationDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const page = ref(1)
  </script>

  <template>
    <TxPagination
      v-model:current-page="page"
      :total="120"
      :page-size="10"
      show-info
      show-first-last
    />
  </template>
---
:::
## API（Lite）
::TuffPropsTable
---
rows:
  - name: currentPage
    type: number
    default: '1'
    description: 当前页
  - name: pageSize
    type: number
    default: '10'
    description: 每页数量
  - name: total
    type: number
    default: '0'
    description: 总数量
  - name: totalPages
    type: number
    default: '-'
    description: 显式总页数；未提供 total 时使用
  - name: prevIcon
    type: string
    default: "'chevron-left'"
    description: 上一页图标
  - name: nextIcon
    type: string
    default: "'chevron-right'"
    description: 下一页图标
  - name: showInfo
    type: boolean
    default: 'false'
    description: 显示总量信息
  - name: showFirstLast
    type: boolean
    default: 'false'
    description: 显示首末页按钮
---
::

## Events
::TuffPropsTable
---
rows:
  - name: update:currentPage
    type: '(page: number) => void'
    default: '-'
    description: v-model 当前页更新
  - name: pageChange
    type: '(page: number) => void'
    default: '-'
    description: 用户切换页码时触发
---
::

## Slots
::TuffPropsTable
---
rows:
  - name: info
    type: '{ currentPage: number, totalPages: number, total?: number }'
    default: '-'
    description: 自定义页码信息区域
---
::

## 交互契约

- `total` 优先用于按 `pageSize` 计算总页数；未提供 `total` 时使用 `totalPages`。
- `showFirstLast` 会渲染首末页跳转按钮，边界页会禁用首/上一页或下一页/末页。
- 当前页按钮带 `aria-current="page"`，上一页/下一页/首末页按钮带可读 `aria-label`。

## 后台数据运维面板

分页建议紧贴表格底部，和当前筛选/选择状态共享同一组响应式数据；不要让分页脱离列表容器单独悬浮。

::TuffDemoWrapper{demo="ComponentsDataOperationsDemo" code-lang="vue" title="数据运维面板" description="分页与 DataTable、Skeleton 共同承担数据区状态。"}
---
code: |
  <template>
    <TxPagination
      v-model:current-page="page"
      :total="rows.length"
      :page-size="4"
      show-info
      show-first-last
    />
  </template>
---
::

## 审阅说明

- 源码：`packages/tuffex/packages/components/src/pagination/src/TxPagination.vue`
- 建议：优先传 `total + pageSize`，只有后端只返回页数时再传 `totalPages`。
- 可访问性：当前页使用 `aria-current="page"`；上一页、下一页、首末页按钮都带可读 `aria-label`。

## Source
<TuffDocSourceLink />
