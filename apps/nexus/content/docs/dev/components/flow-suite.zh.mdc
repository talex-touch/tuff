---
title: 流程套件
description: 工作流编排：点阵画布上的节点、分支与连线
category: FlowSuite
status: beta
since: 0.6.0
tags: [flow, workflow, canvas, suite, overview]
syncStatus: reviewed
verified: false
---

## 用法

每个格子都是可交互的真实组件，点击左上角名称进入对应组件文档。

::DocsComponentsGallery{suite="flow"}
::

## 分组一览

下面按分组列出该套件的**全部**组件，数据来自每个文档的 `category` frontmatter，新增组件会自动出现。

::DocsSuiteCatalog{suite="flow"}
::

## 概述

流程套件负责把一串操作画成一张可读、可改的图。**Flowchart** 是这套能力的画布：点阵背景、绝对定位的节点、随卡片实测高度重算的贝塞尔连线，以及可选的拖拽与栅格吸附。它刻意只管画布、连线和分类胶囊三件事——卡片里是什么由宿主通过插槽给，所以同一块画布既可以画自动化规则，也可以画 agent 的工具调用链或一次部署流水线。运行时经 `@talex-touch/tuffex/ai` 入口引入：套件是**文档层**的划分，组件桶仍是 base / pro / ai 三分。
