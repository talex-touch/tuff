---
title: "PermissionState 权限不足"
description: "用于权限不足或未授权场景。"
category: Feedback
status: beta
since: 1.0.0
tags: [permission, state, access]
syncStatus: migrated
---
# PermissionState 权限不足

`TxPermissionState` 是 `TxEmptyState` 的快捷封装，自动设置 `variant="permission"`，用于权限不足或未授权场景。

## 基础用法

### PermissionState
:::TuffDemoWrapper{demo="PermissionStatePermissionStateDemo" code-lang="vue"}
---
code: |
  <template>
    <TxPermissionState
      description="Request access to continue."
      :primary-action="{ label: 'Request access', type: 'primary' }"
    />
  </template>
---
:::

## API

`TxPermissionState` 继承 `TxEmptyState` 的全部 Props（除 `variant`）、Slots 和 Events，详见 [EmptyState](./empty-state.zh.md)。

## 交互契约

- `TxPermissionState` 始终向 `TxEmptyState` 透传 `variant="permission"`。
- 组件不额外改写 `title`、`description`、`surface` 或 action 配置；显式 props 会原样透传。
- `icon`、`title`、`description`、`actions` 插槽会原样转发给 `TxEmptyState`。
