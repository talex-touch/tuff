---
title: Tag 标签
description: 分类与标记
category: Basic
status: beta
since: 1.0.0
tags: [tag, label, badge]
syncStatus: migrated
verified: true
---

# Tag 标签

## Demo
### Tags
颜色与尺寸组合
:::TuffDemoWrapper{demo="TagTagsDemo" code-lang="vue"}
---
code: |
  <template>
    <TxTag label="默认" size="sm" />
    <TxTag label="成功" size="md" color="var(--tx-color-success)" />
    <TxTag label="警告" size="sm" color="var(--tx-color-warning)" />
    <TxTag label="危险" size="md" color="var(--tx-color-danger)" />
  </template>
---
:::

### 可关闭
用于可移除的筛选项或用户自定义标签。
:::TuffDemoWrapper{demo="TagClosableDemo" code-lang="vue"}
---
code: |
  <script setup>
  import { ref } from 'vue'

  const tags = ref(['设计', '文档', '待确认'])

  function removeTag(tag) {
    tags.value = tags.value.filter(item => item !== tag)
  }
  </script>

  <template>
    <TxTag
      v-for="tag in tags"
      :key="tag"
      :label="tag"
      closable
      @close="removeTag(tag)"
    />
  </template>
---
:::

### 图标与自定义色
通过 `icon`、`color`、`background` 和 `border` 组合语义标签。
:::TuffDemoWrapper{demo="TagIconDemo" code-lang="vue"}
---
code: |
  <template>
    <TxTag label="AI 推荐" icon="i-carbon-ai" color="#8b5cf6" />
    <TxTag label="已同步" icon="i-carbon-checkmark-filled" color="var(--tx-color-success)" />
    <TxTag
      label="受保护"
      icon="i-carbon-locked"
      color="#f59e0b"
      background="rgba(245, 158, 11, 0.14)"
      border="rgba(245, 158, 11, 0.36)"
    />
  </template>
---
:::

### 交互与禁用
点击标签可用于切换轻量状态；禁用后会阻断 `click` 和 `close`。
:::TuffDemoWrapper{demo="TagStateDemo" code-lang="vue"}
---
code: |
  <script setup>
  import { ref } from 'vue'

  const active = ref(false)
  </script>

  <template>
    <TxTag
      :label="active ? '已选择' : '可点击'"
      :color="active ? 'var(--tx-color-success)' : 'var(--tx-color-primary)'"
      @click="active = !active"
    />
    <TxTag label="禁用" disabled />
  </template>
---
:::

## API（简版）
::TuffPropsTable
---
rows:
  - name: label
    type: 'string'
    default: '-'
    description: '文本内容'
  - name: color
    type: 'string'
    default: '-'
    description: '主文字/图标色'
  - name: background
    type: 'string'
    default: "''"
    description: '自定义背景色'
  - name: border
    type: 'string'
    default: "''"
    description: '自定义边框色'
  - name: icon
    type: 'string'
    default: "''"
    description: '前置图标 class'
  - name: size
    type: "'sm' | 'md'"
    default: 'sm'
    description: '尺寸'
  - name: closable
    type: 'boolean'
    default: 'false'
    description: '显示关闭按钮'
  - name: disabled
    type: 'boolean'
    default: 'false'
    description: '禁用 click 与 close 事件'
---
::

## 契约说明

Tag 渲染 `role="status"`。默认插槽会替换 `label`。关闭按钮会阻止事件冒泡，因此关闭标签不会同时触发标签自身的 `click` 事件。禁用状态会阻断 `click` 与 `close`。

## 组合示例
### 标签 + 按钮
标签配合按钮突出操作。
:::TuffDemoWrapper{demo="TagTagButtonDemo" code-lang="vue"}
---
code: |
  <template>
    <TxTag class="tag-action-demo__tag" label="测试版" size="md" />
    <TxButton size="sm">升级</TxButton>
  </template>

  <style scoped>
  .tag-action-demo__tag {
    box-sizing: border-box;
    height: 26px;
  }
  </style>
---
:::
