---
title: Files 文件管理
description: 文件夹树、面包屑、网格与列表视图、多选批量操作、右键菜单、拖入上传与快速预览组成的文件管理器
category: TemplateContent
status: beta
since: 0.6.0
tags: [template, files, tree, upload, context-menu]
syncStatus: reviewed
verified: false
---

## 用法

### 工作区文件

:::TuffDemoWrapper{demo="TemplateFilesDemo" code-lang="vue" title="工作区文件" description="单击选择，⌘ / Ctrl 或 Shift 点击多选；双击或回车打开，空格快速预览，F2 重命名；右键或 Shift+F10 打开菜单。从桌面把文件拖进文件区即可上传，插件文件夹会按真实限额拒绝超限的文件。滚进视口后会从 MacBook 同步两张截图；展开到 1200px 以上出现预览栏。图中均为示例数据，文件只留在浏览器里，不会真的上传或下载。"}
---
code: |
  <script setup lang="ts">
  import { reactive, ref } from 'vue'
  import TemplateFrame from './TemplateFrame.vue'

  // 插件存储按宿主的真实规则校验：单文件 10 MB、总量 100 MB、1,000 个文件、扁平
  const PLUGIN_FILE_NAME = /^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/

  // 整个模板只有一个 manual 菜单：文件格和空白处都从这里打开
  const menuRef = ref()
  const menu = reactive({ ids: [] as string[], anchorId: null as string | null })
  function onContextMenu(event: MouseEvent) {
    event.preventDefault()
    prepareMenu(event.target) // 填好 menu.ids 与 menu.anchorId
    menuRef.value.openAt(event)
  }

  // TxContextMenu 关闭后不还焦点，由模板交回打开它的文件格
  function onMenuClose() {
    tileOf(menu.anchorId)?.focus({ preventScroll: true })
  }
  </script>

  <template>
    <TemplateFrame title="工作区文件" :height="580" @enter="syncFromMacBook">
      <TxBreadcrumb :items="crumbs" separator-icon="i-carbon-chevron-right" @click="onCrumb" />

      <TxTree :nodes="folders" :model-value="folder" :expanded-keys="expanded" @update:model-value="open">
        <template #item="{ node, expanded, hasChildren, toggleExpand }">
          <!-- 整行点击是「选择」；展开按钮要 .stop，否则会同时选中这一行 -->
          <button v-if="hasChildren" type="button" tabindex="-1" @click.stop="toggleExpand()">…</button>
          <span :class="expanded ? 'i-carbon-folder-open' : 'i-carbon-folder'" />
          {{ node.label }}
        </template>
      </TxTree>

      <div role="listbox" aria-multiselectable="true" @keydown="onGridKeydown" @contextmenu="onContextMenu">
        <div v-for="file in files" :key="file.id" role="option" :aria-selected="selected.has(file.id)">…</div>
      </div>

      <!-- 拖入桌面文件时铺在文件区上。它只交出 File：进度、缩略图和回收 URL 都归宿主 -->
      <div v-show="draggingFiles" class="drop-layer">
        <TxFileUploader ref="uploader" :model-value="[]" multiple @add="queueUploads" />
      </div>

      <TxContextMenu ref="menuRef" trigger="manual" @close="onMenuClose">
        <template #menu>
          <TxContextMenuItem shortcut="F2" @select="rename">重命名</TxContextMenuItem>
          <TxContextMenuDivider />
          <TxContextMenuItem danger shortcut="⌫" @select="remove">删除</TxContextMenuItem>
        </template>
      </TxContextMenu>

      <!-- 进度每秒变好几次：托盘本身不播报，另用一个读屏状态只报开始和结束 -->
      <TxToastPanel :open="trayOpen" live="off">…</TxToastPanel>
    </TemplateFrame>
  </template>
---
:::

## 概述

Tuff 工作区的文件：文档、截图、剪贴板导出和主题在登录的设备之间同步；另有「插件存储」分区，每个插件一个文件夹，限额按宿主的真实规则来——单个文件不超过 10 MB，总量不超过 100 MB，最多 1,000 个文件，不能有子文件夹，文件名只能用字母、数字和 `. _ -`。

适合「树 + 文件区 + 预览」形态的界面：同步盘与网盘、素材库、应用或插件的数据目录、工单附件管理。

- 选择：单击选中，⌘ / Ctrl 点击加选，Shift 点击连选，⌘ / Ctrl + A 全选；方向键按行列移动（每行几格从网格本身读出），Shift + 方向键扩选，Esc 清空选择。这些键都挂在文件区上，焦点不在文件区时不生效。
- 打开：双击或回车打开文件夹或文件；空格快速预览，预览里 ← / → 切换，再按空格或 Esc 关闭；F2 重命名，回车确认、Esc 取消，插件存储里的文件名按宿主的规则就地校验；⌫ / Delete 删除，可以撤销。「下载」只说明宿主会做什么，不会真的下载。
- 右键或按 Shift + F10 / 菜单键打开同一个菜单；用键盘打开时锚在当前文件格，而不是整个文件区的角上。菜单关闭后焦点回到打开它的文件格；选了「重命名」「快速预览」「移动到…」时焦点交给它们。把文件格拖到树上的文件夹可以移动，插件存储里的文件不参与移动。
- 从桌面拖入文件：文件区出现投放层，松开后「上传」按钮下的托盘逐个显示进度。插件文件夹会拒绝超过 10 MB 的单个文件、会让总量超过 100 MB 的文件和不合规的文件名，并提供「改存到工作区」。图片生成缩略图，256 KB 以内的文本文件可以直接预览。在模板其他位置松开会被拒绝，浏览器不会跳去打开那个文件；所有文件都只在浏览器内存里，离开页面就消失。
- 模板第一次滚进视口后，托盘显示「正在从 MacBook 同步 2 个截图」，约 2 秒后两张截图出现在「截图 / 2026-09」并标「新」，托盘随后收起，指针停在托盘上时不收。开启「减弱动态效果」时两张截图一开始就在，不出现托盘。
- 列内宽度是 208px 的树加文件区；展开后（≥ 960px）树宽 240px、格子更大，工具栏多出「新建文件夹」，≥ 1200px 再多出预览栏；窄于 640px 时隐藏树，子文件夹以格子出现，路径只留末级、返回按钮和上级菜单。

## 自定义

- 接真实存储时，把模拟进度换成上传请求的进度事件。TxFileUploader 只负责接收 `File`，校验放在宿主里，并和宿主的真实限额共用同一份常量，前后端的提示才不会互相矛盾。
- 文件很多时把网格换成按行分组的虚拟列表（每行格数由容器宽度决定），列表视图继续用 TxDataTable 的受控排序配合服务端分页。
- 需要跨文件夹搜索时，把搜索结果当成一个临时的虚拟文件夹，每一项带上所在路径，而不是在树里临时展开一串文件夹。
- 缩略图来自服务端时，保持格子高度固定、图片 `draggable="false"`，加载时网格不会跳；object URL 在删除、重置和卸载时统一回收。

## 相关组件

| 区域 | 组件 | 作用 |
|---|---|---|
| 路径与工具栏 | [TxBreadcrumb](/docs/dev/components/breadcrumb)、[TxDropdownMenu](/docs/dev/components/dropdown-menu)、[TxIconButton](/docs/dev/components/button)、[TxSearchInput](/docs/dev/components/search-input)、[TxFlatRadio](/docs/dev/components/flat-radio)、[TxButton](/docs/dev/components/button) | 面包屑回到上级（不传 `href`，不会让文档页跳走）；窄屏只留末级，上级收进 ⋯ 菜单；在当前文件夹中搜索；网格与列表切换；新建文件夹与上传 |
| 文件夹树 | [TxTree](/docs/dev/components/tree) | `#item` 插槽画 28px 的行、打开与合上的文件夹图标和文件数；展开集合受控，进入文件夹时自动展开它的上级；把文件格拖到树上的文件夹即可移动 |
| 配额 | [TxProgressBar](/docs/dev/components/progress-bar)、[TxStatusBadge](/docs/dev/components/status-badge) | 侧栏列出每个插件的用量；插件文件夹顶部是分段配额条（对话历史、索引、其他）和限额说明，超过 90% 时标出「接近上限」；工作区只显示已用量，没有套餐或升级入口 |
| 网格 | [TxIconChip](/docs/dev/components/icon-chip)、[TxCornerOverlay](/docs/dev/components/corner-overlay)、[TxCheckbox](/docs/dev/components/checkbox)、[TxTag](/docs/dev/components/tag) | 截图缩略图由代码绘制，右下角是类型角标；悬停出现的复选框用来不按 ⌘ 加选；刚同步或上传的文件带「新」；文件夹以格子出现，双击进入 |
| 列表 | [TxDataTable](/docs/dev/components/data-table) | 与网格共用一个选择集；名称、大小、类型、修改时间可排序，文件夹永远排在最前 |
| 右键菜单 | [TxContextMenu / TxContextMenuItem / TxContextMenuDivider / TxContextMenuSubmenu](/docs/dev/components/context-menu) | 一个 `trigger="manual"` 的实例服务所有文件格和空白处：打开、快速预览、重命名、复制路径、移动到…、下载、删除；空白处是新建文件夹、上传、排序方式与显示方式 |
| 拖入上传 | [TxFileUploader](/docs/dev/components/file-uploader)、[TxToastPanel](/docs/dev/components/toast-panel)、[TxCardItem](/docs/dev/components/card-item)、[TxProgressBar](/docs/dev/components/progress-bar) | 铺在文件区上的投放层；挂在「上传」按钮下的托盘，每个文件一行进度，被拒绝的文件就地写明原因 |
| 预览 | [TxModal](/docs/dev/components/modal)、[TxMarkdownView](/docs/dev/components/markdown-view)、[TxImageGallery](/docs/dev/components/image-gallery)、[TxEmptyState](/docs/dev/components/empty-state)、[TxSplitter](/docs/dev/components/splitter)、[TxNoSelection](/docs/dev/components/no-selection) | 空格打开快速预览：图片、Markdown、文本和 JSON；≥ 1200px 右侧多出可拖动宽度的预览栏，同文件夹的图片排成缩略条；无法预览的类型说明宿主会怎么打开 |
| 批量与反馈 | [TxButton](/docs/dev/components/button)、[TxToastPanel](/docs/dev/components/toast-panel)、[TxSearchEmpty](/docs/dev/components/search-empty) | 有选中时出现批量栏（下载、移动到…、删除）；删除、移动、重命名都能从提示里撤销；搜不到时给出「清除搜索」 |
| 移动到… | [TxModal](/docs/dev/components/modal)、[TxTree](/docs/dev/components/tree) | 只列出工作区里能放的文件夹，被移动的文件夹和它自己的子文件夹不可选 |
