---
title: AgentChat 智能体对话
description: 编码智能体从规划、澄清、检索、申请写权限到写码、跑测试和总结的完整会话，旁边是实时计划、改动文件与插件预览
category: TemplateAi
status: beta
since: 0.6.0
tags: [template, ai, agent, chat, tool-call]
syncStatus: reviewed
verified: false
---

## 场景

「插件工程师」智能体接到一条需求：给 clipboard-history 插件加上「阅后即焚」，复制进历史的密钥过一会儿自动清除。它先列计划，再问两个澄清问题，检索代码，申请写文件的权限，写出改动、跑通测试，最后流式给出总结。会话旁的面板同步显示计划进度、改动的文件和插件预览。

适合所有「让 AI 替你动手」的产品：编码助手、运维助手、能调用工具的数据或客服智能体。关键决定始终在人手里：澄清问题由人回答，写文件由人放行。

第二种风格「侧边 Copilot」把智能体换成停靠在页面旁的文档助手：你在写 clipboard-history 插件的 README，划词让它解释、润色或翻译，改不改由你决定。

## 模板

### 编码智能体工作台

:::TuffDemoWrapper{demo="TemplateAgentChatDemo" code-lang="vue" title="编码智能体工作台" description="进入视口后自动演示；写文件前停在权限闸门，点「允许」或「拒绝」后才继续。"}
---
code: |
  <script setup lang="ts">
  import { reactive } from 'vue'
  import TemplateFrame from './TemplateFrame.vue'

  // 会话项只存 id 和 kind，显示的内容从响应式状态里读
  const state = reactive({
    items: [{ id: 'request', kind: 'user' }, { id: 'tail', kind: 'tail' }],
    gate: 'waiting',
  })

  // 新行插在固定的 tail 前面，已有的行不会换位置、不会被重建
  function push(kind: string) {
    state.items.splice(state.items.length - 1, 0, { id: kind, kind })
  }

  // 没有任何计时器会调用它：写文件只等读者点击
  function onAllow() {
    state.gate = 'allowed'
    push('code')
  }
  </script>

  <template>
    <TemplateFrame title="AgentChat 智能体对话" @enter="run">
      <template #default="{ width }">
        <div class="agent-chat">
          <TxAgentsList v-if="width >= 1200" :agents="agents" :selected-id="agentId" @select="agentId = $event" />
          <main class="agent-chat__main">
            <header class="agent-chat__head">
              <TxThinkingOrb :state="orbState" :label="phaseLabel" :paused="!running" />
              <TxWorkingIndicator v-if="working" :label="phaseLabel" :started-at="startedAt" />
              <TxContextIndicator :used-tokens="tokens" :max-tokens="200000" />
            </header>

            <TxConversationStream :items="state.items" item-key="id" :overscan="24" :streaming="working">
              <template #item="{ item }">
                <TxChatMessage v-if="item.kind === 'user'" :message="request" :markdown="false" />
                <TxAgentTrace v-else-if="item.kind === 'plan'" :rows="planRows" :working="planning" />
                <TxApprovalCard
                  v-else-if="item.kind === 'ask'"
                  v-model="answers" v-model:index="page" :questions="questions"
                  @answer="stopAutoPick"
                />
                <TxToolCallCard v-else-if="item.kind === 'search'" :tool-call="searchCall" default-expanded />
                <TxToolConfirmation
                  v-else-if="item.kind === 'gate' && state.gate === 'waiting'"
                  tool-name="apply_patch" risk="write" :input="patchFiles"
                  @approve="onAllow" @deny="onDeny"
                />
                <TxCodeStream v-else-if="item.kind === 'code'" :code="code" :diff="diff" :revealed-lines="lines" lang="ts" />
                <TxStreamMarkdown v-else-if="item.kind === 'summary'" :content="summary" :streaming="streaming" />
              </template>
            </TxConversationStream>

            <TxPromptBar v-model="draft" :sources="sources" :commands="commands" :models="models" :submitting="running" @send="send">
              <template #actions>
                <TxIconButton v-if="running" icon="i-carbon-stop-filled-alt" label="停止" @click="stop" />
              </template>
            </TxPromptBar>
          </main>

          <aside v-if="width >= 640" class="agent-chat__side">
            <TxTaskRows :rows="tasks" done-text="" />
            <TxDiffChips :diffs="diffs" @select="jumpToCode" />
            <TxAgentScreen :state="screen" :cursor="cursor" label="插件预览 · CoreBox" />
          </aside>
        </div>
      </template>
    </TemplateFrame>
  </template>
---
:::

### 侧边 Copilot

适合给已有页面配一个助手：文档或 README 编辑器、CMS 正文、工单与邮件回复。和上面的工作台不同，这里的主角是页面本身，助手没有工具调用和写文件闸门，只给出能应用到页面的建议；应用哪一版、应不应用，都由你点击决定。

:::TuffDemoWrapper{demo="TemplateAgentChatCopilotDemo" code-lang="vue" title="侧边 Copilot" description="进入视口后自动演示一次润色，停在建议上等你点「应用」；也可以在文章里任意划词试试。演示中的回答都是预写的。"}
---
code: |
  <script setup lang="ts">
  import type { SelectionPayload } from '@talex-touch/tuffex/selection-actions'
  import { useSelectionAnchor } from '@talex-touch/tuffex/selection-actions'
  import { markRaw, nextTick, onMounted, ref } from 'vue'
  import TemplateFrame from './TemplateFrame.vue'

  const articleRef = ref<HTMLElement | null>(null)
  const barRef = ref()
  const { selection } = useSelectionAnchor({ root: articleRef, ignore: () => [barRef.value?.el] })

  // 浮条每次重新定位都会读 rects：从 Range 现量，页面或文章滚动时它才贴着文字
  function livePayload(text: string, range: Range): SelectionPayload {
    return markRaw({ text, range, get rects() { return Array.from(range.getClientRects()) } })
  }

  // 没有滚动事件的布局变化（收起侧栏、展开舞台、文字重排）要请它重新定位
  onMounted(() => {
    new ResizeObserver(() => nextTick(() => barRef.value?.updatePosition())).observe(articleRef.value!)
  })

  // 只有读者点「应用」才会改动页面
  function apply(card: Card) {
    texts[card.blockId] = card.body
    card.phase = 'applied'
  }
  </script>

  <template>
    <TemplateFrame title="侧边 Copilot" :height="560" @enter="run">
      <template #default="{ width }">
        <article ref="articleRef" class="copilot__page">
          <p v-for="block in blocks" :key="block.id" :data-block="block.id">{{ texts[block.id] }}</p>
        </article>

        <aside v-if="width >= 640" class="copilot__side">
          <TxThinkingOrb :state="orbState" :paused="!working" />
          <TxModeChip :label="readonly ? '只读 · 仅解释' : '编辑需确认'" @click="readonly = !readonly" />
          <TxTag v-if="target" label="选区" closable @close="dismiss" />
          <div v-for="card in cards" :key="card.id">
            <TxChatMessage :message="echo(card)" :markdown="false" />
            <TxTypingIndicator v-if="card.phase === 'thinking'" text="正在改写…" />
            <template v-else>
              <del>{{ card.original }}</del>
              <ins>{{ card.body.slice(0, card.shown) }}</ins>
              <TxButton variant="primary" size="sm" @click="apply(card)">应用到页面</TxButton>
              <TxDiffChips :diffs="[{ file: 'README.md', add: 1, del: 1 }]" @select="reveal(card.blockId)" />
            </template>
          </div>
          <TxSuggestionChips :suggestions="next" @select="runSuggestion" />
          <TxPromptBar v-model="draft" :sources="sources" :commands="commands" @send="send" />
        </aside>

        <TxSelectionActions
          ref="barRef" :selection="payload" :state="barState" :actions="actions" keep-label="应用"
          @action="run($event.id)" @keep="apply(activeCard)" @discard="discard(activeCard)" @retry="retry(activeCard)"
        >
          <template #action-icon="{ action }">
            <span :class="icons[action.id]" />
          </template>
        </TxSelectionActions>
      </template>
    </TemplateFrame>
  </template>
---
:::

## 组成

| 区域 | 组件 | 作用 |
|---|---|---|
| 顶栏 | [TxThinkingOrb](/docs/dev/components/thinking-orb)、[TxWorkingIndicator](/docs/dev/components/working-indicator)、[TxStatusBadge](/docs/dev/components/status-badge)、[TxContextIndicator](/docs/dev/components/context-indicator) | 智能体的在场感、当前阶段与耗时、等待或完成状态、上下文用量 |
| 会话 | [TxConversationStream](/docs/dev/components/conversation-stream)、[TxChatMessage](/docs/dev/components/chat) | 虚拟化会话流，停在底部时自动跟随；用户的需求气泡 |
| 规划 | [TxAgentTrace](/docs/dev/components/agent-trace) | 4 步计划逐行出现，规划完成后自动折叠 |
| 澄清 | [TxApprovalCard](/docs/dev/components/approval-card) | 两道问题：密钥保留多久（单选）、哪些内容算密钥（多选）；答完收成一行结论 |
| 工具调用 | [TxToolCallCard](/docs/dev/components/tool-call-card) | `search_repo` 与 `run_tests` 的输入、流式日志和结果 |
| 权限闸门 | [TxToolConfirmation](/docs/dev/components/tool-confirmation) | `apply_patch` 写文件前的允许 / 拒绝，决定后换成一行结论 |
| 改动 | [TxCodeStream](/docs/dev/components/code-stream) | `clipboard-items.ts` 的 diff 逐行写出 |
| 总结 | [TxTypingIndicator](/docs/dev/components/typing-indicator)、[TxStreamMarkdown](/docs/dev/components/stream-markdown)、[TxToolChips](/docs/dev/components/tool-chips)、[TxMessageActions](/docs/dev/components/message-actions) | 整理中提示、流式总结、本轮工具调用摘要、复制与重新生成 |
| 侧栏 | [TxFlatRadio](/docs/dev/components/flat-radio)、[TxTaskRows](/docs/dev/components/task-rows)、[TxDiffChips](/docs/dev/components/tool-chips)、[TxAgentScreen](/docs/dev/components/agent-screen)、[TxSkeleton](/docs/dev/components/skeleton) | 任务 / 变更 / 预览切换，计划进度，改动文件（点击跳到代码），插件预览 |
| 智能体列表（≥ 1200px） | [TxAgentsList](/docs/dev/components/agents)、[TxSuggestionChips](/docs/dev/components/suggestion-chips) | 切换智能体；其他智能体给出可以问的问题 |
| 输入 | [TxPromptBar](/docs/dev/components/prompt-bar) | `@` 引用资料、`/` 命令、模型选择；运行中显示「停止」 |
| 宿主页面（侧边 Copilot） | 普通 DOM 段落 | clipboard-history 插件的 README，每块标 `data-block`，选区才有真实的矩形可以定位 |
| 划词浮条（侧边 Copilot） | [TxSelectionActions](/docs/dev/components/selection-actions) | 出现在选区下方：解释、润色、翻译和折叠起来的精简；有了建议后直接「应用 / 放弃 / 换一种写法」 |
| 助手面板（侧边 Copilot） | [TxModeChip](/docs/dev/components/mode-chip)、[TxTag](/docs/dev/components/tag) | 「编辑需确认 ↔ 只读 · 仅解释」；它能看到什么：文档、选区（可移除）、剪贴板（示例开关）。头部、回显、追问与输入沿用上面的组件 |
| 建议卡（侧边 Copilot） | [TxButton](/docs/dev/components/button)、[TxDiffChips](/docs/dev/components/tool-chips) | 原文删除线、建议下划线并逐字写出；「应用到页面 / 放弃」；改动摘要点一下跳回对应段落 |
| 改动提示（侧边 Copilot） | [TxToastPanel](/docs/dev/components/toast-panel) | 「已应用到 README.md · 撤销」 |

## 交互要点

- 模板进入视口才开始自动演示：规划 → 澄清问题 → 检索代码 → 申请写权限。窗口的「重置」或顶栏的「重新运行」都能从头再看一遍。
- 澄清问题闲置 3 秒后会先按推荐项作答，卡片下方有提示；你点任意选项就改为等你提交，你选的时长和密钥类型会带进总结与任务明细。
- 写文件的权限闸门不会自动放行，一直等你点「允许」或「拒绝」。允许后写码、跑测试、流式总结；拒绝则跳过写入和测试，改为导出补丁。
- 运行中可以点输入框右侧的「停止」：进行中的工具卡片标记为已停止，会话里留下一行记录。
- 侧栏的任务行可以展开看明细；点改动的文件（侧栏或总结里的工具摘要）会把会话滚到对应代码。会话只滚动自己的容器，不会带动整页，演示期间也不会抢焦点。
- 列内（640–959px）侧栏用「任务 / 变更 / 预览」切换；960px 起三块纵排，1200px 起左侧再加智能体列表；窄屏（< 640px）收起侧栏，输入框上方显示一条可展开的任务进度。开启「减弱动态效果」时直接呈现到闸门为止的状态，放行后立即给出结果，不跑任何计时器。
- 侧边 Copilot：进入视口约 1 秒后，模板高亮「权限」里的一句话、弹出划词浮条，再自动「润色」，侧栏逐字写出建议和删改对照后停住。页面只在你点「应用」（浮条或侧栏里都行）之后才改动，改完可在提示里撤销。脚本的选区是模板自己的高亮，不会动你在页面上的真实选区；开启「减弱动态效果」时直接显示写好的建议。
- 侧边 Copilot：在文章里任意划词就接管演示：浮条出现在选区下方，页面或文章滚动、收起侧栏、展开全屏时都跟着文字走，选区滚出可视范围时暂时隐藏。「解释」只回答不改页面；标题、列表和代码只能解释。
- 侧边 Copilot：切到「只读 · 仅解释」后浮条只剩「解释」，已有建议不能应用；「剪贴板」标签只是示意，不会读取你的剪贴板。宽屏（≥ 1200px）左侧多一栏本页目录；窄屏（< 640px）助手收进顶栏的「助手」按钮，打开后是舞台内的底部面板。

## 改造建议

- 接真实智能体：把演示时间轴换成你的事件流。会话项只存 `id` 和 `kind`，内容从响应式状态里读，流式事件只需要更新状态，不必替换数组元素。
- 在 TxConversationStream 末尾保留一个固定的 tail 项。组件总把最后一项放在虚拟列表之外，下一条消息到来时会重建它；有了 tail，每条真实消息只挂载一次，工具卡片的展开状态和入场动画不会重放。
- 权限按风险分级：`risk="read"` 的调用可以直接执行并在会话里留痕，`write` / `execute` 一律等人确认；「本次会话内记住」的选择由宿主保存。
- 澄清问卷的答案用受控的 `v-model` 与 `v-model:index` 持有，卡片被重建也不会丢；只在读者没有动过时才替他选推荐项。
- 侧边 Copilot：划词浮条挂在 body 上、锚在一个虚拟参考上，组件只在窗口滚动或缩放时自己重新定位。把 `selection.rects` 写成从 Range 现量的 getter，并在收起侧栏、展开舞台、文字重排之后调用 `updatePosition()`，它才能一直贴着文字。
