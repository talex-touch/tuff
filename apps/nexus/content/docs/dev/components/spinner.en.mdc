---
title: "Spinner"
description: "Loading"
category: Feedback
status: beta
since: 1.0.0
tags: [spinner, loading, progress]
syncStatus: migrated
---
# Spinner

Loading

<script setup lang="ts">
import SpinnerBasicDemo from '~/components/content/demos/SpinnerBasicDemo.vue'
import SpinnerBasicDemoSource from '~/components/content/demos/SpinnerBasicDemo.vue?raw'

import SpinnerSizesDemo from '~/components/content/demos/SpinnerSizesDemo.vue'
import SpinnerSizesDemoSource from '~/components/content/demos/SpinnerSizesDemo.vue?raw'

import SpinnerToggleDemo from '~/components/content/demos/SpinnerToggleDemo.vue'
import SpinnerToggleDemoSource from '~/components/content/demos/SpinnerToggleDemo.vue?raw'
</script>

## Basic Usage

### Spinner
:::TuffDemoWrapper{demo="SpinnerSpinnerDemo" code-lang="vue"}
---
code: |
  <template>
    <TxSpinner />
  </template>
---
:::

## Sizes

### Spinner sizes
:::TuffDemoWrapper{demo="SpinnerSpinnerSizesDemo" code-lang="vue"}
---
code: |
  <template>
    <div style="display: flex; gap: 12px; align-items: center;">
      <TxSpinner :size="12" />
      <TxSpinner :size="16" />
      <TxSpinner :size="24" />
      <TxSpinner :size="32" />
    </div>
  </template>
---
:::

## Visibility Toggle (v-if vs visible)

### Toggle
:::TuffDemoWrapper{demo="SpinnerToggleDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const showByIf = ref(true)
  const visible = ref(true)
  </script>

  <template>
    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 16px; align-items: start;">
      <div style="display: flex; flex-direction: column; gap: 10px;">
        <div style="display: flex; align-items: center; justify-content: space-between;">
          <div style="font-weight: 600;">
            v-if (no built-in transition)
          </div>
          <TxButton size="small" @click="showByIf = !showByIf">
            Toggle
          </TxButton>
        </div>
        <div style="display: flex; align-items: center; gap: 10px; height: 28px;">
          <TxSpinner v-if="showByIf" />
          <span style="color: var(--tx-text-color-secondary);">Status: {{ showByIf ? 'mounted' : 'unmounted' }}</span>
        </div>
      </div>

      <div style="display: flex; flex-direction: column; gap: 10px;">
        <div style="display: flex; align-items: center; justify-content: space-between;">
          <div style="font-weight: 600;">
            :visible (built-in transition)
          </div>
          <TxButton size="small" @click="visible = !visible">
            Toggle
          </TxButton>
        </div>
        <div style="display: flex; align-items: center; gap: 10px; height: 28px;">
          <TxSpinner :visible="visible" />
          <span style="color: var(--tx-text-color-secondary);">Status: {{ visible ? 'mounted' : 'unmounted (after leave)' }}</span>
        </div>
      </div>
    </div>
  </template>
---
:::

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|------|
| `size` | `number` | `16` | Description for size. |
| `strokeWidth` | `number` | `2` | Description for strokeWidth. |
| `fallback` | `boolean` | `false` | Description for fallback. |
| `visible` | `boolean` | `true` | Description for visible. |
