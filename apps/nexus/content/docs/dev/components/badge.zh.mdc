---
title: Badge 徽标
description: 用于展示数量或状态的轻量标记
category: Feedback
syncStatus: migrated
---

# Badge 徽标

用于展示计数或状态提示。

## 基础用法
### 徽标样式
数量、文本与点状提示。
:::TuffDemoWrapper{demo="BadgeBadgeVariantsDemo" code-lang="vue"}
---
code: |
  <template>
    <TxBadge :value="8" />
    <TxBadge value="New" variant="primary" />
    <TxBadge value="99+" variant="success" />
    <TxBadge dot variant="error" />
  </template>
---
:::
## 契约说明

`TxBadge` 渲染内联 pill 或 dot。默认插槽会替换徽标内容，不负责把徽标定位到某个目标元素周围。点状模式会隐藏 `value` 文本，仅渲染视觉圆点。

## API（Lite）
::TuffPropsTable
---
rows:
  - name: variant
    type: "'default' | 'primary' | 'success' | 'warning' | 'error'"
    default: "'default'"
    description: 颜色类型
  - name: value
    type: "string | number"
    default: '0'
    description: 徽标内容
  - name: dot
    type: boolean
    default: 'false'
    description: 点状模式
  - name: color
    type: string
    default: '-'
    description: 自定义颜色
---
:::

## Source
<TuffDocSourceLink />
