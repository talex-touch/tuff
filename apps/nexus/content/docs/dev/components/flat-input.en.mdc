---
title: FlatInput
description: Minimal input with flat styling
category: Form
syncStatus: migrated
---

# FlatInput

Lightweight input with a flat, compact look.

<script setup lang="ts">
import { ref } from 'vue'

const text = ref('')
const secret = ref('')
</script>

## Basic Usage
::TuffDemo{title="Flat Input" description="Text and password variants." code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const text = ref('')
  const secret = ref('')
  </script>

  <template>
    <FlatInput v-model="text" placeholder="Type something" />
    <FlatInput v-model="secret" placeholder="Password" password />
  </template>
---
#preview
<div style="display: flex; flex-direction: column; gap: 12px; max-width: 360px;">
  <flat-input v-model="text" placeholder="Type something" />
  <flat-input v-model="secret" placeholder="Password" password />
</div>
:::

## API (Lite)
::TuffPropsTable
---
rows:
  - name: modelValue
    type: string
    default: "''"
    description: Input value
  - name: placeholder
    type: string
    default: "''"
    description: Placeholder text
  - name: icon
    type: string
    default: "''"
    description: Prefix icon class
  - name: password
    type: boolean
    default: 'false'
    description: Password mode
  - name: area
    type: boolean
    default: 'false'
    description: Textarea mode
---
:::

## Source
<TuffDocSourceLink />
