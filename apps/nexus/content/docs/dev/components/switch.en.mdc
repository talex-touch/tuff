---
title: Switch
description: Tactile toggles with state feedback
category: Form
status: beta
since: 1.0.0
tags: [switch, toggle, state]
syncStatus: reviewed
verified: true
---

# Switch

## Basic Usage
::TuffCodeBlock{lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const enabled = ref(true)
  </script>

  <template>
    <TuffSwitch v-model="enabled" />
  </template>
---
::

## Demo
### Toggle State
Click, Enter, or Space toggles the switch. Movement and color change together.
:::TuffDemoWrapper{demo="SwitchToggleStateDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const firstSwitch = ref(false)
  const secondSwitch = ref(true)
  </script>

  <template>
    <div style="display: inline-flex; align-items: center; gap: 12px;">
      <TuffSwitch v-model="firstSwitch" />
      <TuffSwitch v-model="secondSwitch" />
    </div>
  </template>
---
:::

### Sizes
Small works well in dense lists, default fits forms, and large highlights important settings.
:::TuffDemoWrapper{demo="SwitchSizesDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const smallEnabled = ref(true)
  const defaultEnabled = ref(true)
  const largeEnabled = ref(true)
  </script>

  <template>
    <div style="display: flex; align-items: center; gap: 14px;">
      <TuffSwitch v-model="smallEnabled" size="small" />
      <TuffSwitch v-model="defaultEnabled" />
      <TuffSwitch v-model="largeEnabled" size="large" />
    </div>
  </template>
---
:::

### Disabled
Disabled switches reduce opacity, remove focusability, and block pointer or keyboard toggles.
:::TuffDemoWrapper{demo="SwitchDisabledDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const enabled = ref(true)
  const disabledOff = ref(false)
  const disabledOn = ref(true)
  </script>

  <template>
    <div style="display: flex; align-items: center; gap: 14px;">
      <TuffSwitch v-model="enabled" />
      <TuffSwitch v-model="disabledOff" disabled />
      <TuffSwitch v-model="disabledOn" disabled />
    </div>
  </template>
---
:::

### Labels Before and After
Place state labels before or after the switch, or combine it with setting copy.
:::TuffDemoWrapper{demo="SwitchLabelDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const compact = ref(false)
  const autoSync = ref(true)
  </script>

  <template>
    <label style="display: inline-flex; align-items: center; gap: 10px;">
      <span>Off</span>
      <TuffSwitch v-model="compact" />
      <span>On</span>
      <strong>Compact mode</strong>
    </label>
  </template>
---
:::

### Custom Colors
Switch does not expose a color prop yet. Override the active state with CSS variables or local classes.
:::TuffDemoWrapper{demo="SwitchCustomColorDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const success = ref(true)
  const warning = ref(true)
  </script>

  <template>
    <div class="custom-switch-row">
      <TuffSwitch v-model="success" class="custom-switch custom-switch--success" />
      <TuffSwitch v-model="warning" class="custom-switch custom-switch--warning" />
    </div>
  </template>

  <style scoped>
  .custom-switch-row :deep(.custom-switch--success.is-active) {
    background-color: var(--tx-color-success);
  }

  .custom-switch-row :deep(.custom-switch--warning.is-active) {
    background-color: var(--tx-color-warning);
  }
  </style>
---
:::

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `modelValue` | `boolean` | `false` | Checked state controlled by `v-model`. |
| `size` | `'small' \| 'default' \| 'large'` | `'default'` | Visual size variant. The default size does not add a modifier class. |
| `disabled` | `boolean` | `false` | Removes focusability and blocks click, Enter, and Space toggles. |

### Events

| Event | Params | Description |
|------|--------|-------------|
| `update:modelValue` | `(value: boolean) => void` | Emitted with the next checked state after an enabled click, Enter, or Space toggle. |
| `change` | `(value: boolean) => void` | Emitted with the same next checked state after an enabled user toggle. |

### Slots

| Slot | Props | Description |
|------|------|-------------|
| - | - | `TuffSwitch` has no slots. Pair it with external labels or setting-row copy. |

## Interaction Contract

- The root renders `role="switch"`, `aria-checked`, `aria-disabled`, and `tabindex="0"` while enabled.
- `disabled=true` sets `tabindex="-1"`, adds `is-disabled`, and prevents click, Enter, and Space from emitting events.
- Enabled toggles emit both `update:modelValue` and `change`; the component does not mutate external state without `v-model` update handling.
- `size="small"` and `size="large"` add modifier classes; `size="default"` intentionally leaves the base class only.

## Composite Patterns

### Settings Row
Switch paired with labels, helper copy, and live state text for settings lists.
::::TuffDemoWrapper{demo="SwitchSettingsRowDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { reactive } from 'vue'

  const settings = reactive({
    notifications: true,
    focusMode: false,
    autoUpdate: true,
  })
  </script>

  <template>
    <div class="settings-panel">
      <div class="settings-row">
        <div>
          <TxTag label="Notifications" />
          <p>Receive important system and plugin alerts.</p>
        </div>
        <TuffSwitch v-model="settings.notifications" />
      </div>
      <div class="settings-row">
        <div>
          <TxTag label="Focus mode" color="var(--tx-color-warning)" />
          <p>Hide non-essential prompts to reduce interruptions.</p>
        </div>
        <TuffSwitch v-model="settings.focusMode" />
      </div>
    </div>
  </template>

  <style scoped>
  .settings-panel {
    display: flex;
    flex-direction: column;
    width: min(100%, 560px);
  }

  .settings-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }
  </style>
---
::::

## Best Practices

- Use switches for immediate boolean settings. Use checkboxes when the value is part of a submitted form or multi-select group.
- Place a readable text label next to the switch; the component itself intentionally renders only the control.
- Keep labels vertically centered and leave enough hit area around dense settings rows.
- Override active colors with local classes only; preserve disabled, focus, and size classes from the base component.

## Review Notes

- **Model contract:** `TxSwitch` is controlled by `modelValue`. User interaction emits both `update:modelValue` and `change` with the next boolean; the visual state updates after the parent writes the new prop back.
- **Keyboard contract:** Enabled switches respond to click, Enter, and Space. Disabled switches set `tabindex="-1"`, keep `aria-disabled="true"`, and emit nothing.
- **Verified coverage:** `switch.test.ts` covers active aria state, size classes, click and keyboard emissions, disabled blocking, and disabled tabindex/aria state.

## Source

- Component source: `packages/tuffex/packages/components/src/switch/src/TxSwitch.vue`.
- Style source: `packages/tuffex/packages/components/src/switch/style/index.scss`.
- Export entry: `packages/tuffex/packages/components/src/switch/index.ts` exports `TuffSwitch`, aliases `TxSwitch`, and installs the component under its `TuffSwitch` name.
- Coverage: `packages/tuffex/packages/components/src/switch/__tests__/switch.test.ts` verifies semantics, size classing, click/keyboard updates, and disabled behavior.

<TuffDocSourceLink />

