---
title: "Fusion 交融"
description: "两个插槽的 gooey 交融动效，支持 hover、click 或手动受控状态。"
category: Effects
status: beta
since: 1.0.0
tags: [fusion, gooey, motion]
syncStatus: reviewed
verified: true
---
# Fusion 交融

`TxFusion` 通过 SVG gooey filter 渲染两个重叠插槽。它可以在 hover 或 click 时自行管理 active 状态，也可以在 `trigger="manual"` 下完全交给外部 `v-model` 控制。

## 基础用法

### Fusion
:::TuffDemoWrapper{demo="FusionFusionDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const active = ref(false)
  </script>

  <template>
    <!-- 两个 blob 在 hover / 受控激活时通过 gooey filter 交融；完整 demo 见源码。 -->
    <TxFusion v-model="active" trigger="hover" :gap="240" :blur="19" :alpha="29" :alpha-offset="-10">
      <template #a>
        <div style="width: 260px; height: 260px; border-radius: 999px; background: hsla(256, 65%, 60%, 0.76);" />
      </template>
      <template #b>
        <div style="width: 110px; height: 110px; border-radius: 999px; background: hsla(77, 100%, 55%, 0.78);" />
      </template>
    </TxFusion>
  </template>
---
:::

## 实际场景

以下配方都共用同一套交融参数，只是插槽内容不同；`gap` / `direction` 需按内容尺寸微调，完整样式见各 demo 源码。

### Button + Tooltip Bubble
:::TuffDemoWrapper{demo="FusionFusionButtonTooltipDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const active = ref(false)
  </script>

  <template>
    <!-- direction="y" 让按钮与提示气泡上下交融。 -->
    <TxFusion v-model="active" trigger="hover" :gap="44" direction="y" :duration="260" :blur="19" :alpha="29" :alpha-offset="-10">
      <template #a><!-- 主按钮：Save ⌘S --></template>
      <template #b><!-- 提示气泡：Saved to drafts --></template>
    </TxFusion>
  </template>
---
:::

### Avatar + Badge
:::TuffDemoWrapper{demo="FusionFusionAvatarBadgeDemo" code-lang="vue"}
---
code: |
  <template>
    <!-- 头像与通知角标交融（hover 触发，无需 v-model）。 -->
    <TxFusion trigger="hover" :gap="64" :duration="260" :blur="19" :alpha="29" :alpha-offset="-10">
      <template #a><!-- 头像 TA --></template>
      <template #b><!-- 角标 8 --></template>
    </TxFusion>
  </template>
---
:::

### Chip + Icon
:::TuffDemoWrapper{demo="FusionFusionChipIconDemo" code-lang="vue"}
---
code: |
  <template>
    <!-- 玻璃态状态 chip 与圆形图标徽标交融（hover 触发，无需 v-model）。 -->
    <TxFusion trigger="hover" :gap="54" :duration="260" :blur="19" :alpha="29" :alpha-offset="-10">
      <template #a><!-- 状态 chip：Synced --></template>
      <template #b><!-- 圆形徽标：✓ --></template>
    </TxFusion>
  </template>
---
:::

### Mini Card + FAB
:::TuffDemoWrapper{demo="FusionFusionMiniCardFabDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const active = ref(false)
  </script>

  <template>
    <!-- 卡片与浮动操作按钮交融；较大 gap（92）需要更高的 blur / alpha 才自然。 -->
    <TxFusion v-model="active" trigger="hover" :gap="92" :duration="260" :blur="19" :alpha="29" :alpha-offset="-10">
      <template #a><!-- Quick Note 卡片 --></template>
      <template #b><!-- 浮动 + 按钮 --></template>
    </TxFusion>
  </template>
---
:::

## API

### TxFusion Props

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|---------|------|
| `modelValue` | `boolean \| undefined` | `undefined` | 受控 active 状态；省略时组件使用内部 active 状态。 |
| `disabled` | `boolean` | `false` | 禁用指针触发的状态变更，并应用禁用视觉状态。 |
| `trigger` | `'hover' \| 'click' \| 'manual'` | `'hover'` | 交互模式；`manual` 会忽略指针事件，只接受外部 `v-model` 控制。 |
| `direction` | `'x' \| 'y'` | `'x'` | 未交融时两个 blob 分离的轴向。 |
| `gap` | `number` | `40` | 未激活时两个 blob 的距离，单位 px。 |
| `duration` | `number` | `260` | 过渡时长，单位 ms；负数会收敛为 `0ms`。 |
| `easing` | `string` | `'cubic-bezier(0.2, 0.8, 0.2, 1)'` | blob transform 过渡使用的 CSS easing。 |
| `blur` | `number` | `19` | SVG `feGaussianBlur.stdDeviation`，控制 gooey filter 模糊强度。 |
| `alpha` | `number` | `29` | SVG color matrix 的 alpha 倍率；数值越大越“粘”。 |
| `alphaOffset` | `number` | `-10` | SVG color matrix 的 alpha 偏移。 |

### Events

| 事件名 | 参数 | 说明 |
|------|------|------|
| `change` | `(v: boolean)` | 非禁用触发器请求新 active 状态时发出。 |
| `update:modelValue` | `(v: boolean)` | `v-model` 更新事件；受控调用方必须把值写回组件。 |

### Slots

| 插槽名 | Props | 说明 |
|------|-------|------|
| `a` | - | 第一个 blob 图层；未激活时位于 `direction` 的负方向。 |
| `b` | - | 第二个 blob 图层；未激活时位于 `direction` 的正方向。 |

### Exposed Methods

不暴露公开实例方法。

## 交互契约

- `modelValue` 为 boolean 时组件进入受控模式；省略时 `TxFusion` 保存内部 active 状态。
- `trigger="hover"` 在 mouse enter 时激活，在 mouse leave 时取消激活。
- `trigger="click"` 点击根节点时切换 active；受控调用方必须写回发出的值，视觉状态才会变化。
- `trigger="manual"` 会忽略 hover、leave 和 click。外部控件完全拥有状态机时使用它。
- `disabled=true` 会阻止指针触发的状态写入，不从这些交互发出更新，添加 `is-disabled`，并设置根节点 `pointer-events: none`。
- `duration` 为负数时收敛到 `0ms`；`gap` 和 `blur` 会写入像素单位 CSS 变量。
- 非有限的 `alpha` 与 `alphaOffset` 会先回退到安全 color-matrix 值，再渲染 SVG filter。
- 插槽内容处于经过 filter 的 SVG gooey 图层内；插槽控件仍需自行负责 label、焦点状态和键盘语义。

## 最佳实践

- `TxFusion` 适合装饰性动效、成对 chip、紧凑动作入口和状态点缀；不要把它作为表达关键状态的唯一方式。
- 当父级状态机、表单、路由状态或埋点流需要确定性控制时，优先使用 `trigger="manual"`。
- 两个插槽形状的尺寸应相对接近，这样 SVG filter 看起来像交融，而不是碰撞。
- `gap`、`blur`、`alpha` 和 `alphaOffset` 需要一起调；大间距通常需要更高 blur / alpha 和更克制的时长。
- 避免在密集文本或表单控件周围使用快速动效。通过宿主的 reduced-motion 偏好驱动 `disabled` 或手动 inactive 状态。
- 如果插槽内容可交互，确保根节点 trigger 模式不会和子按钮或链接行为冲突。

## 审阅说明

- 已人工核对 `packages/tuffex/packages/components/src/fusion/src/types.ts`、`TxFusion.vue` 与 `fusion.test.ts`。
- 组件在非受控模式下也会通过 computed setter 发出事件，因此文档需要把 `change` 和 `update:modelValue` 都描述为状态请求。
- `manual` 设计上不响应指针事件；使用外部 Toggle 的示例若不希望 hover / click 同时改状态，应优先使用它。
- 根节点是带 click / hover 监听的 `div`，语义化交互应放在插槽控件或外层 UI 上，而不是 Fusion 根节点本身。

## Source

- Component source: `packages/tuffex/packages/components/src/fusion/src/TxFusion.vue`。
- Types: `packages/tuffex/packages/components/src/fusion/src/types.ts`。
- **实测覆盖:** Coverage: `packages/tuffex/packages/components/src/fusion/__tests__/fusion.test.ts` 验证插槽渲染、视觉 props CSS 变量、SVG filter 接线、duration 收敛、hover 状态事件、受控 click 请求、manual trigger no-op 和 disabled 交互保护。

<TuffDocSourceLink />
