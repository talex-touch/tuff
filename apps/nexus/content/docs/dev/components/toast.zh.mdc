---
title: Toast 提示
description: 轻量通知与短暂状态反馈
category: Feedback
status: beta
since: 1.0.0
tags: [toast, feedback, status]
syncStatus: reviewed
verified: true
---

# Toast 提示

轻量通知提示（类似 shadcn/sonner）。需要在应用根部挂载一次 `<TxToastHost />`，然后在任意位置调用 `toast()`。

## 基础用法

### Toast
挂载 host 后触发提示。
:::TuffDemoWrapper{demo="ToastToastDemo" code-lang="vue"}
---
code: |
  <template>
    <TxToastHost />
    <TxButton @click="toast({ title: 'Saved', description: 'Your changes have been saved.' })">
      Show toast
    </TxButton>
    <TxButton
      style="margin-left: 8px;"
      @click="toast({ title: 'Success', description: 'Done', variant: 'success' })"
    >
      Success
    </TxButton>
  </template>
---
:::
## API

### TxToastHost Props

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| - | - | - | `TxToastHost` 没有 props，建议只在应用根部附近挂载一次。 |

### toast(options)

从 `@talex-touch/tuffex/utils` 导入 `toast`：

:::TuffCodeBlock{lang="ts"}
---
code: |
  toast({
    id?: string
    title?: string
    description?: string
    variant?: 'default' | 'success' | 'warning' | 'danger'
    duration?: number // 默认 2600，0 = 不自动关闭
  }): string
---
:::

### dismissToast / clearToasts

:::TuffCodeBlock{lang="ts"}
---
code: |
  import { clearToasts, dismissToast, toast } from '@talex-touch/tuffex/utils'

  const id = toast({ title: '已排队', duration: 0 })
  dismissToast(id)
  clearToasts()
---
:::

### Events

| 事件名 | 参数 | 说明 |
|------|------|------|
| - | - | 没有组件自定义事件。关闭按钮内部调用 `dismissToast(id)`。 |

### Slots

| 插槽名 | Props | 说明 |
|------|-------|------|
| - | - | `TxToastHost` 从全局 toast store 渲染，不提供插槽。 |

## 交互契约

- `<TxToastHost />` 通过 teleport 在 `body` 渲染通知区域，带 `role="region"` 与 `aria-label="Notifications"`。
- 每条 toast 都有可键盘聚焦的关闭按钮，按钮名称为 `Dismiss notification`。
- `toast()` 返回最终 id；传入相同 `id` 会替换现有 toast，避免同一业务提示重复堆叠。
- `duration > 0` 时自动关闭，`duration: 0` 时保持显示直到调用 `dismissToast(id)` 或 `clearToasts()`。
- 每次调用都会通过共享 z-index 管理器提升 host 层级，让新通知位于当前交互层之上。

## 后台反馈中心

`TxToastHost` 建议只在应用或页面根部挂一次；业务动作通过 `toast()` 触发，常驻提示用稳定 `id + duration: 0`，避免同一队列状态重复堆叠。后台任务中心里，Toast 负责短反馈，`TxTooltip` 解释动作，`TxLoadingOverlay` 阻断局部刷新，`TxSpinner` 承担行内等待。

::TuffDemoWrapper{demo="ComponentsFeedbackTaskCenterDemo" code-lang="vue" title="后台任务反馈中心" description="截图验证的 Toast / Tooltip / LoadingOverlay / Spinner 组合。"}
---
code: |
  <script setup lang="ts">
  import { clearToasts, toast } from '@talex-touch/tuffex/utils'
  import { ref } from 'vue'

  const syncing = ref(true)

  function showFeedbackToast() {
    toast({
      id: 'feedback-task-center',
      title: '同步任务已排队',
      description: '将在 2 分钟内完成截图核验。',
      variant: 'warning',
      duration: 0,
    })
  }
  </script>

  <template>
    <section class="grid gap-3">
      <TxToastHost />
      <TxTooltip content="Tooltip 只解释当前动作。">
        <TxButton @click="showFeedbackToast">发送提示</TxButton>
      </TxTooltip>
      <TxLoadingOverlay :loading="syncing" text="正在刷新任务队列…">
        <TxSpinner :size="16" />
      </TxLoadingOverlay>
      <TxButton variant="ghost" @click="clearToasts()">清空提示</TxButton>
    </section>
  </template>
---
:::

## 最佳实践

- 只挂载一个 host。多个 host 会读取同一个全局 store，可能重复渲染通知区域。
- 任务、保存、同步、重试类通知使用稳定 id，让重复状态更新替换上一条 toast。
- 描述保持简短；长进度、带恢复动作的错误或表单应放到面板、抽屉或页面里。
- 离开所属页面或底层任务结束时，清理常驻 toast。

## 审阅说明

- **可访问性说明:** Host 暴露带标签的通知区域，每条 toast 有关闭按钮，但 toast item 不是 `role="status"`/`alert`，区域本身也不是 `aria-live`。关键失败或长任务需要用持久可见文案或页面级状态文本补充。
- **计时器说明:** 复用 `id` 会替换 store item，但已有自动关闭定时器不会取消。基于 id 的常驻任务 toast 请使用 `duration: 0`；重复的短成功提示则使用唯一 id。
- **实测覆盖:** `toast.test.ts` 覆盖 id 替换、返回 id、自动关闭、常驻 toast、dismiss/clear 辅助函数、host region 语义、variant class、标题/描述渲染、可访问关闭按钮，以及关闭按钮移除。

## Source

- Host source: `packages/tuffex/packages/components/src/toast/src/TxToastHost.vue`。
- Utility source: `packages/tuffex/packages/utils/toast.ts` 导出 `toast`、`dismissToast`、`clearToasts`、`toastStore`、`TxToastOptions`、`TxToastItem` 与 `TxToastVariant`。
- Export alias: `packages/tuffex/packages/components/src/toast/index.ts` 导出可安装的 `ToastHost`、`TxToastHost` 与 `TxToastHostInstance`。
- Coverage: `packages/tuffex/packages/components/src/toast/__tests__/toast.test.ts` 覆盖 store 行为、计时器、host 渲染与移除。

<TuffDocSourceLink />
