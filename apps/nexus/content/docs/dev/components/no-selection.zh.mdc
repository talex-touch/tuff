---
title: "NoSelection 未选择"
description: "用于详情面板等未选择任何项的状态提示。"
category: Feedback
syncStatus: migrated
---
# NoSelection 未选择

`TxNoSelection` 是 `TxEmptyState` 的快捷封装，自动设置 `variant="no-selection"`，用于详情面板等未选择任何项的状态提示。

## 基础用法

### NoSelection
:::TuffDemoWrapper{demo="NoSelectionNoSelectionDemo" code-lang="vue"}
---
code: |
  <template>
    <TxNoSelection description="Select an item to see its details." />
  </template>
---
:::

## API

`TxNoSelection` 继承 `TxEmptyState` 的全部 Props（除 `variant`）、Slots 和 Events，详见 [EmptyState](./empty-state.zh.md)。

## 交互契约

- `TxNoSelection` 始终向 `TxEmptyState` 透传 `variant="no-selection"`。
- 组件不额外改写 `title`、`description`、`surface` 或 action 配置；显式 props 会原样透传。
- `icon`、`title`、`description`、`actions` 插槽会原样转发给 `TxEmptyState`。
