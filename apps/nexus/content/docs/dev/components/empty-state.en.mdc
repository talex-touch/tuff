---
title: "EmptyState"
description: "Component documentation for EmptyState."
category: Feedback
status: beta
since: 1.0.0
tags: [empty, state, guide]
syncStatus: migrated
---
# EmptyState

Component documentation for EmptyState.

## Basic Usage

### EmptyState (variant)
:::TuffDemoWrapper{demo="EmptyStateEmptyStateVariantDemo" code-lang="vue"}
---
code: |
  <template>
    <TxEmptyState
      variant="no-data"
      :primary-action="{ label: 'Create', type: 'primary' }"
      :secondary-action="{ label: 'Refresh' }"
    />
  </template>
---
:::

## Horizontal Layout

### EmptyState (horizontal)
:::TuffDemoWrapper{demo="EmptyStateEmptyStateHorizontalDemo" code-lang="vue"}
---
code: |
  <template>
    <TxEmptyState
      variant="no-selection"
      layout="horizontal"
      surface="card"
      title="No selection"
      description="Pick an item from the left to continue."
    />
  </template>
---
:::

## Custom Slots

### EmptyState (slots)
:::TuffDemoWrapper{demo="EmptyStateEmptyStateSlotsDemo" code-lang="vue"}
---
code: |
  <template>
    <TxEmptyState variant="custom">
      <template #icon>
        <TxIcon name="i-carbon-search" :size="28" />
      </template>
      <template #title>
        Setup required
      </template>
      <template #description>
        Connect a workspace to enable this feature.
      </template>
      <template #actions>
        <TxButton type="primary">Connect</TxButton>
        <TxButton>Learn more</TxButton>
      </template>
    </TxEmptyState>
  </template>
---
:::

## Preset Components

- `TxBlankSlate`: large blank state (default `size="large"`)
- `TxLoadingState`: loading placeholder (default `variant="loading"`)
- `TxNoSelection`: no-selection placeholder (default `variant="no-selection"`)
- `TxNoData`: no-data placeholder (default `variant="no-data"`)
- `TxSearchEmpty`: empty search state (default `variant="search-empty"`)
- `TxOfflineState`: offline hint (default `variant="offline"`)
- `TxPermissionState`: permission denied (default `variant="permission"`)

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|------|
| `variant` | `EmptyStateVariant` | `'empty'` | Description for variant. |
| `title` | `string` | - | Description for title. |
| `description` | `string` | - | Description for description. |
| `icon` | `TxIconSource \| string \| null` | - | Icon（string Text class/name） |
| `iconSize` | `number` | - | Description for iconSize. |
| `layout` | `'vertical' \| 'horizontal'` | `'vertical'` | LayoutDirection |
| `align` | `'start' \| 'center' \| 'end'` | `'center'` | Description for align. |
| `size` | `'small' \| 'medium' \| 'large'` | `'medium'` | SizeText |
| `surface` | `'plain' \| 'card'` | `'plain'` | Description for surface. |
| `primaryAction` | `EmptyStateAction` | - | Description for primaryAction. |
| `secondaryAction` | `EmptyStateAction` | - | Description for secondaryAction. |
| `actionSize` | `TxButtonProps['size']` | `'small'` | Description for actionSize. |
| `loading` | `boolean` | `false` | Description for loading. |

### EmptyStateAction

| Prop | Type | Default | Description |
|------|------|---------|------|
| `label` | `string` | - | Description for label. |
| `type` | `TxButtonProps['type']` | - | Description for type. |
| `variant` | `TxButtonProps['variant']` | - | Description for variant. |
| `size` | `TxButtonProps['size']` | - | Description for size. |
| `disabled` | `boolean` | `false` | Description for disabled. |
| `icon` | `string` | - | Description for icon. |

### Slots

| Name | Description |
|------|------|
| `icon` | Description for icon. |
| `title` | Description for title. |
| `description` | Description for description. |
| `actions` | Description for actions. |

### Events

| Event | Description |
|------|------|
| `primary` | Description for primary. |
| `secondary` | Description for secondary. |
