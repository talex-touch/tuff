---
title: "FlatRadio 平铺选择器"
description: "用于 2-5 个选项的平铺选择（`TxFlatRadio` + `TxFlatRadioItem`）。提供指示器滑动动画、键盘导航和完整的 TypeScript 类型支持。"
category: Form
status: beta
since: 2.4.7
tags: [select, flat, form, selection, inline]
syncStatus: reviewed
verified: true
---
# FlatRadio 平铺选择器

用于 2-5 个选项的平铺选择（`TxFlatRadio` + `TxFlatRadioItem`）。相比下拉选择器操作更直接，相比 Radio 更紧凑。

## 基础用法

最简单的平铺选择器，适合少量选项的场景。

### FlatRadio (basic)
:::TuffDemoWrapper{demo="FlatRadioBasicDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref<'light' | 'dark' | 'auto'>('light')
  </script>

  <template>
    <TxFlatRadio v-model="value">
      <TxFlatRadioItem value="light" label="Light" />
      <TxFlatRadioItem value="dark" label="Dark" />
      <TxFlatRadioItem value="auto" label="Auto" />
    </TxFlatRadio>
  </template>
---
:::

## 尺寸

支持 `sm`、`md`（默认）、`lg` 三种尺寸。

### FlatRadio (sizes)
:::TuffDemoWrapper{demo="FlatRadioSizesDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const sm = ref('a')
  const md = ref('a')
  const lg = ref('a')
  </script>

  <template>
    <TxFlatRadio v-model="sm" size="sm">
      <TxFlatRadioItem value="a" label="Option A" />
      <TxFlatRadioItem value="b" label="Option B" />
      <TxFlatRadioItem value="c" label="Option C" />
    </TxFlatRadio>

    <TxFlatRadio v-model="md" size="md">
      <TxFlatRadioItem value="a" label="Option A" />
      <TxFlatRadioItem value="b" label="Option B" />
      <TxFlatRadioItem value="c" label="Option C" />
    </TxFlatRadio>

    <TxFlatRadio v-model="lg" size="lg">
      <TxFlatRadioItem value="a" label="Option A" />
      <TxFlatRadioItem value="b" label="Option B" />
      <TxFlatRadioItem value="c" label="Option C" />
    </TxFlatRadio>
  </template>
---
:::

## 图标

通过 `icon` 属性可以为选项添加图标（CSS class 方式，如 UnoCSS 图标）。

### FlatRadio (icon)
:::TuffDemoWrapper{demo="FlatRadioIconDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref('grid')
  </script>

  <template>
    <TxFlatRadio v-model="value">
      <TxFlatRadioItem value="grid" icon="i-carbon-grid" label="Grid" />
      <TxFlatRadioItem value="list" icon="i-carbon-list" label="List" />
      <TxFlatRadioItem value="kanban" icon="i-carbon-column" label="Kanban" />
    </TxFlatRadio>
  </template>
---
:::

## 禁用状态

支持整组禁用和单项禁用。

### FlatRadio (disabled)
:::TuffDemoWrapper{demo="FlatRadioDisabledDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref('a')
  </script>

  <template>
    <!-- 单项禁用 -->
    <TxFlatRadio v-model="value">
      <TxFlatRadioItem value="a" label="Option A" />
      <TxFlatRadioItem value="b" label="Option B" disabled />
      <TxFlatRadioItem value="c" label="Option C" />
    </TxFlatRadio>

    <!-- 整组禁用 -->
    <TxFlatRadio v-model="value" disabled>
      <TxFlatRadioItem value="a" label="Option A" />
      <TxFlatRadioItem value="b" label="Option B" />
      <TxFlatRadioItem value="c" label="Option C" />
    </TxFlatRadio>
  </template>
---
:::

## 带边框

设置 `bordered` 属性可以为选择器添加外边框。

### FlatRadio (bordered)
:::TuffDemoWrapper{demo="FlatRadioBorderedDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref('a')
  </script>

  <template>
    <TxFlatRadio v-model="value" bordered>
      <TxFlatRadioItem value="a" label="Option A" />
      <TxFlatRadioItem value="b" label="Option B" />
      <TxFlatRadioItem value="c" label="Option C" />
    </TxFlatRadio>
  </template>
---
:::

## 多选模式

设置 `multiple` 后，组件会从单选切换为类似复选的多选行为。

### FlatRadio (multiple)
:::TuffDemoWrapper{demo="FlatRadioMultipleDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref(['mention', 'reply'])
  </script>

  <template>
    <TxFlatRadio v-model="value" multiple>
      <TxFlatRadioItem value="mention" label="Mention" icon="i-carbon-at" />
      <TxFlatRadioItem value="reply" label="Reply" icon="i-carbon-reply" />
      <TxFlatRadioItem value="archive" label="Archive" icon="i-carbon-archive" />
      <TxFlatRadioItem value="mute" label="Mute" icon="i-carbon-volume-mute" />
    </TxFlatRadio>
  </template>
---
:::

## 键盘导航

### FlatRadio (keyboard)
:::TuffDemoWrapper{demo="FlatRadioKeyboardDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const single = ref('overview')
  const multiple = ref(['email'])
  </script>

  <template>
    <TxFlatRadio v-model="single">
      <TxFlatRadioItem value="overview" label="Overview" />
      <TxFlatRadioItem value="activity" label="Activity" />
      <TxFlatRadioItem value="settings" label="Settings" />
    </TxFlatRadio>

    <TxFlatRadio v-model="multiple" multiple>
      <TxFlatRadioItem value="email" label="Email" />
      <TxFlatRadioItem value="push" label="Push" />
      <TxFlatRadioItem value="sms" label="SMS" />
    </TxFlatRadio>
  </template>
---
:::

容器聚焦后可通过键盘控制：

| 按键 | 行为 |
|------|------|
| `→` / `↓` | 选中下一个非禁用项（循环） |
| `←` / `↑` | 选中上一个非禁用项（循环） |
| `Home` | 选中第一个非禁用项 |
| `End` | 选中最后一个非禁用项 |
| `Enter` / `Space` | 多选模式下切换当前聚焦项 |

## API

### TxFlatRadio

#### Props

| 属性名 | 说明 | 类型 | 默认值 |
|--------|------|------|--------|
| `modelValue` / `v-model` | 单选时为单值，多选时为值数组。 | `string \| number \| (string \| number)[]` | - |
| `multiple` | 启用类似复选框的多选行为；开启后隐藏单选滑动指示器。 | `boolean` | `false` |
| `disabled` | 禁用整组，移除容器焦点并阻止点击/键盘变化。 | `boolean` | `false` |
| `size` | 尺寸变体。 | `'sm' \| 'md' \| 'lg'` | `'md'` |
| `bordered` | 为选择器添加外边框。 | `boolean` | `false` |

#### Events

| 事件名 | 说明 | 回调参数 |
|--------|------|----------|
| `update:modelValue` | 以新的单选值或多选值数组触发。 | `(value: string \| number \| (string \| number)[]) => void` |
| `change` | 选择变化后以相同载荷触发。 | `(value: string \| number \| (string \| number)[]) => void` |

#### Slots

| 插槽名 | 说明 |
|--------|------|
| `default` | `TxFlatRadioItem` 子组件。 |

### TxFlatRadioItem

#### Props

| 属性名 | 说明 | 类型 | 默认值 |
|--------|------|------|--------|
| `value` | 注册到父选择器的当前项值。 | `string \| number` | *必填* |
| `label` | 未提供默认插槽时的可见文本。 | `string` | - |
| `icon` | 未提供 `icon` 插槽时使用的图标 CSS class。 | `string` | - |
| `disabled` | 禁用当前项，并在键盘导航中跳过。 | `boolean` | `false` |

#### Slots

| 插槽名 | 说明 |
|--------|------|
| `default` | 自定义可见内容，替代 `label`。 |
| `icon` | 自定义图标内容，替代 `icon` class。 |

## 交互契约

- 单选模式下 `TxFlatRadio` 使用 `role="radiogroup"`，每个 item 使用 `role="radio"`。
- 多选模式下容器使用 `role="group"`，每个 item 使用 `role="checkbox"`，并隐藏滑动指示器。
- item 按钮保持 `tabindex="-1"`；焦点停留在容器上，由方向键驱动整组选择。
- 方向键、Home、End 会跳过禁用项，并在可用项之间循环。
- 多选模式下方向键移动当前聚焦值，Enter / Space 切换该值；单选模式下方向键会立即选中下一个值。
- 整组 `disabled` 会把容器 `tabindex` 设为 `-1`，让所有 item 进入禁用态，并阻止点击/键盘变化。

## 最佳实践

- FlatRadio 适合 2 到 5 个短选项，且用户需要直接比较选项的场景；更长列表使用 `TxSelect`。
- 标签尽量保持单行；item 布局有意使用 `white-space: nowrap`。
- 在紧凑 `sm` 尺寸中避免混用过长自定义插槽内容和图标。
- 只有当多个值是彼此独立的开关项时才使用 `multiple`；否则用独立开关或复选框表达更清晰。
- `value` 应在渲染间保持稳定，因为父级会按 value 注册 item DOM，用于指示器和键盘顺序。

## 审阅说明

- **可访问性说明：** 整组只有容器进入 Tab 顺序；item 按钮保持 `tabindex="-1"`，从而集中处理方向键。需要用邻近字段文案或外部 label 为整组命名。
- **实测覆盖:** `flat-radio.test.ts` 覆盖单选 `radiogroup`/`radio` 语义、item 禁用态、禁用整组时移除焦点、方向键循环并跳过禁用项、Home/End 选择，以及 `multiple` 模式下 Enter/Space 切换。

## Source

- Component sources: `packages/tuffex/packages/components/src/flat-radio/src/TxFlatRadio.vue` and `TxFlatRadioItem.vue`.
- Types: `packages/tuffex/packages/components/src/flat-radio/src/types.ts`.
- Coverage: `packages/tuffex/packages/components/src/flat-radio/__tests__/flat-radio.test.ts` verifies ARIA roles, disabled behavior, arrow-key wrapping, Home / End, and Enter / Space toggles in multiple mode.

<TuffDocSourceLink />

