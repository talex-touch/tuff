---
title: Steps 步骤条
description: 多步骤进度展示
category: Layout
syncStatus: migrated
---

# Steps 步骤条

用于展示流程进度。

## 基础用法
### 步骤条
展示当前进度。
:::TuffDemoWrapper{demo="StepsStepsDemo" code-lang="vue"}
---
code: |
  <template>
    <TxSteps :active="1">
      <TxStep title="开始" description="收集基础信息" :step="0" />
      <TxStep title="填写" description="补充详细内容" :step="1" />
      <TxStep title="完成" description="确认提交" :step="2" />
    </TxSteps>
  </template>
---
:::
## API（Lite）
### TxSteps Props
::TuffPropsTable
---
rows:
  - name: active
    type: "number | string"
    default: '0'
    description: 当前步骤
  - name: direction
    type: "'horizontal' | 'vertical'"
    default: "'horizontal'"
    description: 方向
  - name: size
    type: "'small' | 'medium' | 'large'"
    default: "'medium'"
    description: 尺寸
---
:::

### TxStep Props
::TuffPropsTable
---
rows:
  - name: title
    type: string
    default: '-'
    description: 步骤标题
  - name: description
    type: string
    default: '-'
    description: 步骤描述
  - name: step
    type: "number | string"
    default: '顺序索引'
    description: 步骤标识；未提供时使用子项顺序索引
  - name: status
    type: "'wait' | 'active' | 'completed' | 'error'"
    default: "'wait'"
    description: 手动状态
  - name: clickable
    type: boolean
    default: 'true'
    description: 是否允许点击/键盘切换到该步骤
  - name: disabled
    type: boolean
    default: 'false'
    description: 禁用该步骤交互
  - name: icon
    type: string
    default: '-'
    description: 自定义步骤图标
  - name: completedIcon
    type: string
    default: "'check'"
    description: 已完成图标
---
:::

## 交互契约

- `TxStep` 未传 `step` 时按子项顺序自动使用 `0` 起始索引，与 `active` 数字索引对齐。
- 可点击步骤的头部暴露 `role="button"`、`tabindex="0"`；当前步骤暴露 `aria-current="step"`。
- 点击、Enter 与 Space 会切换内部 active step；`disabled` 或 `clickable=false` 的步骤不会响应交互。
- 最后一项不会渲染连接线。

## Source
<TuffDocSourceLink />
