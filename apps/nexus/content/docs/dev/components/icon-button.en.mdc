---
title: IconButton
description: Accessible icon-only action button with pressed state and semantic status tones.
category: Basic
status: beta
since: 1.0.0
tags: [button, icon, action]
syncStatus: reviewed
verified: true
---
# IconButton

## Basic Usage

### IconButton
::::TuffDemoWrapper{demo="IconButtonIconButtonDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const pinned = ref(false)
  </script>

  <template>
    <div class="flex flex-wrap items-center gap-3">
      <TxIconButton
        icon="i-carbon-star"
        label="Pin workspace"
        shape="circle"
        :pressed="pinned"
        @click="pinned = !pinned"
      />
      <TxIconButton icon="i-carbon-edit" label="Edit item" shape="square" status="info" />
      <TxIconButton icon="i-carbon-add" label="Add item" shape="pill" size="lg" status="success" />
      <TxIconButton icon="i-carbon-warning" label="Action needs attention" status="warning" />
      <TxIconButton icon="i-carbon-trash-can" label="Delete item" status="danger" disabled />
    </div>
  </template>
---
::::

## Interaction Contract

- The root element is a real `button`; keyboard activation and disabled behavior are native.
- `label` becomes `aria-label`; provide it for icon-only controls.
- When `pressed` is boolean, the component emits `aria-pressed` and applies active styling.
- `disabled` blocks the native button and suppresses the component `click` emit.
- The default slot receives `{ hover, pressed }` for custom icon animations.

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `icon` | `string` | `''` | Icon class/name rendered through `TxIcon` when no default slot is provided. |
| `label` | `string` | `''` | Accessible label. Required for icon-only actions. |
| `size` | `'xs' \| 'sm' \| 'md' \| 'lg'` | `'md'` | Button size. |
| `shape` | `'square' \| 'circle' \| 'pill'` | `'square'` | Hit-area silhouette. |
| `status` | `'success' \| 'warning' \| 'danger' \| 'info'` | - | Semantic visual tone; omitted keeps the neutral style. |
| `pressed` | `boolean` | - | Toggle state; forwarded to `aria-pressed` when defined. |
| `disabled` | `boolean` | `false` | Native disabled state and visual disabled class. |
| `nativeType` | `'button' \| 'submit' \| 'reset'` | `'button'` | Native `type` attribute. |
- `status` changes the semantic icon color, hover, pressed, and focus treatments; it does not change behavior or provide authorization.

### Events

| Event | Params | Description |
|------|--------|-------------|
| `click` | `(event: MouseEvent)` | Emitted for enabled clicks. |

### Slots

| Slot | Props | Description |
|------|------|-------------|
| `default` | `{ hover, pressed }` | Custom icon or animated content. |

## Best Practices

- Always set `label` unless visible text is supplied through the slot.
- Use `pressed` only for persistent toggles; use normal click buttons for one-shot actions.
- Use a semantic `status` tone to reinforce the action, but keep destructive actions distinguishable through surrounding copy or confirmation—not color alone.
- Prefer `nativeType="button"` inside forms unless the button intentionally submits the form.

## Review Notes

- **Accessibility note:** `label` is the only built-in accessible name source for an icon-only button; if a custom slot includes visible text, verify it remains visible to assistive technologies.
- **Verified coverage:** `packages/tuffex/packages/components/src/icon-button/__tests__/icon-button.test.ts` covers accessible-name warnings and semantic status classes; `apps/nexus/test/docs/tuffex-component-docs-coverage.test.ts` verifies the component page plus demo registry remain loadable.

## Source

- Component source: `packages/tuffex/packages/components/src/icon-button/src/TxIconButton.vue`.
- Export alias: `packages/tuffex/packages/components/src/icon-button/index.ts` exports installable `TxIconButton` and `TxIconButtonInstance`.
- Coverage: `packages/tuffex/packages/components/src/icon-button/__tests__/icon-button.test.ts` verifies accessible-name warnings and semantic status-class rendering; Nexus docs coverage verifies this page and its demo registry are loadable.

<TuffDocSourceLink />
