---
title: 数据套件
description: 可视化与图表：主包内的数据展示组件，以及图表家族子路径
category: DataSuite
status: beta
since: 0.4.0
tags: [data, charts, visualization, suite, overview]
syncStatus: reviewed
verified: false
---

## 用法

每个格子都是可交互的真实组件，点击左上角名称进入对应组件文档。

::DocsComponentsGallery{suite="data"}
::

## 分组一览

下面按分组列出该套件的**全部**组件，数据来自每个文档的 `category` frontmatter，新增组件会自动出现。

::DocsSuiteCatalog{suite="data"}
::

## 概述

数据套件负责把数字变成图形，汇集了 Tuffex 在这件事上的全部能力。**可视化**（SparkChart、AllocationBar、DiffTable、SignalMeter）是轻量内联数据展示；**图表**是完整的图表家族，经 `@talex-touch/tuffex/charts` 子路径引入——与 kumo 同构的 API 面、Vue 直渲 SVG、不依赖 echarts。两者都在主包内，运行时经 `@talex-touch/tuffex/pro` 入口引入；按族整体引入图表时用 `@talex-touch/tuffex/charts`。图表家族从《[Charts 图表](./charts.zh.mdc)》开始看；当版面放不下一张完整图表时，用可视化组件。整套图表按 ECharts 的默认时序播放动画——1000ms 入场、500ms 数据更新、300ms 状态切换——并在 `prefers-reduced-motion: reduce` 下一律直接落到终帧。
