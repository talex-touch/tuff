---
title: "AgentScreen 智能体屏幕"
description: "智能体正在看的画面：定比例画框、指针叠层、加载占位与一行说明。"
category: AiAgent
status: beta
since: 0.6.0
tags: [agent, screen, capture, ai]
syncStatus: reviewed
verified: true
---
# AgentScreen 智能体屏幕

## 基础用法

### AgentScreen

:::TuffDemoWrapper{demo="AgentScreenAgentScreenDemo" code-lang="vue" title="智能体屏幕" description="切到「加载中」看占位态；指针叠层带一行当前动作。"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const state = ref<'working' | 'loading'>('working')
  </script>

  <template>
    <TxAgentScreen
      :state="state"
      label="智能体的屏幕"
      :cursor="{ x: 46, y: 62, label: '正在打开「照片」' }"
    >
      <!-- 画面由宿主给：截图、canvas、video 都行 -->
      <canvas ref="surface" />
    </TxAgentScreen>
  </template>
---
:::

## 画框只管画框

组件拥有的是**画框、指针和说明**三件事，画面本身来自 `src` 或默认插槽。所以同一个画框既能贴一张截图，也能挂一块实时 canvas，或者一路 video 流——它不关心里面是什么。

默认插槽优先于 `src`：一块实时画布和一张过期截图不能叠在一起。

`ratio` 是画框的 CSS `aspect-ratio`，默认取上游那张采集图自身的比例 `2964 / 1856`。定比例而不是定高度，是因为画框宽度随容器变，而采集画面的比例不变。

## 指针用百分比

`cursor` 的 `x` / `y` 是**相对画框自身的百分比**，不是像素。画框是响应式的，而一个采集到的指针坐标只在它来源的那次采集里有像素意义；换算成百分比之后，画框缩放到多大都落在同一个位置上。

越界的坐标会被**夹到 0–100**。采集端偶尔会给出略微超出画面的坐标，不夹的话指针会跑出裁剪区、画到页面上。

指针是内联 SVG 而不是图标字体：它要在任意画面上都看得见，所以需要浅色填充配深色描边，单色字形做不到这件事。

## 加载态

`state` 为 `loading` 时，画框换成骨架占位，并且**把画面和 `overlay` 插槽一起撤掉**——叠层是用来标注画面的，没有画面时它没有标注对象。

占位复用库里统一的骨架条（`skeleton-surface`），不另起一套微光：同一套时长、同一个 reduced-motion 守卫，只把颜色钉到 BUI 的表面色，让占位和它填充的那个画框是一个色系。占位带 `role="status" aria-live="polite"`——它在没有用户动作的情况下发生变化。

## API

### Props

| 属性名 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `src` | `string` | - | 画面图片地址。默认插槽有内容时忽略 |
| `alt` | `string` | - | 画面的无障碍描述。**用了 `src` 就必须给**——别人屏幕的截图是内容，不是装饰 |
| `label` | `string` | - | 画框下方的说明。不传则不渲染 |
| `state` | `'working' \| 'loading'` | `'working'` | 画框当前显示画面还是占位 |
| `cursor` | `AgentScreenCursor` | - | 指针叠层。不传则不渲染 |
| `ratio` | `string` | `'2964 / 1856'` | 画框的 CSS `aspect-ratio` |
| `ariaLabel` | `string` | `'Agent screen'` | 整个区域的无障碍名 |
| `loadingLabel` | `string` | `'Waiting for the agent's screen'` | 加载态朗读的文本 |

### AgentScreenCursor

| 字段 | 类型 | 说明 |
|---|---|---|
| `x` / `y` | `number` | 0–100，相对画框的百分比；越界会被夹住 |
| `label` | `string` | 指针旁的一行文案，通常是当前动作 |

### Slots

| 插槽名 | 说明 |
|---|---|
| `default` | 替换画面内容，优先于 `src` |
| `overlay` | 画面之上、裁剪之内的叠层；加载态下一并撤掉 |
| `label` | 替换画框下方的说明 |

## 最佳实践

- 画面用实时 canvas 或 video 时走默认插槽，`src` 留给静态截图。
- `label` 写「这是谁的屏幕」，指针的 `label` 写「它正在做什么」——两者分工不同，别挤在一起。
- 采集帧之间的空档把 `state` 切到 `loading`，而不是让画框留一帧旧画面——旧画面会被读成「它卡住了」。

## Source

- 组件源码：`packages/tuffex/packages/components/src/agent-screen/src/TxAgentScreen.vue`。
- 类型：`packages/tuffex/packages/components/src/agent-screen/src/types.ts`。
- **实测覆盖:** `packages/tuffex/packages/components/src/agent-screen/__tests__/agent-screen.test.ts`（13 项）覆盖 `src` 与 alt、默认插槽压过 `src`、加载态换占位并带 live region、无内容时的占位、指针百分比定位与越界夹取、指针缺省、指针文案、`ratio` 透传、说明的有无、区域无障碍名、`overlay` 在裁剪内以及加载态下一并撤掉。
- 移植自 Beautiful UI（https://www.beautifului.dev）第 21 例 Agent Screen，© 2026 Shane Levine，MIT。

<TuffDocSourceLink />
