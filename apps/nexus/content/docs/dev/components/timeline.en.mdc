---
title: Timeline
description: Event timeline for status history
category: Data
status: beta
since: 1.0.0
tags: [timeline, audit, status]
syncStatus: reviewed
verified: true
---

# Timeline

Chronological list of events.

## Basic Usage
### Timeline
Highlight recent updates.
:::TuffDemoWrapper{demo="TimelineTimelineDemo" code-lang="vue"}
---
code: |
  <template>
    <TxTimeline>
      <TxTimelineItem title="Design phase" time="09:30" color="primary" />
      <TxTimelineItem title="Build" time="12:00" color="success" />
      <TxTimelineItem title="Launch" time="16:10" color="warning" />
    </TxTimeline>
  </template>
---
:::

## Admin Audit Flow

`TxTimeline` works well for authorization, release, sync, and other admin task event streams. Use `active` to highlight the current step and treat color as a semantic aid only; the important state should still be present in `title` or slot text.

::TuffDemoWrapper{demo="ComponentsPermissionOrchestrationDemo" code-lang="vue" title="Permission orchestration panel" description="A screenshot-verified authorization audit timeline."}
---
code: |
  <template>
    <TxTimeline>
      <TxTimelineItem title="Pick scope" time="09:30" color="primary">
        release
      </TxTimelineItem>
      <TxTimelineItem title="Assign resources" time="10:20" color="success" active>
        docs + notes
      </TxTimelineItem>
      <TxTimelineItem title="Await review" time="11:05" color="warning">
        security review
      </TxTimelineItem>
    </TxTimeline>
  </template>
---
::
## API (Lite)
### TxTimeline Props
::TuffPropsTable
---
rows:
  - name: layout
    type: "'vertical' | 'horizontal'"
    default: "'vertical'"
    description: Layout direction
---
:::

### TxTimelineItem Props
::TuffPropsTable
---
rows:
  - name: title
    type: string
    default: "''"
    description: Timeline item title
  - name: time
    type: string
    default: "''"
    description: Time label
  - name: color
    type: "'default' | 'primary' | 'success' | 'warning' | 'error'"
    default: "'default'"
    description: Dot color
  - name: icon
    type: string
    default: '-'
    description: Dot icon
  - name: active
    type: boolean
    default: 'false'
    description: Highlight the current event item
---
:::

## Interaction Contract

- `TxTimeline` exposes `role="list"` and each `TxTimelineItem` exposes `role="listitem"`.
- `active` applies to both the item and dot, highlighting the current event.
- `layout="horizontal"` is provided to child items through context, switching the event stream to a horizontal layout.

## Source
<TuffDocSourceLink />

## Review Notes

- Source: `packages/tuffex/packages/components/src/timeline/src/TxTimeline.vue` and `packages/tuffex/packages/components/src/timeline/src/TxTimelineItem.vue`.
- Screenshot: `.codex-screenshots/nexus-timeline-permission-orchestration-demo-playwright-2026-05-28.png`.
- Recommendation: mark the current event with `active`; keep `color` within `'default' | 'primary' | 'success' | 'warning' | 'error'` and avoid undefined semantic tones.
