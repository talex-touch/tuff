---
title: "EmptyState 空态引导"
description: "通用空态/引导组件，适用于页面为空、未选择、无权限等场景。"
category: Feedback
status: beta
since: 1.0.0
tags: [empty, state, guide]
syncStatus: reviewed
verified: true
---
# EmptyState 空态引导

通用空态/引导组件，适用于页面为空、未选择、无权限等场景。

## 基础用法

### EmptyState (variant)
:::TuffDemoWrapper{demo="EmptyStateEmptyStateVariantDemo" code-lang="vue"}
---
code: |
  <template>
    <TxEmptyState
      variant="no-data"
      :primary-action="{ label: 'Create', type: 'primary' }"
      :secondary-action="{ label: 'Refresh' }"
    />
  </template>
---
:::

## 横向布局

### EmptyState (horizontal)
:::TuffDemoWrapper{demo="EmptyStateEmptyStateHorizontalDemo" code-lang="vue"}
---
code: |
  <template>
    <TxEmptyState
      variant="no-selection"
      layout="horizontal"
      surface="card"
      title="No selection"
      description="Pick an item from the left to continue."
    />
  </template>
---
:::

## 自定义插槽

### EmptyState (slots)
:::TuffDemoWrapper{demo="EmptyStateEmptyStateSlotsDemo" code-lang="vue"}
---
code: |
  <template>
    <TxEmptyState variant="custom">
      <template #icon>
        <TxIcon name="i-carbon-search" :size="28" />
      </template>
      <template #title>
        Setup required
      </template>
      <template #description>
        Connect a workspace to enable this feature.
      </template>
      <template #actions>
        <TxButton type="primary">Connect</TxButton>
        <TxButton>Learn more</TxButton>
      </template>
    </TxEmptyState>
  </template>
---
:::

## 后台恢复状态

Dashboard 数据区建议把加载、空态、错误态放在同一容器内切换，避免页面跳动或多个状态组件重复占位。

:::TuffDemoWrapper{demo="ComponentsRecoveryStatesDemo" code-lang="vue" title="后台恢复状态" description="加载、空态、错误态和小尺寸预设组件的组合示例。"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const mode = ref<'loading' | 'empty' | 'error'>('empty')
  </script>

  <template>
    <section class="grid gap-3">
      <TxLoadingState v-if="mode === 'loading'" title="正在加载规则" surface="card" />
      <TxEmptyState
        v-else-if="mode === 'empty'"
        variant="no-data"
        title="还没有自动化规则"
        surface="card"
        :primary-action="{ label: '创建规则', type: 'primary' }"
      />
      <TxErrorState v-else title="规则加载失败" surface="card" />
    </section>
  </template>
---
:::

## 预设组件

- `TxBlankSlate`：大尺寸空白页（默认 `size="large"`）
- `TxLoadingState`：加载态占位（默认 `variant="loading"`）
- `TxNoSelection`：未选择占位（默认 `variant="no-selection"`）
- `TxNoData`：无数据占位（默认 `variant="no-data"`）
- `TxSearchEmpty`：搜索空态（默认 `variant="search-empty"`）
- `TxOfflineState`：离线提示（默认 `variant="offline"`）
- `TxPermissionState`：权限不足（默认 `variant="permission"`）
- `TxErrorState`：错误状态（默认 `variant="error"`），详见 [ErrorState](./error-state.zh.md)
- `TxGuideState`：引导状态（默认 `variant="guide"`），详见 [GuideState](./guide-state.zh.md)

## API

### Props

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|---------|------|
| `variant` | `EmptyStateVariant` | `'empty'` | 预设场景 |
| `title` | `string` | - | 标题 |
| `description` | `string` | - | 描述 |
| `icon` | `TxIconSource \| string \| null` | - | 图标（string 为 class/name） |
| `iconSize` | `number` | - | 图标尺寸 |
| `layout` | `'vertical' \| 'horizontal'` | `'vertical'` | 布局方向 |
| `align` | `'start' \| 'center' \| 'end'` | `'center'` | 对齐方式 |
| `size` | `'small' \| 'medium' \| 'large'` | `'medium'` | 尺寸等级 |
| `surface` | `'plain' \| 'card'` | `'plain'` | 表面风格 |
| `primaryAction` | `EmptyStateAction` | - | 主操作配置 |
| `secondaryAction` | `EmptyStateAction` | - | 次操作配置 |
| `actionSize` | `TxButtonProps['size']` | `'small'` | 操作按钮尺寸 |
| `loading` | `boolean` | `false` | 强制 loading 图标 |

### EmptyStateAction

| 属性名 | 类型 | 默认值 | 说明 |
|------|------|---------|------|
| `label` | `string` | - | 按钮文本 |
| `type` | `TxButtonProps['type']` | - | 按钮类型 |
| `variant` | `TxButtonProps['variant']` | - | 按钮变体 |
| `size` | `TxButtonProps['size']` | - | 按钮尺寸 |
| `disabled` | `boolean` | `false` | 禁用状态 |
| `icon` | `string` | - | 图标 class |

### Slots

| 名称 | 说明 |
|------|------|
| `icon` | 自定义图标区 |
| `title` | 标题 |
| `description` | 描述 |
| `actions` | 操作区 |

### Events

| 事件名 | 说明 |
|------|------|
| `primary` | 主操作点击 |
| `secondary` | 次操作点击 |

## 审阅说明

- 源码：`packages/tuffex/packages/components/src/empty-state/src/TxEmptyState.vue`
- 建议：数据容器优先用 `variant` 表达状态；只有文案/图标需要业务化时才覆盖 `title`、`description`、`icon`。
- 可访问性：空态标题需说明用户当前处境，操作按钮需表达下一步；不要只写“暂无”。
