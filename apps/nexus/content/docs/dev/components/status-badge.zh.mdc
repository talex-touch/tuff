---
title: StatusBadge 状态徽标
description: 状态标识与系统反馈
category: Feedback
status: beta
since: 1.0.0
tags: [badge, status, signal]
syncStatus: reviewed
verified: true
---

# StatusBadge 状态徽标

> 用于状态指示与系统反馈的轻量徽标。  
> **状态**：Beta


## Demo
### Status Signals

成功/警告/错误/信息。

::TuffDemoWrapper{demo="StatusBadgeSignalsDemo" code-lang="vue"}
---
code: |
  <template>
    <TxStatusBadge text="成功" status="success" />
    <TxStatusBadge text="警告" status="warning" />
    <TxStatusBadge text="错误" status="danger" />
    <TxStatusBadge text="信息" status="info" />
  </template>
---
::

### 后台运营状态

在 Dashboard 首屏中，`TxStatusBadge` 通常与 `TxStatCard`、`TxProgressBar`、`TuffSwitch` 一起呈现发布、构建与同步状态。

::TuffDemoWrapper{demo="ComponentsOperationsStatusDemo" code-lang="vue" title="运营状态面板" description="已通过本地 Nexus 页面截图验证的后台状态组合。"}
---
code: |
  <template>
    <section class="grid gap-3">
      <TxStatusBadge text="审阅完成" status="success" size="sm" />
      <TxStatusBadge text="构建稳定" status="success" os="linux" size="sm" />
      <TxStatusBadge text="同步健康" status-key="granted" size="sm" />
      <TxProgressBar :percentage="86" status="success" show-text />
    </section>
  </template>
---
::

## API（简版）
::TuffPropsTable
---
rows:
  - name: text
    type: 'string'
    default: '-'
    description: '显示文字'
  - name: status
    type: "'success' | 'warning' | 'danger' | 'info' | 'muted'"
    default: 'info'
    description: '状态类型'
  - name: statusKey
    type: "'granted' | 'denied' | 'notDetermined' | 'unsupported' | string"
    default: "''"
    description: '映射到色调的权限/状态键'
  - name: icon
    type: 'string'
    default: "''"
    description: '自定义图标 class'
  - name: size
    type: "'sm' | 'md'"
    default: 'md'
    description: '尺寸'
  - name: os
    type: "'macos' | 'windows' | 'linux'"
    default: '-'
    description: '可选平台图标'
  - name: osOnly
    type: 'boolean'
    default: 'false'
    description: '仅渲染平台图标'
---
::

## 契约说明

显式 `status` 优先于 `statusKey`。`statusKey` 会把 `granted/denied/notDetermined/unsupported` 映射到 success/danger/warning/muted，未知 key 回退到 info。平台图标会渲染在状态图标前；`osOnly` 会隐藏状态图标。点击徽标会触发 `click` 事件。

## 审阅说明

- 源码：`packages/tuffex/packages/components/src/status-badge/src/TxStatusBadge.vue`
- 建议：状态来自后端枚举时优先传 `statusKey`，页面本地判断时再显式传 `status`。
- 可访问性：组件使用 `role="status"`；同屏多个徽标时，文本必须能独立表达状态，不依赖颜色。

## 组合示例
### 状态行

徽标搭配头像进行状态展示。

::TuffDemoWrapper{demo="StatusBadgeRowDemo" code-lang="vue"}
---
code: |
  <template>
    <TxAvatar name="TA" />
    <TxStatusBadge text="在线" status="success" />
  </template>
---
::
