---
title: "Slider"
description: "Range input with value display and optional elastic tooltip feedback."
category: Form
status: beta
since: 1.0.0
tags: [slider, form, input]
syncStatus: reviewed
verified: true
---
# Slider

Range input with value display and optional elastic tooltip feedback.

## Basic Usage

### Slider
:::TuffDemoWrapper{demo="SliderSliderDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref(30)
  </script>

  <template>
    <div style="width: 320px; padding: 16px; border: 1px solid var(--tx-border-color); border-radius: 12px;">
      <TxSlider v-model="value" :min="0" :max="100" :step="1" />
      <div style="margin-top: 8px; color: var(--tx-text-color-secondary);">
        Value: {{ value }}
      </div>
    </div>
  </template>
---
:::

## Disabled

### Slider (disabled)
:::TuffDemoWrapper{demo="SliderSliderDisabledDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref(42)
  </script>

  <template>
    <div style="width: 320px; padding: 16px; border: 1px solid var(--tx-border-color); border-radius: 12px;">
      <TxSlider v-model="value" disabled show-value />
    </div>
  </template>
---
:::

## Formatted Display

### Slider (formatValue)
:::TuffDemoWrapper{demo="SliderSliderFormatValueDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref(30)
  const formatValue = (next: number) => `${next}%`
  </script>

  <template>
    <div style="width: 320px; padding: 16px; border: 1px solid var(--tx-border-color); border-radius: 12px;">
      <TxSlider
        v-model="value"
        :min="0"
        :max="100"
        show-value
        :format-value="formatValue"
      />
    </div>
  </template>
---
:::

## Show Values

### Slider (show value)
:::TuffDemoWrapper{demo="SliderSliderShowValueDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref(60)
  </script>

  <template>
    <div style="width: 320px; padding: 16px; border: 1px solid var(--tx-border-color); border-radius: 12px;">
      <TxSlider v-model="value" :min="0" :max="100" :step="1" show-value />
    </div>
  </template>
---
:::

## Elastic Tooltip (speed + acceleration)

While dragging, the tooltip tilts, trails, and wobbles based on pointer speed/acceleration. The demo below drives every `tooltip-*` prop from six presets; this snippet shows the minimal binding only — the full interactive panel lives in the demo source, and each parameter is documented in the API below.

### Slider (elastic tooltip)
:::TuffDemoWrapper{demo="SliderSliderElasticTooltipDemo" code-lang="vue"}
---
code: |
  <script setup lang="ts">
  import { ref } from 'vue'

  const value = ref(30)
  </script>

  <template>
    <TxSlider
      v-model="value"
      :min="0"
      :max="100"
      show-value
      tooltip-tilt
      tooltip-trigger="drag"
      tooltip-motion="blur"
      :tooltip-tilt-max-deg="22"
      :tooltip-offset-max-px="36"
      :tooltip-accel-boost="0.8"
      :tooltip-spring-stiffness="420"
      :tooltip-spring-damping="26"
      tooltip-jelly
      :tooltip-jelly-frequency="11"
      :tooltip-jelly-squash="0.32"
    />
  </template>
---
:::

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|------|
| `modelValue` | `number` | `0` | Current slider value. The rendered input clamps it to the `min..max` range. |
| `min` | `number` | `0` | Minimum range value forwarded to the native range input. |
| `max` | `number` | `100` | Maximum range value forwarded to the native range input. |
| `step` | `number` | `1` | Step size forwarded to the native range input. |
| `disabled` | `boolean` | `false` | Disables the native range input and suppresses tooltip display. |
| `showValue` | `boolean` | `false` | Renders the formatted current value beside the slider. |
| `formatValue` | `(value: number) => string` | - | Formats the visible side value; also feeds tooltip text when `tooltipFormatter` is absent. |
| `showTooltip` | `boolean` | `true` | Enables tooltip rendering when the trigger condition is met. |
| `tooltipTrigger` | `'drag' \| 'hover' \| 'always'` | `'drag'` | Controls when the tooltip is visible: while dragging, on hover/drag, or always. |
| `tooltipFormatter` | `(value: number) => string` | - | Formats tooltip text independently from `formatValue`. |
| `tooltipPlacement` | `'top' \| 'bottom'` | `'top'` | Places the tooltip above or below the thumb. |
| `tooltipTilt` | `boolean` | `false` | Enables velocity-based tooltip follow, tilt, offset, squash, and skew motion. |
| `tooltipTiltMaxDeg` | `number` | `18` | Maximum tilt angle used by the velocity response. |
| `tooltipOffsetMaxPx` | `number` | `28` | Maximum horizontal tooltip offset used by the velocity response. |
| `tooltipAccelBoost` | `number` | `0.65` | Multiplier for acceleration contribution when calculating elastic tooltip intensity. |
| `tooltipSpringStiffness` | `number` | `320` | Spring stiffness for tooltip follow, offset, and tilt interpolation. |
| `tooltipSpringDamping` | `number` | `24` | Spring damping for tooltip follow, offset, and tilt interpolation. |
| `tooltipMotion` | `'blur' \| 'fade' \| 'none'` | `'blur'` | Tooltip enter/leave motion. `none` renders without the transition wrapper. |
| `tooltipMotionDuration` | `number` | `160` | Tooltip enter/leave duration in ms; negative values are clamped to `0ms`. |
| `tooltipMotionBlurPx` | `number` | `10` | Blur radius for the `blur` tooltip motion; negative values are clamped to `0px`. |
| `tooltipDistortSkewDeg` | `number` | `8` | Maximum skew used by tooltip distortion while moving. |
| `tooltipJelly` | `boolean` | `true` | Enables acceleration-triggered wobble on top of the tilt motion. |
| `tooltipJellyFrequency` | `number` | `8.5` | Wobble frequency in hertz. |
| `tooltipJellyDecay` | `number` | `10` | Wobble decay rate; larger values settle faster. |
| `tooltipJellyRotateDeg` | `number` | `10` | Maximum wobble rotation contribution. |
| `tooltipJellySkewDeg` | `number` | `12` | Maximum wobble skew contribution. |
| `tooltipJellySquash` | `number` | `0.16` | Maximum wobble squash contribution. |
| `tooltipJellyTriggerAccel` | `number` | `2800` | Acceleration threshold used to kick jelly wobble. |

### Events

| Event | Params | Description |
|------|------|------|
| `update:modelValue` | `number` | Emitted on native input with the next clamped value. |
| `change` | `number` | Emitted on native change with the input's current numeric value. |

### Slots

`TxSlider` does not expose public slots. Tooltip, thumb, track, and value display are internal so pointer math and elastic tooltip transforms stay in sync.

## Interaction Contract

- The rendered native range input clamps `modelValue` into the `min..max` range.
- Native `input` emits `update:modelValue` with the clamped value; native `change` emits `change`.
- `disabled` blocks value updates and suppresses tooltip rendering.
- `formatValue` controls the visible side value and is also used for tooltip text when `tooltipFormatter` is absent.
- Tooltip visibility follows `tooltipTrigger`: `drag`, `hover`, or `always`; `showTooltip=false` disables it.
- Negative `tooltipMotionDuration` and `tooltipMotionBlurPx` values are clamped to `0ms` and `0px`.
- Global pointer listeners are cleaned up on unmount.

## Best Practices

- Use sliders for continuous or near-continuous numeric values; choose `TxSegmentedSlider` when every stop has separate semantic meaning.
- Always show surrounding copy for units. Use `formatValue` / `tooltipFormatter` for `%`, `ms`, or quota units instead of adding unit text into `modelValue`.
- Keep `min`, `max`, and `step` stable while the user is dragging; changing range math mid-drag can make the thumb jump.
- Use `tooltipTilt` / jelly motion for exploratory tuning surfaces, not dense enterprise forms where motion may distract.

## Source

- Component source: `packages/tuffex/packages/components/src/slider/src/TxSlider.vue`.
- Types: `packages/tuffex/packages/components/src/slider/src/types.ts`.
- **Verified coverage:** `packages/tuffex/packages/components/src/slider/__tests__/slider.test.ts` covers display and emitted-value clamping, disabled interaction and tooltip suppression, formatted bottom tooltips, non-transition tooltip rendering, and global pointer-listener cleanup.

<TuffDocSourceLink />

## Review Notes

- Source: `packages/tuffex/packages/components/src/slider/src/TxSlider.vue`.
- Screenshot: `.codex-screenshots/nexus-slider-release-policy-demo-playwright-2026-05-28.png`.
- Recommendation: sliders should own continuous thresholds only; release traffic, quota ratios, and sampling rates fit here, while risk tiers should use `TxSegmentedSlider`.
