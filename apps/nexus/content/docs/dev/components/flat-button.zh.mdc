---
title: FlatButton 扁平按钮
description: 低强调的轻量按钮
category: Basic
syncStatus: migrated
---

# FlatButton 扁平按钮

用于次要动作的轻量按钮样式。

## 基础用法
### 扁平按钮
基础、主色、小尺寸与加载状态。
:::TuffDemoWrapper{demo="FlatButtonFlatButtonDemo" code-lang="vue"}
---
code: |
  <template>
    <TuffFlatButton>默认</TuffFlatButton>
    <TuffFlatButton primary>主色</TuffFlatButton>
    <TuffFlatButton mini>小尺寸</TuffFlatButton>
    <TuffFlatButton loading>加载中</TuffFlatButton>
  </template>
---
:::
## API（Lite）
::TuffPropsTable
---
rows:
  - name: primary
    type: boolean
    default: 'false'
    description: 是否主色样式
  - name: mini
    type: boolean
    default: 'false'
    description: 紧凑尺寸
  - name: disabled
    type: boolean
    default: 'false'
    description: 是否禁用
  - name: loading
    type: boolean
    default: 'false'
    description: 加载状态
---
:::

## Source
<TuffDocSourceLink />

## 交互契约

- 根节点是原生 `<button type="button">`，默认不会提交外层表单。
- `disabled` 与 `loading` 都会设置原生 `disabled` 属性，并阻断 `click` 事件。
- `loading` 会渲染 spinner，同时保留按钮内容。
- `primary` 只切换主色视觉；非 primary 默认带 `fake-background` hover 背景。
- `mini` 切换紧凑尺寸，不改变按钮语义。
