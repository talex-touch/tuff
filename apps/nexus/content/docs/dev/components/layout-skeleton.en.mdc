---
title: LayoutSkeleton
description: Layout-level skeleton placeholder
category: Layout
status: beta
since: 1.0.0
tags: [skeleton, layout, loading]
syncStatus: reviewed
verified: true
---

# LayoutSkeleton

> Layout-level placeholder for page or panel loading.  
> **Status**: Beta


## Demo
### Layout Placeholder
The wrapper controls the size.
:::TuffDemoWrapper{demo="LayoutSkeletonLayoutPlaceholderDemo" code-lang="vue"}
---
code: |
  <template>
    <div style="height: 240px;">
      <TxLayoutSkeleton />
    </div>
  </template>
---
:::
## Basic Usage
::TuffCodeBlock{lang="vue"}
---
code: |
  <template>
    <div style="height: 240px;">
      <TxLayoutSkeleton />
    </div>
  </template>
---
::

## Usage Notes
- The wrapper controls size and layout ratio.
- Ideal for page-level loading with consistent rhythm.
- Content line widths use a fixed sequence, keeping SSR, hydration, and tests deterministic.

## API

### Props
::TuffPropsTable
---
rows:
  - name: '—'
    type: '-'
    default: '-'
    description: 'No extra props; size follows the container'
---
::

### Events

`TxLayoutSkeleton` does not emit custom events.

### Slots

`TxLayoutSkeleton` does not expose slots. It always renders the fixed layout scaffold documented below.

## Interaction Contract

- `TxLayoutSkeleton` has no props, events, or slots.
- The component always renders a header, 6 sidebar items, and 8 content lines.
- Content line widths are deterministic and do not depend on runtime randomness.

## Dashboard Data Operations

Use `TxLayoutSkeleton` in detail panes, side summaries, or page-level placeholders to show that a layout is coming; do not use it as a replacement for concrete table rows.

:::TuffDemoWrapper{demo="ComponentsDataOperationsDemo" code-lang="vue" title="Data operations panel" description="Layout skeletons cover side/detail regions while DataTable owns the primary table body."}
---
code: |
  <template>
    <div style="height: 190px;">
      <TxLayoutSkeleton />
    </div>
  </template>
---
:::

## Composite Patterns
### Panel Placeholder
Skeleton paired with card containers.
:::TuffDemoWrapper{demo="LayoutSkeletonPanelPlaceholderDemo" code-lang="vue"}
---
code: |
  <template>
    <TxCard>
      <TxLayoutSkeleton />
    </TxCard>
  </template>
---
:::

## Best Practices

- Give the wrapper an explicit height; the component fills `width: 100%` and `height: 100%` rather than deciding page dimensions itself.
- Use it for page, panel, side-summary, and detail-pane loading where the final layout shape is stable.
- Do not use it as a table row placeholder. Use `TxSkeleton`, row-level placeholders, or `TxDataTable loading` for row bodies.
- Keep it deterministic for docs, SSR, hydration, and visual tests; content line widths intentionally use a fixed sequence instead of runtime randomness.

## Review Notes

- Reviewed against `packages/tuffex/packages/components/src/layout-skeleton/src/TxLayoutSkeleton.vue` and `layout-skeleton.test.ts`.
- **Verified coverage:** fixed scaffold shape, six sidebar items, eight content rows, deterministic content widths, and install registration.
- API note: this component intentionally has no props. Size, aspect ratio, and placement must come from the parent container.
- Rendering note: content line widths are fixed, not random, so SSR, hydration, docs snapshots, and visual tests stay stable.

## Source

- Component source: `packages/tuffex/packages/components/src/layout-skeleton/src/TxLayoutSkeleton.vue`.
- Types: `packages/tuffex/packages/components/src/layout-skeleton/index.ts` exports `TxLayoutSkeletonInstance`.
- Coverage: `packages/tuffex/packages/components/src/layout-skeleton/__tests__/layout-skeleton.test.ts` verifies fixed scaffold counts, deterministic content widths, and install registration.

<TuffDocSourceLink />

