import { readFileSync } from 'node:fs'
import { describe, expect, it } from 'vitest'

describe('PluginDetailDrawer analytics contract', () => {
  it('renders owner review analytics from private plugin analytics payload', () => {
    const component = readFileSync(new URL('./PluginDetailDrawer.vue', import.meta.url), 'utf8')

    expect(component).toContain('analytics.reviews?.total')
    expect(component).toContain("dashboard.sections.plugins.analytics.stats.reviews")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.title")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.averageMeta")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.noRatings")
    expect(component).toContain('reviewAnalytics.ratingDistribution')
    expect(component).toContain('latestReviewTrendPoint')
    expect(component).toContain('reviewAnalytics.value?.statusTrend')
    expect(component).toContain('formatReviewTrendMeta')
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.trendMeta")
    expect(component).toContain('latestReviewRatingTrendPoint')
    expect(component).toContain('reviewAnalytics.value?.ratingTrend')
    expect(component).toContain('formatReviewRatingTrendMeta')
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.ratingTrend")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.lowRatingRate")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.ratingTrendMeta")
    expect(component).toContain('reviewCommentItems')
    expect(component).toContain('reviewAnalytics.value?.comments')
    expect(component).toContain('latestReviewCommentTrendPoint')
    expect(component).toContain('reviewCommentTrendMeta')
    expect(component).toContain('reviewCommentQualityBuckets')
    expect(component).toContain('reviewAnalytics.value?.comments?.qualityBuckets')
    expect(component).toContain('DashboardPluginReviewCommentQualityBucket')
    expect(component).toContain('formatReviewCommentQualityLabel')
    expect(component).toContain('formatReviewCommentQualityMeta')
    expect(component).toContain('reviewActionQueue')
    expect(component).toContain('reviewAnalytics.value?.actionQueue')
    expect(component).toContain('DashboardPluginReviewActionQueueItem')
    expect(component).toContain('formatReviewActionQueueSuggestedAction')
    expect(component).toContain('formatReviewActionQueueReason')
    expect(component).toContain('formatReviewActionQueueMeta')
    expect(component).toContain('reviewActionQueuePriorityTone')
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.titleCoverage")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.contentCoverage")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.averageLength")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.commentTrendMeta")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.commentQuality")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.commentQualityBuckets")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.commentQualityMeta")
    expect(component).toContain('item.lowRatingRate')
    expect(component).toContain('reviewModerationTimingItems')
    expect(component).toContain('reviewAnalytics.value?.moderationTiming')
    expect(component).toContain('reviewModerationTimingBuckets')
    expect(component).toContain('DashboardPluginReviewModerationTimingBucket')
    expect(component).toContain('formatReviewModerationTimingBucketLabel')
    expect(component).toContain('formatReviewModerationTimingBucketMeta')
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.moderationTiming.title")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.moderationTiming.buckets")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.actionQueue.title")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.actionQueue.suggestedActions")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.actionQueue.reasons")
    expect(component).toContain("dashboard.sections.plugins.analytics.reviews.actionQueue.itemMeta")
    expect(component).toContain('ownerActionQueue')
    expect(component).toContain('props.analytics?.actionQueue')
    expect(component).toContain('DashboardPluginOwnerActionQueueItem')
    expect(component).toContain('formatOwnerActionQueueSuggestedAction')
    expect(component).toContain('formatOwnerActionQueueReason')
    expect(component).toContain('formatOwnerActionQueueMeta')
    expect(component).toContain('ownerActionQueuePriorityTone')
    expect(component).toContain("dashboard.sections.plugins.analytics.actionQueue.title")
    expect(component).toContain("dashboard.sections.plugins.analytics.actionQueue.suggestedActions")
    expect(component).toContain("dashboard.sections.plugins.analytics.actionQueue.reasons")
    expect(component).toContain("dashboard.sections.plugins.analytics.actionQueue.itemMeta")
    expect(component).toContain('analyticsConversionItems')
    expect(component).toContain('analytics?.conversion')
    expect(component).toContain('latestConversionTrendPoint')
    expect(component).toContain('analytics?.conversionTrend')
    expect(component).toContain('formatConversionTrendMeta')
    expect(component).toContain("dashboard.sections.plugins.analytics.conversion.title")
    expect(component).toContain("dashboard.sections.plugins.analytics.conversion.installRate")
    expect(component).toContain("dashboard.sections.plugins.analytics.conversion.trendMeta")
    expect(component).toContain('retentionAnalytics')
    expect(component).toContain('retentionItems')
    expect(component).toContain('latestRetentionTrendPoint')
    expect(component).toContain('recentRetentionTrend')
    expect(component).toContain('formatRetentionTrendMeta')
    expect(component).toContain("dashboard.sections.plugins.analytics.retention.title")
    expect(component).toContain("dashboard.sections.plugins.analytics.retention.retentionRate")
    expect(component).toContain("dashboard.sections.plugins.analytics.retention.returningActors")
    expect(component).toContain("dashboard.sections.plugins.analytics.retention.repeatRate")
    expect(component).toContain("dashboard.sections.plugins.analytics.retention.activeDays")
    expect(component).toContain("dashboard.sections.plugins.analytics.retention.trendMeta")
    expect(component).toContain('usageTiming')
    expect(component).toContain('usageTimingBreakdowns')
    expect(component).toContain('recentUsageTimingTrend')
    expect(component).toContain('formatUsageTimingTrend')
    expect(component).toContain("dashboard.sections.plugins.analytics.usageTiming.title")
    expect(component).toContain("dashboard.sections.plugins.analytics.usageTiming.hours")
    expect(component).toContain("dashboard.sections.plugins.analytics.usageTiming.weekdays")
    expect(component).toContain("dashboard.sections.plugins.analytics.usageTiming.timeSlots")
    expect(component).toContain("dashboard.sections.plugins.analytics.usageTiming.trendMeta")
    expect(component).toContain('invocationHealth')
    expect(component).toContain('latestInvocationTrendPoint')
    expect(component).toContain('invocationHealthItems')
    expect(component).toContain('invocationContextBreakdowns')
    expect(component).toContain('invocationStatusItems')
    expect(component).toContain('formatInvocationTrendMeta')
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.title")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.uniqueActors")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.successRate")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.failureReasons")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.surfaces")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.countries")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.regions")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.channels")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.versions")
    expect(component).toContain("dashboard.sections.plugins.analytics.invocationHealth.localTimeSlots")
    expect(component).toContain('recentActionTrend')
    expect(component).toContain('analytics?.actionTrend')
    expect(component).toContain("dashboard.sections.plugins.analytics.actionTrend.title")
    expect(component).toContain('recentLocationTrend')
    expect(component).toContain('analytics?.locationTrend')
    expect(component).toContain("dashboard.sections.plugins.analytics.locationTrend.title")
    expect(component).toContain('recentChannelTrend')
    expect(component).toContain('analytics?.channelTrend')
    expect(component).toContain("dashboard.sections.plugins.analytics.channelTrend.title")
    expect(component).toContain('recentVersionTrend')
    expect(component).toContain('analytics?.versionTrend')
    expect(component).toContain("dashboard.sections.plugins.analytics.versionTrend.title")
    expect(component).toContain('formatTrendMetricList')
  })
})
