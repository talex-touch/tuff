/**
 * Per-layer UnoCSS virtual entries (`uno:<layer>.css`). The `unocss` package declares
 * `uno.css` only; this app imports the icons layer on its own so it can load after mount.
 */
declare module 'uno:icons.css' {}
